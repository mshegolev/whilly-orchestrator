---
milestone: v1.5
audited: 2026-06-19
status: passed
scores:
  requirements: 12/12
  phases: 4/4
  integration: 11/11 wired
  flows: 5/5 verified
gaps: {}
tech_debt:
  - phase: 31-cluster-parallel-run-reporting
    items:
      - "No offline regression guard that every CLUSTERS slug resolves >=1 matrix module (only spec-existence is guarded). A future COVERAGE-MATRIX edit dropping a slug's module mapping would not red any offline test — only surfaces in scheduled CI behind ANTHROPIC_API_KEY. Recommend an offline assertion: every CLUSTERS slug resolves >=1 module."
      - "--max-workers accepts 0/negative; ThreadPoolExecutor(max_workers=0) raises ValueError at fleet start. Low impact (manual-dispatch only; cron uses default 6) but an unvalidated flag edge. Recommend argparse validation (>=1)."
---

# Milestone v1.5 Semantic Drift-Guard — Audit

**Status:** passed (all requirements satisfied + verified; 2 audit tech-debt items closed post-audit)
**Score:** 12/12 requirements satisfied, 4/4 phases verified, 11/11 integrations wired, 5/5 E2E flows verified.

## Definition of Done

Add a repeatable, agent-assisted semantic spec-fidelity check that catches when a capability
spec's SHALL/MUST no longer matches live code — drift the v1.4 mechanical gate cannot detect.
Achieved: a single-spec engine (P30) → cluster-parallel 32-spec fleet runner (P31) → scheduled CI
gate (P32) → known-drift fixture validation (P33), all shipping as tooling under `scripts/` with
**zero `whilly/` behavior change** (no opsx spec delta obligation; OpenSpec end-wing re-confirmed
32/0 validate + 275/275 coverage intact).

## Requirements Coverage (3-source cross-reference)

| REQ | Phase | VERIFICATION | Final |
|-----|-------|--------------|-------|
| DETECT-01..04 | 30 | passed (4/4) | satisfied |
| RUN-01..03 | 31 | passed (8/8) | satisfied |
| REPORT-01..02 | 31 | passed | satisfied |
| CI-01..02 | 32 | passed (9/9) | satisfied |
| VALID-01 | 33 | human_needed (6/6) — live canary accepted (ran during execution: drifted→HIGH, clean→clean) | satisfied |

All 12 checked `[x]` in REQUIREMENTS.md traceability; all listed in phase SUMMARY frontmatter.

## Phase Verifications

| Phase | Name | Status |
|-------|------|--------|
| 30 | Detection Engine Core | passed |
| 31 | Cluster-Parallel Run & Reporting | passed |
| 32 | Scheduled CI Integration | passed |
| 33 | Known-Drift Fixture Validation | human_needed (accepted) |

## Cross-Phase Integration

All 11 requirement integrations verified WIRED end-to-end by the integration checker (63 offline
tests pass, 1 live canary skipped offline). Core spine: `main(--all)` → `run_fleet` → `review_spec`
→ `--fail-on high` gate on fleet findings → CI workflow `--all --fail-on "$FAIL_ON"` (value contract
matches the workflow posture allowlist). Phase-33 fixtures drive the REAL `review_spec` pipeline.

## Tech Debt (non-blocking)

1. **Offline guard gap (P31):** no offline test asserts every `CLUSTERS` slug resolves ≥1 matrix
   module; a matrix edit dropping a mapping would only fail in scheduled CI. Manually confirmed all
   32 resolve today. Fix: offline assertion.
2. **`--max-workers` edge (P31):** 0/negative unvalidated → `ValueError` at fleet start. Fix:
   argparse validation `>=1`.

Both are quick, robustness-only fixes; neither breaks a requirement or the milestone goal.

**Resolution (2026-06-19):** both closed — `_positive_int` argparse type rejects --max-workers<1; `test_clusters_slugs_all_resolve_at_least_one_module` adds the offline guard. 65 offline tests pass.
