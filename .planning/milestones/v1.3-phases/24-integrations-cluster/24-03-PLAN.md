---
phase: 24-integrations-cluster
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/gitlab-integration/spec.md
  - openspec/specs/notifications/spec.md
  - openspec/specs/mcp-integration/spec.md
autonomous: true
requirements: [INT-02, INT-05, INT-06]
must_haves:
  truths:
    - "openspec validate gitlab-integration --strict reports the spec is valid (0 errors, 0 warnings)"
    - "openspec validate notifications --strict reports the spec is valid (0 errors, 0 warnings)"
    - "openspec validate mcp-integration --strict reports the spec is valid (0 errors, 0 warnings)"
    - "gitlab-integration states token resolution (GITLAB_TOKEN/GITLAB_API_TOKEN/WHILLY_GITLAB_API_TOKEN/glab fallback) and read-only smoke vs mutating MR-create boundary"
    - "notifications states per-channel auth/config (Slack token+channel, SMTP env, confluence) and the outbound-dispatch best-effort boundary"
    - "mcp-integration states the tool/profile registry contract and api_key_env credential expectation"
  artifacts:
    - path: "openspec/specs/gitlab-integration/spec.md"
      provides: "INT-02 normative capability spec for GitLab CLI smoke + MR sink"
      contains: "## Requirements"
    - path: "openspec/specs/notifications/spec.md"
      provides: "INT-05 normative capability spec for outbound notification dispatch"
      contains: "## Requirements"
    - path: "openspec/specs/mcp-integration/spec.md"
      provides: "INT-06 normative capability spec for the MCP tool/profile registry surface"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/gitlab-integration/spec.md"
      to: "whilly/cli/gitlab.py + whilly/sinks/gitlab_mr.py"
      via: "smoke-read vs MR-create-mutate requirements"
      pattern: "SHALL|MUST"
    - from: "openspec/specs/notifications/spec.md"
      to: "whilly/adapters/notifications/* + api/mailer.py + adapters/confluence/publisher.py"
      via: "per-channel dispatch requirements"
      pattern: "SHALL|MUST"
    - from: "openspec/specs/mcp-integration/spec.md"
      to: "whilly/mcp/registry.py + profiles.py"
      via: "registry/profile contract requirements"
      pattern: "SHALL|MUST"
---

<objective>
Write three independent normative OpenSpec capability specs:
`gitlab-integration` (INT-02), `notifications` (INT-05), and `mcp-integration`
(INT-06). Each is reverse-spec'd from real v4.7.0 code, states auth/config
expectations, and draws the read-only vs mutating (or dispatch best-effort)
boundary. All three MUST pass `openspec validate <slug> --strict` with zero
errors and zero warnings.

Purpose: Lock the GitLab CLI/MR-sink surface, the outbound notification dispatch
surface (Slack/email/confluence), and the MCP tool/profile registry as
machine-checkable contract baselines.
Output: openspec/specs/gitlab-integration/spec.md, openspec/specs/notifications/spec.md, openspec/specs/mcp-integration/spec.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-integrations-cluster/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec the observed v4 behavior of GitLab CLI smoke + MR sink (INT-02)</name>
  <files>openspec/specs/gitlab-integration/spec.md</files>
  <read_first>
    - whilly/cli/gitlab.py (whilly gitlab smoke: READ-ONLY auth+project+repo-hint checks; token precedence GITLAB_TOKEN → GITLAB_API_TOKEN → WHILLY_GITLAB_API_TOKEN → glab config get token; Bearer GET /api/v4/user and /projects; redacted JSON report)
    - whilly/sinks/gitlab_mr.py (open_mr_for_task MUTATING: git push --force then glab mr create; token resolution mirrors CLI; up-to-date→no_diff handling; failure_mode results)
    - openspec/AUTHORING.md
    - openspec/specs/task-model-fsm/spec.md
  </read_first>
  <action>
    Create openspec/specs/gitlab-integration/spec.md for slug `gitlab-integration`. Follow exemplar +
    AUTHORING rules (Purpose ≥50 chars; Requirements with SHALL/MUST first body line ≤500 chars; ≥1
    four-hashtag Scenario each with WHEN/THEN). Reverse-spec ONLY observed v4 behavior: (1) Auth — GitLab
    token SHALL resolve in order GITLAB_TOKEN → GITLAB_API_TOKEN → WHILLY_GITLAB_API_TOKEN → `glab config
    get token -h <host>` fallback, and the host SHALL be derived from the repository URL; reports MUST
    redact tokens/credentials. (2) Read-only boundary — `whilly gitlab smoke` SHALL only perform Bearer
    GET probes (GET /api/v4/user to verify the token, GET /projects/<encoded> for access) and write a
    redacted JSON report; it MUST NOT mutate any GitLab state. (3) Mutating boundary — open_mr_for_task
    SHALL be the only mutating path (git push to the worker-owned feature branch then `glab mr create`),
    MUST treat "everything up-to-date / nothing to push" as a no-diff outcome rather than a hard failure,
    and MUST return a structured failure_mode on push/create errors rather than raising into the loop.
    State the read-only-smoke vs mutating-MR split as an explicit requirement. No fenced code blocks in
    requirement bodies; do not invent behavior absent from the two modules.
  </action>
  <verify>
    <automated>openspec validate gitlab-integration --strict --json</automated>
  </verify>
  <acceptance_criteria>
    - openspec/specs/gitlab-integration/spec.md exists
    - `openspec validate gitlab-integration --strict` reports "is valid" and exits 0
    - `## Purpose` (≥50 chars) and `## Requirements` (≥1 `### Requirement:`) present
    - every requirement's first body line contains SHALL or MUST and is ≤500 chars
    - every requirement has ≥1 `#### Scenario:` with WHEN/THEN bullets
    - at least one requirement states GitLab token resolution / auth expectations
    - at least one requirement states the read-only smoke vs mutating MR-create boundary
    - no `## ADDED Requirements` / delta headers anywhere in the file
  </acceptance_criteria>
  <done>gitlab-integration spec exists, passes strict validation, and captures GitLab auth + read/mutating boundary from real v4 code.</done>
</task>

<task type="auto">
  <name>Task 2: Spec the observed v4 behavior of outbound notification dispatch (INT-05)</name>
  <files>openspec/specs/notifications/spec.md</files>
  <read_first>
    - whilly/core/notifications.py (RunCompletedEvent, NotificationPort protocol, MessageTemplate — pure data/protocol, no I/O)
    - whilly/adapters/notifications/factory.py (make_notifier: builds SlackNotifier only when SLACK_ENABLED + SLACK_ACCESS_TOKEN + SLACK_CHANNEL set, else NullNotifier)
    - whilly/adapters/notifications/slack.py (SlackNotifier.notify_run_completed via urllib_post; constructor-injected token/channel; no env reads)
    - whilly/adapters/notifications/null.py (no-op fallback)
    - whilly/slack_task_notify.py (best-effort Slack webhook for per-task demo runs; WHILLY_SLACK_WEBHOOK_URL / WHILLY_SLACK_ACCESS_TOKEN env + legacy aliases; never blocks)
    - whilly/api/mailer.py (Mailer: SMTP via WHILLY_SMTP_HOST/PORT/USER/PASSWORD/FROM; empty host disables SMTP and falls back to whilly_events.jsonl; never raises)
    - whilly/adapters/confluence/publisher.py (release-doc publishing — auth/config + LOCKED capability assignment)
    - whilly/notifications.py (macOS `say` voice notifications, WHILLY_VOICE gate, noop when unavailable)
    - openspec/AUTHORING.md
    - openspec/specs/task-model-fsm/spec.md
  </read_first>
  <action>
    Create openspec/specs/notifications/spec.md for slug `notifications`. Follow exemplar + AUTHORING rules.
    Scope = OUTBOUND dispatch only (Slack, email, confluence, local voice); reference — do NOT duplicate —
    the events/audit layer. Reverse-spec ONLY observed v4 behavior as separate requirements: (1) Port/factory
    — the NotificationPort SHALL be satisfied by SlackNotifier only when SLACK_ENABLED + SLACK_ACCESS_TOKEN +
    SLACK_CHANNEL are all configured; otherwise make_notifier SHALL return a NullNotifier (no-op). Auth/config
    flows through the factory, not the adapters (adapters perform no env reads). (2) Slack — dispatch SHALL be
    best-effort: a Slack send (token API or webhook) MUST NOT block or raise into the run loop on transport
    failure. (3) Email — the Mailer SHALL attempt SMTP only when WHILLY_SMTP_HOST is set, MUST fall back to
    appending an event to whilly_events.jsonl when SMTP is unconfigured or fails, and MUST never raise.
    (4) Confluence — release-doc publishing SHALL be the confluence-publisher outbound surface with its
    configured credentials. (5) Local voice — `say` notifications SHALL be a no-op when disabled (WHILLY_VOICE)
    or the `say` binary is absent. State per-channel auth/config expectations and the best-effort dispatch
    boundary explicitly (notifications never gate or block orchestration). No fenced code blocks in bodies;
    do not invent behavior absent from the modules.
  </action>
  <verify>
    <automated>openspec validate notifications --strict --json</automated>
  </verify>
  <acceptance_criteria>
    - openspec/specs/notifications/spec.md exists
    - `openspec validate notifications --strict` reports "is valid" and exits 0
    - `## Purpose` (≥50 chars) and `## Requirements` (≥1 `### Requirement:`) present
    - every requirement's first body line contains SHALL or MUST and is ≤500 chars
    - every requirement has ≥1 `#### Scenario:` with WHEN/THEN bullets
    - at least one requirement states per-channel auth/config expectations (Slack token+channel, SMTP env, confluence)
    - at least one requirement states the best-effort outbound-dispatch boundary (never blocks/raises into the loop)
    - no `## ADDED Requirements` / delta headers anywhere in the file
  </acceptance_criteria>
  <done>notifications spec exists, passes strict validation, and captures outbound dispatch auth/config + best-effort boundary from real v4 code.</done>
</task>

<task type="auto">
  <name>Task 3: Spec the observed v4 behavior of the MCP tool/profile registry (INT-06)</name>
  <files>openspec/specs/mcp-integration/spec.md</files>
  <read_first>
    - whilly/mcp/registry.py (MCPRegistry: register_tool/get_tool/list_tools(by category)/list_categories; load_from_json/to_json; global get_registry singleton; MCPTool with provider/url/api_key_env credential field)
    - whilly/mcp/profiles.py (MCPProfileRegistry: register/get/list profiles; MCPProfile to_dict/from_dict; load_from_json/to_json; global get_profile_registry singleton)
    - whilly/mcp/__init__.py (public surface re-exports)
    - openspec/AUTHORING.md
    - openspec/specs/task-model-fsm/spec.md
  </read_first>
  <action>
    Create openspec/specs/mcp-integration/spec.md for slug `mcp-integration`. Follow exemplar + AUTHORING
    rules. Reverse-spec ONLY observed v4 behavior: (1) Tool registry — MCPRegistry SHALL register tools by
    name, index them by category, and expose get_tool / list_tools(category) / list_categories; get_registry
    SHALL return a process-global singleton. (2) Persistence — the registry SHALL load tool definitions from
    a JSON file (load_from_json) and export them (to_json) round-tripping name/description/category/parameters/
    url/provider/api_key_env. (3) Profiles — MCPProfileRegistry SHALL register named profiles that each
    reference a set of tool names, with JSON load/export and a global get_profile_registry singleton. (4) Auth
    expectation — each tool's required credential SHALL be identified by an `api_key_env` env-var name on the
    MCPTool definition (the registry itself holds no secrets; it names the env var the caller must supply).
    State that this capability is a read/registration surface — it defines and discovers tools, it does not
    itself perform mutating external calls. No fenced code blocks in requirement bodies; do not invent
    behavior absent from the three modules.
  </action>
  <verify>
    <automated>openspec validate mcp-integration --strict --json</automated>
  </verify>
  <acceptance_criteria>
    - openspec/specs/mcp-integration/spec.md exists
    - `openspec validate mcp-integration --strict` reports "is valid" and exits 0
    - `## Purpose` (≥50 chars) and `## Requirements` (≥1 `### Requirement:`) present
    - every requirement's first body line contains SHALL or MUST and is ≤500 chars
    - every requirement has ≥1 `#### Scenario:` with WHEN/THEN bullets
    - at least one requirement states the credential expectation (per-tool api_key_env names the required env var)
    - at least one requirement states the registry/discovery boundary (defines/discovers tools; not itself a mutating external caller)
    - no `## ADDED Requirements` / delta headers anywhere in the file
  </acceptance_criteria>
  <done>mcp-integration spec exists, passes strict validation, and captures the tool/profile registry contract + credential expectation from real v4 code.</done>
</task>

</tasks>

<verification>
All three specs pass `openspec validate <slug> --strict` (0 errors, 0 warnings),
each reverse-spec'd from its real v4 modules, stating auth/config expectations and
the read-only vs mutating (or best-effort dispatch / registration) boundary.
</verification>

<success_criteria>
INT-02 (gitlab-integration), INT-05 (notifications), and INT-06 (mcp-integration)
specs exist at openspec/specs/&lt;slug&gt;/spec.md, mirror the exemplar, and pass strict
validation.
</success_criteria>

<output>
Create `.planning/phases/24-integrations-cluster/24-03-SUMMARY.md` when done
</output>
