---
phase: 24-integrations-cluster
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/github-integration/spec.md
autonomous: true
requirements: [INT-03]
must_haves:
  truths:
    - "openspec validate github-integration --strict reports the spec is valid (0 errors, 0 warnings)"
    - "github-integration spec states GitHub auth expectations (gh CLI token resolution: WHILLY_GH_TOKEN/GITHUB_TOKEN/GH_TOKEN/[github].token/keyring) and the read-only vs mutating boundary (issue/PR/CI reads vs PR creation, issue creation, project status writes)"
    - "github-integration requirements are at subsystem-contract altitude (PR sink, projects sync, issue→plan conversion/intake, issue/PR sources, workflow engine, CI poll) — not one requirement per module"
  artifacts:
    - path: "openspec/specs/github-integration/spec.md"
      provides: "INT-03 normative subsystem-altitude capability spec covering the 32 GitHub modules"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/github-integration/spec.md"
      to: "whilly/github_pr.py + sinks/github_pr.py + github_projects.py + github_converter.py + forge/intake.py + sources/github_issues*.py + workflow/* + ci/github.py + gh_utils.py"
      via: "subsystem-contract requirements reverse-spec'd from observed v4 behavior"
      pattern: "SHALL|MUST"
---

<objective>
Write one normative OpenSpec capability spec — `github-integration` (INT-03) —
that captures the contracts of Whilly's broad GitHub subsystem (32 modules) at
SUBSYSTEM altitude. The spec states GitHub auth expectations and the read-only
vs mutating boundary, and MUST pass `openspec validate github-integration --strict`
with zero errors and zero warnings. Per-module accounting stays in the coverage
matrix; this spec captures only the guaranteed subsystem contracts.

Purpose: Lock the GitHub PR/projects/converter/intake/sources/workflow/CI surface
as one machine-checkable contract baseline so future GitHub behavior changes flow
through delta proposals.
Output: openspec/specs/github-integration/spec.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-integrations-cluster/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
@openspec/COVERAGE-MATRIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec the observed v4 behavior of the GitHub subsystem at contract altitude (INT-03)</name>
  <files>openspec/specs/github-integration/spec.md</files>
  <read_first>
    - whilly/gh_utils.py (gh_subprocess_env token resolution: WHILLY_GH_TOKEN → GITHUB_TOKEN, WHILLY_GH_PREFER_KEYRING strips ambient tokens, [github].token from toml; the auth contract for every gh subprocess)
    - whilly/github_pr.py and whilly/sinks/github_pr.py (open_pr_for_task MUTATING: rollback preflight → git push --force-with-lease → gh pr create; PRResult/failure_mode)
    - whilly/github_projects.py (GitHubProjectsConverter: fetch_project_items read; sync_status_changes / _update_project_item_status / _create_github_issue MUTATING)
    - whilly/github_converter.py (fetch_github_issues read; convert_issues_to_tasks / create_whilly_plan / generate_tasks_from_github — issue→plan conversion)
    - whilly/forge/intake.py and whilly/forge/__init__.py (whilly forge intake owner/repo/N: read issue via gh, idempotent insert to Postgres, no Claude tokens, source/done label contract)
    - whilly/sources/github_issues.py and github_issues_and_project.py and github_pr_feedback.py (gh issue list read-only fetch → idempotent merge_into_plan; secret detection)
    - whilly/workflow/__init__.py + base.py + registry.py (BoardSink protocol, LifecycleEvent, WorkflowMapping, available_boards/get_board, event registry)
    - whilly/ci/github.py (GitHubCIPollAdapter: bounded read-only PR status rollup probe; unauthenticated/unavailable explicit results)
    - whilly/hierarchy/github.py (issue hierarchy mapping — read)
    - openspec/COVERAGE-MATRIX.md (which modules belong to github-integration — for grounding the sub-surfaces, NOT per-module requirements)
    - openspec/AUTHORING.md
    - openspec/specs/task-model-fsm/spec.md
  </read_first>
  <action>
    Create openspec/specs/github-integration/spec.md for slug `github-integration`. Mirror the exemplar
    + AUTHORING rules (Purpose ≥50 chars plain prose; Requirements with SHALL/MUST as the FIRST body
    line ≤500 chars; ≥1 four-hashtag `#### Scenario:` per requirement with WHEN/THEN bullets). Keep every
    requirement at SUBSYSTEM-CONTRACT altitude — describe what a sub-surface guarantees, NOT one requirement
    per module. Reverse-spec ONLY observed v4 behavior across these sub-surfaces, as separate requirements:
    (1) Auth — every gh-CLI invocation SHALL resolve its token through gh_subprocess_env (WHILLY_GH_TOKEN
    overrides into GITHUB_TOKEN; WHILLY_GH_PREFER_KEYRING strips ambient GITHUB_TOKEN/GH_TOKEN to force
    keyring; [github].token from whilly.toml; otherwise ambient passthrough). (2) Issue/PR/CI reads (read-only
    boundary) — sources (gh issue list), github_converter.fetch_github_issues, project item fetch, and the
    CI poll adapter SHALL only read GitHub state; fetching SHALL merge issues idempotently into the plan and
    detect/strip secrets; the CI adapter SHALL return explicit unauthenticated/unavailable evidence rather
    than raising. (3) Issue→plan conversion/intake — convert_issues_to_tasks/generate_tasks_from_github and
    forge intake SHALL deterministically map a GitHub issue to a whilly Task/plan, and forge intake MUST be
    idempotent (re-running the same owner/repo/N reuses the existing plan, no duplicate, no Claude tokens).
    (4) Mutating boundary — PR creation (open_pr_for_task: force-with-lease push then gh pr create after a
    rollback preflight), project status writes (sync_status_changes), and issue creation SHALL be the only
    GitHub-mutating operations and MUST surface a structured failure_mode rather than crashing the loop.
    (5) Workflow engine — the BoardSink protocol and LifecycleEvent registry SHALL define the pluggable
    board-mapping contract that drives status sync. State the read-only-vs-mutating split as an explicit
    requirement. No fenced code blocks in requirement bodies. Do not invent behavior absent from the modules.
  </action>
  <verify>
    <automated>openspec validate github-integration --strict --json</automated>
  </verify>
  <acceptance_criteria>
    - openspec/specs/github-integration/spec.md exists
    - `openspec validate github-integration --strict` reports "is valid" and exits 0
    - `## Purpose` (≥50 chars) and `## Requirements` (≥1 `### Requirement:`) present
    - every requirement's first body line contains SHALL or MUST and is ≤500 chars
    - every requirement has ≥1 `#### Scenario:` with WHEN/THEN bullets
    - requirements stay at subsystem-contract altitude (no one-requirement-per-module sprawl)
    - at least one requirement states GitHub auth expectations (gh token resolution chain)
    - at least one requirement states the read-only (issue/PR/CI reads) vs mutating (PR create, issue create, project status write) boundary
    - no `## ADDED Requirements` / delta headers anywhere in the file
  </acceptance_criteria>
  <done>github-integration spec exists, passes strict validation, and normatively captures the GitHub subsystem contracts (auth + read/mutating boundary) at subsystem altitude.</done>
</task>

</tasks>

<verification>
The spec passes `openspec validate github-integration --strict` (0 errors, 0
warnings), stays at subsystem-contract altitude, states GitHub auth expectations,
and draws the read-only vs mutating boundary explicitly.
</verification>

<success_criteria>
INT-03 (github-integration) spec exists at openspec/specs/github-integration/spec.md,
mirrors the exemplar, captures the 32-module subsystem at contract altitude, and
passes strict validation.
</success_criteria>

<output>
Create `.planning/phases/24-integrations-cluster/24-02-SUMMARY.md` when done
</output>
