---
phase: 21-spec-baseline-taxonomy
plan: 04
type: execute
wave: 3
depends_on: ["21-01", "21-02"]
files_modified:
  - openspec/COVERAGE-MATRIX.md
autonomous: true
requirements: [BASE-02]
nyquist_compliant: true

must_haves:
  truths:
    - "openspec/COVERAGE-MATRIX.md has one row for every whilly/ .py module, with the body-row count EQUAL to the LIVE `find whilly/ -name '*.py' -not -path '*/__pycache__/*' | wc -l` count computed at execution time (today 275). The historical figure 242 is NEVER the row-count target."
    - "Every module row names exactly one capability slug from the 32 in TAXONOMY.md, or the literal UNMAPPED — no row is blank (zero silent gaps)"
    - "No module is mapped to two capabilities (zero double-mapping)"
    - "The matrix header reconciles the live module count against the historical 242 figure from REQUIREMENTS.md (242 appears ONLY as a header reconciliation note string, never as a gate target) and states the inclusion policy: all .py files are counted, including __init__.py, __main__.py, and generated Alembic migration version files"
    - "whilly/adapters/confluence/__init__.py and whilly/adapters/confluence/publisher.py are explicitly mapped to the notifications capability, never left UNMAPPED by omission"
    - "All whilly/adapters/db/migrations/versions/*.py files are batch-mapped to state-persistence in one sweep (one rule, not per-file judgment)"
    - "The total number of UNMAPPED rows equals an explicitly header-declared UNMAPPED count (target 0)"
  artifacts:
    - path: "openspec/COVERAGE-MATRIX.md"
      provides: "module -> capability coverage matrix (BASE-02)"
      contains: "whilly/task_manager.py"
  key_links:
    - from: "openspec/COVERAGE-MATRIX.md"
      to: "openspec/TAXONOMY.md"
      via: "every capability slug in the matrix is one of the 32 in the taxonomy"
      pattern: "task-model-fsm"
---

<objective>
Build openspec/COVERAGE-MATRIX.md (BASE-02): a single Markdown table mapping every
whilly/ Python module to exactly one of the 32 capability slugs, with zero silent
gaps (anything pending is the explicit literal UNMAPPED) and zero double-mapping.
The module count MUST be derived programmatically at execution time — the LIVE
`find whilly/ -name "*.py" -not -path "*/__pycache__/*" | wc -l` count (275 today)
is authoritative. The historical 242 figure from REQUIREMENTS.md is a pre-growth,
non-__init__ count; it appears ONLY as a header reconciliation note string and is
NEVER a gate or row-count target. Documentation only — NO whilly/ Python changes.

Purpose: Prove (and make auditable for Phase 28 COV-01) that every module is
accounted for under exactly one capability.
Output: openspec/COVERAGE-MATRIX.md with one explicit row per module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-spec-baseline-taxonomy/21-RESEARCH.md
@.planning/phases/21-spec-baseline-taxonomy/21-VALIDATION.md
@.planning/REQUIREMENTS.md
@openspec/TAXONOMY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate modules and write the count/policy header</name>
  <read_first>
    - .planning/phases/21-spec-baseline-taxonomy/21-RESEARCH.md (sections "Module Count", "Pitfall 6: Double-Counting", "Pitfall 7: Treating __init__.py as Non-Modules", and "Open Questions (RESOLVED)" #1 and #3 — the count discrepancy is RESOLVED: live find count is authoritative, 242 is historical only, generated migration files ARE counted)
    - .planning/REQUIREMENTS.md (BASE-02 wording mentions "242"; this is the HISTORICAL figure for the reconciliation note ONLY — never the row-count target)
  </read_first>
  <action>
    Run `find whilly/ -name "*.py" -not -path "*/__pycache__/*" | sort` and capture the
    full list and its `wc -l` count. This LIVE count (275 today) is authoritative — do
    NOT hardcode 242 or 244 anywhere as a target; the codebase has grown. Write the
    matrix header to openspec/COVERAGE-MATRIX.md stating, explicitly:
    (a) the LIVE total module count (the value of the find | wc -l above);
    (b) the inclusion policy — ALL .py files are counted and mapped, including every
    __init__.py ("package initializer"), __main__.py ("entry shim"), AND the generated
    Alembic migration version files under whilly/adapters/db/migrations/versions/
    (generated migrations ARE counted and are batch-mapped to state-persistence);
    (c) a one-line reconciliation NOTE that the live count differs from the historical
    "242" in REQUIREMENTS.md (codebase grew; pre-growth non-__init__ count; the live
    find count supersedes it). The string "242" appears ONLY here as prose in the note,
    never as a gate target;
    (d) a declared UNMAPPED count line, e.g. "Unmapped: 0 (zero silent gaps)" — Task 2
    must make the actual UNMAPPED row count equal this declared number.
    Do not write the body rows yet — Task 2 fills them.
  </action>
  <verify>
    <automated>LIVE=$(find whilly/ -name "*.py" -not -path "*/__pycache__/*" | wc -l | tr -d ' '); echo "live=$LIVE"; test -f openspec/COVERAGE-MATRIX.md && grep -qF "$LIVE" openspec/COVERAGE-MATRIX.md && grep -qiE 'inclusion|policy|__init__|all \.py|migration' openspec/COVERAGE-MATRIX.md && grep -qiE 'unmapped' openspec/COVERAGE-MATRIX.md && grep -qF '242' openspec/COVERAGE-MATRIX.md && echo HEADER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `openspec/COVERAGE-MATRIX.md` exists with a header containing the LIVE module count: `find whilly/ -name "*.py" -not -path "*/__pycache__/*" | wc -l` is grep-found in the header (this is the authoritative number, today 275).
    - The header states the inclusion policy covering __init__.py / __main__.py / generated migration files — `grep -qiE 'inclusion|policy|__init__|migration' openspec/COVERAGE-MATRIX.md`.
    - The header declares the intended UNMAPPED count — `grep -qiE 'unmapped' openspec/COVERAGE-MATRIX.md` (target 0).
    - The header reconciles against the HISTORICAL 242 figure as a prose note only — `grep -qF '242' openspec/COVERAGE-MATRIX.md` is satisfied by the reconciliation note. The 242 string MUST NOT be used as a row-count gate anywhere in this plan.
  </acceptance_criteria>
  <done>openspec/COVERAGE-MATRIX.md has a header with the live module count (275 today), the inclusion policy (all .py incl. __init__/__main__/migrations), the declared UNMAPPED count, and a reconciliation note against REQUIREMENTS.md's historical 242.</done>
</task>

<task type="auto">
  <name>Task 2: Batch-map known patterns, then resolve the remainder (BASE-02)</name>
  <read_first>
    - openspec/TAXONOMY.md (the 32 valid capability slugs — every row MUST use one of these or the literal UNMAPPED)
    - .planning/phases/21-spec-baseline-taxonomy/21-RESEARCH.md (section "Module -> Capability Pre-Assignment" — the high-confidence first-pass mapping table; AND "Open Questions (RESOLVED)" #3 which locks confluence -> notifications and the migrations -> state-persistence batch sweep)
    - openspec/COVERAGE-MATRIX.md (the header + module list from Task 1)
  </read_first>
  <action>
    Fill openspec/COVERAGE-MATRIX.md with one table row per enumerated module:
    `| <module path> | <capability-slug> | <notes> |`. Reduce the ~275-row judgment
    load by applying rules BEFORE per-file judgment, in this order:
    (1) BATCH SWEEP: every whilly/adapters/db/migrations/versions/*.py file maps to
    state-persistence in one rule (Notes: "generated Alembic migration"). Do not make a
    separate judgment per migration file.
    (2) LOCKED: whilly/adapters/confluence/__init__.py and
    whilly/adapters/confluence/publisher.py map to notifications (outbound external
    dispatch — RESOLVED decision; Notes may record "release-doc publishing"). NEVER
    leave confluence UNMAPPED.
    (3) PRE-ASSIGNMENT TABLE: apply the high-confidence "Module -> Capability
    Pre-Assignment" table from 21-RESEARCH.md for all other known patterns
    (task_manager -> task-model-fsm, orchestrator -> batch-planning, etc.).
    (4) REMAINDER: assign each still-unassigned module to the capability it PRIMARILY
    serves (Pitfall 6: never list a module under two slugs — pick the primary
    consumer). Only if a module's home is genuinely undecided, write the literal
    UNMAPPED in the slug column with a Notes reason — never leave the slug column blank,
    and keep the total UNMAPPED count equal to the header-declared number (target 0).
    Every non-UNMAPPED slug MUST be one of the 32 in TAXONOMY.md. The number of body
    rows MUST equal the LIVE module count from Task 1 (today 275) — never 242. This is a
    one-to-one mapping: each module appears in exactly one row.
  </action>
  <verify>
    <automated>LIVE=$(find whilly/ -name "*.py" -not -path "*/__pycache__/*" | wc -l | tr -d ' '); ROWS=$(grep -c '| whilly/' openspec/COVERAGE-MATRIX.md); echo "live=$LIVE rows=$ROWS"; test "$ROWS" -eq "$LIVE" || { echo "ROW COUNT MISMATCH (must equal live find count, NOT 242)"; exit 1; }; for m in $(find whilly/ -name "*.py" -not -path "*/__pycache__/*" | sort); do grep -qF "| $m " openspec/COVERAGE-MATRIX.md || grep -qF "| $m|" openspec/COVERAGE-MATRIX.md || { echo "MISSING ROW: $m"; exit 1; }; done; ! grep -qF '| whilly/adapters/confluence/publisher.py | UNMAPPED' openspec/COVERAGE-MATRIX.md || { echo "CONFLUENCE LEFT UNMAPPED"; exit 1; }; grep -qF '| whilly/adapters/confluence/publisher.py | notifications' openspec/COVERAGE-MATRIX.md || { echo "CONFLUENCE NOT MAPPED TO notifications"; exit 1; }; DECLARED=$(grep -ioE 'unmapped: *[0-9]+' openspec/COVERAGE-MATRIX.md | grep -oE '[0-9]+' | head -1); ACTUAL=$(grep -c '| UNMAPPED ' openspec/COVERAGE-MATRIX.md); echo "declared_unmapped=$DECLARED actual_unmapped=$ACTUAL"; test "${DECLARED:-0}" -eq "$ACTUAL" || { echo "UNMAPPED COUNT MISMATCH vs header"; exit 1; }; echo MATRIX_OK</automated>
  </verify>
  <acceptance_criteria>
    - Body row count equals the LIVE module count (NOT 242): `grep -c '| whilly/' openspec/COVERAGE-MATRIX.md` equals `find whilly/ -name "*.py" -not -path "*/__pycache__/*" | wc -l` — the verify gate exits 1 with "ROW COUNT MISMATCH" otherwise.
    - Every enumerated module path has its own row (the for-loop over `find` output finds each path; no "MISSING ROW").
    - All whilly/adapters/db/migrations/versions/*.py rows carry the state-persistence slug (batch sweep): `for m in $(find whilly/adapters/db/migrations/versions -name '*.py' -not -path '*/__pycache__/*' 2>/dev/null); do grep -qF "| $m | state-persistence" openspec/COVERAGE-MATRIX.md || { echo "MIGRATION NOT BATCH-MAPPED: $m"; exit 1; }; done`.
    - Confluence is NOT UNMAPPED and IS mapped to notifications: `! grep -qF '| whilly/adapters/confluence/publisher.py | UNMAPPED' openspec/COVERAGE-MATRIX.md` AND `grep -qF '| whilly/adapters/confluence/publisher.py | notifications' openspec/COVERAGE-MATRIX.md`.
    - The actual UNMAPPED row count equals the header-declared UNMAPPED count (ideally 0): `grep -c '| UNMAPPED ' openspec/COVERAGE-MATRIX.md` equals the header `Unmapped:` number.
    - No blank slug column — every row's capability cell is a non-empty token (a taxonomy slug or the literal UNMAPPED).
    - Every non-UNMAPPED slug used is one of the 32 in TAXONOMY.md (spot-check: `grep -oE '\| [a-z-]+ \|' openspec/COVERAGE-MATRIX.md | sort -u` yields only taxonomy slugs).
    - No double-mapping: each module path appears in exactly one row (`for m in $(find whilly/ -name "*.py" -not -path "*/__pycache__/*"); do test $(grep -cF "| $m" openspec/COVERAGE-MATRIX.md) -le 1; done`).
  </acceptance_criteria>
  <done>openspec/COVERAGE-MATRIX.md maps every whilly/ module to exactly one capability slug (or explicit UNMAPPED matching the header count), migrations batch-mapped to state-persistence, confluence mapped to notifications, zero blank rows, zero double-mapping, row count equal to the live module count (NOT 242).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

A single Markdown table under openspec/. No runtime code, no input, no auth surface,
no network. No high or medium application threats.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Information Disclosure | module paths in the matrix | accept | Module paths are non-secret repo file paths already public in git; no tokens/secrets are written |
| T-21-05 | Tampering | silent coverage gaps | mitigate | Row count gated == LIVE find count (never the historical 242); per-module presence loop; blank-slug rows forbidden; explicit non-UNMAPPED gate for confluence; actual UNMAPPED count gated == header-declared count; double-mapping check |
| T-21-07 | Tampering | stale hardcoded 242 target masking growth | mitigate | All gates compute the live find count at execution time; 242 permitted only as a header reconciliation prose note, never as a grep gate target |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `grep -c '| whilly/' openspec/COVERAGE-MATRIX.md` equals the LIVE `find` module count (NOT 242).
- Every module path is present (per-module for-loop gate passes).
- All migration version files batch-mapped to state-persistence; confluence mapped to notifications (explicit non-UNMAPPED gate).
- Actual UNMAPPED row count equals the header-declared UNMAPPED count (target 0).
- No blank slug cells; no double-mapping.
- Header reconciles the live count vs the historical 242 figure (prose note only) and states the inclusion policy including generated migrations.
</verification>

<success_criteria>
- openspec/COVERAGE-MATRIX.md maps every whilly/ module to exactly one capability or explicit UNMAPPED (BASE-02).
- Row count equals the live find count; zero silent gaps, zero double-mapping.
- Confluence -> notifications and migrations -> state-persistence applied as locked rules.
- Count reconciliation (242 historical only) and inclusion policy documented in the header.
</success_criteria>

<output>
Create `.planning/phases/21-spec-baseline-taxonomy/21-04-SUMMARY.md` when done.
</output>
