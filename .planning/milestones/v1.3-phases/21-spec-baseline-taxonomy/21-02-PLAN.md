---
phase: 21-spec-baseline-taxonomy
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - openspec/TAXONOMY.md
  - openspec/specs/
autonomous: true
requirements: [BASE-01]
nyquist_compliant: true

must_haves:
  truths:
    - "openspec/TAXONOMY.md lists exactly 32 capabilities across 6 clusters, each with a kebab-case slug and a one-line purpose, matching the clusters in ROADMAP.md phases 22-27"
    - "A stub directory exists under openspec/specs/ for every one of the 32 capability slugs"
    - "The naming convention (kebab-case, slug = directory name = spec ID) is documented in TAXONOMY.md"
  artifacts:
    - path: "openspec/TAXONOMY.md"
      provides: "Capability taxonomy index (BASE-01)"
      contains: "task-model-fsm"
    - path: "openspec/specs/task-model-fsm"
      provides: "One of 32 capability stub directories"
  key_links:
    - from: "openspec/TAXONOMY.md"
      to: "openspec/specs/<slug>/"
      via: "each table row's slug names an existing stub directory"
      pattern: "openspec/specs"
---

<objective>
Formalize the locked capability taxonomy (BASE-01): write openspec/TAXONOMY.md as the
32-capability index and create a stub directory under openspec/specs/ for every slug.
The taxonomy is NOT invented here — it is copied verbatim from the locked list in
21-RESEARCH.md / REQUIREMENTS.md / ROADMAP.md phases 22-27. Documentation/scaffolding
only — NO whilly/ Python changes.

Purpose: Give every later phase a single authoritative index of which capabilities
exist and where their spec files live.
Output: openspec/TAXONOMY.md + 32 empty capability stub directories under openspec/specs/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-spec-baseline-taxonomy/21-RESEARCH.md
@.planning/phases/21-spec-baseline-taxonomy/21-VALIDATION.md
@openspec/AUTHORING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write openspec/TAXONOMY.md capability index (BASE-01)</name>
  <read_first>
    - .planning/phases/21-spec-baseline-taxonomy/21-RESEARCH.md (section "Capability Taxonomy (BASE-01)" — the locked 32-capability table with slug + one-line purpose, and the "Naming Convention" / "Taxonomy Index Format" subsections)
    - .planning/ROADMAP.md (phases 22-27 success criteria — cross-check that the slug clusters match)
    - openspec/AUTHORING.md (slug naming convention reference, from plan 21-01)
  </read_first>
  <action>
    Create openspec/TAXONOMY.md indexing exactly the 32 capabilities locked in the
    "Capability Taxonomy (BASE-01)" table of 21-RESEARCH.md. Group them under the 6
    cluster headings with their counts: Orchestration (7: orchestration-loop,
    task-model-fsm, plan-json-contract, batch-planning, agent-dispatch,
    worktree-isolation, result-collection); PRD Pipeline (5: prd-generation,
    prd-wizard, task-generation, decomposition, decision-gate); Integrations (6:
    jira-integration, gitlab-integration, github-integration, jira-watcher-daemon,
    notifications, mcp-integration); Operator Surface (5: dashboard-tui,
    web-status-ui, reporting, cli-surface, operator-views-logs); Platform (5:
    configuration, auth-security, scheduling, state-persistence, self-update-doctor);
    Safety (4: budget-resource-guards, recovery-self-healing, quality-compliance-audit,
    verification-gates). Each capability gets a Markdown table row: `| <slug> | <one-line purpose> |`
    using the purpose text from the research table. Include a header stating "32
    capabilities across 6 clusters" and a Naming Convention section: slugs are
    kebab-case, slug = openspec/specs/<slug>/ directory name = spec ID. Cross-reference
    AUTHORING.md for the spec format and note that each slug maps to one future spec.md.
    Do NOT add, rename, merge, or drop any slug versus the research table. Keep lines <= 120 chars.
  </action>
  <verify>
    <automated>test -f openspec/TAXONOMY.md && grep -c '^| `' openspec/TAXONOMY.md && for s in orchestration-loop task-model-fsm plan-json-contract batch-planning agent-dispatch worktree-isolation result-collection prd-generation prd-wizard task-generation decomposition decision-gate jira-integration gitlab-integration github-integration jira-watcher-daemon notifications mcp-integration dashboard-tui web-status-ui reporting cli-surface operator-views-logs configuration auth-security scheduling state-persistence self-update-doctor budget-resource-guards recovery-self-healing quality-compliance-audit verification-gates; do grep -q "$s" openspec/TAXONOMY.md || { echo "MISSING $s"; exit 1; }; done && echo "ALL 32 PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f openspec/TAXONOMY.md` succeeds.
    - All 32 exact slugs from the research table appear in openspec/TAXONOMY.md (the for-loop above prints "ALL 32 PRESENT" with exit 0).
    - `grep -c 'Orchestration\|PRD Pipeline\|Integrations\|Operator Surface\|Platform\|Safety' openspec/TAXONOMY.md` shows all 6 cluster headings present.
    - `grep -qi 'kebab' openspec/TAXONOMY.md` succeeds (naming convention documented).
  </acceptance_criteria>
  <done>openspec/TAXONOMY.md lists all 32 locked capability slugs with one-line purposes under 6 clusters, with the kebab-case naming convention documented.</done>
</task>

<task type="auto">
  <name>Task 2: Create 32 capability stub directories under openspec/specs/</name>
  <read_first>
    - openspec/TAXONOMY.md (the authoritative slug list just written in Task 1)
    - .planning/phases/21-spec-baseline-taxonomy/21-RESEARCH.md (section "Physical Location of a Capability Spec" — slug = directory name)
  </read_first>
  <action>
    Create one stub directory under openspec/specs/ for each of the 32 slugs listed in
    openspec/TAXONOMY.md (orchestration-loop, task-model-fsm, plan-json-contract,
    batch-planning, agent-dispatch, worktree-isolation, result-collection,
    prd-generation, prd-wizard, task-generation, decomposition, decision-gate,
    jira-integration, gitlab-integration, github-integration, jira-watcher-daemon,
    notifications, mcp-integration, dashboard-tui, web-status-ui, reporting,
    cli-surface, operator-views-logs, configuration, auth-security, scheduling,
    state-persistence, self-update-doctor, budget-resource-guards,
    recovery-self-healing, quality-compliance-audit, verification-gates). Because git
    does not track empty directories and OpenSpec ignores directories with no spec.md,
    place a single `.gitkeep` file in each stub directory so the scaffold survives
    commit. Do NOT create spec.md files in any stub here — those belong to phases 22-27
    (and task-model-fsm's exemplar spec.md is written in plan 21-03). Exactly 32
    directories, no more, no fewer; directory names MUST exactly match the slugs.
  </action>
  <verify>
    <automated>count=$(ls -d openspec/specs/*/ 2>/dev/null | wc -l | tr -d ' '); echo "dir count=$count"; test "$count" -eq 32 && for s in orchestration-loop task-model-fsm plan-json-contract batch-planning agent-dispatch worktree-isolation result-collection prd-generation prd-wizard task-generation decomposition decision-gate jira-integration gitlab-integration github-integration jira-watcher-daemon notifications mcp-integration dashboard-tui web-status-ui reporting cli-surface operator-views-logs configuration auth-security scheduling state-persistence self-update-doctor budget-resource-guards recovery-self-healing quality-compliance-audit verification-gates; do test -d "openspec/specs/$s" || { echo "MISSING dir $s"; exit 1; }; done && echo "ALL 32 DIRS PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - `ls -d openspec/specs/*/ | wc -l` equals exactly 32.
    - Every one of the 32 slug directories exists under openspec/specs/ (the for-loop prints "ALL 32 DIRS PRESENT" with exit 0).
    - Each stub directory contains a `.gitkeep` (`find openspec/specs -name .gitkeep | wc -l` equals 32).
    - No spec.md files exist yet under the stub directories except none required by this plan (`find openspec/specs -name spec.md | wc -l` equals 0 after this plan; task-model-fsm/spec.md is added by plan 21-03).
  </acceptance_criteria>
  <done>Exactly 32 capability stub directories exist under openspec/specs/, each with a .gitkeep, names matching the taxonomy slugs, no spec.md files yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Docs + empty directory scaffolding under openspec/. No runtime code, no input, no
auth surface, no network. No high or medium application threats.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02 | Tampering | openspec/TAXONOMY.md slug set | accept | Slugs are copied verbatim from the locked research table; the 32-slug grep gate detects drift |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `ls -d openspec/specs/*/ | wc -l` equals 32.
- All 32 slugs present in both TAXONOMY.md and as directories (the two for-loop gates pass).
- Taxonomy clusters/counts match ROADMAP.md phases 22-27 (manual cross-read).
</verification>

<success_criteria>
- openspec/TAXONOMY.md indexes all 32 capabilities with slug + one-line purpose under 6 clusters (BASE-01).
- 32 stub directories exist under openspec/specs/ matching the slugs (BASE-01).
- Naming convention documented; no slug drift from the locked taxonomy.
</success_criteria>

<output>
Create `.planning/phases/21-spec-baseline-taxonomy/21-02-SUMMARY.md` when done.
</output>
