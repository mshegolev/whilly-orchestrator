---
phase: 22-orchestration-cluster
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/plan-json-contract/spec.md
  - openspec/specs/result-collection/spec.md
autonomous: true
requirements: [ORCH-03, ORCH-07]
must_haves:
  truths:
    - "openspec/specs/plan-json-contract/spec.md exists and passes openspec validate plan-json-contract --strict"
    - "openspec/specs/result-collection/spec.md exists and passes openspec validate result-collection --strict"
    - "plan-json-contract enumerates the required task fields (id, phase, category, priority, description, status) and the {project, tasks[]} plan envelope plus the v4 extensions (plan_id, origin/PlanOrigin, repo_targets/RepoTarget, verification_commands/VerificationCommand) matching plan_io.py and task_manager.py"
    - "plan-json-contract specifies atomic writes (temp-then-os.replace), round-trip drop-extra-keys tolerance (Task.from_dict ignores unknown keys), and that validate_schema (in whilly/cli/__init__.py) validates EVERY task id"
    - "result-collection specifies AgentResult(output, usage, exit_code, is_complete) parsing and the <promise>COMPLETE</promise> completion signal"
  artifacts:
    - path: "openspec/specs/plan-json-contract/spec.md"
      provides: "Normative spec of the plan JSON contract (ORCH-03)"
      contains: "## Requirements"
    - path: "openspec/specs/result-collection/spec.md"
      provides: "Normative spec of agent result collection (ORCH-07)"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/plan-json-contract/spec.md"
      to: "whilly/adapters/filesystem/plan_io.py (parse_plan/serialize_plan) + whilly/task_manager.py (Task) + whilly/cli/__init__.py (validate_schema)"
      via: "reverse-spec'd field + envelope + validation requirements"
      pattern: "SHALL|MUST"
    - from: "openspec/specs/result-collection/spec.md"
      to: "whilly/adapters/runner/result_parser.py (AgentResult)"
      via: "reverse-spec'd parsing + completion-signal requirements"
      pattern: "SHALL|MUST"
---

<objective>
Author two independent normative OpenSpec specs: `plan-json-contract` (ORCH-03) and
`result-collection` (ORCH-07), each reverse-spec'd from the real v4 code.

Purpose: Make the plan-on-disk contract and the agent-result parsing contract explicit and machine-checkable.
Output: Two strict-valid spec.md files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-orchestration-cluster/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write plan-json-contract spec.md (ORCH-03)</name>
  <files>openspec/specs/plan-json-contract/spec.md</files>
  <read_first>
    - whilly/adapters/filesystem/plan_io.py (parse_plan / parse_plan_dict / serialize_plan; _REQUIRED_PLAN_FIELDS = ("project", "tasks"); _REQUIRED_TASK_FIELDS = ("status", "priority", "description"); the per-task non-empty id check; optional plan_id; origin/PlanOrigin, repo_targets/RepoTarget, verification_commands/VerificationCommand validation; PlanParseError shapes; the import->export->import round-trip determinism)
    - whilly/task_manager.py (Task dataclass — REAL field set: no-default required fields id, phase, category, priority, description, status; defaulted fields dependencies, key_files, acceptance_criteria, test_steps, prd_requirement, plus optional integration fields; Task.from_dict ignores unknown keys; Task.to_dict round-trip; TaskManager.save atomic temp-then-os.replace write; TaskManager.reload tolerance of partial concurrent writes)
    - whilly/cli/__init__.py (validate_schema — lives HERE, NOT task_manager.py; iterates over EVERY task in the array and validates each id via validate_task_id; no first-N limit)
    - whilly/core/models.py (Plan, PlanOrigin, RepoTarget, VerificationCommand, Priority, TaskStatus — the v4 domain shapes serialize_plan emits)
    - openspec/AUTHORING.md (format + strict-validation rules — MUST follow exactly)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar — copy its structure)
  </read_first>
  <action>
    READ the source modules above FIRST and reverse-spec the OBSERVED v4 behavior. Do NOT carry
    over the v3 CLAUDE.md claim that `validate_schema` "checks only the first 3 tasks" — that is
    FALSE in v4: `validate_schema` is in `whilly/cli/__init__.py` and validates the id of EVERY task
    in the array. Write `openspec/specs/plan-json-contract/spec.md` for the slug
    `plan-json-contract`, mirroring the exemplar: `## Purpose` (plain prose, >=50 chars), then
    `## Requirements` with `### Requirement: <name>` blocks. Every requirement's first body line
    contains SHALL or MUST; every requirement has >=1 `#### Scenario:` (4 hashtags) with
    `- **WHEN**` / `- **THEN**`.
    Capture: (1) the v4 plan envelope — required plan fields `project` and `tasks` (a JSON array),
    plus the OPTIONAL v4 extensions `plan_id`, `origin` (PlanOrigin), `repo_targets` (RepoTarget
    list), and `verification_commands` (VerificationCommand list); parse_plan raises a PlanParseError
    naming the offending field on bad shape. (2) the required per-task fields — ground them in the
    real `Task` dataclass: the no-default (required) fields are `id`, `phase`, `category`,
    `priority`, `description`, `status`; the defaulted fields are `dependencies`, `key_files`,
    `acceptance_criteria`, `test_steps`, `prd_requirement` (and optional integration / repo_target
    fields). Note the v4 parser's own per-task minimum (a non-empty `id` plus `status`, `priority`,
    `description`). (3) `priority` is one of critical|high|medium|low. (4) atomic writes —
    `TaskManager.save` writes to a temp file then `os.replace`s it into place so a crash never
    leaves a half-written plan. (5) round-trip tolerance — `Task.from_dict` ignores unknown on-disk
    keys, so extra keys are tolerated on read but DROPPED on `to_dict` round-trip. (6) validation —
    `validate_schema` validates the id of EVERY task (no first-N limit). Keep `status` value detail
    out — reference `task-model-fsm` for legal statuses rather than re-listing them. Keep each
    requirement body under 500 chars. No fenced code blocks in the spec body.
  </action>
  <verify>
    <automated>cd /opt/develop/whilly-orchestrator && PATH="$HOME/.reflex/.nvm/versions/node/v20.19.6/bin:$PATH" openspec validate plan-json-contract --strict</automated>
  </verify>
  <acceptance_criteria>
    - The file openspec/specs/plan-json-contract/spec.md exists and is non-empty.
    - `openspec validate plan-json-contract --strict` prints "is valid" and exits 0 (zero errors/warnings).
    - The spec contains `## Purpose` (>=50 chars) and `## Requirements`; every requirement has SHALL/MUST on the first body line and >=1 `#### Scenario:`.
    - A requirement enumerates the required task fields (id, phase, category, priority, description, status) and the {project, tasks[]} envelope plus the v4 extensions (plan_id, origin, repo_targets, verification_commands).
    - A requirement captures atomic writes (temp-then-os.replace), round-trip drop-extra-keys tolerance, and that validate_schema validates EVERY task id (no first-3-tasks limit).
    - The spec asserts NO "first 3 tasks only" validation behavior.
  </acceptance_criteria>
  <done>plan-json-contract/spec.md authored, normative, grounded in v4 plan_io + Task + validate_schema, passing strict validation.</done>
</task>

<task type="auto">
  <name>Task 2: Write result-collection spec.md (ORCH-07)</name>
  <files>openspec/specs/result-collection/spec.md</files>
  <read_first>
    - whilly/adapters/runner/result_parser.py (AgentResult dataclass: output, usage, exit_code, is_complete; AgentUsage; COMPLETION_MARKER = "<promise>COMPLETE</promise>"; parse_output defensive fallback for empty/malformed/plaintext stdout; _normalise_payload for object vs stream-event-array shapes; is_complete set iff COMPLETION_MARKER present in output/stdout)
    - whilly/worker/local.py (how the loop consumes is_complete + exit_code to route the outcome — context only)
    - openspec/AUTHORING.md (format + strict-validation rules — MUST follow exactly)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar — copy its structure)
  </read_first>
  <action>
    READ the source modules above FIRST; ground every requirement in observed v4 behavior. Write
    `openspec/specs/result-collection/spec.md` for the slug `result-collection`, mirroring the
    exemplar structure. Every requirement's first body line contains SHALL or MUST; every
    requirement has >=1 `#### Scenario:` with `- **WHEN**` / `- **THEN**`.
    Capture: parsing of the Claude CLI `--output-format json` stdout into an immutable `AgentResult`
    carrying `output`, `usage` (AgentUsage: token counters + cost_usd + num_turns + duration_ms),
    `exit_code` (threaded in by the subprocess wrapper, not parsed from stdout), and `is_complete`;
    normalisation of both the single-object envelope and the newer stream-event-array form (the
    final `{"type": "result", ...}` event is used); the completion signal — `is_complete` is True
    iff `COMPLETION_MARKER` (`<promise>COMPLETE</promise>`) appears in the result text; and the
    defensive fallback contract — empty / malformed / plaintext / usage-less stdout NEVER raises,
    instead yielding a valid `AgentResult` with zeroed `AgentUsage` and the raw stdout as `output`
    (and still scanning that raw stdout for the completion marker). DO NOT specify retry/backoff or
    the FSM transition to DONE here — reference task-model-fsm and Phase 27 recovery for those; this
    spec owns only the parse-and-detect-completion contract. Keep each requirement body under 500
    chars. No fenced code blocks in the spec body.
  </action>
  <verify>
    <automated>cd /opt/develop/whilly-orchestrator && PATH="$HOME/.reflex/.nvm/versions/node/v20.19.6/bin:$PATH" openspec validate result-collection --strict</automated>
  </verify>
  <acceptance_criteria>
    - The file openspec/specs/result-collection/spec.md exists and is non-empty.
    - `openspec validate result-collection --strict` prints "is valid" and exits 0 (zero errors/warnings).
    - The spec contains `## Purpose` (>=50 chars) and `## Requirements`; every requirement has SHALL/MUST on the first body line and >=1 `#### Scenario:`.
    - A requirement specifies AgentResult parsing with output, usage, exit_code, and is_complete (including the defensive no-raise fallback).
    - A requirement specifies the <promise>COMPLETE</promise> completion signal.
  </acceptance_criteria>
  <done>result-collection/spec.md authored, normative, passing strict validation.</done>
</task>

</tasks>

<verification>
Run `openspec validate plan-json-contract --strict` and `openspec validate result-collection --strict`
— both must report 0 errors and 0 warnings. Confirm neither file contains delta headers.
Confirm plan-json-contract contains NO "first 3 tasks" validation claim.
</verification>

<success_criteria>
ORCH-03 and ORCH-07 satisfied: both specs are normative, testable, grounded in real v4 code, and pass strict validation.
</success_criteria>

<output>
Create `.planning/phases/22-orchestration-cluster/22-02-SUMMARY.md` when done.
</output>
