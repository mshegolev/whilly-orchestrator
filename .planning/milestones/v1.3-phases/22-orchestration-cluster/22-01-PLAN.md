---
phase: 22-orchestration-cluster
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/orchestration-loop/spec.md
autonomous: true
requirements: [ORCH-01]
must_haves:
  truths:
    - "openspec/specs/orchestration-loop/spec.md exists and is non-empty"
    - "openspec validate orchestration-loop --strict exits 0 with zero errors and zero warnings"
    - "The spec describes the v4 worker-claim iteration: claim_task (PENDING -> CLAIMED) -> start_task (CLAIMED -> IN_PROGRESS) -> run agent -> route outcome (complete_task on is_complete+exit 0, else fail_task)"
    - "The spec describes the run-command composition root (run_run_command -> _async_run): open asyncpg pool, register the worker row, load the plan via _select_plan_with_tasks, build a workspace-aware runner, run the worker loop, close the pool in finally"
    - "The spec describes the idle path (claim_task returns None -> sleep idle_wait -> poll again) and the termination paths (stop event / max_iterations / outer cancellation), and the VersionConflictError lose-the-race -> log-and-continue behavior"
    - "The spec references task-model-fsm for FSM detail and does NOT re-specify status transitions or budget thresholds"
  artifacts:
    - path: "openspec/specs/orchestration-loop/spec.md"
      provides: "Normative spec of the v4 worker-claim orchestration loop and run composition root (ORCH-01)"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/orchestration-loop/spec.md"
      to: "whilly/worker/local.py::run_local_worker and whilly/cli/run.py::_async_run"
      via: "reverse-spec'd requirements grounded in the real v4 worker-claim iteration code"
      pattern: "SHALL|MUST"
---

<objective>
Author the normative OpenSpec capability spec for `orchestration-loop` (ORCH-01),
reverse-spec'd from the REAL v4 worker-claim run path: the `whilly run` composition root
(`whilly/cli/run.py::run_run_command` -> `_async_run`) and the worker loop it drives
(`whilly/worker/local.py::run_local_worker`).

Purpose: Make the v4 per-iteration claim/dispatch/route guarantees explicit and machine-checkable.
Output: `openspec/specs/orchestration-loop/spec.md` passing `openspec validate orchestration-loop --strict`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-orchestration-cluster/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write orchestration-loop spec.md (ORCH-01)</name>
  <files>openspec/specs/orchestration-loop/spec.md</files>
  <read_first>
    - whilly/cli/run.py (run_run_command -> _async_run — the v4 composition root: pool open, worker registration INSERT, _select_plan_with_tasks load, workspace_runner build, run_worker call, close_pool in finally; exit codes EXIT_OK / EXIT_ENVIRONMENT_ERROR; _PlanNotFoundError mapping)
    - whilly/worker/local.py (run_local_worker — the documented loop contract: claim_task -> start_task -> runner -> outcome routing; the idle-wait poll path; the three termination paths; VersionConflictError log-and-continue)
    - whilly/cli/plan.py (run_plan_command / _select_plan_with_tasks — plan + tasks materialised from Postgres; the 0/1/2 exit-code split)
    - whilly/core/models.py (Plan, Task, WorkerId, TaskStatus — the domain shapes the loop transitions)
    - whilly/core/gates.py, whilly/core/governance.py, whilly/core/task_id.py (domain models touched at the edges of the loop — reference, do not re-spec)
    - whilly/llm_ops.py, whilly/llm_otel.py (LLM session / tracing plumbing wrapped around the runner call)
    - openspec/AUTHORING.md (format + strict-validation rules — MUST follow exactly)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar — copy its structure)
  </read_first>
  <action>
    READ the source modules above FIRST and reverse-spec the OBSERVED v4 behavior — no
    aspirational or descriptive-only claims, and do NOT carry over any v3 narrative from CLAUDE.md.
    There is NO `run_plan` function and NO `_original_cwd`/`chdir` invariant in v4 — do not spec
    them. The v4 run path is the async worker-claim model.
    Write `openspec/specs/orchestration-loop/spec.md` for the slug `orchestration-loop`, mirroring
    the exemplar exactly: a `## Purpose` section of plain prose (>=50 chars, no headings/code
    blocks), then `## Requirements` with one or more `### Requirement: <name>` blocks. Each
    requirement's FIRST body line MUST contain SHALL or MUST (case-sensitive), and each requirement
    MUST have >=1 `#### Scenario:` (exactly 4 hashtags) with `- **WHEN**` / `- **THEN**` bullets
    (`- **AND**` optional). Avoid should/may/might/can.
    Capture these GUARANTEED v4 behaviors observed in the code: (1) the per-iteration ordering of
    `run_local_worker` — `repo.claim_task(worker_id, plan_id)` atomically grabs one PENDING task and
    transitions it to CLAIMED (returns None when the queue is empty); `repo.start_task` flips
    CLAIMED -> IN_PROGRESS; the runner is invoked with the prompt from `build_task_prompt`; the
    outcome is routed by `is_complete` AND `exit_code` (is_complete=True and exit_code==0 ->
    `complete_task`; otherwise `fail_task` with a reason carrying the exit code and a truncated
    stdout snippet). (2) the idle path — when `claim_task` returns None the loop sleeps `idle_wait`
    seconds and polls again. (3) the termination precedence — graceful `stop` event (in-flight task
    released back to PENDING via `release_task`), then `max_iterations`, then outer cancellation.
    (4) the optimistic-locking race rule — on `VersionConflictError` the loop logs and continues
    rather than crashing, because another writer has taken responsibility for the row. (5) the
    composition-root guarantees of `_async_run` — open the asyncpg pool, register the worker row
    (idempotent INSERT ... ON CONFLICT), load plan+tasks via `_select_plan_with_tasks` (missing
    plan -> environment-error exit), build a workspace-aware runner that prepares the task workspace
    before dispatch, run `run_worker`, and always `close_pool` in the `finally`.
    Boundary discipline (per CONTEXT.md): DO NOT re-specify the task status FSM or its transitions —
    reference the `task-model-fsm` capability instead. DO NOT specify budget thresholds / kill
    semantics or verification-command gates — those belong to Phase 27 capabilities; reference, do
    not duplicate. DO NOT specify the workspace directory layout (that is `worktree-isolation`) or
    the result parsing internals (that is `result-collection`) beyond naming the seam. Keep each
    requirement body under 500 characters; split if longer. Do not place fenced code blocks or full
    file contents in the spec body beyond what the exemplar uses (it uses none).
  </action>
  <verify>
    <automated>cd /opt/develop/whilly-orchestrator && PATH="$HOME/.reflex/.nvm/versions/node/v20.19.6/bin:$PATH" openspec validate orchestration-loop --strict</automated>
  </verify>
  <acceptance_criteria>
    - The file openspec/specs/orchestration-loop/spec.md exists and is non-empty.
    - `openspec validate orchestration-loop --strict` prints "is valid" and exits 0 (zero errors, zero warnings).
    - The spec contains a `## Purpose` section (>=50 chars) and a `## Requirements` section.
    - Every `### Requirement:` has SHALL or MUST on its first body line and >=1 `#### Scenario:`.
    - A requirement captures the worker-claim iteration order: claim_task (PENDING -> CLAIMED) -> start_task (CLAIMED -> IN_PROGRESS) -> run agent -> route outcome (complete_task vs fail_task on is_complete + exit_code).
    - A requirement captures the run-command composition root (_async_run): pool open, worker registration, plan load, workspace-aware runner, run_worker, close_pool in finally.
    - A requirement captures the idle-wait poll path and the termination precedence (stop event / max_iterations / outer cancellation) plus the VersionConflictError log-and-continue rule.
    - The spec references task-model-fsm rather than re-specifying status transitions; no budget thresholds are duplicated; no run_plan or _original_cwd invariant is asserted.
  </acceptance_criteria>
  <done>orchestration-loop/spec.md authored, normative, grounded in the v4 worker-claim run path, and passing strict validation.</done>
</task>

</tasks>

<verification>
Run `openspec validate orchestration-loop --strict` — must report 0 errors and 0 warnings.
Confirm no delta headers (`## ADDED Requirements` etc.) appear in the file.
Confirm the spec contains NO reference to `run_plan` or `_original_cwd` (removed v3 behaviors).
</verification>

<success_criteria>
ORCH-01 satisfied: orchestration-loop has a normative, testable spec.md grounded in the v4 worker-claim
run path (cli/run.py::_async_run + worker/local.py::run_local_worker) and passing
`openspec validate orchestration-loop --strict`.
</success_criteria>

<output>
Create `.planning/phases/22-orchestration-cluster/22-01-SUMMARY.md` when done.
</output>
