---
phase: 26-platform-cluster
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/scheduling/spec.md
autonomous: true
requirements: [PLAT-03]
must_haves:
  truths:
    - "openspec validate scheduling --strict reports the spec is valid (0 errors, 0 warnings)"
    - "The spec covers scheduler rules/cycles, JQL execution, deduplication, webhooks, rate limiting, and the Postgres-backed scheduler repository"
  artifacts:
    - path: "openspec/specs/scheduling/spec.md"
      provides: "Normative scheduling capability spec reverse-spec'd from whilly/scheduler/* + core/scheduler.py + cli/scheduler.py"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/scheduling/spec.md"
      to: "whilly/scheduler/worker.py + jql_executor.py"
      via: "reverse-spec of scheduler cycle execution + JQL evaluation"
      pattern: "schedule|jql|cycle"
---

<objective>
Author the normative OpenSpec capability spec for `scheduling` (PLAT-03),
reverse-spec'd from the real v4 scheduler subsystem.

Purpose: Capture Whilly's guaranteed scheduling behavior — rules/cycles, JQL
execution, deduplication, webhooks, rate limiting, and the Postgres-backed
repository — as a machine-checkable spec.
Output: `openspec/specs/scheduling/spec.md` passing `openspec validate
scheduling --strict`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-platform-cluster/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec the observed v4 behavior of the scheduler subsystem</name>
  <files>openspec/specs/scheduling/spec.md</files>
  <read_first>
    - whilly/scheduler/worker.py (scheduler cycle execution loop)
    - whilly/scheduler/models.py (rule/cycle domain models)
    - whilly/scheduler/config.py (scheduler configuration)
    - whilly/scheduler/jql_executor.py (JQL evaluation)
    - whilly/scheduler/deduplicator.py (dedup of scheduled work)
    - whilly/scheduler/webhooks.py (webhook dispatch)
    - whilly/scheduler/rate_limit.py (rate limiting)
    - whilly/scheduler/repository.py, sql_repository.py (Postgres-backed persistence)
    - whilly/scheduler/metrics.py, docs.py
    - whilly/core/scheduler.py (core scheduler model)
    - whilly/cli/scheduler.py (scheduler CLI surface)
    - openspec/AUTHORING.md (format rules — read in full)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar to mirror)
  </read_first>
  <action>
    Reverse-spec the observed v4 behavior of the scheduling capability. Write a
    `## Purpose` (≥50 chars) describing what the scheduler governs and its scope.

    Then write `## Requirements` with `### Requirement:` blocks (each FIRST body
    line begins with SHALL or MUST, ≤500 chars; each has ≥1 `#### Scenario:`
    with WHEN/THEN). Cover at minimum:
    - Scheduler rules and cycles: how a rule defines a recurring cycle and how
      the worker loop evaluates due rules (scheduler/worker.py + models.py).
    - JQL execution: how a rule's JQL query is executed to discover work
      (jql_executor.py).
    - Deduplication: how already-scheduled / already-seen work is suppressed
      from re-dispatch (deduplicator.py).
    - Webhook dispatch on scheduled events (webhooks.py).
    - Rate limiting of scheduled executions / external calls (rate_limit.py).
    - Postgres-backed scheduler repository persistence of rules + cycles
      (repository.py / sql_repository.py) — describe it as the persistence
      backing, consistent with the v4 Postgres state layer.
    - Scheduler CLI surface (cli/scheduler.py) for managing rules.

    Mirror the exemplar's section shape exactly. Do NOT use any delta header.
    Do NOT inline fenced code blocks as requirement bodies — use normative
    prose.
  </action>
  <verify>
    <automated>openspec validate scheduling --strict</automated>
  </verify>
  <acceptance_criteria>
    - `openspec/specs/scheduling/spec.md` exists.
    - `openspec validate scheduling --strict` reports the spec is valid
      (exit 0, 0 errors, 0 warnings).
    - Spec has `## Purpose` (≥50 chars) and `## Requirements` with ≥1
      `### Requirement:`; every requirement's first body line contains SHALL or
      MUST and has ≥1 `#### Scenario:` with WHEN/THEN bullets.
    - Spec covers rules/cycles, JQL execution, dedup, webhooks, rate limiting,
      and the Postgres-backed scheduler repository.
    - No delta headers present in the file.
  </acceptance_criteria>
  <done>scheduling spec authored, reverse-spec'd from real v4 scheduler code, and passes openspec validate scheduling --strict.</done>
</task>

</tasks>

<verification>
- `openspec validate scheduling --strict` → valid.
- All scheduling-mapped modules in the coverage matrix (scheduler/*,
  core/scheduler.py, cli/scheduler.py) are accounted for by the `scheduling`
  capability.
</verification>

<success_criteria>
The `scheduling` capability has a normative spec covering rules/cycles, JQL
execution, dedup, webhooks, rate limiting, and Postgres-backed repository —
passing `openspec validate scheduling --strict`.
</success_criteria>

<output>
Create `.planning/phases/26-platform-cluster/26-03-SUMMARY.md` when done.
</output>
