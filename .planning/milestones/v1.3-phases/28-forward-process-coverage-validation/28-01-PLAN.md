---
phase: 28-forward-process-coverage-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/FORWARD-PROCESS.md
  - openspec/project.md
  - openspec/AUTHORING.md
  - CLAUDE.md
  - AGENTS.md
autonomous: true
requirements: [FWD-01, FWD-02]
must_haves:
  truths:
    - "A contributor reading openspec/FORWARD-PROCESS.md learns that any whilly/ behavior change requires an opsx proposal (propose -> apply -> archive) updating the relevant openspec/specs/<slug>/spec.md before/with the code change."
    - "openspec/project.md and openspec/AUTHORING.md both reference FORWARD-PROCESS.md so a spec author lands on the forward workflow."
    - "CLAUDE.md 'When editing' REQUIRES (not 'should') an opsx spec delta for any behavior change and points at openspec/specs/."
    - "AGENTS.md carries the same require-a-spec-delta rule and points at openspec/specs/."
  artifacts:
    - path: "openspec/FORWARD-PROCESS.md"
      provides: "Forward delta-only workflow doc (propose/apply/archive, OpenSpec=WHAT, GSD=HOW)"
      min_lines: 40
      contains: "propose"
    - path: "CLAUDE.md"
      provides: "Strengthened 'When editing' rule requiring opsx spec delta"
      contains: "openspec/specs/"
    - path: "AGENTS.md"
      provides: "Spec-delta requirement rule for agents"
      contains: "openspec/specs/"
  key_links:
    - from: "openspec/project.md"
      to: "openspec/FORWARD-PROCESS.md"
      via: "markdown reference"
      pattern: "FORWARD-PROCESS"
    - from: "openspec/AUTHORING.md"
      to: "openspec/FORWARD-PROCESS.md"
      via: "markdown reference"
      pattern: "FORWARD-PROCESS"
---

<objective>
Document the forward delta-only process and make the contributor-facing instruction
files (CLAUDE.md, AGENTS.md) require an opsx spec delta for any behavior change.

Purpose: After the v1.3 reverse-spec baseline (32 frozen capability specs), all future
behavior changes must flow through an opsx proposal that updates the relevant capability
spec — OpenSpec is the living WHAT, GSD remains the HOW. This plan writes that rule down
in the one canonical place (FORWARD-PROCESS.md) and enforces it in the files every
contributor/agent reads first.

Output: openspec/FORWARD-PROCESS.md (new), cross-references added to project.md and
AUTHORING.md, and strengthened require-a-delta rules in CLAUDE.md and AGENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-forward-process-coverage-validation/CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@openspec/project.md
@openspec/AUTHORING.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write openspec/FORWARD-PROCESS.md (FWD-01)</name>
  <files>openspec/FORWARD-PROCESS.md, openspec/project.md, openspec/AUTHORING.md</files>
  <read_first>
    - .planning/phases/28-forward-process-coverage-validation/CONTEXT.md (the FWD-01 how-to row; grounding discipline section)
    - openspec/project.md (glossary lines defining "opsx" and "delta spec" — reuse that exact terminology; find the references/glossary section to know where to add the cross-link)
    - openspec/AUTHORING.md (existing authoring conventions — the forward-process doc must point back here for HOW to write the delta spec body)
  </read_first>
  <action>
    Create openspec/FORWARD-PROCESS.md as a plain Markdown doc (NOT a capability spec — it must NOT live under openspec/specs/ and must NOT be subject to an openspec validate gate). Document the forward delta-only workflow for FWD-01: state up front that OpenSpec is the living WHAT and GSD remains the HOW. Establish the core rule: any change to whilly/ runtime behavior REQUIRES an opsx change proposal that updates the relevant openspec/specs/<slug>/spec.md, authored as a delta spec under openspec/changes/<name>/specs/<capability>/spec.md, and the proposal must be applied and archived as part of landing the change. Describe the three real lifecycle stages using the real OpenSpec 1.4.1 / opsx command surface that already exists in this repo (openspec change to manage proposals, openspec validate to gate the delta, openspec archive to fold an applied change back into the baseline; reference the /opsx:* propose/apply/archive workflow as documented in project.md's glossary). Do NOT invent commands or flags — only use the propose -> apply -> archive lifecycle and the openspec subcommands confirmed present. Clarify the boundary: pure docs/test/refactor with no behavior change does not need a delta, but any change to a guaranteed behavior does, and the capability specs are otherwise frozen. Add a short "where things live" section pointing to openspec/specs/ (baseline WHAT), openspec/changes/ (in-flight deltas), TAXONOMY.md (the 32 slugs), COVERAGE-MATRIX.md (module->capability map), and AUTHORING.md (HOW to write the delta body). Then add a one-line cross-reference to FORWARD-PROCESS.md from both openspec/project.md (near the opsx/delta-spec glossary entries) and openspec/AUTHORING.md (near the top, so authors learn the forward gate before writing). Per CONTEXT FWD-01, keep grounding accurate to the real v4 workflow.
  </action>
  <verify>
    <automated>test -f openspec/FORWARD-PROCESS.md && grep -qi 'propose' openspec/FORWARD-PROCESS.md && grep -qi 'apply' openspec/FORWARD-PROCESS.md && grep -qi 'archive' openspec/FORWARD-PROCESS.md && grep -q 'openspec/specs/' openspec/FORWARD-PROCESS.md && grep -q 'FORWARD-PROCESS' openspec/project.md && grep -q 'FORWARD-PROCESS' openspec/AUTHORING.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - openspec/FORWARD-PROCESS.md exists, is plain Markdown, and is NOT under openspec/specs/ (no new capability slug dir created).
    - It documents the propose -> apply -> archive lifecycle and states behavior changes REQUIRE an opsx proposal updating openspec/specs/<slug>/spec.md.
    - It states OpenSpec = living WHAT, GSD = HOW.
    - Only real opsx/openspec commands referenced (no invented commands).
    - openspec/project.md and openspec/AUTHORING.md each contain a reference to FORWARD-PROCESS.md.
  </acceptance_criteria>
  <done>FORWARD-PROCESS.md exists with the propose/apply/archive forward gate, references openspec/specs/, and is linked from project.md and AUTHORING.md; the verify command prints PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen CLAUDE.md + AGENTS.md to require an opsx spec delta (FWD-02)</name>
  <files>CLAUDE.md, AGENTS.md</files>
  <read_first>
    - CLAUDE.md (the "When editing" section near line 79-86 already says behavior changes "should be reflected in the OpenSpec capability spec" — FWD-02 strengthens "should" to a hard "require"; keep all surrounding v4-accurate content intact)
    - AGENTS.md (root — currently has zero openspec mentions; FWD-02 adds the same require-a-delta rule, ideally in the Commit & Pull Request / Coding Style area where contribution rules live)
    - openspec/FORWARD-PROCESS.md (just authored in Task 1 — both files must point readers at it)
    - .planning/phases/28-forward-process-coverage-validation/CONTEXT.md (FWD-02 row + "keep both v4-accurate" / grounding discipline)
  </read_first>
  <action>
    Edit CLAUDE.md "When editing": change the existing soft "Behavior changes should be reflected in the OpenSpec capability spec" bullet into a hard requirement — any change to whilly/ behavior REQUIRES an opsx spec delta (propose -> apply -> archive) updating the relevant openspec/specs/<slug>/spec.md before the change is considered complete, and point readers at openspec/specs/ and openspec/FORWARD-PROCESS.md for the full workflow. Preserve the existing drift-warning note (the v3->v4 drift failure mode) and the rest of the section. Do NOT alter the v4-accurate technical content elsewhere in CLAUDE.md. Then add the same rule to AGENTS.md: insert a concise contributor rule (fits the existing "Commit & Pull Request Guidelines" / coding-style register) that any behavior change to whilly/ MUST ship with an opsx spec delta updating openspec/specs/<slug>/spec.md, and point contributors at openspec/specs/ and openspec/FORWARD-PROCESS.md. Keep AGENTS.md's existing v6/Codex content and structure intact — only add the spec-delta rule. Both files must contain the literal string "openspec/specs/" and a require-a-delta phrasing (not "should").
  </action>
  <verify>
    <automated>grep -niq 'requir' CLAUDE.md && grep -q 'openspec/specs/' CLAUDE.md && grep -q 'FORWARD-PROCESS' CLAUDE.md && grep -q 'openspec/specs/' AGENTS.md && grep -niq 'spec delta\|opsx\|spec delta' AGENTS.md && grep -q 'FORWARD-PROCESS' AGENTS.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md "When editing" uses require/REQUIRES language (not "should") for the opsx spec delta on behavior changes, points at openspec/specs/, and references FORWARD-PROCESS.md.
    - The CLAUDE.md v3->v4 drift-warning note is preserved; no other technical content changed inaccurately.
    - AGENTS.md contains a require-a-spec-delta rule for behavior changes, the literal "openspec/specs/", and a reference to FORWARD-PROCESS.md.
    - Both files remain v4-accurate (no reintroduction of removed v3 lore).
  </acceptance_criteria>
  <done>CLAUDE.md and AGENTS.md both require an opsx spec delta for behavior changes and point at openspec/specs/ + FORWARD-PROCESS.md; the verify command prints PASS.</done>
</task>

</tasks>

<verification>
- openspec/FORWARD-PROCESS.md exists (plain Markdown, not a spec), documents propose -> apply -> archive, references openspec/specs/, and is linked from project.md and AUTHORING.md.
- CLAUDE.md "When editing" REQUIRES an opsx spec delta for behavior changes and points at openspec/specs/ + FORWARD-PROCESS.md, with the drift-warning preserved.
- AGENTS.md carries the same require-a-delta rule and points at openspec/specs/ + FORWARD-PROCESS.md.
- No new openspec/specs/<slug>/ directory was created (FWD docs are plain Markdown).
- No whilly/ Python files modified.
</verification>

<success_criteria>
- FWD-01: forward delta-only workflow documented (propose -> apply -> archive, OpenSpec=WHAT/GSD=HOW).
- FWD-02: CLAUDE.md + AGENTS.md require an opsx spec delta and point at openspec/specs/.
</success_criteria>

<output>
Create `.planning/phases/28-forward-process-coverage-validation/28-01-SUMMARY.md` when done.
</output>
