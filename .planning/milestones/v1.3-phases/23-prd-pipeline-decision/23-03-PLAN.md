---
phase: 23-prd-pipeline-decision
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/decision-gate/spec.md
autonomous: true
requirements: [PRD-05]
must_haves:
  truths:
    - "openspec validate decision-gate --strict reports the spec is valid (0 errors, 0 warnings)"
    - "decision-gate spec captures the per-task refuse/accept criteria (evaluate, parse_decision, short-description auto-refuse, fail-open behavior)"
    - "decision-gate spec captures the plan-level TRIZ contradiction-analysis role (analyze_plan_triz -> PlanTrizReport, TrizFinding)"
    - "decision-gate spec references task-model-fsm for the SKIPPED outcome rather than re-specifying the FSM"
  artifacts:
    - path: "openspec/specs/decision-gate/spec.md"
      provides: "Normative decision-gate capability spec (refuse/accept + TRIZ)"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/decision-gate/spec.md"
      to: "whilly/decision_gate.py"
      via: "reverse-spec of evaluate / parse_decision / build_prompt"
      pattern: "SHALL|MUST"
    - from: "openspec/specs/decision-gate/spec.md"
      to: "whilly/core/triz.py"
      via: "reverse-spec of analyze_plan_triz / PlanTrizReport"
      pattern: "SHALL|MUST"
---

<objective>
Author the normative OpenSpec capability spec for `decision-gate`: the per-task refuse/accept
gate plus the plan-level TRIZ contradiction-analysis role. Reverse-spec'd from real v4.7.0
code and MUST pass `openspec validate decision-gate --strict` with zero errors and zero
warnings.

Purpose: The Decision Gate is the highest-value contract in Phase 23 — it refuses nonsense
tasks before spending tokens and surfaces plan-level contradictions. It draws on three
modules (decision_gate.py, triz_analyzer.py, core/triz.py), so it gets a dedicated plan/file.
Output: openspec/specs/decision-gate/spec.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-prd-pipeline-decision/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
@openspec/COVERAGE-MATRIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec the observed v4 behavior of decision-gate (PRD-05)</name>
  <files>openspec/specs/decision-gate/spec.md</files>
  <read_first>
    - whilly/decision_gate.py (Decision dataclass lines ~40-44; constants DEFAULT_MODEL/DEFAULT_TIMEOUT_S=60/MIN_DESCRIPTION_LEN=20/PROCEED/REFUSE lines ~30-36; build_prompt ~81-96; parse_decision ~105-142; evaluate ~160-203; label_flip_for_gh_task ~209+)
    - whilly/triz_analyzer.py (analyze_plan_triz ~234, challenge_plan ~265, TrizReport/ChallengeReport dataclasses, format_triz_report)
    - whilly/core/triz.py (analyze_plan_triz ~241, PlanTrizReport ~167-178, PlanTrizFinding ~150-164, TrizFinding ~181-208, TrizOutcome ~211-238, _decision_gate_findings ~321, analyze_contradiction ~697)
    - openspec/specs/task-model-fsm/spec.md (REFERENCE for the SKIPPED outcome; exemplar structure)
    - openspec/AUTHORING.md
  </read_first>
  <action>
    Create openspec/specs/decision-gate/spec.md for the decision-gate capability,
    reverse-spec'd from the v4 code. READ all three modules first. "## Purpose" (>=50 chars):
    this capability governs the refuse/accept gate that filters tasks before dispatch and the
    plan-level TRIZ contradiction analysis. "## Requirements" with >=4 "### Requirement:"
    blocks, SHALL/MUST on the first body line (<=500 chars). Capture observed per-task
    behaviors: evaluate auto-refuses without an LLM call when the trimmed description is
    shorter than MIN_DESCRIPTION_LEN (20 chars); otherwise it builds a prompt (build_prompt)
    and runs the gate agent; it is fail-open — a runner exception or a non-zero exit yields a
    PROCEED Decision (false-refuse is costlier than false-proceed); parse_decision returns
    (decision, reason) tolerantly from bare JSON, embedded JSON blob, or a bare keyword,
    defaulting to PROCEED on parse failure; Decision carries decision/reason/cost_usd/raw_text;
    label_flip_for_gh_task, on a REFUSE for a GitHub-sourced task (id starts "GH-" with a
    prd_requirement issue URL), flips the issue labels (add needs-clarification, remove
    whilly:ready). Capture plan-level behaviors: core/triz.analyze_plan_triz runs a
    deterministic (no-LLM) preflight over an imported Plan and returns a PlanTrizReport
    (verdict, ideality_score, findings, mergeable_groups, removable_tasks, summary) built from
    _decision_gate_findings and related detectors; per-task analyze_contradiction returns a
    TrizFinding | None. Add a requirement that references task-model-fsm for the SKIPPED
    outcome of a refused task (do NOT re-spec the FSM). Each requirement MUST have >=1
    "#### Scenario:" (4 hashtags) with WHEN/THEN. No fenced code blocks in the spec body.
  </action>
  <verify>
    <automated>openspec validate decision-gate --strict --json 2>&1 | grep -qi 'valid' && openspec validate decision-gate --strict</automated>
  </verify>
  <acceptance_criteria>
    - openspec/specs/decision-gate/spec.md exists
    - `openspec validate decision-gate --strict` prints "is valid" and exits 0 (zero errors, zero warnings)
    - File contains "## Purpose" (>=50 chars) and "## Requirements"
    - Every "### Requirement:" has SHALL or MUST on its first body line
    - Every requirement has >=1 "#### Scenario:" with WHEN/THEN bullets
    - Spec captures BOTH the per-task refuse/accept criteria AND the plan-level TRIZ contradiction-analysis role
    - SKIPPED outcome references task-model-fsm rather than re-specifying it
  </acceptance_criteria>
  <done>`openspec validate decision-gate --strict` reports the spec is valid; spec is grounded in decision_gate.py + triz_analyzer.py + core/triz.py.</done>
</task>

</tasks>

<verification>
- openspec/specs/decision-gate/spec.md exists.
- `openspec validate decision-gate --strict` reports the spec is valid (0 errors, 0 warnings).
- Spec covers refuse/accept (evaluate, parse_decision, MIN_DESCRIPTION_LEN, fail-open) and TRIZ (analyze_plan_triz / PlanTrizReport / TrizFinding).
- No `whilly/` Python files were modified.
</verification>

<success_criteria>
- PRD-05 (decision-gate) has a normative, strict-valid spec.
- Spec is reverse-spec'd from real v4.7.0 code.
- Refuse/accept criteria and TRIZ contradiction-analysis role are both captured; FSM is referenced, not duplicated.
</success_criteria>

<output>
Create `.planning/phases/23-prd-pipeline-decision/23-03-SUMMARY.md` when done.
</output>
