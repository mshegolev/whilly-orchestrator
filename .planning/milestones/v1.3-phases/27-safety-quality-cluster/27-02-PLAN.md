---
phase: 27-safety-quality-cluster
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/quality-compliance-audit/spec.md
  - openspec/specs/verification-gates/spec.md
autonomous: true
requirements: [SAFE-03, SAFE-04]
must_haves:
  truths:
    - "quality-compliance-audit spec passes openspec validate quality-compliance-audit --strict (exit 0, is valid)"
    - "verification-gates spec passes openspec validate verification-gates --strict (exit 0, is valid)"
    - "quality-compliance-audit specs the v4 quality language runners, compliance reports, audit JSONL sink, and qa-release at subsystem altitude"
    - "verification-gates specs the v4 pipeline verification + human-review gates (wired into worker.py/run.py) and CI verification; marks the legacy verifier.verify_task commit-revert path truthfully"
  artifacts:
    - path: "openspec/specs/quality-compliance-audit/spec.md"
      provides: "SAFE-03 normative spec — quality/compliance/audit/qa-release"
      contains: "## Requirements"
    - path: "openspec/specs/verification-gates/spec.md"
      provides: "SAFE-04 normative spec — verifier + human-review gates"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/quality-compliance-audit/spec.md"
      to: "whilly/quality/_runner.py"
      via: "reverse-spec of per-language quality runners + audit jsonl_sink"
      pattern: "quality|compliance|jsonl"
    - from: "openspec/specs/verification-gates/spec.md"
      to: "whilly/pipeline/verification.py"
      via: "reverse-spec of run_verification_commands + human_review gate"
      pattern: "verification|human_review|gate"
---

<objective>
Write two normative OpenSpec capability specs reverse-spec'd from the REAL v4.7.0 code:
- SAFE-03 `quality-compliance-audit` (whilly/quality/*, whilly/compliance/*, whilly/audit/*, whilly/qa_release/*, whilly/cli/{compliance,qa_release}.py)
- SAFE-04 `verification-gates` (whilly/verifier.py, whilly/pipeline/*, whilly/ci/verification.py + ci/{events,models})

These are subsystem-altitude specs: describe the contract of each subsystem, not every
helper. SAFE-04 must distinguish the LIVE v4 pipeline/CI verification + human-review gates
(wired into worker.py/run.py) from the LEGACY verifier.verify_task commit-revert path.

Purpose: Capture the quality/compliance/audit and verification-gate contracts as
machine-checkable specs for the Phase 28 coverage audit and adversarial verifier.
Output: Two spec.md files, each passing `openspec validate <slug> --strict`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-safety-quality-cluster/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
@openspec/specs/result-collection/spec.md
@openspec/specs/orchestration-loop/spec.md
</context>

<grounding_findings>
The planner surveyed the source. Verify when you read the modules, then spec:

SAFE-03 (quality / compliance / audit / qa_release):
- whilly/quality/: base.py + _runner.py + multi.py + per-language python.py / go.py /
  node.py / rust.py. These run lint/test quality checks per language and aggregate
  multi-language results. Spec at subsystem altitude (the runner contract + per-language
  dispatch + aggregation), not each function.
- whilly/compliance/: target-doc compliance report generation; wired via
  `whilly compliance ...` CLI (whilly/cli/compliance.py, run_compliance_command).
- whilly/audit/jsonl_sink.py: append-only JSONL audit-event sink.
- whilly/qa_release/: autotest_writer, collector, models, test_plan — QA-release
  artifact generation; wired via `whilly qa-release ...` (whilly/cli/qa_release.py,
  run_qa_release_command).
- Both compliance and qa-release are dispatched from whilly/cli/__init__.py.

SAFE-04 (verification gates):
- whilly/verifier.py: LEGACY commit-revert verifier. verify_task(...) → VerifyResult;
  _get_changed_files via git; _run_lint/_run_tests on changed files; _revert_last_commit
  on failure. GREP CONFIRMED: verify_task has NO callers in the v4 worker path (only a
  doc-string mention in compliance/__init__.py). Mark it legacy.
- whilly/pipeline/: the LIVE v4 verification + human-review gates, wired into
  whilly/cli/worker.py, whilly/cli/run.py, whilly/worker/{local,remote}.py.
  - verification.py: run_verification_commands (async), resolve_verification_specs,
    VerificationCommandSpec / VerificationCommandResult / VerificationRunOutcome
    (succeeded / required_failed / warning_count / event_names), plus
    make_verification_started_event / make_verification_result_event; env allowlist;
    timeout/blocked results.
    Required-vs-warning command distinction governs gate pass/fail.
  - human_review.py: HumanReviewCheckpoint / HumanReviewDecision; requires_human_review,
    build_human_review_checkpoint, is_human_review_approved, and the
    make_human_review_required / approved / rejected / changes_requested events.
  - human_review_decisions.py, sinks.py, events.py support the above.
- whilly/ci/: verification.py + events.py + models.py + github.py — CI-side verification
  contract.
- Reference result-collection (AgentResult) and orchestration-loop (where gates run)
  rather than re-speccing them.

Coverage matrix (Phase 28) will check these modules are accounted for.
</grounding_findings>

<tasks>

<task type="auto">
  <name>Task 1: Spec SAFE-03 quality-compliance-audit from real v4 quality runners, compliance, audit sink, qa-release</name>
  <files>openspec/specs/quality-compliance-audit/spec.md</files>
  <read_first>
    - whilly/quality/_runner.py (runner contract)
    - whilly/quality/base.py and whilly/quality/multi.py (aggregation)
    - whilly/quality/{python,go,node,rust}.py (per-language dispatch — skim signatures)
    - whilly/compliance/__init__.py (compliance report generation)
    - whilly/cli/compliance.py (run_compliance_command)
    - whilly/audit/jsonl_sink.py (append-only JSONL audit sink)
    - whilly/qa_release/{autotest_writer,collector,models,test_plan}.py (skim)
    - whilly/cli/qa_release.py (run_qa_release_command)
    - openspec/AUTHORING.md (format rules)
    - openspec/specs/task-model-fsm/spec.md (exemplar structure)
  </read_first>
  <action>
    Create openspec/specs/quality-compliance-audit/spec.md speccing the v4 quality/
    compliance/audit/qa-release subsystem at subsystem altitude. Write a ## Purpose
    (≥50 chars) describing the capability: per-language quality (lint/test) runners with
    multi-language aggregation, target-doc compliance report generation, append-only JSONL
    audit-event sink, and QA-release artifact generation (test plans / autotests).
    Then ## Requirements with ### Requirement: blocks (FIRST body line SHALL/MUST, ≤500
    chars, each ≥1 #### Scenario: WHEN/THEN). Cover at minimum:
    (1) The quality-runner contract: a per-language runner SHALL execute the language's
    lint/test checks and return a structured pass/fail result; the multi-language runner
    SHALL dispatch to the matching language runner(s) and aggregate results.
    (2) Compliance reports SHALL be generated for target docs via the `whilly compliance`
    command (run_compliance_command).
    (3) The audit JSONL sink SHALL append audit events as one JSON object per line
    (append-only).
    (4) QA-release SHALL produce its artifacts (test plan / autotest / collected results)
    via the `whilly qa-release` command (run_qa_release_command).
    Match the real signatures/behaviors you read. No fenced code blocks; normative prose.
  </action>
  <verify>openspec validate quality-compliance-audit --strict ; openspec validate quality-compliance-audit --strict --json</verify>
  <acceptance_criteria>
    - File openspec/specs/quality-compliance-audit/spec.md exists.
    - `openspec validate quality-compliance-audit --strict` reports "is valid" and exits 0 (0 errors, 0 warnings).
    - Spec contains a ## Purpose section (≥50 chars) and a ## Requirements section.
    - Every ### Requirement: has SHALL or MUST on its FIRST body line and ≥1 #### Scenario: (4 hashtags) with - **WHEN** and - **THEN** bullets.
    - Spec covers quality runners, compliance reports, audit JSONL sink, and qa-release at subsystem altitude, grounded in the real modules.
  </acceptance_criteria>
  <done>quality-compliance-audit/spec.md exists, is valid under --strict, and specs the four real v4 subsystems.</done>
</task>

<task type="auto">
  <name>Task 2: Spec SAFE-04 verification-gates from real v4 pipeline + CI gates, marking the legacy verifier.verify_task path</name>
  <files>openspec/specs/verification-gates/spec.md</files>
  <read_first>
    - whilly/pipeline/verification.py (run_verification_commands, resolve_verification_specs, VerificationRunOutcome.succeeded/required_failed/warning_count, started/result events, env allowlist, timeout/blocked)
    - whilly/pipeline/human_review.py (HumanReviewCheckpoint/Decision, requires_human_review, build_human_review_checkpoint, is_human_review_approved, required/approved/rejected/changes_requested events)
    - whilly/pipeline/{human_review_decisions,sinks,events}.py (skim)
    - whilly/ci/verification.py + whilly/ci/{events,models}.py (CI verification contract)
    - whilly/verifier.py (LEGACY verify_task/VerifyResult/_get_changed_files/_revert_last_commit — confirm unwired)
    - whilly/cli/worker.py and whilly/cli/run.py (confirm pipeline gates are wired here)
    - openspec/AUTHORING.md (format rules)
    - openspec/specs/task-model-fsm/spec.md (exemplar)
    - openspec/specs/result-collection/spec.md and openspec/specs/orchestration-loop/spec.md (reference, don't duplicate)
  </read_first>
  <action>
    Create openspec/specs/verification-gates/spec.md speccing the LIVE v4 verification and
    human-review gates, and truthfully marking the legacy verifier.verify_task commit-revert
    path. Write a ## Purpose (≥50 chars) describing: post-task verification commands
    (required vs warning) producing started/result events, the human-review checkpoint gate
    that pauses on approval/rejection/changes-requested, the CI-side verification contract,
    and a note that the v3 verifier.verify_task commit-revert verifier is legacy/unwired in
    the v4 worker path.
    Then ## Requirements with ### Requirement: blocks (FIRST body line SHALL/MUST, ≤500
    chars, each ≥1 #### Scenario: WHEN/THEN). Cover at minimum:
    (1) run_verification_commands SHALL execute resolved verification specs and produce a
    VerificationRunOutcome whose required_failed=True (a required command failing) fails the
    gate while warning-only failures do not; it SHALL emit verification started/result
    events.
    (2) Verification command execution SHALL run under an env allowlist and SHALL produce a
    blocked/timeout result rather than hanging on timeout.
    (3) requires_human_review SHALL detect a human-review cue and build_human_review_checkpoint
    SHALL produce a HumanReviewCheckpoint; the gate SHALL pause until a decision and only
    proceed when is_human_review_approved is True, emitting required/approved/rejected/
    changes_requested events accordingly.
    (4) CI verification (whilly/ci/verification.py) SHALL produce its verification result/
    events per the ci models contract.
    (5) An explicit legacy requirement: verifier.verify_task (commit-revert) SHALL be treated
    as legacy and is NOT wired into the v4 worker-claim path; the live gates are the pipeline/
    CI gates above. Reference result-collection (AgentResult) and orchestration-loop for
    where gates run rather than re-speccing them. No fenced code blocks.
  </action>
  <verify>openspec validate verification-gates --strict ; openspec validate verification-gates --strict --json</verify>
  <acceptance_criteria>
    - File openspec/specs/verification-gates/spec.md exists.
    - `openspec validate verification-gates --strict` reports "is valid" and exits 0 (0 errors, 0 warnings).
    - Spec contains a ## Purpose section (≥50 chars) and a ## Requirements section.
    - Every ### Requirement: has SHALL or MUST on its FIRST body line and ≥1 #### Scenario: (4 hashtags) with - **WHEN** and - **THEN** bullets.
    - Spec specs the LIVE pipeline verification + human-review gates and CI verification, and truthfully marks verifier.verify_task legacy/unwired.
  </acceptance_criteria>
  <done>verification-gates/spec.md exists, is valid under --strict, and specs the v4 live gates while marking the legacy verifier path.</done>
</task>

</tasks>

<verification>
- Both spec files exist under openspec/specs/{quality-compliance-audit,verification-gates}/spec.md.
- `openspec validate quality-compliance-audit --strict` → is valid, exit 0.
- `openspec validate verification-gates --strict` → is valid, exit 0.
- SAFE-04 distinguishes live v4 pipeline/CI gates from the legacy verifier.verify_task path.
- SAFE-03 and SAFE-04 are each covered by exactly this plan's requirements field.
</verification>

<success_criteria>
Two strict-valid OpenSpec specs reverse-spec'd from real v4 code at subsystem altitude,
with truthful live/legacy annotations. SAFE-03 + SAFE-04 satisfied.
</success_criteria>

<output>
Create `.planning/phases/27-safety-quality-cluster/27-02-SUMMARY.md` when done.
</output>
