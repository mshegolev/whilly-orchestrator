---
phase: 25-operator-surface-cluster
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/cli-surface/spec.md
  - openspec/specs/operator-views-logs/spec.md
autonomous: true
requirements: [OPS-04, OPS-05]
must_haves:
  truths:
    - "openspec/specs/cli-surface/spec.md exists and passes `openspec validate cli-surface --strict` with zero errors and zero warnings"
    - "openspec/specs/operator-views-logs/spec.md exists and passes `openspec validate operator-views-logs --strict` with zero errors and zero warnings"
    - "cli-surface spec enumerates the REAL v4 exit codes (0 ok, 1 validation error, 2 environment error, -4 workspace-prep failure) NOT the legacy 0/1/2/3 set"
    - "cli-surface spec states that no-args prints HELP via _print_help (not an interactive menu) and an unknown command returns 2 after printing help to stderr"
    - "cli-surface spec captures the v3 legacy flag shim that rewrites top-level flags into v4 subcommands and sets WHILLY_HEADLESS from --headless"
  artifacts:
    - path: "openspec/specs/cli-surface/spec.md"
      provides: "Normative spec for the v4 CLI: subcommand dispatch, v3-compat shim, real exit-code contract, headless behavior"
      contains: "## Requirements"
    - path: "openspec/specs/operator-views-logs/spec.md"
      provides: "Normative spec for the log viewer, operator views taxonomy, and operator TUI"
      contains: "## Purpose"
  key_links:
    - from: "openspec/specs/cli-surface/spec.md"
      to: "whilly/cli/plan.py"
      via: "reverse-spec of EXIT_OK/EXIT_VALIDATION_ERROR/EXIT_ENVIRONMENT_ERROR"
      pattern: "EXIT_VALIDATION_ERROR|EXIT_ENVIRONMENT_ERROR"
    - from: "openspec/specs/cli-surface/spec.md"
      to: "whilly/workspaces.py"
      via: "reverse-spec of WORKSPACE_FAILED_EXIT_CODE"
      pattern: "WORKSPACE_FAILED_EXIT_CODE"
    - from: "openspec/specs/operator-views-logs/spec.md"
      to: "whilly/log_viewer.py"
      via: "reverse-spec of cmd_list/cmd_show/cmd_tail"
      pattern: "def cmd_(list|show|tail)"
---

<objective>
Spec the observed v4 behavior of two operator surfaces as normative OpenSpec specs: the
CLI surface (OPS-04, `cli-surface`) and operator views + logs (OPS-05,
`operator-views-logs`). Documentation-only; no `whilly/` Python changes.

Purpose: Capture the v4 CLI subcommand dispatch, the v3-compatibility flag shim, the REAL
per-command exit-code contract, and headless JSON behavior (cli-surface); plus the log
viewer (list/show/tail), the operator-views taxonomy, and the operator TUI
(operator-views-logs). The exit-code contract is the critical, headless-caller-facing
part of this phase and MUST be grounded in the real EXIT_* constants.
Output: `openspec/specs/cli-surface/spec.md` and `openspec/specs/operator-views-logs/spec.md`,
each passing `openspec validate <slug> --strict`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-operator-surface-cluster/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
@openspec/COVERAGE-MATRIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec the cli-surface capability with the REAL v4 exit codes (OPS-04)</name>
  <files>openspec/specs/cli-surface/spec.md</files>
  <read_first>
    - whilly/cli/__init__.py (main(argv): runs _apply_legacy_shim first; no-args or -h/--help → _print_help() returns 0; -V/--version prints version returns 0; the long subcommand dispatch table — plan, run, dashboard, server, init, admin, logs, forge, jira, gitlab, qa-release, project-config, project-map, github-projects, compliance, tui, pr-feedback, rollback, update, feedback, quick-setup, scheduler, skill, worker[register|connect|launch|bootstrap|list|remove]; unknown command → writes "unknown command" to stderr, prints help to stderr, returns 2; _print_help; _apply_legacy_shim translating v3 flags --tasks/--init/--prd-wizard/--from-jira/--resume/--reset/--all and consuming --headless by setting os.environ["WHILLY_HEADLESS"]="1"; --no-workspace/--no-worktree no-ops)
    - whilly/cli/plan.py (EXIT_OK=0, EXIT_VALIDATION_ERROR=1, EXIT_ENVIRONMENT_ERROR=2 — and the per-handler returns: schema/missing-field/cycle → EXIT_VALIDATION_ERROR; plan absent / env failure → EXIT_ENVIRONMENT_ERROR)
    - whilly/cli/run.py (EXIT_OK=0, EXIT_ENVIRONMENT_ERROR=2; the docstring note that there is intentionally NO EXIT_VALIDATION_ERROR path here because argparse SystemExits first; run-command maps workspace failure to WORKSPACE_FAILED_EXIT_CODE)
    - whilly/workspaces.py (WORKSPACE_FAILED_EXIT_CODE = -4 — workspace-prep failure exit code, surfaced via run.py)
    - whilly/cli/__main__.py and whilly/__main__.py (entry shims: python -m whilly / whilly.cli)
    - whilly/__init__.py (__version__ used by -V/--version)
    - whilly/cli/feedback.py and whilly/feedback.py (feedback subcommand handler + module)
    - whilly/cli/skill.py (skill subcommand handler)
    - openspec/AUTHORING.md
    - openspec/specs/task-model-fsm/spec.md (exemplar)
  </read_first>
  <action>
    Reverse-spec the observed v4 CLI. Write `## Purpose` (≥50 chars) describing what
    cli-surface covers: the `whilly` console entry, subcommand dispatch, the v3-compat
    flag shim, the exit-code contract relied on by headless callers, and the help/version
    fast paths.

    Write `## Requirements` (each SHALL/MUST first body line + ≥1 `#### Scenario:`
    WHEN/THEN). GROUNDING CAUTION — DO NOT copy the legacy v3 narrative "0=ok, 1=some
    failed, 2=budget exceeded, 3=timeout". Spec the REAL v4 exit codes captured from the
    EXIT_* constants you read:
      * 0 = EXIT_OK (success; also help and version fast paths return 0)
      * 1 = EXIT_VALIDATION_ERROR (plan.py: schema/missing-field/dependency-cycle failures)
      * 2 = EXIT_ENVIRONMENT_ERROR (plan.py + run.py: plan absent / environment failure;
            ALSO the unknown-command path in cli/__init__.py returns 2)
      * -4 = WORKSPACE_FAILED_EXIT_CODE (workspaces.py; surfaced via cli/run.py when
            workspace prep fails)
    Note the asymmetry truthfully: `whilly run` has no validation-error (1) path because
    argparse SystemExits on bad args first.

    Cover at minimum: (a) the exit-code contract above, named per the EXIT_* constants;
    (b) no-args and -h/--help print HELP via _print_help and return 0 (NOT an interactive
    menu); (c) -V/--version prints the version from whilly.__version__ and returns 0;
    (d) an unknown command writes a diagnostic to stderr, prints help to stderr, and
    returns 2; (e) the v3 legacy flag shim rewrites recognized top-level flags
    (--tasks/--init/--prd-wizard/--from-jira/--resume/--reset/--all) into the equivalent
    v4 subcommand before dispatch, consumes --headless by setting WHILLY_HEADLESS=1 in the
    environment, and treats --no-workspace/--no-worktree as no-ops; (f) the headless JSON
    behavior — VERIFY in the source how WHILLY_HEADLESS is consumed before asserting the
    output shape, and spec what the code actually does. Spec observed behavior only.
  </action>
  <verify>
    <automated>openspec validate cli-surface --strict</automated>
  </verify>
  <acceptance_criteria>
    - openspec/specs/cli-surface/spec.md exists with `## Purpose` (≥50 chars) and `## Requirements`.
    - `openspec validate cli-surface --strict` reports the spec is valid (0 errors, 0 warnings, exit 0).
    - The exit-code requirement names the real constants (EXIT_OK=0, EXIT_VALIDATION_ERROR=1, EXIT_ENVIRONMENT_ERROR=2, WORKSPACE_FAILED_EXIT_CODE=-4) and does NOT use the legacy "budget exceeded=2 / timeout=3" wording.
    - A requirement states no-args prints HELP (not a menu) and unknown command returns 2.
    - A requirement captures the v3 legacy flag shim + WHILLY_HEADLESS handling.
    - Every `### Requirement:` has SHALL/MUST on its first body line and ≥1 `#### Scenario:`.
  </acceptance_criteria>
  <done>`openspec validate cli-surface --strict` prints that the spec is valid; the exit-code contract matches the real EXIT_* constants in cli/plan.py, cli/run.py, and workspaces.py.</done>
</task>

<task type="auto">
  <name>Task 2: Spec the operator-views-logs capability (OPS-05)</name>
  <files>openspec/specs/operator-views-logs/spec.md</files>
  <read_first>
    - whilly/log_viewer.py (`whilly logs` command: run_logs_command dispatch on --tail/-f vs default; cmd_list, cmd_show <task_id>, cmd_tail <task_id> live-follow of events+stdout poll loop; discover_tasks, TaskSummary, _read_jsonl, _apply_event; cleanup_old_logs ttl)
    - whilly/operator_views.py (OperatorSurface/OperatorTable/OperatorAction enums, OperatorTableColumn, OperatorActionSpec with hotkeys, OperatorUiArtifact + OperatorUiArtifactStatus, wui_route_prefix mappings, operator_surface_hotkeys/operator_surface_hotkey_help helpers)
    - whilly/cli/tui.py (`whilly tui` command: TuiState, build_tui_parser surface-hotkey help, run_tui_command, handle_tui_key applying one hotkey to mutable state, _SURFACE_BY_KEY, reviewer identity env)
    - openspec/AUTHORING.md
    - openspec/specs/task-model-fsm/spec.md (exemplar)
  </read_first>
  <action>
    Reverse-spec the observed v4 behavior of the operator log/view surfaces. Write
    `## Purpose` (≥50 chars) describing what operator-views-logs covers: per-task log
    discovery/inspection (`whilly logs`), the operator-views taxonomy (surfaces, tables,
    actions, hotkeys, UI artifacts), and the operator TUI (`whilly tui`). State the
    boundary with dashboard-tui (the Rich Live run dashboard) and web-status-ui (the
    browser surfaces) — reference, do not duplicate.

    Write `## Requirements` (each SHALL/MUST first body line + ≥1 `#### Scenario:`
    WHEN/THEN). Cover at minimum: (a) `whilly logs` with no tail flag lists discovered
    tasks (cmd_list) and with a task id shows that task's events+stdout (cmd_show);
    (b) `whilly logs --tail/-f <task_id>` live-follows events and stdout via the poll loop
    until interrupted; (c) cleanup_old_logs removes logs older than the configured TTL;
    (d) the operator-views taxonomy defines the legal surfaces, tables, actions and their
    hotkeys plus each action's web-UI route prefix; (e) operator UI artifacts carry a
    status and follow-up phase; (f) the operator TUI maps a single hotkey to a surface
    switch / action via handle_tui_key and requires the reviewer identity for
    human-review hotkeys. Spec observed behavior; do not invent surfaces/hotkeys.
  </action>
  <verify>
    <automated>openspec validate operator-views-logs --strict</automated>
  </verify>
  <acceptance_criteria>
    - openspec/specs/operator-views-logs/spec.md exists with `## Purpose` (≥50 chars) and `## Requirements`.
    - `openspec validate operator-views-logs --strict` reports the spec is valid (0 errors, 0 warnings, exit 0).
    - Requirements name the real log_viewer commands (list/show/tail) and the operator_views taxonomy enums/hotkeys (no invented surfaces).
    - Every `### Requirement:` has SHALL/MUST on its first body line and ≥1 `#### Scenario:`.
  </acceptance_criteria>
  <done>`openspec validate operator-views-logs --strict` prints that the spec is valid; behavior matches whilly/log_viewer.py + operator_views.py + cli/tui.py.</done>
</task>

</tasks>

<verification>
- Both spec files exist at openspec/specs/cli-surface/spec.md and openspec/specs/operator-views-logs/spec.md.
- `openspec validate cli-surface --strict` and `openspec validate operator-views-logs --strict` both report valid (exit 0).
- cli-surface uses the real EXIT_* constants, not the legacy 0/1/2/3 budget/timeout lore.
- No delta headers appear in either file.
</verification>

<success_criteria>
- OPS-04 and OPS-05 each have a normative, reverse-spec'd capability spec passing `--strict`.
- The cli-surface exit-code contract is grounded in real v4 EXIT_* constants; operator-views-logs matches the real log viewer + operator views + TUI.
</success_criteria>

<output>
Create `.planning/phases/25-operator-surface-cluster/25-03-SUMMARY.md` when done.
</output>
