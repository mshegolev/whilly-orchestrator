---
phase: 25-operator-surface-cluster
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/web-status-ui/spec.md
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - "openspec/specs/web-status-ui/spec.md exists and passes `openspec validate web-status-ui --strict` with zero errors and zero warnings"
    - "web-status-ui spec captures the FastAPI control-plane endpoints (plans, tasks, metrics, SSE) and the worker HTTP transport RPC surface"
    - "web-status-ui spec captures the bootstrap-token vs per-worker-bearer auth split for the transport RPC and references auth-security for the full auth model"
  artifacts:
    - path: "openspec/specs/web-status-ui/spec.md"
      provides: "Normative spec for the FastAPI control plane + worker HTTP transport + SSE + localhost web status surface"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/web-status-ui/spec.md"
      to: "whilly/adapters/transport/auth.py"
      via: "reverse-spec of bearer_auth + bootstrap_auth"
      pattern: "bearer_auth|bootstrap_auth"
    - from: "openspec/specs/web-status-ui/spec.md"
      to: "whilly/api/plans_api.py"
      via: "reverse-spec of /api/v1/plans routes"
      pattern: "/api/v1/plans"
---

<objective>
Spec the observed v4 behavior of the web status / API surface (OPS-02, `web-status-ui`)
as one normative OpenSpec capability spec. This is the largest operator surface — it gets
its own plan. Documentation-only; no `whilly/` Python changes.

Purpose: Capture the FastAPI control plane (plans/tasks/metrics/SSE endpoints + static
mount), the worker ↔ control-plane HTTP transport RPC surface and its two-token auth
split (bootstrap token for registration, per-worker bearer for steady-state RPC), and the
standalone localhost web-status server — as a machine-checkable subsystem-altitude spec.
Output: `openspec/specs/web-status-ui/spec.md` passing `openspec validate web-status-ui --strict`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-operator-surface-cluster/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
@openspec/COVERAGE-MATRIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec the worker HTTP transport RPC + auth split (OPS-02 part A)</name>
  <files>openspec/specs/web-status-ui/spec.md</files>
  <read_first>
    - whilly/adapters/transport/server.py (server-side RPC endpoints for claim/complete/fail/heartbeat/register)
    - whilly/adapters/transport/client.py (worker-side HTTP client that carries the bearer)
    - whilly/adapters/transport/auth.py (make_bearer_auth/bearer_auth reads WHILLY_WORKER_TOKEN for claim/complete/fail/heartbeat; make_bootstrap_auth/bootstrap_auth reads WHILLY_WORKER_BOOTSTRAP_TOKEN for POST /workers/register; 401 + WWW-Authenticate: Bearer realm="whilly" on missing/invalid token; constant-time comparison; fast-fail at startup on missing env)
    - whilly/adapters/transport/schemas.py (request/response payload shapes)
    - whilly/adapters/transport/__init__.py
    - openspec/AUTHORING.md
    - openspec/specs/task-model-fsm/spec.md (exemplar)
  </read_first>
  <action>
    Begin `openspec/specs/web-status-ui/spec.md`. Write `## Purpose` (≥50 chars) describing
    the capability at subsystem altitude: the HTTP control plane workers talk to plus the
    operator-facing status/API surface. State the boundary explicitly — full
    session/OIDC/WebAuthn auth lives in `auth-security` (Phase 26); this spec covers only
    the transport bootstrap-token / per-worker-bearer RPC auth and the API/SSE/status
    endpoints. Reference auth-security; do not re-spec it.

    Open `## Requirements` and add the transport-RPC requirements (each SHALL/MUST first
    body line + ≥1 `#### Scenario:` WHEN/THEN): (a) steady-state worker RPCs
    (claim/complete/fail/heartbeat) require a valid per-worker bearer read from
    WHILLY_WORKER_TOKEN; (b) worker registration (POST /workers/register) requires the
    shared bootstrap token from WHILLY_WORKER_BOOTSTRAP_TOKEN, distinct from the bearer so
    either secret can rotate independently; (c) a missing or invalid bearer returns 401
    with `WWW-Authenticate: Bearer realm="whilly"`; (d) the auth dependency factory
    fast-fails at startup when its env token is unset; (e) bearer comparison is
    constant-time. Spec observed behavior only.
  </action>
  <verify>
    <automated>test -f openspec/specs/web-status-ui/spec.md</automated>
  </verify>
  <acceptance_criteria>
    - openspec/specs/web-status-ui/spec.md exists with `## Purpose` (≥50 chars) and `## Requirements`.
    - Transport requirements name WHILLY_WORKER_TOKEN (bearer) and WHILLY_WORKER_BOOTSTRAP_TOKEN (bootstrap) per auth.py.
    - The spec references auth-security for the full auth model and does not re-spec session/OIDC/WebAuthn.
    - Every added `### Requirement:` has SHALL/MUST on its first body line and ≥1 `#### Scenario:`.
  </acceptance_criteria>
  <done>Transport RPC + two-token auth split is specified from whilly/adapters/transport/*.py with the auth-security boundary referenced.</done>
</task>

<task type="auto">
  <name>Task 2: Spec the FastAPI control-plane endpoints + SSE + web status (OPS-02 part B)</name>
  <files>openspec/specs/web-status-ui/spec.md</files>
  <read_first>
    - whilly/api/main.py (app assembly, include_router, static mount wiring)
    - whilly/api/plans_api.py (GET/POST /api/v1/plans, PATCH /api/v1/plans/{plan_id} with If-Match optimistic concurrency, 201 on create)
    - whilly/api/tasks_api.py and whilly/api/tasks_api_crud.py (task listing/pagination + CRUD payloads)
    - whilly/api/metrics.py (metrics endpoint)
    - whilly/api/sse.py, whilly/api/sse_endpoint.py, whilly/api/event_flusher.py (GET /events/stream SSE channel, event_id/event_type framing, per-subscriber queue, flusher)
    - whilly/api/static_mount.py, whilly/api/__init__.py
    - whilly/cli/server.py (`whilly server` subcommand bootstrap)
    - whilly/web_status.py (standalone localhost:9191 server: GET /api/status JSON + GET / HTML)
    - openspec/AUTHORING.md
  </read_first>
  <action>
    Append the API/SSE/status requirements to the same `openspec/specs/web-status-ui/spec.md`
    (each SHALL/MUST first body line + ≥1 `#### Scenario:` WHEN/THEN). Cover at minimum:
    (a) the plans API exposes list (GET /api/v1/plans), create (POST → 201) and update
    (PATCH /api/v1/plans/{plan_id}) with If-Match optimistic-concurrency semantics;
    (b) the tasks API returns paginated task rows including human-review annotation;
    (c) the metrics endpoint exposes orchestration metrics; (d) the SSE endpoint streams
    events over GET /events/stream with event_id/event_type framing and per-subscriber
    delivery, fed by the event flusher; (e) the static mount serves the dashboard web
    assets; (f) the `whilly server` subcommand boots the FastAPI control plane; (g) the
    standalone web_status server answers GET /api/status (JSON) and GET / (HTML) on
    localhost:9191. Spec observed behavior; reference dashboard-tui for the browser
    rendering layer rather than duplicating it.

    Finish by ensuring no delta headers exist and the file passes strict validation.
  </action>
  <verify>
    <automated>openspec validate web-status-ui --strict</automated>
  </verify>
  <acceptance_criteria>
    - `openspec validate web-status-ui --strict` reports the spec is valid (0 errors, 0 warnings, exit 0).
    - The spec enumerates the real /api/v1/plans + /events/stream + /api/status routes (no invented endpoints).
    - Every `### Requirement:` has SHALL/MUST on its first body line and ≥1 `#### Scenario:` with WHEN/THEN.
    - No `## ADDED Requirements` or other delta headers appear in the file.
  </acceptance_criteria>
  <done>`openspec validate web-status-ui --strict` prints that the spec is valid; endpoints match the whilly/api/* + web_status.py + cli/server.py source.</done>
</task>

</tasks>

<verification>
- openspec/specs/web-status-ui/spec.md exists.
- `openspec validate web-status-ui --strict` reports valid (exit 0).
- The auth-security boundary is referenced, not duplicated.
</verification>

<success_criteria>
- OPS-02 has a normative, reverse-spec'd subsystem-altitude capability spec passing `--strict`.
- Transport two-token auth, the FastAPI endpoints, SSE, and the localhost web status are all captured from real v4 code.
</success_criteria>

<output>
Create `.planning/phases/25-operator-surface-cluster/25-02-SUMMARY.md` when done.
</output>
