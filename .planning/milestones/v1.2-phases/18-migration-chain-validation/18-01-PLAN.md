---
phase: 18-migration-chain-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/test_alembic_full_chain.py
  - .gitignore
autonomous: true
requirements: [MIG-01]
must_haves:
  truths:
    - "Full chain 001→028 upgrades green from an empty Postgres container"
    - "alembic_version equals 028_webauthn_user_handles after upgrade head"
    - "downgrade base returns the schema to empty (no whilly tables remain)"
    - "Re-running upgrade head a second time is a no-op (idempotent)"
    - "A machine-readable evidence file records head revision, migration count, and pass flags"
  artifacts:
    - path: "tests/integration/test_alembic_full_chain.py"
      provides: "Full-chain upgrade/downgrade/idempotency test covering all 28 revisions + evidence write"
      contains: "028_webauthn_user_handles"
    - path: ".gitignore"
      provides: "Ignore rule for the ephemeral evidence file"
      contains: "migration-chain-evidence.json"
  key_links:
    - from: "tests/integration/test_alembic_full_chain.py"
      to: "EXPECTED_CHAIN[-1]"
      via: "head-revision assertions reference the tuple tail, not a literal"
      pattern: "EXPECTED_CHAIN\\[-1\\]"
    - from: "tests/integration/test_alembic_full_chain.py"
      to: "migration-chain-evidence.json"
      via: "Path(...).write_text(json.dumps(evidence))"
      pattern: "migration-chain-evidence\\.json"
---

<objective>
Extend the existing full-chain Alembic integration test so it covers all 28 revisions
(001 → 028_webauthn_user_handles) instead of stopping at 016, and write a machine-readable
evidence file after the idempotency proof passes. This delivers MIG-01: the migration chain
runs green from an empty Postgres in Docker, downgrades cleanly, and re-upgrades idempotently.

Purpose: The test is the source of truth for the chain. EXPECTED_CHAIN is stale at 016 and three
head-revision assertions hardcode `"016_jira_work_sessions"`, so the test currently FAILS against
the real schema. Fixing it is the core deliverable; Plan 02's Make/CI entry points wrap this test.
Output: Updated test file (28-revision coverage, structural assertions for 017–028, evidence write)
and a .gitignore entry for the evidence file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-migration-chain-validation/18-RESEARCH.md
@.planning/phases/18-migration-chain-validation/18-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend EXPECTED_CHAIN to 028 and fix stale head-revision assertions</name>
  <files>tests/integration/test_alembic_full_chain.py</files>
  <read_first>
    - tests/integration/test_alembic_full_chain.py (the file being modified — read all of it first)
    - tests/conftest.py (imports: _build_alembic_config, _retry_colima_flake, DOCKER_REQUIRED, docker_available, resolve_docker_host, HAS_TESTCONTAINERS)
    - .planning/phases/18-migration-chain-validation/18-PATTERNS.md (exact EXPECTED_CHAIN tuple and assertion patterns)
  </read_first>
  <behavior>
    - test_expected_chain_files_exist_on_disk passes: every one of the 28 revisions in EXPECTED_CHAIN has a matching `{revision}.py` under versions/ (including the `019a_plans_archived_at` 'a'-suffix file)
    - test_full_chain_upgrade_then_full_downgrade asserts `head_version == EXPECTED_CHAIN[-1]` after upgrade head (resolves to "028_webauthn_user_handles"), not the literal "016_jira_work_sessions"
    - test_full_chain_then_re_upgrade_idempotent asserts both first_version and second_version equal `EXPECTED_CHAIN[-1]`, not the literal "016_jira_work_sessions"
  </behavior>
  <action>
    Replace the `EXPECTED_CHAIN` tuple (currently ending at line 60 with "016_jira_work_sessions") with the full 28-entry tuple listed verbatim in 18-PATTERNS.md, adding revisions 017 through 028 in order. Use the EXACT string "019a_plans_archived_at" (alphabetic 'a' suffix is intentional — do NOT use a %03d format or `\d{3}_` regex anywhere). Replace all three stale `== "016_jira_work_sessions"` assertions (lines 146, 422, 426) with `== EXPECTED_CHAIN[-1]` so the head reference has a single source of truth and cannot drift. Do not introduce a second literal head string anywhere.
  </action>
  <verify>
    <automated>python -c "import ast,sys; t=ast.parse(open('tests/integration/test_alembic_full_chain.py').read()); print('parse ok')" && grep -c '028_webauthn_user_handles' tests/integration/test_alembic_full_chain.py && grep -v '^#' tests/integration/test_alembic_full_chain.py | grep -c '016_jira_work_sessions'</automated>
  </verify>
  <acceptance_criteria>
    - File parses as valid Python (ast.parse prints "parse ok").
    - EXPECTED_CHAIN contains exactly 28 string entries, last entry is "028_webauthn_user_handles".
    - The exact string "019a_plans_archived_at" appears in EXPECTED_CHAIN.
    - `grep -v '^#' ... | grep -c '016_jira_work_sessions'` returns 1 (only the EXPECTED_CHAIN tuple member; zero remaining `== "016..."` assertions).
    - All head/idempotency assertions compare against `EXPECTED_CHAIN[-1]`.
  </acceptance_criteria>
  <done>EXPECTED_CHAIN covers all 28 revisions and every head-revision assertion references EXPECTED_CHAIN[-1] with no remaining "016_jira_work_sessions" comparison literal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add structural assertions for revisions 017–028 and extend post-downgrade table set</name>
  <files>tests/integration/test_alembic_full_chain.py</files>
  <read_first>
    - tests/integration/test_alembic_full_chain.py (the existing 006–016 structural-assertion blocks are the template to copy)
    - .planning/phases/18-migration-chain-validation/18-PATTERNS.md (scheduler_rules/scheduler_poll_cycles + archived_at/last_event_at assertion snippets and the full post_downgrade_tables set)
    - whilly/adapters/db/migrations/versions/017_scheduler_rules_and_cycles.py (confirms table names scheduler_rules, scheduler_poll_cycles)
    - whilly/adapters/db/migrations/versions/019a_plans_archived_at.py (confirms columns archived_at, last_event_at on plans)
  </read_first>
  <behavior>
    - After upgrade head, the test asserts the 017 tables {scheduler_rules, scheduler_poll_cycles} exist in information_schema.tables
    - After upgrade head, the test asserts the 019a columns archived_at and last_event_at exist on the plans table (count == 2)
    - After downgrade base, post_downgrade_tables is the empty set — including all 017–028 tables (scheduler_rules, scheduler_poll_cycles, sessions, magic_links, users, user_totp_secrets, auth_audit, webauthn_credentials, webauthn_challenges, webauthn_user_handles) which all have real drop_table downgrades
  </behavior>
  <action>
    Following the existing `information_schema` query style (the jira_work_tables and github_issue_ref blocks already in the file, using `_fetchall`/`_fetchval`), add structural assertions inside `test_full_chain_upgrade_then_full_downgrade` for the 017 tables (`scheduler_rules`, `scheduler_poll_cycles` — note `scheduler_poll_cycles`, NOT `scheduler_cycles`) and the 019a `plans` columns (`archived_at`, `last_event_at`, expect count 2). Extend the `post_downgrade_tables` query (currently asserting the empty set near line 408) to include every new 017–028 table listed in 18-PATTERNS.md; PATTERNS confirmed all 12 new migrations ship real `drop_table`/`drop_column` downgrades, so the empty-set assertion holds. Keep using `_retry_colima_flake` around upgrade/downgrade calls.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('tests/integration/test_alembic_full_chain.py').read()); print('ok')" && grep -c 'scheduler_poll_cycles' tests/integration/test_alembic_full_chain.py && grep -c 'webauthn_user_handles' tests/integration/test_alembic_full_chain.py</automated>
  </verify>
  <acceptance_criteria>
    - File parses as valid Python.
    - "scheduler_poll_cycles" appears at least once (017 structural assertion + post-downgrade set); the typo "scheduler_cycles" does NOT appear.
    - "webauthn_user_handles" appears in the post_downgrade_tables set.
    - "archived_at" and "last_event_at" both appear in a 019a column assertion.
    - post_downgrade_tables is still asserted equal to set() (empty after downgrade base).
  </acceptance_criteria>
  <done>Structural assertions cover the 017 tables and 019a columns, and the post-downgrade empty-set assertion lists all 017–028 tables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write machine-readable evidence file and gitignore it</name>
  <files>tests/integration/test_alembic_full_chain.py, .gitignore</files>
  <read_first>
    - tests/integration/test_alembic_full_chain.py (end of test_full_chain_then_re_upgrade_idempotent — where the write goes)
    - .planning/phases/18-migration-chain-validation/18-PATTERNS.md (evidence write snippet and JSON shape)
    - .gitignore (the .whilly_state.json root-level ephemeral-file pattern to mirror)
  </read_first>
  <behavior>
    - After the idempotency proof passes, the test writes migration-chain-evidence.json at repo root containing: timestamp (ISO8601 + "Z"), head_revision == EXPECTED_CHAIN[-1], migration_count == len(EXPECTED_CHAIN) (28), upgrade_ok true, downgrade_ok true, idempotent_ok true
    - The evidence file is git-ignored (not committed) — it is runtime evidence, like .whilly_state.json
  </behavior>
  <action>
    Add the imports `datetime`, `json`, and `from pathlib import Path` to the import block if not already present. At the end of `test_full_chain_then_re_upgrade_idempotent` (after the idempotency assertions pass), write `migration-chain-evidence.json` at the repo root using `Path("migration-chain-evidence.json").write_text(json.dumps(evidence, indent=2))` with the evidence dict from 18-PATTERNS.md: head_revision = `EXPECTED_CHAIN[-1]`, migration_count = `len(EXPECTED_CHAIN)`, and the three boolean pass flags. Do NOT print the DSN or any connection string into the evidence file or test stdout (DSN carries the ephemeral container password). Add `migration-chain-evidence.json` to `.gitignore` next to the `.whilly_state.json` entry.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('tests/integration/test_alembic_full_chain.py').read()); print('ok')" && grep -c 'migration-chain-evidence.json' tests/integration/test_alembic_full_chain.py && grep -c 'migration-chain-evidence.json' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - File parses as valid Python.
    - "migration-chain-evidence.json" appears in the test file (write target) and in .gitignore (ignore rule).
    - The evidence dict uses `EXPECTED_CHAIN[-1]` and `len(EXPECTED_CHAIN)` (no hardcoded "028..." or 28 literal).
    - No DSN / connection-string value is written into the evidence JSON or echoed to stdout.
  </acceptance_criteria>
  <done>The idempotency test writes a machine-readable evidence file with head revision, migration count, and pass flags, and the file is git-ignored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testcontainers → Postgres | Ephemeral container DSN carries a generated password; must not leak into committed artifacts |
| test → evidence file | Test writes a JSON file read by operators/CI; must contain no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | migration-chain-evidence.json | mitigate | Evidence dict contains only revision string, count, and booleans — no DSN, password, or host. Task 3 acceptance criterion forbids writing the DSN. |
| T-18-02 | Information Disclosure | test stdout / CI logs | mitigate | Do not print the testcontainers DSN (carries generated password) to stdout; structural assertions query schema, not credentials. |
| T-18-03 | Tampering | committed evidence file | accept | Evidence file is git-ignored (Task 3) so a stale/tampered copy cannot be committed; CI regenerates it per run. Low value, ephemeral. |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(...)"` confirms the test file stays valid Python after each edit.
- Full behavioral proof requires Docker: `pytest -q tests/integration/test_alembic_full_chain.py --tb=short`
  (skips gracefully if Docker daemon is absent; runs green in CI on ubuntu-latest via Plan 02).
- `grep` gates confirm EXPECTED_CHAIN reaches 028, the 016 literal assertions are gone, and the
  evidence-file wiring exists in both the test and .gitignore.
</verification>

<success_criteria>
- EXPECTED_CHAIN covers all 28 revisions; head/idempotency assertions reference EXPECTED_CHAIN[-1].
- Structural assertions cover 017 tables and 019a columns; post-downgrade set lists all 017–028 tables.
- migration-chain-evidence.json is written by the test and git-ignored.
- No DSN/secret leaks into the evidence file or stdout.
</success_criteria>

<output>
Create `.planning/phases/18-migration-chain-validation/18-01-SUMMARY.md` when done
</output>
