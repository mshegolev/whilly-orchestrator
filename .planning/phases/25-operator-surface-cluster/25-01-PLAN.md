---
phase: 25-operator-surface-cluster
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/dashboard-tui/spec.md
  - openspec/specs/reporting/spec.md
autonomous: true
requirements: [OPS-01, OPS-03]
must_haves:
  truths:
    - "openspec/specs/dashboard-tui/spec.md exists and passes `openspec validate dashboard-tui --strict` with zero errors and zero warnings"
    - "openspec/specs/reporting/spec.md exists and passes `openspec validate reporting --strict` with zero errors and zero warnings"
    - "dashboard-tui spec captures the real Rich Live hotkey set registered in whilly/dashboard.py (d, l, t, s, $, p, g, c, n, r, h, ?)"
    - "reporting spec records that the Reporter/generate_summary surface is legacy/unwired in the v4 worker-claim path"
  artifacts:
    - path: "openspec/specs/dashboard-tui/spec.md"
      provides: "Normative spec for the Rich Live TUI dashboard states + hotkeys and the web SSE dashboard"
      contains: "## Purpose"
    - path: "openspec/specs/reporting/spec.md"
      provides: "Normative spec for per-iteration JSON + end-of-run Markdown reporting and its v4 wiring status"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/dashboard-tui/spec.md"
      to: "whilly/dashboard.py"
      via: "reverse-spec of registered hotkeys + Rich Live states"
      pattern: "keyboard\\.register"
    - from: "openspec/specs/reporting/spec.md"
      to: "whilly/reporter.py"
      via: "reverse-spec of Reporter.finalize + generate_summary"
      pattern: "def finalize|def generate_summary"
---

<objective>
Spec the observed v4 behavior of two operator progress surfaces as normative OpenSpec
capability specs: the Rich Live TUI dashboard (OPS-01, `dashboard-tui`) and the
reporting layer (OPS-03, `reporting`). Both are documentation-only; no `whilly/`
Python changes.

Purpose: Capture the dashboard's render states + hotkey contract and the reporter's
per-iteration JSON + end-of-run Markdown contract — including the truthful v4 wiring
status of the reporter — as machine-checkable specs the verifier can adversarially
check against source.
Output: `openspec/specs/dashboard-tui/spec.md` and `openspec/specs/reporting/spec.md`,
each passing `openspec validate <slug> --strict`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-operator-surface-cluster/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
@openspec/COVERAGE-MATRIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec the dashboard-tui capability (OPS-01)</name>
  <files>openspec/specs/dashboard-tui/spec.md</files>
  <read_first>
    - whilly/dashboard.py (Rich Live TUI: Dashboard class, Live(screen=True, refresh_per_second=1), registered hotkeys d/l/t/s/$/p/g/c/n/r/h/?, overlay modes 'log'|'task_log'|'detail', PRD-wizard mode keys 1/2/3, NullDashboard)
    - whilly/cli/dashboard.py (`whilly dashboard` subcommand: render_dashboard pure renderer, DashboardRow, DashboardState, Live polling cadence, hotkey runtime)
    - whilly/api/dashboard.py (web dashboard: Jinja2 index.html.j2 full page + fragments, live updates over GET /events/stream SSE, hx-get fragment refresh, FileLogStore tail-N)
    - openspec/AUTHORING.md (section format, SHALL/MUST on first body line, #### Scenario rules)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar to mirror)
  </read_first>
  <action>
    Reverse-spec the observed v4 behavior of the Rich Live TUI dashboard and the web
    SSE dashboard. Write `## Purpose` (≥50 chars) describing what dashboard-tui covers
    (the in-terminal Rich Live progress display, its overlay states, its hotkey contract,
    and the browser-side SSE dashboard surface) and the boundary that the FastAPI control
    plane itself belongs to web-status-ui (reference, do not duplicate).

    Write `## Requirements` with `### Requirement:` blocks each having SHALL/MUST on the
    first body line and ≥1 `#### Scenario:` with WHEN/THEN. Cover at minimum: (a) the Rich
    Live full-screen display starts at the documented refresh cadence; (b) the registered
    interactive hotkey contract — name the actual keys bound in whilly/dashboard.py
    (d=task detail, l=log overlay, t=all tasks, s=stats, $=cost panel, p=PRD info,
    g=generate plan, c=challenge plan, n=new idea, r=reset task, h/?=help) and that an
    unbound key is a no-op; (c) overlay-mode state transitions (log / task_log / detail)
    determine refresh behavior; (d) NullDashboard substitution in headless/non-TTY mode
    suppresses the Live display; (e) the web dashboard pushes live updates over the
    /events/stream SSE channel and degrades to fragment hx-get when EventSource is
    unavailable. Spec only what the code renders/handles — do not invent hotkeys or states.
  </action>
  <verify>
    <automated>openspec validate dashboard-tui --strict</automated>
  </verify>
  <acceptance_criteria>
    - openspec/specs/dashboard-tui/spec.md exists.
    - `openspec validate dashboard-tui --strict` reports the spec is valid (0 errors, 0 warnings, exit 0).
    - File contains `## Purpose` (≥50 chars) and `## Requirements`.
    - Every `### Requirement:` has SHALL or MUST on its first body line.
    - Every `### Requirement:` has ≥1 `#### Scenario:` with `- **WHEN**` and `- **THEN**`.
    - Hotkey requirement enumerates the real keys registered in whilly/dashboard.py (no invented keys).
  </acceptance_criteria>
  <done>`openspec validate dashboard-tui --strict` prints that the spec is valid; hotkeys and states match whilly/dashboard.py + cli/dashboard.py + api/dashboard.py.</done>
</task>

<task type="auto">
  <name>Task 2: Spec the reporting capability (OPS-03)</name>
  <files>openspec/specs/reporting/spec.md</files>
  <read_first>
    - whilly/reporter.py (Reporter class: json_path naming `whilly_{base}_{ts}.json`, IterationReport, CostTotals, finalize(), _save_json(), generate_summary() cross-plan Markdown, fmt_tokens/fmt_duration helpers)
    - whilly/dashboard.py line ~34 (the ONLY v4 consumer: imports CostTotals/fmt_duration/fmt_tokens helpers — Reporter itself is NOT instantiated in the worker-claim path)
    - openspec/AUTHORING.md
    - openspec/specs/task-model-fsm/spec.md (exemplar)
  </read_first>
  <action>
    Reverse-spec the observed v4 behavior of whilly/reporter.py. Write `## Purpose`
    (≥50 chars) describing what reporting covers (per-iteration JSON reports and the
    end-of-run / cross-plan Markdown summary) and the truthful wiring status.

    Write `## Requirements` with SHALL/MUST first-body-line + ≥1 `#### Scenario:` each.
    Cover at minimum: (a) the Reporter writes per-iteration JSON to the timestamped
    report path under the report dir; (b) finalize() emits the terminal report including
    cost totals; (c) generate_summary() aggregates multiple plan report JSON files into a
    single Markdown summary and returns the output path (or None when no reports exist);
    (d) the cost/token formatting helpers (fmt_tokens, fmt_duration, CostTotals) define
    the human-readable presentation contract reused by the dashboard.

    GROUNDING CAUTION — state wiring truthfully: VERIFY+STATE the v4 wiring status. The
    Reporter class and generate_summary are NOT instantiated/called in the v4 worker-claim
    execution path; the only live import is whilly/dashboard.py pulling the formatting
    helpers (CostTotals, fmt_duration, fmt_tokens). Write a normative requirement that
    captures this status — e.g. the system SHALL treat the Reporter/generate_summary
    surface as a legacy reporting contract not wired into the v4 worker-claim loop, while
    the formatting helpers SHALL remain the presentation contract consumed by the
    dashboard. Do NOT assert that Reporter runs every iteration in v4.
  </action>
  <verify>
    <automated>openspec validate reporting --strict</automated>
  </verify>
  <acceptance_criteria>
    - openspec/specs/reporting/spec.md exists.
    - `openspec validate reporting --strict` reports the spec is valid (0 errors, 0 warnings, exit 0).
    - File contains `## Purpose` (≥50 chars) and `## Requirements`.
    - Every `### Requirement:` has SHALL or MUST on its first body line and ≥1 `#### Scenario:`.
    - A requirement records the legacy/unwired v4 status of Reporter/generate_summary truthfully (not asserting per-iteration wiring in v4).
  </acceptance_criteria>
  <done>`openspec validate reporting --strict` prints that the spec is valid; wiring status reflects that only the formatter helpers are consumed by the dashboard in v4.</done>
</task>

</tasks>

<verification>
- Both spec files exist at openspec/specs/dashboard-tui/spec.md and openspec/specs/reporting/spec.md.
- `openspec validate dashboard-tui --strict` and `openspec validate reporting --strict` both report valid (exit 0).
- No delta headers (`## ADDED Requirements` etc.) appear in either file.
</verification>

<success_criteria>
- OPS-01 and OPS-03 each have a normative, reverse-spec'd capability spec passing `--strict`.
- dashboard-tui hotkeys/states match whilly/dashboard.py; reporting wiring status is truthful for v4.
</success_criteria>

<output>
Create `.planning/phases/25-operator-surface-cluster/25-01-SUMMARY.md` when done.
</output>
