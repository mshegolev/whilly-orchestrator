---
phase: 14-wui-method-and-fragment-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - whilly/operator_views.py
  - tests/unit/test_wui_contract_static.py
requirements:
  - WUI-01
  - WUI-02
  - WUI-03
must_haves:
  truths:
    - "Active WUI hotkeys use data-surface-tab and canonical 1-5 surfaces."
    - "Active WUI worker controls use /api/v1/admin/workers/* routes."
    - "The logs fragment remains routeable with backend coverage but outside canonical nav."
    - "Admin and PRD fragments remain quarantined from active WUI navigation."
    - "Tests fail if noncanonical fragments become visible active navigation without a parity decision."
---

<objective>
Close Phase 14 by making WUI artifact statuses accurately reflect the resolved route/navigation
boundary and by pinning active navigation against noncanonical fragments.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update artifact classification reasons</name>
  <files>whilly/operator_views.py, tests/unit/test_wui_contract_static.py</files>
  <behavior>
    - `_logs.html` is routeable-noncanonical with backend coverage and Phase 15 parity follow-up.
    - `_admin.html` and `_prd.html` are inactive/quarantined with future route-support follow-up.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard active dashboard navigation</name>
  <files>tests/unit/test_wui_contract_static.py</files>
  <behavior>
    - Active `index.html.j2` must not link `fragment=logs`.
    - Active `index.html.j2` must not include `_admin.html`, `_prd.html`, or stale `/admin/*` and
      `/prd/*` fragment routes.
  </behavior>
</task>

</tasks>

<verification>
- `python3 -m pytest -q tests/unit/test_wui_contract_static.py tests/integration/test_htmx_dashboard.py::test_logs_fragment_is_routeable_noncanonical_artifact tests/integration/test_htmx_dashboard.py::test_dashboard_mirrors_operator_surfaces_and_hotkeys --maxfail=1`
- `python3 -m ruff check whilly/operator_views.py tests/unit/test_wui_contract_static.py`
</verification>
