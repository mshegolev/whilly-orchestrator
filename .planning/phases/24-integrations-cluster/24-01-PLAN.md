---
phase: 24-integrations-cluster
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/jira-integration/spec.md
  - openspec/specs/jira-watcher-daemon/spec.md
autonomous: true
requirements: [INT-01, INT-04]
must_haves:
  truths:
    - "openspec validate jira-integration --strict reports the spec is valid (0 errors, 0 warnings)"
    - "openspec validate jira-watcher-daemon --strict reports the spec is valid (0 errors, 0 warnings)"
    - "jira-integration spec states Jira auth expectations (basic/bearer, env+toml resolution) and separates read-only fetch from mutating ticket transitions"
    - "jira-watcher-daemon spec captures lifecycle, pause gate, readiness gate, and fail-closed behavior from Phase 20 shipped code"
  artifacts:
    - path: "openspec/specs/jira-integration/spec.md"
      provides: "INT-01 normative capability spec reverse-spec'd from real v4 Jira code"
      contains: "## Requirements"
    - path: "openspec/specs/jira-watcher-daemon/spec.md"
      provides: "INT-04 normative capability spec for the watch-loop daemon"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/jira-integration/spec.md"
      to: "whilly/sources/jira.py + whilly/jira_board.py"
      via: "requirements reverse-spec'd from observed v4 behavior"
      pattern: "SHALL|MUST"
    - from: "openspec/specs/jira-watcher-daemon/spec.md"
      to: "whilly/cli/jira_watch_loop.py"
      via: "lifecycle/pause/readiness/fail-closed requirements"
      pattern: "SHALL|MUST"
---

<objective>
Write two normative OpenSpec capability specs that capture Whilly's Jira-facing
contracts as observed in v4.7.0 code: `jira-integration` (INT-01) and
`jira-watcher-daemon` (INT-04). Both are reverse-spec'd from the real modules,
state auth expectations explicitly, and draw a clear read-only vs mutating
boundary. Both MUST pass `openspec validate <slug> --strict` with zero errors
and zero warnings.

Purpose: Lock the Jira read/work-snapshot/board-sync surface and the Phase 20
watch-loop daemon as machine-checkable contracts so future behavior changes go
through delta proposals against an accurate baseline.
Output: openspec/specs/jira-integration/spec.md, openspec/specs/jira-watcher-daemon/spec.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-integrations-cluster/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec the observed v4 behavior of Jira read/source/board-sync (INT-01)</name>
  <files>openspec/specs/jira-integration/spec.md</files>
  <read_first>
    - whilly/sources/jira.py (JiraAuth.from_config auth resolution; fetch_single_jira_issue read-only fetch; ADF flatten; JIRA-&lt;key&gt; task id; secret/SSL/proxy handling; prompt sanitization)
    - whilly/jira_board.py (JiraBoardClient mutating transitions via REST POST /transitions; DEFAULT_JIRA_STATUS_MAPPING; soft-fail-never-raise; from_config enable toggles)
    - whilly/jira_work.py (read-only classification/comment-command/readiness probe — no network)
    - whilly/cli/jira.py (whilly jira import / readiness CLI surface and credential gate)
    - whilly/cli/jira_tui.py (read-only Jira browse TUI)
    - openspec/AUTHORING.md
    - openspec/specs/task-model-fsm/spec.md
  </read_first>
  <action>
    Create openspec/specs/jira-integration/spec.md for slug `jira-integration`. Mirror the
    exemplar structure exactly: a `## Purpose` section (≥50 chars, plain prose, no code blocks)
    naming the capability and its scope, then a `## Requirements` section with several
    `### Requirement:` blocks. Each requirement's FIRST body line MUST contain SHALL or MUST
    (case-sensitive) and stay ≤500 chars; each requirement MUST have ≥1 `#### Scenario:` (exactly
    4 hashtags) with `- **WHEN**` and `- **THEN**` bullets. Reverse-spec ONLY observed v4 behavior:
    (1) Auth expectations — Jira credentials SHALL resolve from the [jira] toml section then env
    (JIRA_SERVER_URL / JIRA_USERNAME / JIRA_API_TOKEN) then optional company-settings YAML; auth_scheme
    basic (Basic base64 user:token) vs bearer (PAT); verify_ssl/ca_file honored; unconfigured auth
    SHALL raise RuntimeError naming the missing fields. (2) Read-only boundary — fetching a single
    issue (fetch_single_jira_issue) SHALL perform only GET requests, flatten ADF descriptions, extract
    Acceptance/Test bullets, sanitize external text, and merge one JIRA-&lt;key&gt; task into the plan
    idempotently. (3) Mutating boundary — JiraBoardClient.set_issue_status SHALL be the only path that
    POSTs a transition, MUST match the target by transition `to.name` case-insensitively, and MUST
    soft-fail (return False, never raise into the loop) on any HTTP/auth/unavailable-transition error.
    (4) Status mapping — the whilly→Jira DEFAULT_JIRA_STATUS_MAPPING applies and is overridable via
    [jira].status_mapping. State the read-only-vs-mutating split as an explicit requirement. Do NOT
    invent behavior the code does not exhibit. No fenced code blocks inside requirement bodies.
  </action>
  <verify>
    <automated>openspec validate jira-integration --strict --json</automated>
  </verify>
  <acceptance_criteria>
    - openspec/specs/jira-integration/spec.md exists
    - `openspec validate jira-integration --strict` reports "is valid" and exits 0
    - `## Purpose` (≥50 chars) and `## Requirements` (≥1 `### Requirement:`) present
    - every requirement's first body line contains SHALL or MUST and is ≤500 chars
    - every requirement has ≥1 `#### Scenario:` with WHEN/THEN bullets
    - at least one requirement states Jira auth expectations (toml/env/company-settings, basic vs bearer)
    - at least one requirement states the read-only fetch vs mutating ticket-transition boundary
    - no `## ADDED Requirements` / delta headers anywhere in the file
  </acceptance_criteria>
  <done>jira-integration spec exists, passes strict validation, and normatively captures Jira auth + read/mutating boundary from real v4 code.</done>
</task>

<task type="auto">
  <name>Task 2: Spec the observed v4 behavior of the Jira watch-loop daemon (INT-04)</name>
  <files>openspec/specs/jira-watcher-daemon/spec.md</files>
  <read_first>
    - whilly/cli/jira_watch_loop.py (lifecycle: interval resolution, interruptible threading.Event sleep, signal handlers, atomic status file, backoff sequence; pause gate via PauseControl/.whilly_pause with watch.paused event; readiness gate with watch.block event and fail-closed None=not-ready; --dispatch default-off; PID single-instance guard EXIT_VALIDATION_ERROR; best-effort DB audit events)
    - whilly/jira_watch.py (collect_jira_work_snapshot read-only refresh; JiraWorkSnapshot)
    - whilly/pause_control.py (PauseControl semantics — read to confirm pause behavior)
    - .planning/phases/20-jira-watcher-daemon/20-CONTEXT.md and 20-01-SUMMARY.md / 20-02-SUMMARY.md / 20-03-SUMMARY.md (shipped intent + locked decisions)
    - openspec/AUTHORING.md
    - openspec/specs/task-model-fsm/spec.md
  </read_first>
  <action>
    Create openspec/specs/jira-watcher-daemon/spec.md for slug `jira-watcher-daemon`. Follow the
    exemplar + AUTHORING format (Purpose ≥50 chars; Requirements with SHALL/MUST first body lines
    ≤500 chars; ≥1 four-hashtag Scenario each with WHEN/THEN). Reverse-spec ONLY the Phase 20 shipped
    behavior: (1) Lifecycle — the watch loop SHALL resolve its poll interval (--interval &gt; WHILLY_JIRA_WATCH_INTERVAL
    &gt; 300s default), sleep via threading.Event.wait (never time.sleep) so SIGTERM/SIGINT stop the loop
    gracefully with EXIT_OK, write an atomic secret-free status JSON each cycle, and apply the 5/10/20/40/60s
    backoff to the NEXT cycle on failure. (2) Single-instance guard — startup SHALL acquire a PID lock
    via os.kill(pid,0) liveness probe and refuse to start (EXIT_VALIDATION_ERROR) when a live watcher holds
    it; an unsignalable/EPERM process MUST be treated as fail-closed (refuse). (3) Pause gate — while
    .whilly_pause is active, read-only polling SHALL continue but dispatch SHALL be suppressed and a
    watch.paused audit event emitted. (4) Readiness gate — dispatch SHALL be blocked with a watch.block
    event whenever the readiness verdict is not ready_for_testing (unless --allow-unready-run), and an
    undeterminable readiness (None) MUST be treated as NOT ready — the gate fails closed. (5) Dispatch is
    default-off behind an explicit --dispatch flag and runs only when unpaused AND readiness satisfied.
    State auth expectation: the credential gate runs in the CLI layer BEFORE the loop starts (the loop
    itself performs read-only collection). Capture audit/persist as best-effort (warn-not-fail). No fenced
    code blocks in requirement bodies. Do not invent behavior beyond the shipped modules/summaries.
  </action>
  <verify>
    <automated>openspec validate jira-watcher-daemon --strict --json</automated>
  </verify>
  <acceptance_criteria>
    - openspec/specs/jira-watcher-daemon/spec.md exists
    - `openspec validate jira-watcher-daemon --strict` reports "is valid" and exits 0
    - `## Purpose` (≥50 chars) and `## Requirements` (≥1 `### Requirement:`) present
    - every requirement's first body line contains SHALL or MUST and is ≤500 chars
    - every requirement has ≥1 `#### Scenario:` with WHEN/THEN bullets
    - requirements capture lifecycle, the pause gate, and the readiness gate
    - at least one requirement states fail-closed behavior (None readiness = not ready; live/EPERM PID = refuse)
    - auth expectation (CLI credential gate before loop) is stated; read-only-polling vs gated-dispatch boundary present
    - no `## ADDED Requirements` / delta headers anywhere in the file
  </acceptance_criteria>
  <done>jira-watcher-daemon spec exists, passes strict validation, and normatively captures lifecycle + pause/readiness gates + fail-closed behavior from Phase 20 code.</done>
</task>

</tasks>

<verification>
Both specs pass `openspec validate <slug> --strict` (0 errors, 0 warnings). Each
spec reverse-spec'd from real v4 modules listed in `<read_first>`, states auth
expectations, and draws the read-only vs mutating boundary explicitly.
</verification>

<success_criteria>
INT-01 (jira-integration) and INT-04 (jira-watcher-daemon) specs exist at
openspec/specs/&lt;slug&gt;/spec.md, mirror the exemplar, and pass strict validation.
</success_criteria>

<output>
Create `.planning/phases/24-integrations-cluster/24-01-SUMMARY.md` when done
</output>
