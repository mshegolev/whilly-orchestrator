---
phase: 26-platform-cluster
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/configuration/spec.md
autonomous: true
requirements: [PLAT-01]
must_haves:
  truths:
    - "openspec validate configuration --strict reports the spec is valid (0 errors, 0 warnings)"
    - "The spec enumerates the WHILLY_ env-var contract and the layered precedence order"
    - "State no-op fields (WORKTREE/USE_WORKSPACE/STATE_FILE) are documented truthfully as no-ops"
  artifacts:
    - path: "openspec/specs/configuration/spec.md"
      provides: "Normative configuration capability spec reverse-spec'd from whilly/config.py"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/configuration/spec.md"
      to: "whilly/config.py"
      via: "reverse-spec of WhillyConfig.from_env / load_layered / resolved schemes"
      pattern: "from_env|load_layered|resolved"
---

<objective>
Author the normative OpenSpec capability spec for `configuration` (PLAT-01),
reverse-spec'd from the real v4 config layer.

Purpose: Capture Whilly's guaranteed configuration behavior — the `WHILLY_`
env-var contract, layered precedence, the `resolved()` env:/keyring:/file:
secret schemes, and the project-config surface — as a machine-checkable spec.
Output: `openspec/specs/configuration/spec.md` passing `openspec validate
configuration --strict`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-platform-cluster/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec the observed v4 behavior of the configuration layer</name>
  <files>openspec/specs/configuration/spec.md</files>
  <read_first>
    - whilly/config.py (WhillyConfig dataclass, from_env, load_layered, _coerce, resolved() with env:/keyring:/file: schemes, defaults: model claude-opus-4-6[1m], max_parallel 3, log_dir whilly_logs/)
    - whilly/config_sections.py (config section parsing)
    - whilly/external_integrations.py (integration config surface)
    - whilly/project_config/loader.py, models.py, plan_builder.py, presets.py, resolver.py
    - whilly/cli/project_config.py, whilly/cli/project_map.py, whilly/cli/quick_setup.py
    - openspec/AUTHORING.md (format rules — read in full)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar to mirror)
  </read_first>
  <action>
    Reverse-spec the observed v4 behavior of the configuration capability. Write
    a `## Purpose` (≥50 chars) describing what the capability governs and its
    scope boundary (env/TOML layering + secret schemes live here; auth-security
    owns secret *handling* — reference, do not duplicate).

    Then write `## Requirements` with `### Requirement:` blocks (each FIRST body
    line begins with SHALL or MUST, ≤500 chars; each has ≥1 `#### Scenario:`
    with WHEN/THEN). Cover at minimum:
    - The `WHILLY_`-prefixed env-var contract read by `WhillyConfig.from_env`,
      naming the documented defaults (model `claude-opus-4-6[1m]`, max_parallel
      3, log_dir `whilly_logs/`, budget, timeout).
    - Layered precedence ordering as actually implemented by `load_layered`
      (defaults → user TOML → repo `whilly.toml` → `.env` → `WHILLY_*` →
      CLI), with a scenario proving a higher layer overrides a lower one.
    - `_coerce` type coercion of string env values to typed config fields.
    - The `resolved()` secret-reference schemes (`env:`, `keyring:`, `file:`)
      and how each is resolved at access time.
    - The project-config surface (project_config loader/resolver/presets and
      the project_config / project_map / quick_setup CLI commands).
    - State no-op fields stated TRUTHFULLY: `WHILLY_WORKTREE`,
      `WHILLY_USE_WORKSPACE`, `WHILLY_STATE_FILE` (and the workspace/worktree/
      state-file flags) are accepted but are NO-OPS in v4.7.0 — the spec MUST
      describe them as no-ops, not as live behavior.

    Mirror the exemplar's section shape exactly. Do NOT use any delta header
    (## ADDED Requirements etc.). Do NOT inline fenced code blocks as
    requirement bodies — use normative prose.
  </action>
  <verify>
    <automated>openspec validate configuration --strict</automated>
  </verify>
  <acceptance_criteria>
    - `openspec/specs/configuration/spec.md` exists.
    - `openspec validate configuration --strict` reports the spec is valid
      (exit 0, 0 errors, 0 warnings).
    - Spec has `## Purpose` (≥50 chars) and `## Requirements` with ≥1
      `### Requirement:`; every requirement's first body line contains SHALL or
      MUST and has ≥1 `#### Scenario:` with WHEN/THEN bullets.
    - Spec enumerates the `WHILLY_` env-var contract + defaults and documents
      the layered precedence order.
    - State no-op fields (WORKTREE/USE_WORKSPACE/STATE_FILE) are documented as
      no-ops, not live behavior.
    - No delta headers present in the file.
  </acceptance_criteria>
  <done>configuration spec authored, reverse-spec'd from real v4 config code, and passes openspec validate configuration --strict.</done>
</task>

</tasks>

<verification>
- `openspec validate configuration --strict` → valid.
- Covered modules (config.py, config_sections.py, external_integrations.py,
  project_config/*, cli project_config/project_map/quick_setup, adapters/__init__.py)
  are all accounted for by the `configuration` capability in the coverage matrix.
</verification>

<success_criteria>
The `configuration` capability has a normative spec enumerating the `WHILLY_`
env-var contract, defaults, layered precedence, secret schemes, and truthful
no-op state fields — passing `openspec validate configuration --strict`.
</success_criteria>

<output>
Create `.planning/phases/26-platform-cluster/26-01-SUMMARY.md` when done.
</output>
