---
phase: 26-platform-cluster
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/self-update-doctor/spec.md
autonomous: true
requirements: [PLAT-05]
must_haves:
  truths:
    - "openspec validate self-update-doctor --strict reports the spec is valid (0 errors, 0 warnings)"
    - "The spec covers self-update, doctor diagnostics, repair, and rollback (git_ops) behaviors"
  artifacts:
    - path: "openspec/specs/self-update-doctor/spec.md"
      provides: "Normative self-update-doctor capability spec reverse-spec'd from whilly/update.py + doctor.py + rollback/* + repair/*"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/self-update-doctor/spec.md"
      to: "whilly/rollback/git_ops.py + rollback/service.py"
      via: "requirement pinning rollback git-ops behavior"
      pattern: "rollback|git"
---

<objective>
Author the normative OpenSpec capability spec for `self-update-doctor`
(PLAT-05), reverse-spec'd from the real v4 update/doctor/repair/rollback code.

Purpose: Capture Whilly's guaranteed self-maintenance behavior — self-update,
doctor diagnostics, automated repair, and git-based rollback — as a
machine-checkable spec.
Output: `openspec/specs/self-update-doctor/spec.md` passing `openspec validate
self-update-doctor --strict`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-platform-cluster/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec the observed v4 behavior of update / doctor / repair / rollback</name>
  <files>openspec/specs/self-update-doctor/spec.md</files>
  <read_first>
    - whilly/update.py (self-update logic)
    - whilly/cli/update.py (update CLI surface)
    - whilly/doctor.py (doctor diagnostics)
    - whilly/rollback/git_ops.py, models.py, service.py (git-based rollback)
    - whilly/cli/rollback.py (rollback CLI surface)
    - whilly/repair/events.py, models.py, policy.py, tasks.py (automated repair)
    - openspec/AUTHORING.md (format rules — read in full)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar to mirror)
  </read_first>
  <action>
    Reverse-spec the observed v4 behavior of the self-update-doctor capability.
    Write a `## Purpose` (≥50 chars) describing the self-maintenance scope
    (update, doctor, repair, rollback).

    Then write `## Requirements` with `### Requirement:` blocks (each FIRST body
    line begins with SHALL or MUST, ≤500 chars; each has ≥1 `#### Scenario:`
    with WHEN/THEN). Cover at minimum:
    - Self-update (update.py + cli/update.py): how Whilly checks for / applies
      an update and what guarantees the update flow provides.
    - Doctor diagnostics (doctor.py): the checks the doctor runs and how it
      reports healthy vs problem states.
    - Repair (repair/policy.py + tasks.py + events.py + models.py): how the
      repair policy decides on and performs automated repair actions, emitting
      repair events.
    - Rollback (rollback/git_ops.py + service.py + models.py): git-based
      rollback behavior and what state it restores.
    - Where any of these surfaces touch v4 state wiring, describe it truthfully
      (consistent with the Postgres-primary state layer; do not invent JSON
      state lore).

    Mirror the exemplar's section shape exactly. Do NOT use any delta header.
    Do NOT inline fenced code blocks as requirement bodies — use normative
    prose.
  </action>
  <verify>
    <automated>openspec validate self-update-doctor --strict</automated>
  </verify>
  <acceptance_criteria>
    - `openspec/specs/self-update-doctor/spec.md` exists.
    - `openspec validate self-update-doctor --strict` reports the spec is valid
      (exit 0, 0 errors, 0 warnings).
    - Spec has `## Purpose` (≥50 chars) and `## Requirements` with ≥1
      `### Requirement:`; every requirement's first body line contains SHALL or
      MUST and has ≥1 `#### Scenario:` with WHEN/THEN bullets.
    - Spec covers self-update, doctor diagnostics, repair, and rollback.
    - No delta headers present in the file.
  </acceptance_criteria>
  <done>self-update-doctor spec authored, reverse-spec'd from real v4 update/doctor/repair/rollback code, and passes openspec validate self-update-doctor --strict.</done>
</task>

</tasks>

<verification>
- `openspec validate self-update-doctor --strict` → valid.
- All self-update-doctor-mapped modules in the coverage matrix (update.py,
  cli/update.py, doctor.py, rollback/*, cli/rollback.py, repair/*) are
  accounted for by the `self-update-doctor` capability.
</verification>

<success_criteria>
The `self-update-doctor` capability has a normative spec covering self-update,
doctor diagnostics, repair, and rollback — passing `openspec validate
self-update-doctor --strict`.
</success_criteria>

<output>
Create `.planning/phases/26-platform-cluster/26-05-SUMMARY.md` when done.
</output>
