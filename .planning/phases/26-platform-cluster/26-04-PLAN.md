---
phase: 26-platform-cluster
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/state-persistence/spec.md
autonomous: true
requirements: [PLAT-04]
must_haves:
  truths:
    - "openspec validate state-persistence --strict reports the spec is valid (0 errors, 0 warnings)"
    - "The spec makes the Postgres persistence layer (asyncpg pool, TaskRepository optimistic-lock on version, events audit, Alembic migrations) the PRIMARY contract"
    - "The spec states truthfully that state_store.py / .whilly_state.json (StateStore resume) is legacy/unwired in v4 — NOT pinned as the live contract"
  artifacts:
    - path: "openspec/specs/state-persistence/spec.md"
      provides: "Normative state-persistence capability spec reverse-spec'd from whilly/adapters/db/* + migrations chain"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/state-persistence/spec.md"
      to: "whilly/adapters/db/repository.py"
      via: "requirement pinning TaskRepository optimistic-lock claim/complete on version"
      pattern: "version|FOR UPDATE SKIP LOCKED|TaskRepository"
---

<objective>
Author the normative OpenSpec capability spec for `state-persistence`
(PLAT-04), reverse-spec'd from the REAL v4 Postgres persistence layer.

Purpose: Capture Whilly's guaranteed state-persistence behavior — the asyncpg
connection pool, the optimistic-locked TaskRepository claim/complete/release
flow, the events audit log, and the Alembic migration chain — as the PRIMARY,
machine-checkable contract. Record the legacy/unwired status of the v3
StateStore JSON-resume path truthfully.
Output: `openspec/specs/state-persistence/spec.md` passing `openspec validate
state-persistence --strict`.

GROUNDING (verified during planning): `StateStore` is NEVER instantiated
anywhere in `whilly/` — zero call sites. The only references to `state_store`
in `api/event_flusher.py` and `cli/jira_watch_loop.py` are docstring/comment
mentions of its atomic-write pattern, not live usage. `.whilly_state.json` /
`WHILLY_STATE_FILE` are no-ops in v4.7.0. The real state lives in Postgres.
The executor MUST re-verify this against the code before speccing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-platform-cluster/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify wiring, then spec the REAL v4 Postgres persistence layer</name>
  <files>openspec/specs/state-persistence/spec.md</files>
  <read_first>
    - whilly/adapters/db/pool.py (asyncpg create_pool/close_pool, DSN coercion postgresql+asyncpg → postgresql, pool sizing, fail-fast health check)
    - whilly/adapters/db/repository.py (TaskRepository: claim_task via SELECT ... FOR UPDATE SKIP LOCKED, complete/fail with optimistic lock WHERE id=$1 AND version=$2, version increment, INSERT INTO events audit per transition, visibility_timeout RELEASE)
    - whilly/adapters/db/migrations/env.py and a representative sample of versions/ (001_initial_schema, 002_workers_status, 003_events_detail, 014_control_state, 028_webauthn_user_handles) — batch-reference the 28-migration chain at subsystem altitude, do not enumerate every file
    - whilly/state_store.py (LEGACY StateStore — verify it is NOT wired into the worker-claim path; spec its status truthfully)
    - whilly/history.py (run history persistence — verify wiring status)
    - whilly/pause_control.py (pause/resume control state — verify wiring status)
    - openspec/AUTHORING.md (format rules — read in full)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar to mirror)
  </read_first>
  <action>
    FIRST verify wiring: grep for `StateStore(` instantiations and for imports
    of `state_store` / `history` / `pause_control` in the worker-claim
    entrypoints (cli/run.py, cli/plan.py, cli/worker.py, orchestrator.py,
    worker/*). Confirm whether each is live or legacy before writing the spec.
    (Planning already found StateStore is unwired/legacy — re-confirm.)

    Then reverse-spec the observed v4 behavior of the state-persistence
    capability with Postgres as PRIMARY. Write a `## Purpose` (≥50 chars)
    stating that v4 state lives in Postgres and that the spec covers the
    asyncpg pool, the optimistic-locked task repository, the events audit log,
    and the migration chain.

    Then write `## Requirements` with `### Requirement:` blocks (each FIRST body
    line begins with SHALL or MUST, ≤500 chars; each has ≥1 `#### Scenario:`
    with WHEN/THEN). Cover at minimum:
    - asyncpg connection pool lifecycle: `create_pool` accepting both
      `postgresql://` and `postgresql+asyncpg://` DSNs (stripping the driver
      suffix), pool sizing from env, fail-fast health check, `close_pool`.
    - Task claim: `TaskRepository.claim_task` performing a PENDING → CLAIMED
      transition via `SELECT ... FOR UPDATE SKIP LOCKED` so concurrent workers
      never claim the same row.
    - Optimistic-locked completion/failure: complete/fail filtered by
      `WHERE id = $1 AND version = $2 AND status IN (...)`, incrementing
      `version`, so a lost update is detected (a stale-version writer's UPDATE
      affects zero rows).
    - Events audit: every state transition inserts a row into `events` in the
      same transaction (the audit log can never disagree with task state),
      including the `visibility_timeout` RELEASE path.
    - Migration chain: the Alembic migrations (001–028) define and evolve the
      schema (tasks, workers, events, sessions, users, totp, webauthn,
      scheduler rules, control_state, etc.); state the chain as the schema
      source of truth at subsystem altitude.
    - Legacy/no-op status (TRUTHFUL): a requirement (or explicit Purpose/scope
      note plus a requirement) stating that the v3 `StateStore` /
      `.whilly_state.json` / `WHILLY_STATE_FILE` resume path is NOT wired into
      the v4 worker-claim flow and is a no-op — the spec MUST NOT pin the v3
      "plan/iteration/cost/sessions" JSON-resume lore as the live contract.
      Reflect the verified wiring status of history.py and pause_control.py the
      same way (live or legacy, per what the code proves).

    Mirror the exemplar's section shape exactly. Do NOT use any delta header.
    Do NOT inline fenced code blocks as requirement bodies — use normative
    prose.
  </action>
  <verify>
    <automated>openspec validate state-persistence --strict</automated>
  </verify>
  <acceptance_criteria>
    - `openspec/specs/state-persistence/spec.md` exists.
    - `openspec validate state-persistence --strict` reports the spec is valid
      (exit 0, 0 errors, 0 warnings).
    - Spec has `## Purpose` (≥50 chars) and `## Requirements` with ≥1
      `### Requirement:`; every requirement's first body line contains SHALL or
      MUST and has ≥1 `#### Scenario:` with WHEN/THEN bullets.
    - The Postgres layer (asyncpg pool, TaskRepository optimistic-lock on
      `version`, events audit, Alembic migrations) is the PRIMARY contract.
    - The spec states truthfully that the v3 StateStore / `.whilly_state.json`
      resume path is legacy/unwired/no-op in v4 — it is NOT pinned as live.
    - No delta headers present in the file.
  </acceptance_criteria>
  <done>state-persistence spec authored with the real v4 Postgres layer as primary and the v3 StateStore truthfully marked legacy/no-op, passing openspec validate state-persistence --strict.</done>
</task>

</tasks>

<verification>
- `openspec validate state-persistence --strict` → valid.
- All state-persistence-mapped modules in the coverage matrix (adapters/db/*
  incl. the 28-migration batch, state_store.py, history.py, pause_control.py)
  are accounted for by the `state-persistence` capability.
</verification>

<success_criteria>
The `state-persistence` capability has a normative spec with the real v4
Postgres layer (asyncpg pool, optimistic-locked TaskRepository, events audit,
migrations) as primary, and the v3 StateStore JSON-resume marked legacy/no-op —
passing `openspec validate state-persistence --strict`.
</success_criteria>

<output>
Create `.planning/phases/26-platform-cluster/26-04-SUMMARY.md` when done.
</output>
