---
phase: 17-jira-work-classification-and-code-readiness-routing
plan: 03
type: execute
wave: 1
depends_on: [17-01-PLAN.md]
files_modified:
  - whilly/jira_work.py
  - tests/unit/test_jira_work.py
requirements:
  - JIRA-04
must_haves:
  truths:
    - "Jira/GitLab/GitHub repo hints reuse the existing QA release context model."
    - "Repo hints can be converted to plan-compatible repo_targets."
---

<objective>
Reuse Jira/GitLab link context instead of adding a second link-parser path.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert release-context repo hints into repo targets</name>
  <files>whilly/jira_work.py, tests/unit/test_jira_work.py</files>
  <behavior>
    - Accept `ReleaseContext.repo_hints` from `whilly.qa_release.collector`.
    - Emit provider-neutral repo target dictionaries.
    - Preserve GitLab clone URLs and repo full names.
  </behavior>
</task>

</tasks>

<verification>
- `python3 -m pytest -q tests/unit/test_jira_work.py --maxfail=1`
</verification>
