---
phase: 17-jira-work-classification-and-code-readiness-routing
plan: 04
type: execute
wave: 1
depends_on: [17-01-PLAN.md]
files_modified:
  - whilly/jira_work.py
  - whilly/cli/jira.py
  - tests/unit/test_jira_work.py
  - tests/unit/test_jira_cli.py
requirements:
  - JIRA-05
must_haves:
  truths:
    - "Readiness probe is read-only."
    - "Autonomous Jira run can stop before worker launch when tests are missing."
---

<objective>
Add read-only code/test readiness detection and wire it into Jira intake.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Probe local checkouts and gate intake run</name>
  <files>whilly/jira_work.py, whilly/cli/jira.py, tests/unit/test_jira_work.py, tests/unit/test_jira_cli.py</files>
  <behavior>
    - Detect Python, Node, Go, Maven, Gradle, and Rust test commands.
    - Detect unit-test files by language conventions.
    - Stop `whilly jira intake --action run` when `--readiness-repo-path` reports missing test evidence unless explicitly overridden.
  </behavior>
</task>

</tasks>

<verification>
- `python3 -m pytest -q tests/unit/test_jira_work.py tests/unit/test_jira_cli.py --maxfail=1`
</verification>
