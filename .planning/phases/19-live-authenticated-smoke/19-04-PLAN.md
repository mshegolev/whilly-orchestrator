---
phase: 19-live-authenticated-smoke
plan: 04
type: execute
wave: 3
depends_on: ["19-02", "19-03"]
files_modified:
  - docs/Whilly-Usage.md
  - tests/unit/test_docs_live_smoke.py
autonomous: true
requirements: [LIVE-01, LIVE-02, LIVE-03]
must_haves:
  truths:
    - "An operator can follow a documented setup to run jira and gitlab smoke and find the report file"
    - "The docs name the required env vars, the commands, the exit codes, and the report location"
    - "The existing docs-regression test (test_ui_parity_docs) still passes (no prohibited strings added)"
  artifacts:
    - path: "docs/Whilly-Usage.md"
      provides: "Live smoke section with setup, commands, exit codes, report path"
      contains: "Live smoke"
    - path: "tests/unit/test_docs_live_smoke.py"
      provides: "Docs regression test pinning the Live smoke section anchors"
  key_links:
    - from: "docs/Whilly-Usage.md Live smoke section"
      to: "whilly jira smoke / whilly gitlab smoke"
      via: "documented commands + env vars + report path"
      pattern: "whilly (jira|gitlab) smoke"
---

<objective>
Add a "Live smoke" section to `docs/Whilly-Usage.md` documenting the setup
steps, commands, env vars, exit codes, and report location for both
`whilly jira smoke` and `whilly gitlab smoke`, and add a small docs-regression
test pinning the new section so it cannot silently disappear.

Purpose: The ROADMAP success criteria require a DOCUMENTED setup path for both
smoke commands (LIVE-01, LIVE-02) and that operators can find the persisted
report (LIVE-03). The existing `test_ui_parity_docs.py` regression must keep
passing, so the new section must not introduce its prohibited hotkey strings.
Output: `docs/Whilly-Usage.md`, `tests/unit/test_docs_live_smoke.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-live-authenticated-smoke/19-CONTEXT.md
@.planning/phases/19-live-authenticated-smoke/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Live smoke section to docs/Whilly-Usage.md</name>
  <read_first>
    - docs/Whilly-Usage.md (existing structure and an existing `##` section for heading/style)
    - tests/unit/test_ui_parity_docs.py (lines 10-21 — prohibited/required strings the new section must not disturb)
    - .planning/phases/19-live-authenticated-smoke/19-CONTEXT.md (decisions block — env vars, exit codes, report path, read-only)
  </read_first>
  <files>docs/Whilly-Usage.md</files>
  <action>
    Add a new `## Live smoke` section. Document: (1) purpose — read-only
    validation of Jira/GitLab integrations against real credentials, safe
    against production; (2) Jira setup — required env vars
    `JIRA_SERVER_URL`, `JIRA_USERNAME` (basic auth), `JIRA_API_TOKEN`, then the
    command `whilly jira smoke --issue KEY`, and that it checks auth, issue
    fetch, comments, changelog, remote links, and classify; (3) GitLab setup —
    `GITLAB_URL` and one of `GITLAB_TOKEN` / `GITLAB_API_TOKEN` /
    `WHILLY_GITLAB_API_TOKEN` (or glab), then `whilly gitlab smoke --repo-url
    URL`; (4) exit codes table: `0` all pass, `1` a check failed, `2` config
    missing; (5) the report location `whilly_logs/smoke/{jira|gitlab}-smoke-
    {timestamp}.json` and that it is redacted (no tokens); (6) a note that
    `--persist` appends a DB audit event only when `WHILLY_DATABASE_URL` is set.
    CRITICAL (Pitfall 7): do NOT add any of the strings the docs-regression
    test prohibits (`q/d/l/t/h`) and do not remove any required strings
    (`1-5=switch`, `p`, `Pause workers`, `a/x/c`). The new section is additive.
    Keep prose concise.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_ui_parity_docs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `docs/Whilly-Usage.md` contains a `## Live smoke` heading
    - The section names `whilly jira smoke`, `whilly gitlab smoke`, the three exit codes, and `whilly_logs/smoke/`
    - `.venv/bin/python -m pytest tests/unit/test_ui_parity_docs.py -q` still passes
    - The string `q/d/l/t/h` does NOT appear anywhere in `docs/Whilly-Usage.md`
  </acceptance_criteria>
  <done>Operators have a documented setup-and-run path for both smoke commands; existing docs regression stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Docs-regression test pinning the Live smoke section</name>
  <read_first>
    - tests/unit/test_ui_parity_docs.py (the `_read` helper + assertion style to mirror)
    - docs/Whilly-Usage.md (the section added in Task 1)
  </read_first>
  <files>tests/unit/test_docs_live_smoke.py</files>
  <action>
    Create `tests/unit/test_docs_live_smoke.py` mirroring the `_read` /
    presence-assertion style of `test_ui_parity_docs.py`. Assert
    `docs/Whilly-Usage.md` contains: `## Live smoke`, `whilly jira smoke`,
    `whilly gitlab smoke`, `whilly_logs/smoke/`, and the three exit-code values
    (`0`, `1`, `2` in the documented context). Add a guard assertion that the
    prohibited string `q/d/l/t/h` is absent (defends Pitfall 7 going forward).
    Keep line length 120.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_docs_live_smoke.py tests/unit/test_ui_parity_docs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/unit/test_docs_live_smoke.py -q` passes
    - The test asserts presence of `## Live smoke`, both smoke commands, and the report path
    - The test asserts absence of `q/d/l/t/h`
    - `.venv/bin/python -m ruff check tests/unit/test_docs_live_smoke.py` reports no errors
  </acceptance_criteria>
  <done>A regression test locks the Live smoke docs section and guards the prohibited-string constraint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs content → operator setup behavior | Docs must not instruct operators to embed tokens in URLs or commit secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-10 | Information Disclosure | example commands in docs | mitigate | Setup examples use env-var references (`JIRA_API_TOKEN`, `GITLAB_TOKEN`), never literal secret values; note that reports are redacted |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH audit: none) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_docs_live_smoke.py tests/unit/test_ui_parity_docs.py -q` passes
- `.venv/bin/python -m pytest tests/unit -q` green
</verification>

<success_criteria>
- docs/Whilly-Usage.md has a Live smoke section with setup, commands, exit codes, and report path
- Existing docs regression still passes; a new regression pins the section
</success_criteria>

<output>
Create `.planning/phases/19-live-authenticated-smoke/19-04-SUMMARY.md` when done
</output>
