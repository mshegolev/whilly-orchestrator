---
phase: 09-profile-native-verification-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - whilly/core/models.py
  - whilly/project_config/plan_builder.py
  - whilly/adapters/filesystem/plan_io.py
  - whilly/adapters/db/schema.sql
  - whilly/adapters/db/migrations/versions/015_plan_verification_commands.py
  - whilly/cli/plan.py
  - whilly/adapters/transport/schemas.py
  - whilly/adapters/transport/server.py
  - tests/unit/test_project_config.py
  - tests/unit/test_plan_io.py
  - tests/integration/test_plan_io.py
  - tests/unit/test_transport_schemas.py
  - tests/integration/test_alembic_015_plan_verification_commands.py
  - tests/integration/test_alembic_full_chain.py
  - tests/integration/test_alembic_013_work_intents.py
autonomous: true
requirements: [VER-01]
must_haves:
  truths:
    - "Generated project-config plans carry profile-native verification commands as typed top-level plan metadata."
    - "Filesystem plan parsing and serialization preserve verification command name, command, required flag, source, and order."
    - "Plan import/export persists profile verification metadata through Postgres without changing task status semantics."
    - "Remote control-plane plan metadata exposes verification commands without shipping sibling task lists."
  artifacts:
    - path: "whilly/core/models.py"
      provides: "Pure `VerificationCommand` value object and `Plan.verification_commands` tuple."
      contains: "class VerificationCommand"
    - path: "whilly/project_config/plan_builder.py"
      provides: "Generated plan payload field from `ProjectConfig.verification_commands`."
      contains: "\"verification_commands\""
    - path: "whilly/adapters/filesystem/plan_io.py"
      provides: "Parser/serializer support for top-level verification metadata."
      contains: "_verification_commands_from_dict"
    - path: "whilly/adapters/db/migrations/versions/015_plan_verification_commands.py"
      provides: "Alembic migration adding `plans.verification_commands` JSONB."
      contains: "015_plan_verification_commands"
    - path: "whilly/cli/plan.py"
      provides: "`whilly plan import/export` persistence of verification metadata."
      contains: "verification_commands"
    - path: "whilly/adapters/transport/schemas.py"
      provides: "Wire schema for plan metadata carrying verification commands."
      contains: "verification_commands"
  key_links:
    - from: "whilly/project_config/plan_builder.py"
      to: "whilly/adapters/filesystem/plan_io.py"
      via: "top-level `verification_commands` generated then parsed"
      pattern: "verification_commands"
    - from: "whilly/adapters/filesystem/plan_io.py"
      to: "whilly/cli/plan.py"
      via: "`Plan.verification_commands` serialized to and from plan import/export"
      pattern: "Plan\\(.*verification_commands"
    - from: "whilly/cli/plan.py"
      to: "plans.verification_commands"
      via: "JSONB insert/select in import/export SQL"
      pattern: "verification_commands"
    - from: "whilly/adapters/transport/server.py"
      to: "whilly/adapters/transport/schemas.py"
      via: "`GET /api/v1/plans/{plan_id}` response includes `verification_commands`"
      pattern: "verification_commands"
---

<objective>
Make profile-native verification commands durable plan metadata.

Purpose: Phase 9 needs `ProjectConfig.verification_commands` to survive generation, parsing, import/export, and remote metadata transport before workers can execute it.
Output: typed plan metadata, JSON round-trip support, Postgres persistence, remote plan metadata exposure, and tests proving order/source preservation.
</objective>

<execution_context>
@/Users/m.v.shchegolev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/m.v.shchegolev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-profile-native-verification-wiring/09-CONTEXT.md
@.planning/phases/09-profile-native-verification-wiring/09-RESEARCH.md
@.planning/phases/09-profile-native-verification-wiring/09-VALIDATION.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-01-SUMMARY.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-03-SUMMARY.md
@whilly/core/models.py
@whilly/project_config/models.py
@whilly/project_config/loader.py
@whilly/project_config/plan_builder.py
@whilly/adapters/filesystem/plan_io.py
@whilly/cli/plan.py
@whilly/adapters/db/schema.sql
@whilly/adapters/transport/schemas.py
@whilly/adapters/transport/server.py

<interfaces>
Current core shape:
```python
@dataclass(frozen=True)
class Plan:
    id: PlanId
    name: str
    tasks: tuple[Task, ...] = ()
    origin: PlanOrigin | None = None
    repo_targets: tuple[RepoTarget, ...] = ()
```

Current project-config verification shape:
```python
@dataclass(frozen=True)
class VerificationCommandConfig:
    name: str
    command: str
    required: bool = True
```

Current generated plan payload lacks verification metadata:
```python
return {
    "plan_id": resolved_plan_id,
    "project": config.name,
    "origin": {...},
    "repo_targets": repo_targets,
    "tasks": tasks,
}
```

Current transport plan metadata shape:
```python
class PlanPayload(_FrozenModel):
    id: NonEmptyShortStr
    name: NonEmptyShortStr
```
</interfaces>

Locked decisions:
- Generate profile-native commands from `ProjectConfig.verification_commands` into task/plan metadata.
- Preserve explicit CLI verification commands; this plan only creates durable profile metadata.
- Use source `"profile"` for project-config generated commands.
- Do not bypass Phase 8 command scanning, redaction, or runner environment allowlist boundaries.
- Do not add new task statuses.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed plan verification metadata and filesystem round-trip</name>
  <files>whilly/core/models.py, whilly/project_config/plan_builder.py, whilly/adapters/filesystem/plan_io.py, tests/unit/test_project_config.py, tests/unit/test_plan_io.py</files>
  <read_first>
    @whilly/core/models.py
    @whilly/project_config/models.py
    @whilly/project_config/loader.py
    @whilly/project_config/plan_builder.py
    @whilly/adapters/filesystem/plan_io.py
    @tests/unit/test_project_config.py
    @tests/unit/test_plan_io.py
  </read_first>
  <behavior>
    - A `ProjectConfig` with `verification_commands=[VerificationCommandConfig(name="unit", command="pytest -q", required=True)]` generates top-level JSON `verification_commands: [{"name": "unit", "command": "pytest -q", "required": true, "source": "profile"}]`.
    - `parse_plan()` returns `Plan.verification_commands` as an ordered tuple with source `"profile"`.
    - `serialize_plan()` emits the same ordered `verification_commands` list when the tuple is non-empty and omits the key when empty.
    - Invalid top-level verification metadata fails with `PlanParseError` that names `verification_commands[{index}]` and the invalid field.
  </behavior>
  <action>
    Add a pure dataclass to `whilly/core/models.py`:
    `VerificationCommand(name: str, command: str, required: bool = True, source: str = "profile")`.
    Add `verification_commands: tuple[VerificationCommand, ...] = ()` to `Plan`.

    Update `whilly/project_config/plan_builder.py::build_plan_payload()` to emit top-level `"verification_commands"` only when `config.verification_commands` is non-empty. Each generated object must contain exact keys `name`, `command`, `required`, and `source`, with source set to `"profile"`.

    Update `whilly/adapters/filesystem/plan_io.py` to parse top-level `verification_commands` into core `VerificationCommand` values. Required validation:
    - `verification_commands` must be a list when present.
    - Each item must be an object.
    - `name` and `command` must be non-empty strings.
    - `required` defaults to `True` and must be bool when provided.
    - `source` defaults to `"profile"` and must be a non-empty string when provided.
    Add `_verification_command_to_dict()` and include `verification_commands` in `serialize_plan()` when non-empty, preserving order.

    Extend `tests/unit/test_project_config.py` for generated project-config payload metadata and source `"profile"`.
    Extend `tests/unit/test_plan_io.py` for parse/serialize round-trip, empty omission, order preservation, and one invalid-shape error.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_project_config.py tests/unit/test_plan_io.py --maxfail=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "class VerificationCommand|verification_commands" whilly/core/models.py whilly/project_config/plan_builder.py whilly/adapters/filesystem/plan_io.py` shows typed model, generated payload, parser, and serializer support.
    - Generated project-config plan JSON contains source `"profile"` and does not bury verification commands in task descriptions or test steps.
    - Filesystem plan round-trip preserves command order exactly.
  </acceptance_criteria>
  <done>Profile verification commands are typed and JSON-round-trippable before any runtime wiring consumes them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist verification metadata through plan import/export</name>
  <files>whilly/adapters/db/schema.sql, whilly/adapters/db/migrations/versions/015_plan_verification_commands.py, whilly/cli/plan.py, tests/integration/test_plan_io.py, tests/integration/test_alembic_015_plan_verification_commands.py, tests/integration/test_alembic_full_chain.py, tests/integration/test_alembic_013_work_intents.py</files>
  <read_first>
    @whilly/adapters/db/schema.sql
    @whilly/adapters/db/migrations/versions/014_control_state.py
    @whilly/cli/plan.py
    @tests/integration/test_plan_io.py
    @tests/integration/test_alembic_full_chain.py
    @tests/integration/test_alembic_013_work_intents.py
  </read_first>
  <behavior>
    - `whilly plan import` stores top-level `verification_commands` in Postgres.
    - `whilly plan export <plan_id>` emits the same verification command list with `name`, `command`, `required`, `source`, and original order.
    - Importing a plan without verification commands stores an empty JSON array and exports without the top-level key.
    - Alembic head becomes `015_plan_verification_commands` and schema.sql contains `verification_commands JSONB NOT NULL DEFAULT '[]'::jsonb`.
  </behavior>
  <action>
    Add Alembic migration `whilly/adapters/db/migrations/versions/015_plan_verification_commands.py` with `revision = "015_plan_verification_commands"` and `down_revision = "014_control_state"`. `upgrade()` must add `plans.verification_commands` as PostgreSQL JSONB, `nullable=False`, `server_default=sa.text("'[]'::jsonb")`. `downgrade()` must drop the column.

    Update `whilly/adapters/db/schema.sql` in the `CREATE TABLE plans` block with:
    `verification_commands JSONB NOT NULL DEFAULT '[]'::jsonb`
    and a short comment stating it stores ordered plan-level profile verification metadata.

    Update `whilly/cli/plan.py`:
    - `_INSERT_PLAN_SQL` must insert `(id, name, verification_commands)` and pass `json.dumps([_verification_command_to_dict(command) ...])`.
    - `_SELECT_PLAN_SQL` must select `id, name, verification_commands`.
    - `_select_plan_with_tasks()` must decode the JSONB value and construct `Plan(..., verification_commands=...)`.
    - Keep `ON CONFLICT (id) DO NOTHING`; do not update existing plans on re-import.
    - Use the same value validation semantics as `plan_io`, not ad hoc string parsing.

    Extend `tests/integration/test_plan_io.py` so import -> export -> import proves `verification_commands` survives Postgres, including a required profile command and an optional profile command.
    Add `tests/integration/test_alembic_015_plan_verification_commands.py` following local migration-test style: script directory sees revision 015, upgrade adds the column with default `[]`, downgrade drops it, and schema.sql mentions the column.
    Update `tests/integration/test_alembic_full_chain.py` and `tests/integration/test_alembic_013_work_intents.py` so their expected head reflects `015_plan_verification_commands` while preserving the 013 revision-specific assertions.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/integration/test_plan_io.py tests/integration/test_alembic_015_plan_verification_commands.py tests/integration/test_alembic_full_chain.py tests/integration/test_alembic_013_work_intents.py --maxfail=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "verification_commands" whilly/adapters/db/schema.sql whilly/adapters/db/migrations/versions/015_plan_verification_commands.py whilly/cli/plan.py` shows schema, migration, import, and export support.
    - Exported canonical JSON is deterministic across two consecutive exports and includes verification metadata only when present.
    - No new task status literals are introduced; required verification failure behavior remains a runtime concern for Plan 09-02.
  </acceptance_criteria>
  <done>Postgres plan import/export has durable ordered profile verification metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose verification metadata through plan transport</name>
  <files>whilly/adapters/transport/schemas.py, whilly/adapters/transport/server.py, tests/unit/test_transport_schemas.py</files>
  <read_first>
    @whilly/adapters/transport/schemas.py
    @whilly/adapters/transport/server.py
    @tests/unit/test_transport_schemas.py
    @tests/unit/test_worker_entrypoint_import_purity.py
  </read_first>
  <behavior>
    - `PlanPayload.from_plan(Plan(..., verification_commands=(...)))` includes an ordered `verification_commands` list.
    - `PlanPayload.model_validate_json(...).to_plan()` or equivalent parser recreates `Plan.verification_commands` without task siblings.
    - `GET /api/v1/plans/{plan_id}` returns `verification_commands` from the `plans` row.
    - Transport schema changes do not import server-only dependencies into worker-entry modules.
  </behavior>
  <action>
    Extend `whilly/adapters/transport/schemas.py::PlanPayload` with `verification_commands: list[VerificationCommandPayload] = Field(default_factory=list)`.
    Add frozen Pydantic `VerificationCommandPayload` with fields `name`, `command`, `required=True`, and `source="profile"`. Add `from_verification_command()` and `to_verification_command()` helpers. Add `PlanPayload.to_plan()` returning `Plan(id=..., name=..., verification_commands=...)` with no sibling tasks.

    Update `whilly/adapters/transport/server.py` `GET /api/v1/plans/{plan_id}` query to select `verification_commands` and return it as a JSON list. Keep the endpoint read-only and unauthenticated as it is today; do not add plan collection/listing behavior.

    Extend `tests/unit/test_transport_schemas.py` for:
    - `PlanPayload.from_plan()` includes ordered verification commands.
    - JSON round-trip preserves `source`.
    - Existing task-free claim metadata behavior still omits `tasks`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_transport_schemas.py --maxfail=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "VerificationCommandPayload|verification_commands|to_plan" whilly/adapters/transport/schemas.py whilly/adapters/transport/server.py` shows the transport metadata path.
    - The plan endpoint response includes `verification_commands` but still does not include sibling `tasks`.
    - Worker-entry import purity remains satisfiable: no `asyncpg`, `fastapi`, `sqlalchemy`, `alembic`, or `uvicorn` imports are added to worker entry modules.
  </acceptance_criteria>
  <done>Remote workers have a typed control-plane metadata surface to fetch profile verification commands.</done>
</task>

</tasks>

<verification>
Run the Phase 9 validation commands relevant to this wave:

```bash
.venv/bin/python -m pytest -q tests/unit/test_project_config.py tests/unit/test_plan_io.py --maxfail=1
.venv/bin/python -m pytest -q tests/integration/test_plan_io.py tests/unit/test_transport_schemas.py --maxfail=1
.venv/bin/python -m pytest -q tests/integration/test_alembic_015_plan_verification_commands.py tests/integration/test_alembic_full_chain.py tests/integration/test_alembic_013_work_intents.py --maxfail=1
```
</verification>

<success_criteria>
- `ProjectConfig.verification_commands` appears as top-level ordered `verification_commands` in generated plans.
- `Plan.verification_commands` survives parse, serialize, import, export, and transport metadata paths.
- The metadata source is explicit (`"profile"`) and ready for Plan 09-02 runtime resolution.
- Phase 8 guard/redaction boundaries are preserved; no runtime verification execution or new task statuses are added in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/09-profile-native-verification-wiring/09-01-SUMMARY.md`.
</output>
