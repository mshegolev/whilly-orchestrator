---
phase: 09-profile-native-verification-wiring
plan: 04
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - whilly/cli/worker.py
  - whilly/worker/local.py
  - whilly/worker/remote.py
  - whilly/compliance/__init__.py
  - tests/unit/test_cli_worker.py
  - tests/unit/test_local_worker.py
  - tests/unit/test_remote_worker.py
  - tests/unit/test_compliance_report.py
autonomous: true
requirements: [VER-01]
must_haves:
  truths:
    - "Remote worker execution uses the same source-aware command resolution semantics after fetching profile plan metadata."
    - "Required profile verification failures block normal `DONE` through the existing `verification_failed` failure path."
    - "Local and remote failure detail distinguish `source: profile` from `source: cli` without leaking raw secrets."
    - "Compliance evidence reports profile-native verification separately from explicit CLI verification and avoids exhaustive-coverage claims."
  artifacts:
    - path: "whilly/cli/worker.py"
      provides: "Remote worker composition using fetched profile metadata and explicit CLI verification flags."
      contains: "verification_runner"
    - path: "whilly/worker/local.py"
      provides: "Local verification failure detail with redacted command/source evidence."
      contains: "verification_failed"
    - path: "whilly/worker/remote.py"
      provides: "Remote verification failure detail with redacted command/source evidence."
      contains: "verification_failed"
    - path: "whilly/compliance/__init__.py"
      provides: "Separate profile-native and explicit CLI verification evidence."
      contains: "Profile-native verification commands"
  key_links:
    - from: "whilly/cli/worker.py"
      to: "whilly/adapters/transport/client.py"
      via: "`RemoteWorkerClient.get_plan(plan_id)` before worker loop"
      pattern: "get_plan\\("
    - from: "whilly/cli/worker.py"
      to: "whilly/pipeline/verification.py"
      via: "`resolve_verification_specs(profile_commands=plan.verification_commands, required_cli=..., optional_cli=...)`"
      pattern: "resolve_verification_specs\\("
    - from: "whilly/worker/local.py"
      to: "whilly/pipeline/verification.py"
      via: "`VerificationCommandResult.source` in failure detail/events"
      pattern: "\"source\""
    - from: "whilly/worker/remote.py"
      to: "whilly/pipeline/verification.py"
      via: "`VerificationCommandResult.source` in failure detail/events"
      pattern: "\"source\""
---

<objective>
Wire remote worker verification and expose source-aware failure/compliance evidence.

Purpose: The metadata and local runtime paths from Plans 09-01 through 09-03 must also govern remote workers and visible compliance evidence without weakening worker-entry import purity or redaction.
Output: remote worker metadata fetch and verification runner wiring, source-aware failure detail, and honest profile-native compliance evidence.
</objective>

<execution_context>
@/Users/mshegolev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mshegolev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-profile-native-verification-wiring/09-CONTEXT.md
@.planning/phases/09-profile-native-verification-wiring/09-RESEARCH.md
@.planning/phases/09-profile-native-verification-wiring/09-VALIDATION.md
@.planning/phases/09-profile-native-verification-wiring/09-03-SUMMARY.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-02-SUMMARY.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-03-SUMMARY.md
@whilly/cli/worker.py
@whilly/worker/local.py
@whilly/worker/remote.py
@whilly/compliance/__init__.py

<interfaces>
Plan 09-02 must provide:
```python
async def RemoteWorkerClient.get_plan(self, plan_id: str) -> Plan: ...
```

Plan 09-03 must provide:
```python
def resolve_verification_specs(
    *,
    profile_commands,
    required_cli=(),
    optional_cli=(),
) -> tuple[VerificationCommandSpec, ...]: ...
```

Existing failure path:
```python
await repo.fail_task(running.id, running.version, "verification_failed", detail=detail)
await client.fail(claimed.id, worker_id, claimed.version, "verification_failed", detail=detail)
```
</interfaces>

Locked decisions:
- Local and remote workers must use the same resolution semantics.
- Required verification failures continue through existing `verification_failed` behavior and no new task statuses.
- Preserve Phase 8 secret lint and runner env allowlist boundaries.
- Compliance wording must not claim every project profile has exhaustive test coverage.
- Worker entry modules must not import `asyncpg`, FastAPI, SQLAlchemy, Alembic, project-config loader, or server modules.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire remote worker metadata fetch and verification runner</name>
  <files>whilly/cli/worker.py, tests/unit/test_cli_worker.py</files>
  <read_first>
    @whilly/cli/worker.py
    @whilly/adapters/transport/client.py
    @whilly/pipeline/verification.py
    @tests/unit/test_cli_worker.py
    @tests/unit/test_worker_entrypoint_import_purity.py
    @.planning/phases/09-profile-native-verification-wiring/09-03-SUMMARY.md
  </read_first>
  <behavior>
    - `whilly-worker --plan P-1` fetches plan metadata before entering `run_remote_worker_with_heartbeat` and passes a verification runner when profile commands exist.
    - `whilly-worker --verify-command req=pytest -q --optional-verify-command opt=ruff check whilly` is accepted and uses the same profile/required/optional order as local run.
    - URL-rotation mode fetches plan metadata once per new client session before calling `run_remote_worker_with_heartbeat`.
    - Worker-entry import purity remains satisfiable.
  </behavior>
  <action>
    In `whilly/cli/worker.py`:
    - Add argparse flags matching local run names: `--verify-command NAME=COMMAND` with `action="append"` and `dest="verify_commands"`, plus `--optional-verify-command NAME=COMMAND` with `action="append"` and `dest="optional_verify_commands"`.
    - Pass those sequences through `run_worker_command()` into `_async_worker()`.
    - Change `_async_worker()` to fetch `plan = await client.get_plan(plan_id)` in static mode before calling `run_remote_worker_with_heartbeat`.
    - In URL-rotation mode, fetch plan metadata once per new client session before calling `run_remote_worker_with_heartbeat`.
    - Build remote `verification_runner` with `resolve_verification_specs(profile_commands=plan.verification_commands, required_cli=verify_commands, optional_cli=optional_verify_commands)` and `run_verification_commands(..., cwd=Path.cwd(), env_allowlist=_VERIFICATION_ENV_ALLOWLIST or the same allowlist used by local run)`.
    - Keep worker-entry import purity: do not import `asyncpg`, FastAPI, SQLAlchemy, Alembic, project-config loader, or server modules.

    Extend `tests/unit/test_cli_worker.py` for new verification flags parsing and reaching `_async_worker`, fetched plan metadata producing a non-None verification runner, CLI-only remote verification still working, and URL-rotation mode fetching plan metadata per client session.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_cli_worker.py --maxfail=1</automated>
  </verify>
  <done>Remote worker composition fetches profile metadata and uses the shared source-aware verification runner path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add source-aware failure detail and compliance evidence</name>
  <files>whilly/worker/local.py, whilly/worker/remote.py, whilly/compliance/__init__.py, tests/unit/test_local_worker.py, tests/unit/test_remote_worker.py, tests/unit/test_compliance_report.py</files>
  <read_first>
    @whilly/worker/local.py
    @whilly/worker/remote.py
    @whilly/compliance/__init__.py
    @tests/unit/test_local_worker.py
    @tests/unit/test_remote_worker.py
    @tests/unit/test_compliance_report.py
    @.planning/phases/09-profile-native-verification-wiring/09-03-SUMMARY.md
  </read_first>
  <behavior>
    - Local and remote `_verification_failure_detail()` include `name`, redacted `command`, `source`, `returncode`, `timed_out`, `blocked`, and `pattern_matched` for required failures.
    - Required profile failures still block normal `DONE` through `reason="verification_failed"` and existing `FAILED` status behavior.
    - Compliance includes separate evidence for explicit CLI verification support and profile-native verification wiring.
  </behavior>
  <action>
    In `whilly/worker/local.py` and `whilly/worker/remote.py`, update `_verification_failure_detail()` to include `"source": result.source` and to use `redact_secrets(result.command)` or omit raw command. Preserve reason `"verification_failed"` and existing `FAILED` status behavior.

    In `whilly/compliance/__init__.py`, keep the existing capability `"Required verification before DONE"` focused on worker enforcement and explicit CLI configuration. Add or update a separate capability named exactly `"Profile-native verification commands"` with evidence that mentions `ProjectConfig.verification_commands`, `Plan.verification_commands`, `resolve_verification_specs`, and remote plan metadata. The gap/recommended action must say configured profile commands feed runtime verification, not that every profile has exhaustive tests.

    Extend tests:
    - `tests/unit/test_local_worker.py` and `tests/unit/test_remote_worker.py`: required profile failure blocks `DONE` through `verification_failed` and failure detail includes source with redacted command.
    - `tests/unit/test_compliance_report.py`: separate profile-native capability and honest wording.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_local_worker.py tests/unit/test_remote_worker.py tests/unit/test_compliance_report.py --maxfail=1</automated>
  </verify>
  <done>Local and remote failure evidence is source-aware and compliance distinguishes profile-native verification without overclaiming.</done>
</task>

</tasks>

<verification>
Run the Phase 9 validation commands relevant to this wave:

```bash
.venv/bin/python -m pytest -q tests/unit/test_cli_worker.py tests/unit/test_local_worker.py tests/unit/test_remote_worker.py tests/unit/test_compliance_report.py --maxfail=1
.venv/bin/python -m pytest -q tests/unit/test_project_config.py tests/unit/test_plan_io.py tests/unit/test_transport_schemas.py tests/unit/test_remote_client.py tests/unit/test_cli_run.py tests/unit/test_verification_runner.py tests/unit/test_cli_worker.py tests/unit/test_local_worker.py tests/unit/test_remote_worker.py tests/unit/test_compliance_report.py --maxfail=1
.venv/bin/lint-imports --config .importlinter
```

Before `$gsd-verify-work`, run when practical:

```bash
make lint
.venv/bin/lint-imports --config .importlinter
make test
```
</verification>

<success_criteria>
- Remote worker paths fetch plan metadata and execute profile-native commands before explicit CLI required and optional commands.
- Both local and remote worker paths block normal `DONE` on required verification failure through the existing `verification_failed` behavior.
- Failure detail and verification evidence include command source and respect Phase 8 redaction/guard boundaries.
- Compliance reports profile-native verification separately from ad hoc CLI verification without claiming exhaustive profile coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/09-profile-native-verification-wiring/09-04-SUMMARY.md`.
</output>
