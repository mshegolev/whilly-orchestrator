---
phase: 09-profile-native-verification-wiring
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - whilly/pipeline/verification.py
  - whilly/cli/run.py
  - whilly/adapters/transport/client.py
  - whilly/cli/worker.py
  - whilly/worker/local.py
  - whilly/worker/remote.py
  - whilly/compliance/__init__.py
  - tests/unit/test_verification_runner.py
  - tests/unit/test_cli_run.py
  - tests/unit/test_remote_client.py
  - tests/unit/test_cli_worker.py
  - tests/unit/test_local_worker.py
  - tests/unit/test_remote_worker.py
  - tests/unit/test_compliance_report.py
autonomous: true
requirements: [VER-01]
must_haves:
  truths:
    - "Local worker execution resolves profile-native commands first, explicit required CLI commands second, and explicit optional CLI commands third."
    - "Remote worker execution uses the same source-aware command resolution semantics after fetching profile plan metadata."
    - "Required profile verification failures block normal `DONE` through the existing `verification_failed` failure path."
    - "Verification result events and failure detail distinguish `source: profile` from `source: cli` without leaking raw secrets."
    - "Compliance evidence reports profile-native verification separately from explicit CLI verification and avoids exhaustive-coverage claims."
  artifacts:
    - path: "whilly/pipeline/verification.py"
      provides: "Source-aware verification specs, results, events, and shared command resolution helper."
      contains: "resolve_verification_specs"
    - path: "whilly/cli/run.py"
      provides: "Local worker composition using profile + CLI verification command union."
      contains: "plan.verification_commands"
    - path: "whilly/adapters/transport/client.py"
      provides: "Remote client plan metadata fetch."
      contains: "get_plan"
    - path: "whilly/cli/worker.py"
      provides: "Remote worker composition using fetched profile metadata and explicit CLI verification flags."
      contains: "verification_runner"
    - path: "whilly/worker/local.py"
      provides: "Local verification failure detail with redacted command/source evidence."
      contains: "verification_failed"
    - path: "whilly/worker/remote.py"
      provides: "Remote verification failure detail with redacted command/source evidence."
      contains: "verification_failed"
    - path: "whilly/compliance/__init__.py"
      provides: "Separate profile-native and explicit CLI verification evidence."
      contains: "Profile-native verification commands"
  key_links:
    - from: "whilly/cli/run.py"
      to: "whilly/pipeline/verification.py"
      via: "`resolve_verification_specs(profile_commands=plan.verification_commands, required_cli=..., optional_cli=...)`"
      pattern: "resolve_verification_specs\\("
    - from: "whilly/cli/worker.py"
      to: "whilly/adapters/transport/client.py"
      via: "`RemoteWorkerClient.get_plan(plan_id)` before worker loop"
      pattern: "get_plan\\("
    - from: "whilly/worker/local.py"
      to: "whilly/pipeline/verification.py"
      via: "`VerificationCommandResult.source` in failure detail/events"
      pattern: "\"source\""
    - from: "whilly/worker/remote.py"
      to: "whilly/pipeline/verification.py"
      via: "`VerificationCommandResult.source` in failure detail/events"
      pattern: "\"source\""
---

<objective>
Execute profile-native verification commands through local and remote workers.

Purpose: `ProjectConfig.verification_commands` should affect runtime completion semantics without replacing explicit CLI verification commands or weakening Phase 8 guards/redaction.
Output: source-aware command resolution, local/remote worker wiring, source-aware audit/failure detail, and compliance evidence.
</objective>

<execution_context>
@/Users/mshegolev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mshegolev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-profile-native-verification-wiring/09-CONTEXT.md
@.planning/phases/09-profile-native-verification-wiring/09-RESEARCH.md
@.planning/phases/09-profile-native-verification-wiring/09-VALIDATION.md
@.planning/phases/09-profile-native-verification-wiring/09-01-SUMMARY.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-02-SUMMARY.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-03-SUMMARY.md
@whilly/pipeline/verification.py
@whilly/cli/run.py
@whilly/adapters/transport/client.py
@whilly/cli/worker.py
@whilly/worker/local.py
@whilly/worker/remote.py
@whilly/compliance/__init__.py

<interfaces>
Existing verification runner shape:
```python
@dataclass(frozen=True)
class VerificationCommandSpec:
    name: str
    command: str
    required: bool = True

async def run_verification_commands(commands, *, cwd, timeout_s=600.0, env_allowlist=(), output_limit=20000):
    ...
```

Existing local CLI command builder:
```python
def _build_verification_specs(*, required: Sequence[str], optional: Sequence[str]) -> tuple[VerificationCommandSpec, ...]:
    ...
```

Existing failure path:
```python
await repo.fail_task(running.id, running.version, "verification_failed", detail=detail)
await client.fail(claimed.id, worker_id, claimed.version, "verification_failed", detail=detail)
```

Plan 09-01 must provide:
```python
Plan.verification_commands: tuple[VerificationCommand, ...]
PlanPayload.to_plan()
```
</interfaces>

Locked decisions:
- Deterministic order is profile-native first, explicit required CLI second, explicit optional CLI third.
- Explicit CLI verification commands must remain supported and must not be silently replaced.
- Local and remote workers must use the same resolution semantics.
- Required verification failures continue through existing `verification_failed` behavior and no new task statuses.
- Reuse `whilly/pipeline/verification.py` result models, redaction, command scanning, and event builders.
- Preserve Phase 8 secret lint and runner env allowlist boundaries.
- Compliance wording must not claim every project profile has exhaustive test coverage.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add source-aware verification specs and local command resolution</name>
  <files>whilly/pipeline/verification.py, whilly/cli/run.py, tests/unit/test_verification_runner.py, tests/unit/test_cli_run.py</files>
  <read_first>
    @whilly/pipeline/verification.py
    @whilly/cli/run.py
    @tests/unit/test_verification_runner.py
    @tests/unit/test_cli_run.py
    @.planning/phases/09-profile-native-verification-wiring/09-01-SUMMARY.md
  </read_first>
  <behavior>
    - `resolve_verification_specs(profile_commands=[profile_a], required_cli=["cli_req=pytest -q"], optional_cli=["cli_opt=ruff check whilly"])` returns specs in exact source order: `profile`, `cli`, `cli`.
    - Required CLI specs remain `required=True`; optional CLI specs remain `required=False`.
    - `VerificationCommandResult.source` is copied into `make_verification_result_event()` payload as `"source": "profile"` or `"source": "cli"`.
    - Local `whilly run --verify-command ... --optional-verify-command ...` keeps existing CLI behavior when `plan.verification_commands` is empty.
  </behavior>
  <action>
    In `whilly/pipeline/verification.py`:
    - Add constants `PROFILE_VERIFICATION_SOURCE = "profile"` and `CLI_VERIFICATION_SOURCE = "cli"`.
    - Extend `VerificationCommandSpec` with `source: str = CLI_VERIFICATION_SOURCE`.
    - Extend `VerificationCommandResult` with `source: str`.
    - Update `_run_one()`, `_blocked_result()`, and `_timeout_result()` to copy `spec.source` into results.
    - Update `make_verification_result_event()` payload to include `"source": result.source`.
    - Update `run_verification_commands()` so it does not drop source when the caller passes `VerificationCommandSpec` objects.
    - Add `resolve_verification_specs(*, profile_commands, required_cli=(), optional_cli=()) -> tuple[VerificationCommandSpec, ...]`. It must:
      1. Convert every profile command to `VerificationCommandSpec(name=..., command=..., required=..., source="profile")`.
      2. Append every required CLI command parsed by existing `NAME=COMMAND` behavior with `source="cli"` and `required=True`.
      3. Append every optional CLI command parsed by existing `NAME=COMMAND` behavior with `source="cli"` and `required=False`.

    In `whilly/cli/run.py`, replace `_build_verification_specs()` usage with `resolve_verification_specs(profile_commands=plan.verification_commands, required_cli=verify_commands, optional_cli=optional_verify_commands)`. Keep `_split_verification_command()` behavior available either by moving it into `verification.py` or delegating from the new helper; do not change the user-facing `NAME=COMMAND` parsing.

    Extend `tests/unit/test_verification_runner.py` for source preservation in specs, results, blocked results, timeout results, and event payloads. Extend `tests/unit/test_cli_run.py` for deterministic order `["profile", "cli", "cli"]` and backward compatibility with CLI-only commands.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_cli_run.py tests/unit/test_verification_runner.py --maxfail=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "resolve_verification_specs|PROFILE_VERIFICATION_SOURCE|CLI_VERIFICATION_SOURCE|source" whilly/pipeline/verification.py whilly/cli/run.py` shows one shared source-aware helper and local runtime usage.
    - Local runtime creates a verification runner when either profile commands or CLI commands exist.
    - Existing CLI-only tests still pass; no explicit CLI verification flag behavior is removed.
  </acceptance_criteria>
  <done>Local worker verification uses source-aware profile + CLI command union with deterministic order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire remote worker metadata, failure details, and compliance evidence</name>
  <files>whilly/adapters/transport/client.py, whilly/cli/worker.py, whilly/worker/local.py, whilly/worker/remote.py, whilly/compliance/__init__.py, tests/unit/test_remote_client.py, tests/unit/test_cli_worker.py, tests/unit/test_local_worker.py, tests/unit/test_remote_worker.py, tests/unit/test_compliance_report.py</files>
  <read_first>
    @whilly/adapters/transport/client.py
    @whilly/adapters/transport/schemas.py
    @whilly/cli/worker.py
    @whilly/worker/local.py
    @whilly/worker/remote.py
    @whilly/compliance/__init__.py
    @tests/unit/test_remote_client.py
    @tests/unit/test_cli_worker.py
    @tests/unit/test_local_worker.py
    @tests/unit/test_remote_worker.py
    @tests/unit/test_compliance_report.py
    @.planning/phases/09-profile-native-verification-wiring/09-01-SUMMARY.md
  </read_first>
  <behavior>
    - `RemoteWorkerClient.get_plan("P-1")` requests `/api/v1/plans/P-1` and returns a `Plan` with `verification_commands`.
    - `whilly-worker --plan P-1` fetches plan metadata before entering `run_remote_worker_with_heartbeat` and passes a verification runner when profile or CLI commands exist.
    - `whilly-worker --verify-command req=pytest -q --optional-verify-command opt=ruff check whilly` is accepted and uses the same profile/required/optional order as local run.
    - Local and remote `_verification_failure_detail()` include `name`, redacted `command`, `source`, `returncode`, `timed_out`, `blocked`, and `pattern_matched` for required failures.
    - Compliance includes separate evidence for explicit CLI verification support and profile-native verification wiring.
  </behavior>
  <action>
    In `whilly/adapters/transport/client.py`, add `PLAN_PATH_PREFIX = "/api/v1/plans"` or equivalent helper and `async def get_plan(self, plan_id: str) -> Plan`. It must call `GET /api/v1/plans/{plan_id}`, parse `PlanPayload`, and return `payload.to_plan()`.

    In `whilly/cli/worker.py`:
    - Add argparse flags matching local run names: `--verify-command NAME=COMMAND` with `action="append"` and `dest="verify_commands"`, plus `--optional-verify-command NAME=COMMAND` with `action="append"` and `dest="optional_verify_commands"`.
    - Pass those sequences through `run_worker_command()` into `_async_worker()`.
    - Change `_async_worker()` to fetch `plan = await client.get_plan(plan_id)` in static mode before calling `run_remote_worker_with_heartbeat`.
    - In URL-rotation mode, fetch plan metadata once per new client session before calling `run_remote_worker_with_heartbeat`.
    - Build remote `verification_runner` with `resolve_verification_specs(profile_commands=plan.verification_commands, required_cli=verify_commands, optional_cli=optional_verify_commands)` and `run_verification_commands(..., cwd=Path.cwd(), env_allowlist=_VERIFICATION_ENV_ALLOWLIST or the same allowlist used by local run)`.
    - Keep worker-entry import purity: do not import `asyncpg`, FastAPI, SQLAlchemy, Alembic, project-config loader, or server modules.

    In `whilly/worker/local.py` and `whilly/worker/remote.py`, update `_verification_failure_detail()` to include `"source": result.source` and to use `redact_secrets(result.command)` or omit raw command. Preserve reason `"verification_failed"` and existing `FAILED` status behavior.

    In `whilly/compliance/__init__.py`, keep the existing capability `"Required verification before DONE"` focused on worker enforcement and explicit CLI configuration. Add or update a separate capability named exactly `"Profile-native verification commands"` with evidence that mentions `ProjectConfig.verification_commands`, `Plan.verification_commands`, `resolve_verification_specs`, and remote plan metadata. The gap/recommended action must say configured profile commands feed runtime verification, not that every profile has exhaustive tests.

    Extend tests:
    - `tests/unit/test_remote_client.py`: `get_plan()` path, parse success, and 404 behavior consistent with current client error mapping.
    - `tests/unit/test_cli_worker.py`: new verification flags parse and reach `_async_worker`; `_async_worker` passes a non-None verification runner when fetched plan metadata contains commands.
    - `tests/unit/test_local_worker.py` and `tests/unit/test_remote_worker.py`: required profile failure blocks DONE through `verification_failed` and failure detail includes source with redacted command.
    - `tests/unit/test_compliance_report.py`: separate profile-native capability and honest wording.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_remote_client.py tests/unit/test_cli_worker.py tests/unit/test_local_worker.py tests/unit/test_remote_worker.py tests/unit/test_compliance_report.py --maxfail=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "get_plan|--verify-command|--optional-verify-command|resolve_verification_specs|verification_failed|Profile-native verification commands" whilly/adapters/transport/client.py whilly/cli/worker.py whilly/worker/local.py whilly/worker/remote.py whilly/compliance/__init__.py` shows remote metadata fetch, CLI flags, shared resolution, failure behavior, and compliance evidence.
    - Remote worker no longer constructs only `Plan(id=plan_id, name=plan_id)` when the control plane exposes plan metadata.
    - Required failures still call `fail(..., reason="verification_failed", ...)`; no new `TaskStatus` values or reason statuses are added.
    - Failure detail and event payloads include `source` and do not contain unredacted token-like command text.
  </acceptance_criteria>
  <done>Remote and local workers execute profile-native verification through the same source-aware path and compliance can distinguish the evidence.</done>
</task>

</tasks>

<verification>
Run the Phase 9 validation commands relevant to runtime wiring:

```bash
.venv/bin/python -m pytest -q tests/unit/test_cli_run.py tests/unit/test_cli_worker.py tests/unit/test_verification_runner.py --maxfail=1
.venv/bin/python -m pytest -q tests/unit/test_local_worker.py tests/unit/test_remote_worker.py tests/unit/test_compliance_report.py --maxfail=1
.venv/bin/python -m pytest -q tests/unit/test_project_config.py tests/unit/test_plan_io.py tests/unit/test_cli_run.py tests/unit/test_verification_runner.py tests/unit/test_local_worker.py tests/unit/test_remote_worker.py --maxfail=1
.venv/bin/lint-imports --config .importlinter
```

Before `$gsd-verify-work`, run when practical:

```bash
make lint
.venv/bin/lint-imports --config .importlinter
make test
```
</verification>

<success_criteria>
- Profile-native commands execute before explicit CLI required commands and explicit CLI optional commands.
- Both local and remote worker paths use source-aware resolution and block normal `DONE` on required verification failure.
- Verification audit/failure evidence includes command source and respects Phase 8 redaction/guard boundaries.
- Compliance reports profile-native verification separately from ad hoc CLI verification without claiming exhaustive profile coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/09-profile-native-verification-wiring/09-02-SUMMARY.md`.
</output>
