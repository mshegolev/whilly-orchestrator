---
phase: 15-tui-capability-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_tui.py
requirements:
  - TUI-01
  - TUI-02
must_haves:
  truths:
    - "TUI surface mapping equals active WUI navigation surfaces."
    - "TUI help and key handling derive from the shared operator contract."
    - "Logs/admin/PRD are explicit nonactive WUI artifacts and are not rendered as TUI capabilities."
---

<objective>
Lock TUI capability parity against the current active WUI contract and explicitly test excluded
noncanonical fragments.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add active WUI navigation parity assertion</name>
  <files>tests/unit/test_tui.py</files>
  <behavior>
    - TUI surface key mapping equals `operator_surface_items()`.
    - Rendered TUI header includes every active surface label with matching numeric index.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit noncanonical fragment exclusion assertion</name>
  <files>tests/unit/test_tui.py</files>
  <behavior>
    - `_logs.html`, `_admin.html`, and `_prd.html` are the only nonactive WUI artifacts.
    - Rendered TUI output does not expose logs/admin/PRD placeholder capabilities.
  </behavior>
</task>

</tasks>

<verification>
- `python3 -m pytest -q tests/unit/test_tui.py tests/unit/test_operator_views.py tests/unit/test_operator_ui_contract.py tests/unit/test_wui_contract_static.py --maxfail=1`
- `python3 -m ruff check tests/unit/test_tui.py whilly/cli/tui.py whilly/operator_views.py tests/unit/test_wui_contract_static.py`
</verification>
