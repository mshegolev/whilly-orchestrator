---
phase: 11-ci-polling-and-bounded-repair
plan: 06
title: Scoped compliance wording and report tests
type: execute
wave: 6
depends_on: [11-04, 11-05]
files_modified:
  - whilly/compliance/__init__.py
  - tests/unit/test_compliance_report.py
autonomous: true
requirements: [CI-01, CI-02]
must_haves:
  truths:
    - "Compliance reports a capability named exactly `Bounded CI polling and repair` only when concrete local and remote runtime signals exist."
    - "Compliance wording says `explicit configured CI polling`, `bounded repair attempts`, and `repair.escalated`."
    - "Compliance report tests reject wording that implies continuous polling, auto-merge, production recovery, or unbounded repair."
  artifacts:
    - path: "whilly/compliance/__init__.py"
      provides: "Scoped capability detection and wording for bounded CI polling and repair."
      contains: "Bounded CI polling and repair"
    - path: "tests/unit/test_compliance_report.py"
      provides: "Compliance report regression tests for evidence and non-overclaiming language."
      contains: "test_bounded_ci_polling_and_repair_compliance_is_scoped"
  key_links:
    - from: "whilly/compliance/__init__.py"
      to: "whilly/worker/local.py"
      via: "capability PASS checks local runtime CI and repair evidence wiring"
      pattern: "ci.poll.result"
    - from: "whilly/compliance/__init__.py"
      to: "whilly/worker/remote.py"
      via: "capability PASS checks remote runtime CI and repair evidence wiring"
      pattern: "repair.attempt.completed"
    - from: "tests/unit/test_compliance_report.py"
      to: "whilly/compliance/__init__.py"
      via: "tests pin allowed positive wording and forbidden overclaim terms"
      pattern: "No continuous polling"
---

<objective>
Update compliance reporting to describe Phase 11 as explicit configured CI polling and bounded repair only after the concrete runtime evidence exists.

Purpose: Phase 11 must not be presented as a continuous autonomous repair system, auto-merge system, or production recovery capability.
Output: Scoped compliance capability and tests that pin both evidence requirements and non-overclaiming wording.
</objective>

<execution_context>
@/Users/m.v.shchegolev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/m.v.shchegolev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-ci-polling-and-bounded-repair/11-CONTEXT.md
@.planning/phases/11-ci-polling-and-bounded-repair/11-RESEARCH.md
@.planning/phases/11-ci-polling-and-bounded-repair/11-VALIDATION.md
@.planning/phases/11-ci-polling-and-bounded-repair/11-04-SUMMARY.md
@.planning/phases/11-ci-polling-and-bounded-repair/11-05-SUMMARY.md
@docs/CODEX-MISSION.md
@whilly/compliance/__init__.py
@tests/unit/test_compliance_report.py

Locked decisions:
- Compliance may report CI polling/bounded repair as implemented only when concrete code and tests exist.
- Wording must not claim a continuous autonomous developer loop, automatic production recovery, auto-merge, or unbounded PR review remediation.
- Keep Whilly framed as an operator-controlled control plane, not an autonomous AI developer.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scoped bounded CI polling and repair compliance evidence</name>
  <files>whilly/compliance/__init__.py, tests/unit/test_compliance_report.py</files>
  <read_first>
    @whilly/compliance/__init__.py
    @tests/unit/test_compliance_report.py
    @.planning/phases/11-ci-polling-and-bounded-repair/11-CONTEXT.md
    @.planning/phases/11-ci-polling-and-bounded-repair/11-VALIDATION.md
    @.planning/phases/11-ci-polling-and-bounded-repair/11-04-SUMMARY.md
    @.planning/phases/11-ci-polling-and-bounded-repair/11-05-SUMMARY.md
  </read_first>
  <behavior>
    - Add or update capability name exactly `Bounded CI polling and repair`.
    - PASS only when concrete signals exist: `whilly/ci/verification.py`, `whilly/repair/policy.py`, `whilly/worker/local.py` contains `ci.poll.result`, `repair.escalated`, and `repair.attempt.completed`; `whilly/worker/remote.py` contains `ci.poll.result`, `repair.escalated`, and `repair.attempt.completed`; transport server contains `ci.` and `repair.`; tests include the local and remote focused CI/repair runtime cases.
    - PASS evidence includes exact phrases `explicit configured CI polling`, `bounded repair attempts`, and `repair.escalated`.
    - PASS gap/action text includes exact sentence `No continuous polling, auto-merge, production recovery, or unbounded repair is claimed.`
    - Tests fail if the positive capability text uses forbidden overclaim terms as implemented claims: `continuous autonomous`, `auto-merge`, `production recovery`, or `unbounded repair`.
  </behavior>
  <action>
    In `whilly/compliance/__init__.py`:
    - Add capability `Bounded CI polling and repair`.
    - Implement evidence detection using file-content signals from the completed Phase 11 plans, including both local and remote runtime wiring.
    - Report PASS only when local/remote CI poll events, repair escalation, terminal repair completion, transport prefixes, and focused runtime tests are present.
    - Use scoped wording: `explicit configured CI polling`, `bounded repair attempts`, and `repair.escalated`.
    - Add or preserve negative scope wording exactly: `No continuous polling, auto-merge, production recovery, or unbounded repair is claimed.`

    In `tests/unit/test_compliance_report.py`, add:
    - `test_bounded_ci_polling_and_repair_compliance_is_scoped`
    - `test_bounded_ci_polling_and_repair_compliance_requires_runtime_evidence`
    - `test_bounded_ci_polling_and_repair_compliance_does_not_overclaim_autonomy`
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_compliance_report.py --maxfail=1</automated>
    <automated>.venv/bin/python -m whilly compliance report --format markdown --out out/compliance-report.md && rg -n "Bounded CI polling and repair|explicit configured CI polling|bounded repair attempts|repair\\.escalated|No continuous polling" out/compliance-report.md</automated>
  </verify>
  <done>Compliance reports bounded CI polling and repair only with concrete runtime evidence and rejects autonomous/continuous overclaims.</done>
</task>

</tasks>

<verification>
Run the focused compliance gate and then the Phase 11 integration set:

```bash
.venv/bin/python -m pytest -q tests/unit/test_compliance_report.py --maxfail=1
.venv/bin/python -m whilly compliance report --format markdown --out out/compliance-report.md
rg -n "Bounded CI polling and repair|explicit configured CI polling|bounded repair attempts|repair\\.escalated|No continuous polling" out/compliance-report.md
.venv/bin/python -m pytest -q tests/unit/test_ci_polling.py tests/unit/test_repair_loop.py tests/unit/test_project_config.py tests/unit/test_plan_io.py tests/integration/test_plan_io.py tests/unit/test_transport_schemas.py tests/unit/test_remote_client.py tests/unit/test_cli_run.py tests/unit/test_cli_worker.py tests/unit/test_local_worker.py tests/unit/test_remote_worker.py tests/integration/test_transport_tasks.py tests/unit/test_configured_sinks.py tests/unit/test_compliance_report.py --maxfail=1
make lint
.venv/bin/lint-imports --config .importlinter
```
</verification>

<success_criteria>
- Compliance capability is named exactly `Bounded CI polling and repair`.
- Compliance PASS requires CI/repair primitives plus local and remote runtime evidence.
- Compliance wording includes explicit configured CI polling, bounded repair attempts, repair escalation, and negative scope.
- Report tests prevent continuous/autonomous/auto-merge/production-recovery overclaims.
</success_criteria>

<output>
After completion, create `.planning/phases/11-ci-polling-and-bounded-repair/11-06-SUMMARY.md`.
</output>
