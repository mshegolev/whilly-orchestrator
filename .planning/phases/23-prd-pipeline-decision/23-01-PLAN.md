---
phase: 23-prd-pipeline-decision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/prd-generation/spec.md
  - openspec/specs/task-generation/spec.md
autonomous: true
requirements: [PRD-01, PRD-03]
must_haves:
  truths:
    - "openspec validate prd-generation --strict reports the spec is valid (0 errors, 0 warnings)"
    - "openspec validate task-generation --strict reports the spec is valid (0 errors, 0 warnings)"
    - "prd-generation spec describes PRD-document synthesis only (generate_prd / _call_claude), not the tasks.json contract"
    - "task-generation spec describes the PRD -> tasks.json contract (generate_tasks / generate_tasks_dict / run_init_command), referencing plan-json-contract for task fields"
  artifacts:
    - path: "openspec/specs/prd-generation/spec.md"
      provides: "Normative prd-generation capability spec"
      contains: "## Purpose"
    - path: "openspec/specs/task-generation/spec.md"
      provides: "Normative task-generation capability spec"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/prd-generation/spec.md"
      to: "whilly/prd_generator.py"
      via: "reverse-spec of generate_prd behavior"
      pattern: "SHALL|MUST"
    - from: "openspec/specs/task-generation/spec.md"
      to: "whilly/cli/init.py"
      via: "reverse-spec of run_init_command -> generate_tasks_dict pipeline"
      pattern: "SHALL|MUST"
---

<objective>
Author two normative OpenSpec capability specs that split the behavior co-hosted in
`whilly/prd_generator.py`: `prd-generation` (PRD-document synthesis) and
`task-generation` (the PRD -> tasks.json contract). Each spec is reverse-spec'd from the
real v4.7.0 code and MUST pass `openspec validate <slug> --strict` with zero errors and
zero warnings.

Purpose: Phase 23 captures the PRD pipeline contracts as machine-checkable specs. These
two capabilities share a source module but are distinct behaviors per CONTEXT.md
boundaries, so they get separate spec files with no overlap.
Output: openspec/specs/prd-generation/spec.md, openspec/specs/task-generation/spec.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-prd-pipeline-decision/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
@openspec/COVERAGE-MATRIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec the observed v4 behavior of prd-generation (PRD-01)</name>
  <files>openspec/specs/prd-generation/spec.md</files>
  <read_first>
    - whilly/prd_generator.py (generate_prd lines ~150-210, _call_claude lines ~371+; spec ONLY the PRD-document synthesis path, NOT generate_tasks)
    - whilly/classifier/__init__.py, base.py, router.py, heuristic.py, llm.py, matcher.py, epic_inferrer.py, rebuilder.py (task/epic classification mapped to prd-generation in COVERAGE-MATRIX.md)
    - whilly/core/prompts.py (prompt building)
    - openspec/AUTHORING.md (format + strict-validation rules)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar — copy section structure exactly)
  </read_first>
  <action>
    Create openspec/specs/prd-generation/spec.md for the prd-generation capability,
    reverse-spec'd from the v4 code. READ the modules first; ground every requirement in
    observed behavior. Write a "## Purpose" prose block (>=50 chars) stating this capability
    governs non-interactive PRD-document synthesis from a brief project description, and
    explicitly scoping OUT the PRD->tasks.json contract (that is task-generation per
    CONTEXT.md boundaries). Then "## Requirements" with >=3 "### Requirement:" blocks whose
    FIRST body line contains SHALL or MUST (case-sensitive, <=500 chars). Capture observed
    behaviors: generate_prd takes a description and writes a PRD-*.md markdown file via the
    Claude CLI (_call_claude); the prompt for synthesis is built from whilly/core/prompts.py;
    the classifier package (router/heuristic/llm/matcher/epic_inferrer/rebuilder) performs
    task/epic classification feeding PRD structure. Each requirement MUST have >=1
    "#### Scenario:" (exactly 4 hashtags) with "- **WHEN**" and "- **THEN**" bullets. Do NOT
    re-spec the tasks.json shape — reference task-generation / plan-json-contract instead. No
    fenced code blocks or full implementations in the spec body.
  </action>
  <verify>
    <automated>openspec validate prd-generation --strict --json 2>&1 | grep -qi 'valid' && openspec validate prd-generation --strict</automated>
  </verify>
  <acceptance_criteria>
    - openspec/specs/prd-generation/spec.md exists
    - `openspec validate prd-generation --strict` prints "is valid" and exits 0 (zero errors, zero warnings)
    - File contains a "## Purpose" section (>=50 chars) and a "## Requirements" section
    - Every "### Requirement:" has SHALL or MUST on its first body line
    - Every requirement has >=1 "#### Scenario:" with WHEN/THEN bullets
    - Scope is PRD-document synthesis only; tasks.json contract is referenced, not duplicated
  </acceptance_criteria>
  <done>`openspec validate prd-generation --strict` reports the spec is valid; spec is grounded in the real generate_prd / classifier / prompts behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Spec the observed v4 behavior of task-generation (PRD-03)</name>
  <files>openspec/specs/task-generation/spec.md</files>
  <read_first>
    - whilly/cli/init.py (run_init_command lines ~215+, and _default_tasks_builder -> generate_tasks_dict around lines ~431-438)
    - whilly/prd_generator.py (generate_tasks lines ~294-321, generate_tasks_dict lines ~323-365, _build_tasks_payload lines ~213-260 — the shared PRD->tasks pipeline)
    - openspec/specs/plan-json-contract/spec.md (Phase 22 — REFERENCE for task fields; do NOT re-spec them)
    - openspec/AUTHORING.md
    - openspec/specs/task-model-fsm/spec.md (exemplar)
  </read_first>
  <action>
    Create openspec/specs/task-generation/spec.md for the task-generation capability,
    reverse-spec'd from the v4 code. READ the modules first. "## Purpose" (>=50 chars):
    this capability governs the PRD -> tasks.json generation contract — given an existing
    PRD file, produce a tasks payload. Scope IN: the v3 file flow (generate_tasks writes
    <slug>_tasks.json) and the v4 dict flow (generate_tasks_dict returns the same-shape dict
    consumed by run_init_command's plan import), both built by the shared _build_tasks_payload.
    Scope OUT: the task FIELD schema itself — reference plan-json-contract, do not duplicate.
    "## Requirements" with >=3 "### Requirement:" blocks, SHALL/MUST on the first body line
    (<=500 chars each). Capture observed behaviors: run_init_command parses argv, slugifies
    the idea, resolves PRD-<slug>.md, refuses to overwrite an existing PRD without --force
    (idempotency), then drives generate_tasks_dict to build the plan; generate_tasks_dict and
    generate_tasks both call _build_tasks_payload (same shape, dict vs on-disk file); the
    payload is produced via the Claude CLI (_call_claude). Each requirement MUST have >=1
    "#### Scenario:" (4 hashtags) with WHEN/THEN. No fenced code blocks in the spec body.
  </action>
  <verify>
    <automated>openspec validate task-generation --strict --json 2>&1 | grep -qi 'valid' && openspec validate task-generation --strict</automated>
  </verify>
  <acceptance_criteria>
    - openspec/specs/task-generation/spec.md exists
    - `openspec validate task-generation --strict` prints "is valid" and exits 0 (zero errors, zero warnings)
    - File contains "## Purpose" (>=50 chars) and "## Requirements"
    - Every "### Requirement:" has SHALL or MUST on its first body line
    - Every requirement has >=1 "#### Scenario:" with WHEN/THEN bullets
    - The PRD->tasks.json contract is specified; task-field schema is referenced (plan-json-contract), not duplicated
  </acceptance_criteria>
  <done>`openspec validate task-generation --strict` reports the spec is valid; spec is grounded in run_init_command + generate_tasks_dict + _build_tasks_payload.</done>
</task>

</tasks>

<verification>
- Both spec.md files exist under openspec/specs/{prd-generation,task-generation}/.
- `openspec validate prd-generation --strict` and `openspec validate task-generation --strict` each report the spec is valid (0 errors, 0 warnings).
- prd-generation and task-generation describe disjoint behaviors (no duplicated tasks.json field schema).
- No `whilly/` Python files were modified (documentation-only phase).
</verification>

<success_criteria>
- PRD-01 (prd-generation) and PRD-03 (task-generation) each have a normative, strict-valid spec.
- Specs are reverse-spec'd from real v4.7.0 code, not aspirational.
- Boundary between PRD-document synthesis and PRD->tasks contract is honored.
</success_criteria>

<output>
Create `.planning/phases/23-prd-pipeline-decision/23-01-SUMMARY.md` when done.
</output>
