---
phase: 23-prd-pipeline-decision
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/prd-wizard/spec.md
  - openspec/specs/decomposition/spec.md
autonomous: true
requirements: [PRD-02, PRD-04]
must_haves:
  truths:
    - "openspec validate prd-wizard --strict reports the spec is valid (0 errors, 0 warnings)"
    - "openspec validate decomposition --strict reports the spec is valid (0 errors, 0 warnings)"
    - "prd-wizard spec describes interactive PRD authoring via Claude CLI (PrdWizard background thread, WizardResult, merge_tasks_into_plan)"
    - "decomposition spec truthfully states the v4 wiring status: needs_decompose/run_decompose consume the legacy in-memory TaskManager + use_tmux and are NOT invoked from the v4 worker-claim run path"
  artifacts:
    - path: "openspec/specs/prd-wizard/spec.md"
      provides: "Normative prd-wizard capability spec"
      contains: "## Purpose"
    - path: "openspec/specs/decomposition/spec.md"
      provides: "Normative decomposition capability spec"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/prd-wizard/spec.md"
      to: "whilly/prd_wizard.py"
      via: "reverse-spec of PrdWizard / merge_tasks_into_plan"
      pattern: "SHALL|MUST"
    - from: "openspec/specs/decomposition/spec.md"
      to: "whilly/decomposer.py"
      via: "reverse-spec of needs_decompose / run_decompose + wiring-status statement"
      pattern: "SHALL|MUST"
---

<objective>
Author two normative OpenSpec capability specs: `prd-wizard` (interactive PRD authoring via
the Claude CLI) and `decomposition` (mid-run splitting of oversized pending tasks). Both are
reverse-spec'd from real v4.7.0 code and MUST pass `openspec validate <slug> --strict` with
zero errors and zero warnings. The decomposition spec MUST state its true v4 wiring status.

Purpose: Phase 23 captures the interactive-authoring and task-splitting contracts as
machine-checkable specs. Files are disjoint, so this plan runs in parallel with 23-01 and
23-03.
Output: openspec/specs/prd-wizard/spec.md, openspec/specs/decomposition/spec.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-prd-pipeline-decision/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
@openspec/COVERAGE-MATRIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec the observed v4 behavior of prd-wizard (PRD-02)</name>
  <files>openspec/specs/prd-wizard/spec.md</files>
  <read_first>
    - whilly/prd_wizard.py (PrdWizard class lines ~105+, WizardResult dataclass lines ~93-103, start/_run/_run_claude_interactive/_run_claude_noninteractive/_generate_tasks, merge_tasks_into_plan lines ~326+)
    - whilly/prd_launcher.py (run_prd_wizard lines ~116+, _build_system_prompt lines ~90-115, slug helpers)
    - openspec/AUTHORING.md
    - openspec/specs/task-model-fsm/spec.md (exemplar)
  </read_first>
  <action>
    Create openspec/specs/prd-wizard/spec.md for the prd-wizard capability, reverse-spec'd
    from the v4 code. READ the modules first. "## Purpose" (>=50 chars): this capability
    governs interactive PRD authoring driven through the Claude CLI, run alongside the
    orchestrator. "## Requirements" with >=3 "### Requirement:" blocks, SHALL/MUST on the
    first body line (<=500 chars). Capture observed behaviors: PrdWizard.start launches the
    session on a background daemon thread and is guarded against double-start (is_running);
    the session runs Claude interactively (_run_claude_interactive) or non-interactively
    (_run_claude_noninteractive) to produce a PRD file, then _generate_tasks derives a tasks
    file; WizardResult carries success/prd_path/tasks_path/task_count/error/idea/elapsed_sec;
    run_prd_wizard (prd_launcher) builds the system prompt (_build_system_prompt) and launches
    the interactive Claude CLI; merge_tasks_into_plan merges wizard-generated tasks into an
    existing plan. Each requirement MUST have >=1 "#### Scenario:" (4 hashtags) with
    WHEN/THEN. No fenced code blocks in the spec body.
  </action>
  <verify>
    <automated>openspec validate prd-wizard --strict --json 2>&1 | grep -qi 'valid' && openspec validate prd-wizard --strict</automated>
  </verify>
  <acceptance_criteria>
    - openspec/specs/prd-wizard/spec.md exists
    - `openspec validate prd-wizard --strict` prints "is valid" and exits 0 (zero errors, zero warnings)
    - File contains "## Purpose" (>=50 chars) and "## Requirements"
    - Every "### Requirement:" has SHALL or MUST on its first body line
    - Every requirement has >=1 "#### Scenario:" with WHEN/THEN bullets
  </acceptance_criteria>
  <done>`openspec validate prd-wizard --strict` reports the spec is valid; spec is grounded in PrdWizard / run_prd_wizard / merge_tasks_into_plan.</done>
</task>

<task type="auto">
  <name>Task 2: Spec the observed v4 behavior + wiring status of decomposition (PRD-04)</name>
  <files>openspec/specs/decomposition/spec.md</files>
  <read_first>
    - whilly/decomposer.py (needs_decompose lines ~14-31, build_decompose_prompt lines ~34-59, _tasks_hash lines ~67-70, run_decompose lines ~73-108 — note: signature run_decompose(tm: TaskManager, agent_model, use_tmux, log_dir))
    - whilly/config.py line 76 (DECOMPOSE_EVERY default 5)
    - openspec/specs/task-model-fsm/spec.md (REFERENCE for pending status; exemplar)
    - openspec/specs/plan-json-contract/spec.md (REFERENCE for task fields)
    - openspec/AUTHORING.md
    GROUNDING NOTE: grep confirms decomposer.py is NOT imported/invoked anywhere in the v4
    worker-claim run path (whilly/cli/run.py, whilly/cli/worker.py, whilly/worker/main.py do
    not call needs_decompose/run_decompose). It consumes the legacy in-memory TaskManager and
    a use_tmux flag. The executor MUST verify this with grep and state it truthfully.
  </read_first>
  <action>
    Create openspec/specs/decomposition/spec.md for the decomposition capability,
    reverse-spec'd from the v4 code. READ decomposer.py and grep the run loop first.
    "## Purpose" (>=50 chars): this capability governs mid-run splitting of oversized pending
    tasks, and it MUST honestly state the v4 wiring status. "## Requirements" with >=3
    "### Requirement:" blocks, SHALL/MUST on the first body line (<=500 chars). Capture the
    function contracts: needs_decompose returns True when any PENDING task has >=6
    acceptance_criteria, or its description contains 2+ " и " occurrences, or 1+ " + "
    occurrence; build_decompose_prompt produces an LLM prompt instructing 2-5 subtasks with
    TASK-XXXa/b IDs that inherit phase/category/priority and never touch done/in_progress/
    failed tasks; run_decompose caches on _tasks_hash (SHA256 of pending IDs+descriptions),
    short-circuits on NO_DECOMPOSE cache hits, runs the agent, reloads the TaskManager, and
    returns the count delta; DECOMPOSE_EVERY defaults to 5. Include at least one requirement
    that NORMATIVELY states the wiring status: the system MUST treat decomposition as a legacy
    capability that consumes the in-memory TaskManager + use_tmux and is NOT invoked by the v4
    worker-claim run path (do not assert a behavior the code does not have). Reference
    task-model-fsm for the pending status; do not re-spec the FSM. Each requirement MUST have
    >=1 "#### Scenario:" (4 hashtags) with WHEN/THEN. No fenced code blocks in the spec body.
  </action>
  <verify>
    <automated>openspec validate decomposition --strict --json 2>&1 | grep -qi 'valid' && openspec validate decomposition --strict</automated>
  </verify>
  <acceptance_criteria>
    - openspec/specs/decomposition/spec.md exists
    - `openspec validate decomposition --strict` prints "is valid" and exits 0 (zero errors, zero warnings)
    - File contains "## Purpose" (>=50 chars) and "## Requirements"
    - Every "### Requirement:" has SHALL or MUST on its first body line
    - Every requirement has >=1 "#### Scenario:" with WHEN/THEN bullets
    - A requirement truthfully states decomposition's legacy/unwired status in the v4 worker-claim path
  </acceptance_criteria>
  <done>`openspec validate decomposition --strict` reports the spec is valid; spec is grounded in decomposer.py and states the true v4 wiring status.</done>
</task>

</tasks>

<verification>
- Both spec.md files exist under openspec/specs/{prd-wizard,decomposition}/.
- `openspec validate prd-wizard --strict` and `openspec validate decomposition --strict` each report the spec is valid (0 errors, 0 warnings).
- decomposition spec does not assert v4-wiring it lacks; it states the legacy/in-memory-TaskManager status truthfully.
- No `whilly/` Python files were modified.
</verification>

<success_criteria>
- PRD-02 (prd-wizard) and PRD-04 (decomposition) each have a normative, strict-valid spec.
- Specs are reverse-spec'd from real v4.7.0 code.
- decomposition wiring status is documented truthfully.
</success_criteria>

<output>
Create `.planning/phases/23-prd-pipeline-decision/23-02-SUMMARY.md` when done.
</output>
