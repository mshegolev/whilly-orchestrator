---
phase: 05-shared-operator-table-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - whilly/operator_views.py
  - whilly/cli/tui.py
  - whilly/api/dashboard.py
  - whilly/api/templates/index.html.j2
  - whilly/api/templates/_tasks_table.html
  - whilly/api/templates/_workers_table.html
  - tests/unit/test_operator_views.py
  - tests/unit/test_tui.py
  - tests/integration/test_htmx_dashboard.py
autonomous: true
requirements:
  - OPUI-07
must_haves:
  truths:
    - "Operators see one explicit table-column contract for TUI and WUI task, worker, review, and event surfaces."
    - "WUI task columns use Worker instead of Claimed by while keeping Updated as a documented WUI-only field."
    - "WUI and TUI worker rows use the same field-key order: worker_id, hostname, owner_email, status, last_heartbeat."
    - "Review queue and event table labels and field keys are pinned by shared metadata and tests."
    - "Future mobile layout work can rely on tested labels and field mapping instead of reverse-engineering templates."
  artifacts:
    - path: "whilly/operator_views.py"
      provides: "Pure shared operator surface and table-column metadata"
      exports:
        - "OperatorTable"
        - "OperatorMedium"
        - "OperatorTableColumn"
        - "operator_surface_items"
        - "operator_table_columns"
        - "operator_table_labels"
    - path: "whilly/cli/tui.py"
      provides: "TUI table headers and surface tabs rendered from shared metadata"
      contains: "operator_table_columns"
    - path: "whilly/api/dashboard.py"
      provides: "WUI template context populated from shared metadata"
      contains: "table_columns"
    - path: "whilly/api/templates/_tasks_table.html"
      provides: "WUI task headers using shared contract labels"
      contains: "Worker"
    - path: "whilly/api/templates/_workers_table.html"
      provides: "WUI worker headers and cells in shared contract order"
      contains: "Hostname"
    - path: "tests/unit/test_operator_views.py"
      provides: "Pure contract tests for labels, field keys, and medium-specific differences"
      contains: "operator_table_labels"
    - path: "tests/unit/test_tui.py"
      provides: "TUI rendering tests for shared table labels"
      contains: "table contract"
    - path: "tests/integration/test_htmx_dashboard.py"
      provides: "WUI dashboard tests for shared table labels and worker order"
      contains: "table contract"
  key_links:
    - from: "whilly/cli/tui.py"
      to: "whilly/operator_views.py"
      via: "imports operator_surface_items and operator_table_columns"
      pattern: "from whilly\\.operator_views import .*operator_table_columns"
    - from: "whilly/api/dashboard.py"
      to: "whilly/operator_views.py"
      via: "builds surfaces and table_columns context from shared metadata"
      pattern: "operator_table_columns\\(.*\"wui\""
    - from: "whilly/api/templates/_tasks_table.html"
      to: "whilly/api/dashboard.py"
      via: "uses table_columns.tasks for headers and colspan"
      pattern: "table_columns\\.tasks"
    - from: "whilly/api/templates/_workers_table.html"
      to: "whilly/api/dashboard.py"
      via: "uses table_columns.workers for headers and worker cell order"
      pattern: "table_columns\\.workers"
---

<objective>
Define and apply the smallest shared operator table-column contract for TUI and WUI tables.

Purpose: close OPUI-07 by making table labels and field order explicit before Phase 6 changes mobile row layout.
Output: pure metadata in `whilly/operator_views.py`, both renderers consuming that metadata, and tests pinning labels and field mapping.
</objective>

<execution_context>
@/Users/mshegolev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mshegolev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP-ANALYSIS.md
@.planning/phases/05-shared-operator-table-contract/05-CONTEXT.md
@.planning/phases/05-shared-operator-table-contract/05-UI-SPEC.md
@.planning/phases/01-operator-pause-parity/01-01-SUMMARY.md
@.planning/phases/02-shared-review-decision-path/02-01-SUMMARY.md
@.planning/phases/03-wui-state-preserving-refresh/03-01-SUMMARY.md
@.planning/phases/04-compact-operator-identity-panel/04-01-SUMMARY.md
@docs/superpowers/reviews/2026-05-08-whilly-operator-ui-review.md
@whilly/operator_views.py
@whilly/cli/tui.py
@whilly/api/dashboard.py
@whilly/api/templates/index.html.j2
@whilly/api/templates/_tasks_table.html
@whilly/api/templates/_workers_table.html
@tests/unit/test_operator_views.py
@tests/unit/test_tui.py
@tests/integration/test_htmx_dashboard.py

<interfaces>
Current shared row models live in `whilly/operator_views.py`:

```python
class OperatorSurface(str, Enum):
    OVERVIEW = "overview"
    COMPLIANCE = "compliance"
    PLANS_TASKS = "plans_tasks"
    WORKERS = "workers"
    EVENTS = "events"

@dataclass(frozen=True)
class OperatorTaskRow:
    task_id: str
    plan_id: str
    status: str
    priority: str
    claimed_by: str | None
    started_at: datetime | None
    updated_at: datetime
    acceptance_criteria: tuple[str, ...] = ()
    test_steps: tuple[str, ...] = ()
    human_review: HumanReviewState = field(default_factory=HumanReviewState)

@dataclass(frozen=True)
class WorkerRow:
    worker_id: str
    hostname: str
    owner_email: str | None
    status: str
    last_heartbeat: datetime

@dataclass(frozen=True)
class ReviewGap:
    task_id: str
    plan_id: str
    reason: str
    stage_id: str = ""
    reviewer: str | None = None
    approval_channel: str = ""
    actionable: bool = False

@dataclass(frozen=True)
class EventRow:
    event_id: int
    task_id: str | None
    plan_id: str | None
    event_type: str
    created_at: datetime
    detail: Mapping[str, Any]
```

Current known drift to fix:
- WUI task header says `Claimed by`; canonical visible label is `Worker`.
- WUI worker order is `Worker`, `Owner`, `Hostname`, `Status`, `Last heartbeat`; contract order is `Worker`, `Hostname`, `Owner`, `Status`, `Last heartbeat`.
- TUI worker compact labels `Host` and `Heartbeat` are allowed medium-specific labels, but field-key order must match WUI.
- TUI may omit task `updated_at`; WUI must show it as `Updated`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure shared table-column metadata</name>
  <read_first>
    @.planning/phases/05-shared-operator-table-contract/05-UI-SPEC.md
    @whilly/operator_views.py
    @tests/unit/test_operator_views.py
  </read_first>
  <files>whilly/operator_views.py, tests/unit/test_operator_views.py</files>
  <behavior>
    - Surface items are exactly: overview/Overview, compliance/Compliance, plans_tasks/Plans/Tasks, workers/Workers, events/Events.
    - Task field keys are exactly: task_id, plan_id, status, priority, claimed_by, human_review, updated_at.
    - Task WUI labels are exactly: Task, Plan, Status, Priority, Worker, Review, Updated.
    - Task TUI labels are exactly: Task, Plan, Status, Priority, Worker, Review.
    - Worker field keys for both media are exactly: worker_id, hostname, owner_email, status, last_heartbeat.
    - Worker WUI labels are exactly: Worker, Hostname, Owner, Status, Last heartbeat.
    - Worker TUI labels are exactly: Worker, Host, Owner, Status, Heartbeat.
    - Review WUI labels are exactly: Task, Plan, Reason, Stage, Reviewer, Actions.
    - Review TUI labels are exactly: Sel, Task, Plan, Reason, Stage, Reviewer, Actions.
    - Event labels for both media are exactly: Id, Task, Plan, Type, At.
  </behavior>
  <action>
    Add pure metadata to `whilly/operator_views.py`; do not import Rich, FastAPI, Jinja, asyncpg, or filesystem APIs.

    Implement these public contracts:
    - `class OperatorTable(str, Enum)` with values `tasks`, `workers`, `review_gaps`, `events`.
    - `OperatorMedium = Literal["tui", "wui"]`.
    - `@dataclass(frozen=True) class OperatorTableColumn` with fields:
      `field_key: str`, `canonical_label: str`, `tui_label: str | None = None`,
      `wui_label: str | None = None`, `show_tui: bool = True`, `show_wui: bool = True`,
      `medium_note: str = ""`.
      Add a method `label_for(self, medium: OperatorMedium) -> str` that returns the medium label override or the canonical label.
    - `OPERATOR_SURFACE_LABELS: Final[Mapping[OperatorSurface, str]]` matching the approved surface labels and order.
    - `operator_surface_items() -> tuple[tuple[OperatorSurface, str], ...]` returning enum members and labels in `OperatorSurface` order.
    - `OPERATOR_TABLE_COLUMNS: Final[Mapping[OperatorTable, tuple[OperatorTableColumn, ...]]]` with the exact field keys and labels from the behavior block.
    - `operator_table_columns(table: OperatorTable | str, medium: OperatorMedium) -> tuple[OperatorTableColumn, ...]` filtering `show_tui` or `show_wui`.
    - `operator_table_labels(table: OperatorTable | str, medium: OperatorMedium) -> tuple[str, ...]`.

    Encode intentional differences in metadata:
    - Task `updated_at` has `canonical_label="Updated"`, `show_tui=False`, `show_wui=True`, and `medium_note="WUI shows update time; TUI omits it for width."`.
    - Worker `hostname` has `canonical_label="Hostname"`, `tui_label="Host"`, `wui_label=None`, and a medium note saying TUI uses a compact label only.
    - Worker `last_heartbeat` has `canonical_label="Last heartbeat"`, `tui_label="Heartbeat"`, `wui_label=None`, and a medium note saying TUI uses a compact label only.
    - Review `selected` has `canonical_label="Sel"`, `show_tui=True`, `show_wui=False`, and a medium note saying WUI uses selected row outline and `aria-selected`.

    Add the new names to `__all__`.

    In `tests/unit/test_operator_views.py`, add pure contract tests before modifying renderer code. The tests must assert exact tuples for field keys and labels listed in the behavior block, plus assert the task `updated_at` and review `selected` medium notes are non-empty.
  </action>
  <acceptance_criteria>
    - `operator_table_labels("tasks", "wui")` returns `("Task", "Plan", "Status", "Priority", "Worker", "Review", "Updated")`.
    - `operator_table_labels("tasks", "tui")` returns `("Task", "Plan", "Status", "Priority", "Worker", "Review")`.
    - `operator_table_columns("workers", "wui")` and `operator_table_columns("workers", "tui")` return the same field-key order.
    - No rendering framework dependency is introduced into `whilly/operator_views.py`.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_operator_views.py</automated>
  </verify>
  <done>Shared pure metadata exists, exports are updated, and unit tests pin the exact field keys, labels, and medium-specific exceptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the TUI consume the shared contract</name>
  <read_first>
    @whilly/operator_views.py
    @whilly/cli/tui.py
    @tests/unit/test_tui.py
    @.planning/phases/05-shared-operator-table-contract/05-UI-SPEC.md
  </read_first>
  <files>whilly/cli/tui.py, tests/unit/test_tui.py</files>
  <behavior>
    - TUI surface tabs still render `1 Overview`, `2 Compliance`, `3 Plans/Tasks`, `4 Workers`, `5 Events`.
    - TUI tasks table headers come from `operator_table_columns("tasks", "tui")`: Task, Plan, Status, Priority, Worker, Review.
    - TUI compliance table headers come from `operator_table_columns("review_gaps", "tui")`: Sel, Task, Plan, Reason, Stage, Reviewer, Actions.
    - TUI workers table headers come from `operator_table_columns("workers", "tui")`: Worker, Host, Owner, Status, Heartbeat.
    - TUI events table headers come from `operator_table_columns("events", "tui")`: Id, Task, Plan, Type, At.
    - Existing hotkeys, review decision semantics, pause/resume semantics, filtering, and empty-state aliases stay unchanged.
  </behavior>
  <action>
    Update `whilly/cli/tui.py` to import and use `operator_surface_items`, `operator_table_columns`, and `OperatorTable`.

    Replace the local `_SURFACE_LABEL` mapping with labels from `operator_surface_items()`. Keep `_SURFACE_BY_KEY` and hotkey order unchanged.

    Add a small private helper in `whilly/cli/tui.py`:
    - `_add_contract_columns(table: Table, operator_table: OperatorTable) -> tuple[str, ...]`
    - It calls `operator_table_columns(operator_table, "tui")`, adds each returned column label to the Rich table, and returns the field-key tuple for local row-order assertions or comments.

    Use the helper in:
    - `_compliance_table`: `OperatorTable.REVIEW_GAPS`; keep row values ordered as selected marker, task_id, plan_id, reason, stage_id, reviewer, actions.
    - `_tasks_table`: `OperatorTable.TASKS`; keep row values ordered as task_id, plan_id, status, priority, claimed_by, human_review.
    - `_workers_table`: `OperatorTable.WORKERS`; keep row values ordered as worker_id, hostname, owner_email, status, last_heartbeat.
    - `_events_table`: `OperatorTable.EVENTS`; keep row values ordered as event_id, task_id, plan_id, event_type, created_at.

    Update empty-state rows to use the number of visible contract columns instead of hard-coded counts where practical. Preserve exact text aliases: `(no tasks)`, `(no workers)`, `(no gaps)`, `(no events)`.

    In `tests/unit/test_tui.py`, add or extend tests to assert rendered text includes the contract labels for each surface:
    - Plans/Tasks surface includes Task, Plan, Status, Priority, Worker, Review and does not include Updated.
    - Workers surface includes Worker, Host, Owner, Status, Heartbeat.
    - Compliance surface includes Sel, Task, Plan, Reason, Stage, Reviewer, Actions.
    - Events surface includes Id, Task, Plan, Type, At.
  </action>
  <acceptance_criteria>
    - There is no local `_SURFACE_LABEL` label map left in `whilly/cli/tui.py`.
    - All TUI table header labels come from `operator_table_columns(..., "tui")`.
    - TUI task `Updated` remains intentionally omitted.
    - TUI worker row values are still worker id, hostname, owner, status, heartbeat time in that order.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_operator_views.py tests/unit/test_tui.py</automated>
  </verify>
  <done>TUI rendering consumes shared contract metadata without changing operator hotkeys, filtering, selection, pause/resume, or review-decision behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make the WUI consume the shared contract</name>
  <read_first>
    @whilly/operator_views.py
    @whilly/api/dashboard.py
    @whilly/api/templates/index.html.j2
    @whilly/api/templates/_tasks_table.html
    @whilly/api/templates/_workers_table.html
    @tests/integration/test_htmx_dashboard.py
    @.planning/phases/05-shared-operator-table-contract/05-UI-SPEC.md
  </read_first>
  <files>whilly/api/dashboard.py, whilly/api/templates/index.html.j2, whilly/api/templates/_tasks_table.html, whilly/api/templates/_workers_table.html, tests/integration/test_htmx_dashboard.py</files>
  <behavior>
    - WUI surface tabs still render Overview, Compliance, Plans/Tasks, Workers, Events in that order.
    - WUI tasks table headers are exactly: Task, Plan, Status, Priority, Worker, Review, Updated.
    - WUI workers table headers and cells are ordered exactly: Worker, Hostname, Owner, Status, Last heartbeat.
    - WUI review queue headers are exactly: Task, Plan, Reason, Stage, Reviewer, Actions.
    - WUI event headers are exactly: Id, Task, Plan, Type, At.
    - WUI keeps em dash null cells and existing empty-state copy: `No tasks in queue.`, `No workers connected yet.`, `No human review or verification gaps.`, `No events recorded.`
  </behavior>
  <action>
    Update `whilly/api/dashboard.py` to import `OperatorTable`, `operator_surface_items`, and `operator_table_columns`.

    Replace `_SURFACE_LABELS` with `operator_surface_items()` in the context:
    - `surfaces`: `[(surface.value, label) for surface, label in operator_surface_items()]`
    - `table_columns`: a dict with keys `tasks`, `workers`, `review_gaps`, `events`, each containing `operator_table_columns(OperatorTable.<name>, "wui")`.

    Update `whilly/api/templates/_tasks_table.html`:
    - Render the `<th>` elements by looping over `table_columns.tasks`.
    - The visible fifth header must become `Worker`, not `Claimed by`.
    - Keep row cells in this exact order: task_id, plan_id, status, priority, claimed_by, human_review, updated_at.
    - Use `colspan="{{ table_columns.tasks|length }}"` for the empty row.

    Update `whilly/api/templates/_workers_table.html`:
    - Render the `<th>` elements by looping over `table_columns.workers`.
    - Reorder body cells to match the contract: worker_id, hostname, owner_email, status, last_heartbeat.
    - Use `colspan="{{ table_columns.workers|length }}"` for the empty row.

    Update `whilly/api/templates/index.html.j2`:
    - Render review queue headers by looping over `table_columns.review_gaps`.
    - Render event headers by looping over `table_columns.events`.
    - Use `table_columns.review_gaps|length` and `table_columns.events|length` for empty-state colspans.
    - Keep current review action button labels A/X/C, prompts, `aria-selected`, and `data-review-*` attributes unchanged.

    In `tests/integration/test_htmx_dashboard.py`, add a static or endpoint test named with `table_contract` that asserts:
    - The task header block contains `Task`, `Plan`, `Status`, `Priority`, `Worker`, `Review`, `Updated` in order and does not contain `Claimed by`.
    - The worker header block contains `Worker`, `Hostname`, `Owner`, `Status`, `Last heartbeat` in order.
    - For a seeded worker with hostname `alpha.local` and owner `a@x.com`, the rendered row places `alpha.local` before `a@x.com`.
    - The review and events header blocks contain their exact label sequences from the behavior block.
  </action>
  <acceptance_criteria>
    - WUI task visible label drift is removed by using `Worker`.
    - WUI worker order matches the shared field-key contract.
    - WUI review and event tables use shared column metadata for headers and empty-state colspans.
    - Existing dashboard state preservation, SSE/HTMX attributes, review actions, and identity-panel ids remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_operator_views.py tests/unit/test_tui.py tests/integration/test_htmx_dashboard.py</automated>
  </verify>
  <done>WUI rendering consumes shared contract metadata and integration tests pin task, worker, review, and event label/order behavior.</done>
</task>

</tasks>

<verification>
Run the focused validation suite:

```bash
.venv/bin/python -m pytest -q tests/unit/test_operator_views.py tests/unit/test_tui.py tests/integration/test_htmx_dashboard.py
```

Then run the normal focused lint check for touched Python files:

```bash
.venv/bin/python -m ruff check whilly/operator_views.py whilly/cli/tui.py whilly/api/dashboard.py tests/unit/test_operator_views.py tests/unit/test_tui.py tests/integration/test_htmx_dashboard.py
```
</verification>

<success_criteria>
- OPUI-07 appears in this plan frontmatter and is fully addressed.
- Shared operator table metadata exists in pure Python and is exported from `whilly/operator_views.py`.
- TUI and WUI consume shared surface/table metadata instead of maintaining independent label maps for touched surfaces.
- WUI tasks show `Worker`, not `Claimed by`.
- WUI workers show `Worker`, `Hostname`, `Owner`, `Status`, `Last heartbeat` in that order.
- Tests pin labels, field-key order, and documented medium-specific differences before Phase 6 mobile layout work.
</success_criteria>

<output>
After completion, create `.planning/phases/05-shared-operator-table-contract/05-01-SUMMARY.md`.
</output>
