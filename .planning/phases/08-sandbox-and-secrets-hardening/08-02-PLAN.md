---
phase: 08-sandbox-and-secrets-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - whilly/adapters/runner/env.py
  - whilly/adapters/runner/proxy.py
  - whilly/adapters/runner/claude_cli.py
  - whilly/agents/claude.py
  - whilly/agents/opencode.py
  - whilly/agents/claude_handoff.py
  - whilly/prd_generator.py
  - tests/unit/test_runner_env.py
  - tests/unit/test_claude_proxy_helpers.py
  - tests/unit/test_claude_subprocess_env.py
  - tests/test_agent_backend_claude.py
  - tests/test_agent_backend_opencode.py
  - tests/test_claude_handoff.py
autonomous: true
requirements:
  - SEC-02
must_haves:
  truths:
    - "Agent subprocesses receive only explicit base runner environment names plus provider credentials required by the selected runner/model."
    - "Operational secrets such as worker tokens, admin tokens, database URLs, Slack tokens, and GitHub tokens are not forwarded to coding-agent child processes."
    - "Claude proxy behavior still works, but inactive proxy handling no longer copies the full parent environment."
  artifacts:
    - path: "whilly/adapters/runner/env.py"
      provides: "Pure runner environment allowlist and provider credential inference."
      contains: "BASE_RUNNER_ENV_ALLOWLIST"
    - path: "whilly/adapters/runner/proxy.py"
      provides: "Proxy env layering over scrubbed runner env."
      contains: "build_subprocess_env"
    - path: "tests/unit/test_runner_env.py"
      provides: "Unit coverage for env allowlist, provider credentials, and hidden secret exclusion."
      min_lines: 80
  key_links:
    - from: "whilly/adapters/runner/claude_cli.py"
      to: "whilly/adapters/runner/proxy.py"
      via: "spawn_env_for_claude(model=model)"
      pattern: "spawn_env_for_claude\\(model=model"
    - from: "whilly/agents/opencode.py"
      to: "whilly/adapters/runner/env.py"
      via: "build_runner_env(... backend='opencode')"
      pattern: "build_runner_env\\("
---

<objective>
Create and apply the SEC-02 runner environment allowlist contract.

Purpose: Coding-agent child processes should not inherit the full Whilly parent environment. They need only stable OS/tool variables, selected runner configuration, and provider credentials required by the selected backend/model.
Output: `whilly/adapters/runner/env.py`, scrubbed Claude/OpenCode/handoff subprocess env wiring, and regression tests proving hidden operational secrets are absent.
</objective>

<execution_context>
@/Users/mshegolev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mshegolev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-CONTEXT.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-RESEARCH.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-VALIDATION.md
@docs/CODEX-MISSION.md
@whilly/adapters/runner/proxy.py
@whilly/adapters/runner/claude_cli.py
@whilly/agents/claude.py
@whilly/agents/opencode.py
@whilly/agents/claude_handoff.py
@whilly/prd_generator.py
@tests/unit/test_claude_proxy_helpers.py
@tests/unit/test_claude_subprocess_env.py
@tests/test_agent_backend_claude.py
@tests/test_agent_backend_opencode.py
@tests/test_claude_handoff.py

<interfaces>
From `whilly/adapters/runner/proxy.py`:
```python
def resolve_proxy_settings(*, cli_url: str | None = None, cli_disabled: bool = False, env: Mapping[str, str] | None = None, default_no_proxy: str = DEFAULT_NO_PROXY) -> ProxySettings: ...
def build_subprocess_env(parent_env: Mapping[str, str], settings: ProxySettings) -> dict[str, str]: ...
def spawn_env_for_claude(parent_env: Mapping[str, str] | None = None) -> dict[str, str]: ...
```

From `whilly/agents/base.py`:
```python
class AgentBackend(Protocol):
    def run(self, prompt: str, model: str | None = None, timeout: int | None = None, cwd: Path | None = None) -> AgentResult: ...
    def run_async(self, prompt: str, model: str | None = None, log_file: Path | None = None, cwd: Path | None = None) -> subprocess.Popen: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure runner environment builder</name>
  <files>whilly/adapters/runner/env.py, tests/unit/test_runner_env.py</files>
  <read_first>
    .planning/phases/08-sandbox-and-secrets-hardening/08-CONTEXT.md
    .planning/phases/08-sandbox-and-secrets-hardening/08-RESEARCH.md
    .planning/phases/08-sandbox-and-secrets-hardening/08-VALIDATION.md
    docs/CODEX-MISSION.md
    whilly/adapters/runner/proxy.py
    whilly/adapters/runner/claude_cli.py
    whilly/agents/claude.py
    whilly/agents/opencode.py
    whilly/agents/claude_handoff.py
    tests/unit/test_claude_proxy_helpers.py
    whilly/adapters/runner/env.py
    tests/unit/test_runner_env.py
  </read_first>
  <behavior>
    - `build_runner_env()` includes allowlisted base names from a supplied parent mapping and excludes everything else.
    - `required_env_for_model("claude-opus-4-6[1m]", backend="claude")` returns `("ANTHROPIC_API_KEY",)`.
    - `required_env_for_model("groq/openai/gpt-oss-120b", backend="opencode")` returns `("GROQ_API_KEY",)`.
    - `required_env_for_model("opencode/big-pickle", backend="opencode")` returns an empty tuple so zero-key onboarding remains possible.
    - Hidden names such as `WHILLY_DATABASE_URL`, `WHILLY_WORKER_TOKEN`, `WHILLY_WORKER_BOOTSTRAP_TOKEN`, `WHILLY_ADMIN_TOKEN`, `GH_TOKEN`, `GITHUB_TOKEN`, `WHILLY_GH_TOKEN`, `SLACK_ACCESS_TOKEN`, and `SLACK_BOT_TOKEN` are absent unless explicitly passed as `required_env`.
  </behavior>
  <action>
    Create `whilly/adapters/runner/env.py` as a pure stdlib module accepting parent mappings for deterministic tests.

    Implement this exact public tuple:
    `BASE_RUNNER_ENV_ALLOWLIST = ("PATH", "HOME", "LANG", "LC_ALL", "LC_CTYPE", "TMPDIR", "XDG_CONFIG_HOME", "XDG_CACHE_HOME", "SSL_CERT_FILE", "REQUESTS_CA_BUNDLE", "NODE_EXTRA_CA_CERTS", "WHILLY_MODEL", "CLAUDE_BIN", "WHILLY_OPENCODE_BIN", "WHILLY_CLAUDE_SAFE", "WHILLY_AGENT_ALLOW_SHELL", "WHILLY_OPENCODE_SAFE", "WHILLY_HANDOFF_DIR", "WHILLY_HANDOFF_TIMEOUT")`

    Implement:
    - `def required_env_for_model(model: str | None, *, backend: str) -> tuple[str, ...]`
    - `def build_runner_env(parent: Mapping[str, str], *, required_env: Iterable[str] = (), model: str | None = None, backend: str = "") -> dict[str, str]`

    Provider inference must include these exact credential names:
    - Claude/Anthropic models: `ANTHROPIC_API_KEY`
    - OpenAI models: `OPENAI_API_KEY`
    - Groq models: `GROQ_API_KEY`
    - Gemini/Google models: `GEMINI_API_KEY`
    - OpenRouter models: `OPENROUTER_API_KEY`
    - OpenCode non-`opencode/big-pickle` models with `opencode/` prefix: `OPENCODE_API_KEY`, `OPENCODE_ZEN_API_KEY`

    Sort output keys for deterministic test assertions. Do not read `os.environ` in this module.
  </action>
  <acceptance_criteria>
    - `rg -n "BASE_RUNNER_ENV_ALLOWLIST|required_env_for_model|build_runner_env" whilly/adapters/runner/env.py tests/unit/test_runner_env.py` shows the public env contract and tests.
    - `rg -n "ANTHROPIC_API_KEY|OPENAI_API_KEY|GROQ_API_KEY|GEMINI_API_KEY|OPENROUTER_API_KEY|OPENCODE_API_KEY|OPENCODE_ZEN_API_KEY" whilly/adapters/runner/env.py tests/unit/test_runner_env.py` shows provider credential coverage.
    - `rg -n "WHILLY_DATABASE_URL|WHILLY_WORKER_TOKEN|WHILLY_WORKER_BOOTSTRAP_TOKEN|WHILLY_ADMIN_TOKEN|GH_TOKEN|GITHUB_TOKEN|SLACK_ACCESS_TOKEN" tests/unit/test_runner_env.py` shows explicit hidden-secret exclusion tests.
    - `.venv/bin/python -m pytest -q tests/unit/test_runner_env.py --maxfail=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_runner_env.py --maxfail=1</automated>
  </verify>
  <done>`env.py` can build scrubbed child environments from a supplied parent mapping and infer only selected provider credentials.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire scrubbed envs through Claude, OpenCode, proxy, PRD, and handoff subprocesses</name>
  <files>whilly/adapters/runner/proxy.py, whilly/adapters/runner/claude_cli.py, whilly/agents/claude.py, whilly/agents/opencode.py, whilly/agents/claude_handoff.py, whilly/prd_generator.py, tests/unit/test_claude_proxy_helpers.py, tests/unit/test_claude_subprocess_env.py, tests/test_agent_backend_claude.py, tests/test_agent_backend_opencode.py, tests/test_claude_handoff.py</files>
  <read_first>
    .planning/phases/08-sandbox-and-secrets-hardening/08-CONTEXT.md
    .planning/phases/08-sandbox-and-secrets-hardening/08-RESEARCH.md
    .planning/phases/08-sandbox-and-secrets-hardening/08-VALIDATION.md
    docs/CODEX-MISSION.md
    whilly/adapters/runner/env.py
    whilly/adapters/runner/proxy.py
    whilly/adapters/runner/claude_cli.py
    whilly/agents/claude.py
    whilly/agents/opencode.py
    whilly/agents/claude_handoff.py
    whilly/prd_generator.py
    tests/unit/test_claude_proxy_helpers.py
    tests/unit/test_claude_subprocess_env.py
    tests/test_agent_backend_claude.py
    tests/test_agent_backend_opencode.py
    tests/test_claude_handoff.py
  </read_first>
  <behavior>
    - Inactive Claude proxy env no longer equals `dict(parent_env)`; hidden parent secrets are absent from the returned child env.
    - Active Claude proxy still injects `HTTPS_PROXY` and `NO_PROXY` over the scrubbed env.
    - Async Claude, sync Claude, OpenCode, and handoff `Popen`/`subprocess.run` calls all pass `env=...`.
    - Tests plant `WHILLY_DATABASE_URL`, `WHILLY_WORKER_TOKEN`, `GH_TOKEN`, and `SLACK_ACCESS_TOKEN` and assert they are absent from child envs.
  </behavior>
  <action>
    Update `whilly/adapters/runner/proxy.py` so `build_subprocess_env()` has this signature:
    `def build_subprocess_env(parent_env: Mapping[str, str], settings: ProxySettings, *, required_env: tuple[str, ...] = (), model: str | None = None) -> dict[str, str]`

    Inside `build_subprocess_env()`, start from `build_runner_env(parent_env, required_env=required_env, model=model, backend="claude")`. Then, only when `settings.url` is truthy, set `HTTPS_PROXY` to `settings.url` and `NO_PROXY` to `settings.no_proxy`.

    Update `spawn_env_for_claude()` to accept `model: str | None = None` and `required_env: tuple[str, ...] = ()`, resolve proxy settings from the parent mapping, and call the new `build_subprocess_env(..., model=model, required_env=required_env)`.

    Update `whilly/adapters/runner/claude_cli.py` so `_spawn_and_collect(prompt, model, ...)` calls `proxy.spawn_env_for_claude(model=model)`.

    Update `whilly/prd_generator.py` so `_call_claude(..., model)` calls `proxy.spawn_env_for_claude(model=model)`.

    Update `whilly/agents/claude.py` so both `run()` and `run_async()` pass `env=proxy.spawn_env_for_claude(model=model or self.default_model())` to `subprocess.run()` / `subprocess.Popen()`.

    Update `whilly/agents/opencode.py` so both `run()` and `run_async()` compute `resolved = self.normalize_model(model or self.default_model())`, build commands with that same resolved model, and pass `env=build_runner_env(os.environ, model=resolved, backend="opencode")`.

    Update `whilly/agents/claude_handoff.py` so `run_async()` uses `env = build_runner_env(os.environ, backend="handoff")`, then sets only `WHILLY_HANDOFF_RESULT_PATH` and `WHILLY_HANDOFF_TIMEOUT` on that scrubbed env before `subprocess.Popen()`.

    Stage the wiring by subprocess family so failures stay attributable: first proxy/async Claude CLI/PRD generator, then sync Claude backend, then OpenCode backend, then handoff backend. Run the staged verification command for each family before moving to the next.

    Keep existing command argv, default-deny flags, proxy priority order, and zero-key `opencode/big-pickle` behavior unchanged.
  </action>
  <acceptance_criteria>
    - `rg -n "spawn_env_for_claude\\(model=model|env=proxy.spawn_env_for_claude|build_runner_env\\(" whilly/adapters/runner/claude_cli.py whilly/prd_generator.py whilly/agents/claude.py whilly/agents/opencode.py whilly/agents/claude_handoff.py` shows each subprocess path uses scrubbed env wiring.
    - `rg -n "dict\\(os.environ\\)|os.environ.copy\\(\\)" whilly/adapters/runner/proxy.py whilly/agents/claude.py whilly/agents/opencode.py whilly/agents/claude_handoff.py` returns no matches for coding-agent child env construction.
    - After proxy/async Claude CLI/PRD wiring, `.venv/bin/python -m pytest -q tests/unit/test_runner_env.py tests/unit/test_claude_proxy_helpers.py tests/unit/test_claude_subprocess_env.py --maxfail=1` passes.
    - After sync Claude backend wiring, `.venv/bin/python -m pytest -q tests/test_agent_backend_claude.py tests/unit/test_claude_subprocess_env.py --maxfail=1` passes.
    - After OpenCode backend wiring, `.venv/bin/python -m pytest -q tests/test_agent_backend_opencode.py --maxfail=1` passes.
    - After handoff backend wiring, `.venv/bin/python -m pytest -q tests/test_claude_handoff.py --maxfail=1` passes.
    - `.venv/bin/python -m pytest -q tests/unit/test_runner_env.py tests/unit/test_claude_proxy_helpers.py tests/unit/test_claude_subprocess_env.py tests/test_agent_backend_claude.py tests/test_agent_backend_opencode.py tests/test_claude_handoff.py --maxfail=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_runner_env.py tests/unit/test_claude_proxy_helpers.py tests/unit/test_claude_subprocess_env.py tests/test_agent_backend_claude.py tests/test_agent_backend_opencode.py tests/test_claude_handoff.py --maxfail=1</automated>
  </verify>
  <done>Every Whilly-owned coding-agent subprocess path passes an explicit scrubbed env, proxy layering still works, and hidden operational secrets are excluded by tests.</done>
</task>

</tasks>

<verification>
Run the plan-level focused check:

```bash
.venv/bin/python -m pytest -q tests/unit/test_runner_env.py tests/unit/test_claude_proxy_helpers.py tests/unit/test_claude_subprocess_env.py tests/test_agent_backend_claude.py tests/test_agent_backend_opencode.py tests/test_claude_handoff.py tests/unit/test_worker_default_deny.py --maxfail=1
.venv/bin/python -m ruff check whilly/adapters/runner/env.py whilly/adapters/runner/proxy.py whilly/adapters/runner/claude_cli.py whilly/agents/claude.py whilly/agents/opencode.py whilly/agents/claude_handoff.py whilly/prd_generator.py tests/unit/test_runner_env.py tests/unit/test_claude_proxy_helpers.py tests/unit/test_claude_subprocess_env.py tests/test_agent_backend_claude.py tests/test_agent_backend_opencode.py tests/test_claude_handoff.py
```
</verification>

<success_criteria>
- Agent subprocess child envs are allowlist-based.
- Provider credentials are forwarded only when selected runner/model requires them.
- Worker/admin/database/GitHub/Slack operational secrets are not forwarded to coding-agent child processes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-sandbox-and-secrets-hardening/08-02-SUMMARY.md`.
</output>
