---
phase: 08-sandbox-and-secrets-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - whilly/core/agent_runner.py
  - whilly/worker/local.py
  - whilly/worker/remote.py
  - whilly/adapters/transport/server.py
  - whilly/pipeline/verification.py
  - whilly/compliance/__init__.py
  - docs/CODEX-MISSION.md
  - docs/Current-vs-Target.md
  - tests/unit/test_local_worker.py
  - tests/unit/test_remote_worker.py
  - tests/integration/test_transport_tasks.py
  - tests/unit/test_verification_runner.py
  - tests/unit/test_compliance_report.py
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
must_haves:
  truths:
    - "Secret-lint blocked work fails before runner invocation on local and remote workers."
    - "Blocked secret guard paths emit deterministic audit payloads with reason, pattern id, field path, task id, plan id, and redacted excerpt."
    - "Remote fail requests preserve `secret_lint_blocked` as a security prelude event just like prompt and shell guard failures."
    - "Verification stdout/stderr and event payload command values are redacted before audit persistence."
    - "Compliance and docs report improved guards while keeping full sandbox/VM isolation as partial or future work."
  artifacts:
    - path: "whilly/core/agent_runner.py"
      provides: "Task secret-surface scanner consumed by workers."
      contains: "scan_task_secret_surface"
    - path: "whilly/worker/local.py"
      provides: "Local pre-run secret guard failure path."
      contains: "SECRET_LINT_FAIL_REASON"
    - path: "whilly/worker/remote.py"
      provides: "Remote pre-run secret guard failure path."
      contains: "SECRET_LINT_FAIL_REASON"
    - path: "whilly/adapters/transport/server.py"
      provides: "Remote security prelude allowlist for secret guard failures."
      contains: "SECRET_LINT_BLOCKED_EVENT_TYPE"
    - path: "whilly/compliance/__init__.py"
      provides: "Residual-risk compliance evidence for sandbox and guards."
      contains: "runner-env guards"
  key_links:
    - from: "whilly/worker/local.py"
      to: "whilly/core/agent_runner.py"
      via: "scan_task_secret_surface before runner"
      pattern: "scan_task_secret_surface\\("
    - from: "whilly/worker/remote.py"
      to: "whilly/adapters/transport/server.py"
      via: "client.fail detail event_type=secret_lint_blocked accepted as prelude"
      pattern: "SECRET_LINT_BLOCKED_EVENT_TYPE"
    - from: "whilly/pipeline/verification.py"
      to: "whilly/security/secret_lint.py"
      via: "redact_secrets at event boundary"
      pattern: "redact_secrets"
---

<objective>
Wire Phase 8 guard contracts into worker failure paths, audit events, verification detail redaction, and residual-risk evidence.

Purpose: SEC-01 and SEC-02 become meaningful only when workers block before runner execution and audits/compliance explain what happened without leaking secrets or overclaiming VM isolation.
Output: worker secret guard branches, remote prelude support, redacted verification details, compliance/docs residual-risk updates, and focused tests.
</objective>

<execution_context>
@/Users/m.v.shchegolev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/m.v.shchegolev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-CONTEXT.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-RESEARCH.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-VALIDATION.md
@docs/CODEX-MISSION.md
@docs/Current-vs-Target.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-01-SUMMARY.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-02-SUMMARY.md
@whilly/security/secret_lint.py
@whilly/adapters/runner/env.py
@whilly/core/agent_runner.py
@whilly/worker/local.py
@whilly/worker/remote.py
@whilly/adapters/transport/server.py
@whilly/pipeline/verification.py
@whilly/compliance/__init__.py
@tests/unit/test_local_worker.py
@tests/unit/test_remote_worker.py
@tests/integration/test_transport_tasks.py
@tests/unit/test_verification_runner.py
@tests/unit/test_compliance_report.py

<interfaces>
Created by Plan 08-01:
```python
SECRET_LINT_BLOCKED_EVENT_TYPE = "secret_lint_blocked"
SECRET_LINT_FAIL_REASON = "secret_lint_blocked"

class SecretFinding:
    pattern_id: str
    field_path: str
    redacted_excerpt: str
    def event_payload(self, *, task_id: str, plan_id: str) -> dict[str, str]: ...

def first_secret_finding(surfaces: Mapping[str, object]) -> SecretFinding | None: ...
def redact_secrets(text: str) -> str: ...
```

Existing worker guard branch pattern from `whilly/worker/local.py`:
```python
payload = shell_scan.event_payload(task_id=running.id, plan_id=plan.id)
await repo.fail_task(
    running.id,
    running.version,
    SHELL_COMMAND_FAIL_REASON,
    detail=payload,
    prelude_event_type=SHELL_COMMAND_BLOCKED_EVENT_TYPE,
    prelude_payload=payload,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Block secret findings before local and remote runner calls</name>
  <files>whilly/core/agent_runner.py, whilly/worker/local.py, whilly/worker/remote.py, whilly/adapters/transport/server.py, tests/unit/test_local_worker.py, tests/unit/test_remote_worker.py, tests/integration/test_transport_tasks.py</files>
  <read_first>
    .planning/phases/08-sandbox-and-secrets-hardening/08-CONTEXT.md
    .planning/phases/08-sandbox-and-secrets-hardening/08-RESEARCH.md
    .planning/phases/08-sandbox-and-secrets-hardening/08-VALIDATION.md
    docs/CODEX-MISSION.md
    whilly/security/secret_lint.py
    whilly/core/agent_runner.py
    whilly/core/prompts.py
    whilly/worker/local.py
    whilly/worker/remote.py
    whilly/adapters/transport/server.py
    whilly/adapters/db/repository.py
    tests/unit/test_local_worker.py
    tests/unit/test_remote_worker.py
    tests/integration/test_transport_tasks.py
  </read_first>
  <behavior>
    - Local worker task with `description="Use token sk-ant-" + "A" * 32` fails before `runner()` and records reason `secret_lint_blocked`.
    - Local worker prelude event type is `secret_lint_blocked`, payload contains `pattern_id`, `field_path`, `task_id`, `plan_id`, `redacted_excerpt`, and payload does not contain the raw fake token.
    - Remote worker sends `client.fail(... reason="secret_lint_blocked", detail=payload)` before runner invocation.
    - Transport server treats fail detail `event_type == "secret_lint_blocked"` as a security prelude event.
  </behavior>
  <action>
    In `whilly/core/agent_runner.py`, import `SECRET_LINT_BLOCKED_EVENT_TYPE`, `SECRET_LINT_FAIL_REASON`, `SecretFinding`, and `first_secret_finding` from `whilly.security.secret_lint`.

    Add:
    `def scan_task_secret_surface(task: Task, *, prompt: str = "") -> SecretFinding | None:`

    It must scan this exact mapping:
    - `"task.description": task.description`
    - `"task.prd_requirement": task.prd_requirement`
    - `"task.acceptance_criteria[{idx}]": criterion` for every criterion
    - `"task.test_steps[{idx}]": step` for every test step
    - `"runner.prompt": prompt`

    In `whilly/worker/local.py`, after `prompt = build_task_prompt(running, plan)` succeeds and before `shell_scan = scan_task_command_surface(running)`, call `scan_task_secret_surface(running, prompt=prompt)`. If it returns a finding, build `payload = finding.event_payload(task_id=running.id, plan_id=plan.id)`, record `make_stage_failed_event(stage_context, reason=SECRET_LINT_FAIL_REASON, detail=payload)`, and call `repo.fail_task(..., SECRET_LINT_FAIL_REASON, detail=payload, prelude_event_type=SECRET_LINT_BLOCKED_EVENT_TYPE, prelude_payload=payload)`. Increment `failed`, log the pattern id, and `continue`. The runner must not be called.

    In `whilly/worker/remote.py`, mirror the same branch after prompt construction and before shell scanning. Use `client.fail(claimed.id, worker_id, claimed.version, SECRET_LINT_FAIL_REASON, detail=payload)`.

    In `whilly/adapters/transport/server.py`, import `SECRET_LINT_BLOCKED_EVENT_TYPE` and add it to the `security_prelude_events` set beside prompt and shell guard event types.

    Stage this task by ownership boundary: first add `scan_task_secret_surface()` and its direct unit expectations, then wire local worker, then remote worker, then transport server prelude acceptance. Run the staged verification command for each boundary before moving to the next.

    Extend worker tests beside the existing prompt and shell guard tests. Extend transport integration coverage so `secret_lint_blocked` detail is accepted as a prelude event on the remote fail route.
  </action>
  <acceptance_criteria>
    - `rg -n "SECRET_LINT_BLOCKED_EVENT_TYPE|SECRET_LINT_FAIL_REASON|scan_task_secret_surface" whilly/core/agent_runner.py whilly/worker/local.py whilly/worker/remote.py whilly/adapters/transport/server.py` shows every new guard integration point.
    - `rg -n "secret_lint_blocked|pattern_id|field_path|redacted_excerpt" tests/unit/test_local_worker.py tests/unit/test_remote_worker.py tests/integration/test_transport_tasks.py` shows local, remote, and transport tests pin audit payload shape.
    - After adding `scan_task_secret_surface()`, `.venv/bin/python -m pytest -q tests/unit/test_secret_lint.py --maxfail=1` passes and the helper has direct coverage in the worker or agent-runner test added by this task.
    - After local worker wiring, `.venv/bin/python -m pytest -q tests/unit/test_local_worker.py --maxfail=1` passes.
    - After remote worker wiring, `.venv/bin/python -m pytest -q tests/unit/test_remote_worker.py --maxfail=1` passes.
    - After transport prelude wiring, `.venv/bin/python -m pytest -q tests/integration/test_transport_tasks.py --maxfail=1` passes.
    - `.venv/bin/python -m pytest -q tests/unit/test_secret_lint.py tests/unit/test_local_worker.py tests/unit/test_remote_worker.py tests/integration/test_transport_tasks.py --maxfail=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_secret_lint.py tests/unit/test_local_worker.py tests/unit/test_remote_worker.py tests/integration/test_transport_tasks.py --maxfail=1</automated>
  </verify>
  <done>Local and remote workers block secret findings before runner execution and emit auditable `secret_lint_blocked` reason/payload data without raw secrets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redact verification audit details and update residual-risk evidence</name>
  <files>whilly/pipeline/verification.py, whilly/compliance/__init__.py, docs/CODEX-MISSION.md, docs/Current-vs-Target.md, tests/unit/test_verification_runner.py, tests/unit/test_compliance_report.py</files>
  <read_first>
    .planning/phases/08-sandbox-and-secrets-hardening/08-CONTEXT.md
    .planning/phases/08-sandbox-and-secrets-hardening/08-RESEARCH.md
    .planning/phases/08-sandbox-and-secrets-hardening/08-VALIDATION.md
    docs/CODEX-MISSION.md
    docs/Current-vs-Target.md
    whilly/security/secret_lint.py
    whilly/pipeline/verification.py
    whilly/compliance/__init__.py
    tests/unit/test_verification_runner.py
    tests/unit/test_compliance_report.py
  </read_first>
  <behavior>
    - Verification command stdout/stderr containing a fake token are redacted before they appear in `VerificationCommandResult`.
    - `make_verification_result_event()` redacts `payload["command"]`, `detail["stdout"]`, and `detail["stderr"]` without changing safe values.
    - Compliance capability `Sandbox/VM isolation` remains `PARTIAL` and evidence mentions prompt/shell/secret guards plus runner-env allowlists.
    - Docs state that full per-task VM/container isolation remains future/residual risk.
  </behavior>
  <action>
    In `whilly/pipeline/verification.py`, import `redact_secrets` from `whilly.security.secret_lint`.

    After `_decode_and_cap()` returns stdout/stderr in `_run_one()`, set `stdout = redact_secrets(stdout)` and `stderr = redact_secrets(stderr)` before constructing `VerificationCommandResult`.

    In `make_verification_result_event()`, set payload key `"command"` to `redact_secrets(result.command)`, and set event detail to `{"stdout": redact_secrets(result.stdout), "stderr": redact_secrets(result.stderr)}`.

    In `whilly/compliance/__init__.py`, keep capability `"Sandbox/VM isolation"` status as `CapabilityStatus.PARTIAL`. Update its evidence string to include this exact phrase: `prompt, shell, secret, and runner-env guards`. Keep its gap string including this exact sentence: `No per-task VM/container sandbox isolation is enforced by the worker runtime.`

    Update the first `security_risks` item to this exact string:
    `No per-task VM/container sandbox isolation; prompt, shell, secret, and runner-env guards reduce but do not eliminate agent execution risk.`

    In `docs/CODEX-MISSION.md`, add `a3-a4-sandbox-and-secrets-lint` under completed Block A after implementation, with wording that secret linting, runner env allowlists, and auditable guard reasons are implemented while full per-task VM/container isolation remains future work.

    In `docs/Current-vs-Target.md`, change the partial sandbox wording so it says sandbox/VM isolation has improved guards but still lacks full per-task VM/container isolation. Do not change semantic memory, rollback, CI, or profile-native verification status.

    Stage this task by artifact family: verification redaction first, compliance wording second, docs wording last. Run the staged verification command for each family before moving to the next.
  </action>
  <acceptance_criteria>
    - `rg -n "redact_secrets" whilly/pipeline/verification.py` shows redaction at verification boundaries.
    - `rg -n "prompt, shell, secret, and runner-env guards|No per-task VM/container sandbox isolation is enforced" whilly/compliance/__init__.py tests/unit/test_compliance_report.py` shows compliance evidence and tests pin the residual-risk wording.
    - `rg -n "a3-a4-sandbox-and-secrets-lint|full per-task VM/container isolation remains|improved guards" docs/CODEX-MISSION.md docs/Current-vs-Target.md` shows docs state improved guards without claiming full isolation.
    - After verification redaction, `.venv/bin/python -m pytest -q tests/unit/test_verification_runner.py --maxfail=1` passes.
    - After compliance wording, `.venv/bin/python -m pytest -q tests/unit/test_compliance_report.py --maxfail=1` passes.
    - After docs wording, the docs `rg` command above shows residual-risk wording and no claim that full per-task VM/container isolation is complete.
    - `.venv/bin/python -m pytest -q tests/unit/test_verification_runner.py tests/unit/test_compliance_report.py --maxfail=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_verification_runner.py tests/unit/test_compliance_report.py --maxfail=1</automated>
  </verify>
  <done>Verification audit details are redacted, compliance evidence names concrete guards, and docs keep full VM/container isolation as a residual risk.</done>
</task>

</tasks>

<verification>
Run the final Phase 8 focused and structural checks:

```bash
.venv/bin/python -m pytest -q tests/unit/test_secret_lint.py tests/unit/test_runner_env.py tests/unit/test_prompt_sanitizer.py tests/unit/test_prompt_sanitizer_wiring.py tests/unit/test_local_worker.py tests/unit/test_remote_worker.py tests/unit/test_verification_runner.py tests/unit/test_compliance_report.py tests/unit/test_claude_subprocess_env.py tests/unit/test_claude_proxy_helpers.py tests/test_agent_backend_claude.py tests/test_agent_backend_opencode.py tests/test_claude_handoff.py tests/test_github_issues_source.py tests/integration/test_transport_tasks.py --maxfail=1
.venv/bin/python -m ruff check whilly/ tests/
.venv/bin/python -m ruff format --check whilly/ tests/
.venv/bin/lint-imports --config .importlinter
```

Run `make test` when practical before closing the phase because this plan touches worker, transport, runner, and compliance behavior.
</verification>

<success_criteria>
- SEC-01: secret lint covers task fields, external feedback through sanitizer, runner prompt text, verification/audit text, and config-like mappings.
- SEC-02: runner subprocess envs use explicit allowlists plus selected provider credentials from Plan 08-02.
- SEC-03: prompt, shell, and secret guard failures have deterministic audit reasons and block before runner invocation.
- Compliance/docs report concrete guard improvements and keep full per-task VM/container isolation out of current claims.
</success_criteria>

<output>
After completion, create `.planning/phases/08-sandbox-and-secrets-hardening/08-03-SUMMARY.md`.
</output>
