---
phase: 08-sandbox-and-secrets-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_project_config.py
  - whilly/sources/github_issues.py
autonomous: true
gap_closure: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
must_haves:
  truths:
    - "Only `tests/unit/test_project_config.py` and `whilly/sources/github_issues.py` are reformatted for this gap closure."
    - "The repository formatting gate passes: `.venv/bin/python -m ruff format --check whilly/ tests/`."
    - "Focused tests covering project-config secret lint and GitHub issue secret warning behavior still pass after formatting."
  artifacts:
    - path: "tests/unit/test_project_config.py"
      provides: "Ruff-formatted project-config secret-lint tests."
      contains: "test_project_config_cli_validate_reports_secret_lint_blocked"
    - path: "whilly/sources/github_issues.py"
      provides: "Ruff-formatted GitHub issue source secret warning code."
      contains: "@dataclass"
  key_links:
    - from: "tests/unit/test_project_config.py"
      to: ".venv/bin/python -m ruff format --check whilly/ tests/"
      via: "repository-wide Ruff format gate"
      pattern: "All checks passed"
    - from: "whilly/sources/github_issues.py"
      to: ".venv/bin/python -m ruff format --check whilly/ tests/"
      via: "repository-wide Ruff format gate"
      pattern: "All checks passed"
---

<objective>
Close the Phase 8 verification gap caused by Ruff formatting failures in two Phase 8-touched files.

Purpose: Phase 8 security behavior already verifies; this plan only makes the repository formatting gate pass without replanning sandbox or secrets behavior.
Output: Ruff-formatted `tests/unit/test_project_config.py` and `whilly/sources/github_issues.py`, with the formatting gate and focused sanity checks passing.
</objective>

<execution_context>
@/Users/mshegolev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mshegolev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-VERIFICATION.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-CONTEXT.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-RESEARCH.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-01-PLAN.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-02-PLAN.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-03-PLAN.md
@tests/unit/test_project_config.py
@whilly/sources/github_issues.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Ruff formatting to the two failed files</name>
  <files>tests/unit/test_project_config.py, whilly/sources/github_issues.py</files>
  <read_first>
    .planning/phases/08-sandbox-and-secrets-hardening/08-VERIFICATION.md
    tests/unit/test_project_config.py
    whilly/sources/github_issues.py
  </read_first>
  <action>
    Run Ruff formatting only on the two files named in the verification gap:

    `./.venv/bin/python -m ruff format tests/unit/test_project_config.py whilly/sources/github_issues.py`

    Do not rework Phase 8 security behavior, rename tests, alter assertions, change imports beyond what Ruff formatting requires, or edit unrelated files. After formatting, inspect the diff for these two files and confirm the changes are purely mechanical formatting: the long `test_project_config_cli_validate_reports_secret_lint_blocked` signature is split as Ruff expects, and `whilly/sources/github_issues.py` has the blank line before `@dataclass` that Ruff requires.
  </action>
  <acceptance_criteria>
    - `git diff -- tests/unit/test_project_config.py whilly/sources/github_issues.py` shows only Ruff formatting changes.
    - `git diff --name-only` includes no new files from this plan except `tests/unit/test_project_config.py` and `whilly/sources/github_issues.py`.
    - No Phase 8 security implementation, docs, tests beyond the two named files, or planning files are edited by execution.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m ruff format --check tests/unit/test_project_config.py whilly/sources/github_issues.py</automated>
  </verify>
  <done>The two files named in `08-VERIFICATION.md` are Ruff-formatted and no unrelated files are changed.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run formatting gate and focused sanity checks</name>
  <files>tests/unit/test_project_config.py, whilly/sources/github_issues.py</files>
  <read_first>
    .planning/phases/08-sandbox-and-secrets-hardening/08-VERIFICATION.md
    tests/unit/test_project_config.py
    whilly/sources/github_issues.py
  </read_first>
  <action>
    Run the exact repository format gate that failed in verification:

    `./.venv/bin/python -m ruff format --check whilly/ tests/`

    Then run the focused sanity checks for behavior covered by the two formatted files:

    `./.venv/bin/python -m pytest -q tests/unit/test_project_config.py tests/test_github_issues_source.py --maxfail=1`

    If either command fails, fix only formatting fallout in `tests/unit/test_project_config.py` or `whilly/sources/github_issues.py`. Do not broaden scope into new sandbox/secrets behavior.
  </action>
  <acceptance_criteria>
    - `./.venv/bin/python -m ruff format --check whilly/ tests/` passes.
    - `./.venv/bin/python -m pytest -q tests/unit/test_project_config.py tests/test_github_issues_source.py --maxfail=1` passes.
    - The Phase 8 verification gap "Phase 8 touched files pass the repository formatting gate" is closable without any behavior change.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m ruff format --check whilly/ tests/</automated>
    <automated>./.venv/bin/python -m pytest -q tests/unit/test_project_config.py tests/test_github_issues_source.py --maxfail=1</automated>
  </verify>
  <done>The repository-wide Ruff formatting gate passes and focused project-config/GitHub issue sanity tests still pass.</done>
</task>

</tasks>

<verification>
Run these checks before closing the gap:

```bash
./.venv/bin/python -m ruff format --check whilly/ tests/
./.venv/bin/python -m pytest -q tests/unit/test_project_config.py tests/test_github_issues_source.py --maxfail=1
```
</verification>

<success_criteria>
- The only current Phase 8 verification gap is resolved.
- Ruff formatting passes across `whilly/` and `tests/`.
- Focused tests for the two formatted files pass.
- No Phase 8 security behavior, docs, roadmap state, or unrelated working-tree changes are modified by this gap closure.
</success_criteria>

<output>
After completion, create `.planning/phases/08-sandbox-and-secrets-hardening/08-04-SUMMARY.md`.
</output>
