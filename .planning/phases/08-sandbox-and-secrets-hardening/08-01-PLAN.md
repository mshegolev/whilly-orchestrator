---
phase: 08-sandbox-and-secrets-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - whilly/security/secret_lint.py
  - whilly/security/prompt_sanitizer.py
  - whilly/project_config/loader.py
  - whilly/sources/github_issues.py
  - tests/unit/test_secret_lint.py
  - tests/unit/test_prompt_sanitizer.py
  - tests/unit/test_prompt_sanitizer_wiring.py
  - tests/unit/test_project_config.py
  - tests/test_github_issues_source.py
autonomous: true
requirements:
  - SEC-01
must_haves:
  truths:
    - "Secret-like text in task fields, external feedback, runner prompt text, and config-like mappings can be detected through one shared contract."
    - "Persisted project configs reject plaintext token-like config values through the real `project-config` validation path while allowing `env:`, `keyring:`, and `file:` references."
    - "Sanitizer and GitHub issue warning code no longer maintain separate secret regex registries."
    - "Secret findings expose stable pattern ids and redacted excerpts without persisting raw token values."
  artifacts:
    - path: "whilly/security/secret_lint.py"
      provides: "Pure secret pattern registry, scan helpers, redaction helpers, and audit-safe finding values."
      contains: "SECRET_LINT_BLOCKED_EVENT_TYPE"
    - path: "whilly/security/prompt_sanitizer.py"
      provides: "External text sanitizer backed by shared secret redaction."
      contains: "redact_secrets"
    - path: "whilly/sources/github_issues.py"
      provides: "GitHub issue warning detection backed by shared secret scanning."
      contains: "scan_text"
    - path: "whilly/project_config/loader.py"
      provides: "Project-config validation hook that applies shared config-value secret linting to persisted config data."
      contains: "secret_lint_blocked"
    - path: "tests/unit/test_secret_lint.py"
      provides: "Unit coverage for pattern ids, redaction, mapping scans, and audit payload safety."
      min_lines: 80
    - path: "tests/unit/test_project_config.py"
      provides: "Project-config validation coverage for plaintext token rejection and secret-reference allowance."
  key_links:
    - from: "whilly/security/prompt_sanitizer.py"
      to: "whilly/security/secret_lint.py"
      via: "redact_secrets import"
      pattern: "from whilly\\.security\\.secret_lint import .*redact_secrets"
    - from: "whilly/sources/github_issues.py"
      to: "whilly/security/secret_lint.py"
      via: "scan_text import"
      pattern: "from whilly\\.security\\.secret_lint import .*scan_text"
    - from: "whilly/project_config/loader.py"
      to: "whilly/security/secret_lint.py"
      via: "scan_mapping import in project_config_from_dict validation"
      pattern: "from whilly\\.security\\.secret_lint import .*scan_mapping"
---

<objective>
Create the shared SEC-01 secret-lint contract and migrate existing sanitizer/source warning/config validation code to it.

Purpose: Phase 8 needs one deterministic secret-pattern registry so task fields, external content, runner prompts, config-like values, and audit excerpts cannot drift across modules.
Output: `whilly/security/secret_lint.py`, focused tests, migrated sanitizer/GitHub issue warning paths, and project-config validation wiring.
</objective>

<execution_context>
@/Users/m.v.shchegolev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/m.v.shchegolev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-CONTEXT.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-RESEARCH.md
@.planning/phases/08-sandbox-and-secrets-hardening/08-VALIDATION.md
@docs/CODEX-MISSION.md
@whilly/security/__init__.py
@whilly/security/prompt_sanitizer.py
@whilly/project_config/loader.py
@whilly/project_config/models.py
@whilly/cli/project_config.py
@whilly/sources/github_issues.py
@tests/unit/test_prompt_sanitizer.py
@tests/unit/test_prompt_sanitizer_wiring.py
@tests/unit/test_project_config.py
@tests/test_github_issues_source.py

<interfaces>
From `whilly/security/prompt_sanitizer.py`:
```python
def sanitize_external_text(text: str, *, scope: str, max_chars: int = 8000) -> str: ...
def sanitize_title_slot(text: str, *, max_chars: int = 60) -> str: ...
```

From `whilly/sources/github_issues.py`:
```python
def _detect_secrets(text: str) -> list[str]: ...
def issue_to_task(issue: dict) -> tuple[Task, list[str]]: ...
```

From `whilly/project_config/loader.py`:
```python
class ProjectConfigError(ValueError): ...
def project_config_from_dict(data: dict[str, Any], *, source: str = "<dict>") -> ProjectConfig: ...
def load_project_config(path: str | Path) -> ProjectConfig: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure shared secret-lint registry</name>
  <files>whilly/security/secret_lint.py, tests/unit/test_secret_lint.py</files>
  <read_first>
    .planning/phases/08-sandbox-and-secrets-hardening/08-CONTEXT.md
    .planning/phases/08-sandbox-and-secrets-hardening/08-RESEARCH.md
    .planning/phases/08-sandbox-and-secrets-hardening/08-VALIDATION.md
    docs/CODEX-MISSION.md
    whilly/security/__init__.py
    whilly/security/prompt_sanitizer.py
    whilly/sources/github_issues.py
    tests/unit/test_prompt_sanitizer.py
    tests/test_github_issues_source.py
    whilly/security/secret_lint.py
    tests/unit/test_secret_lint.py
  </read_first>
  <behavior>
    - Test pattern ids for these exact ids: `aws-access-key-id`, `github-token`, `slack-token`, `openai-api-key`, `anthropic-api-key`, `groq-api-key`, `private-key`, `auth-header`, `database-url`, `sensitive-config-key`.
    - Test `redact_secrets()` removes synthetic raw values from output and uses non-empty `[REDACTED:...]` placeholders.
    - Test `scan_text("token " + fake_secret, field_path="task.description")` returns one `SecretFinding` with `field_path == "task.description"` and no raw fake secret in `redacted_excerpt`.
    - Test `scan_mapping()` flags non-empty config-like values whose keys contain `TOKEN`, `SECRET`, `PASSWORD`, `API_KEY`, `DATABASE_URL`, or `DSN`, while allowing reference values beginning with `env:`, `keyring:`, or `file:`.
  </behavior>
  <action>
    Create `whilly/security/secret_lint.py` as a pure stdlib module with no I/O, subprocess, network, database, or framework imports.

    Implement these exact public constants and values:
    - `SECRET_LINT_BLOCKED_EVENT_TYPE: Final[str] = "secret_lint_blocked"`
    - `SECRET_LINT_FAIL_REASON: Final[str] = "secret_lint_blocked"`
    - `SECRET_KEY_NAME_FRAGMENTS: tuple[str, ...] = ("TOKEN", "SECRET", "PASSWORD", "API_KEY", "DATABASE_URL", "DSN")`

    Implement these exact dataclasses:
    - `@dataclass(frozen=True) class SecretPattern: pattern_id: str; regex: re.Pattern[str]; replacement: str`
    - `@dataclass(frozen=True) class SecretFinding: pattern_id: str; field_path: str; redacted_excerpt: str`

    Add `SecretFinding.event_payload(self, *, task_id: str, plan_id: str) -> dict[str, str]` returning exactly these keys: `event_type`, `pattern_id`, `field_path`, `task_id`, `plan_id`, `redacted_excerpt`.

    Implement these exact functions:
    - `redact_secrets(text: str) -> str`
    - `scan_text(text: str, *, field_path: str) -> SecretFinding | None`
    - `scan_mapping(mapping: Mapping[str, object], *, field_path_prefix: str = "config") -> SecretFinding | None`
    - `first_secret_finding(surfaces: Mapping[str, object]) -> SecretFinding | None`
    - `contains_secret(text: str) -> bool`

    Pattern coverage must include these concrete regex categories:
    - AWS access key id: `AKIA[0-9A-Z]{16}`
    - GitHub PATs: `gh[pousr]_[A-Za-z0-9]{36,}`
    - Slack tokens: `xox[abposr]-[A-Za-z0-9-]{10,}`
    - OpenAI keys: `sk-(?:proj-)?[A-Za-z0-9_-]{20,}`
    - Anthropic keys: `sk-ant-[A-Za-z0-9_-]{20,}`
    - Groq keys: `gsk_[A-Za-z0-9_-]{20,}`
    - Private key headers: `-----BEGIN [A-Z0-9 ]*PRIVATE KEY-----`
    - Authorization headers: case-insensitive `authorization` or `proxy-authorization` followed by `bearer` or `basic`
    - Credential-bearing database URLs: `postgres://`, `postgresql://`, `mysql://`, `mongodb://`, `mongodb+srv://`, or `redis://` with `user:password@`

    Use synthetic fake test values only. Do not add real secrets or environment-derived values to fixtures.
  </action>
  <acceptance_criteria>
    - `rg -n "SECRET_LINT_BLOCKED_EVENT_TYPE|SECRET_LINT_FAIL_REASON|class SecretFinding|def redact_secrets|def scan_text|def scan_mapping|def first_secret_finding" whilly/security/secret_lint.py` shows all required public names.
    - `rg -n "aws-access-key-id|github-token|slack-token|openai-api-key|anthropic-api-key|groq-api-key|private-key|auth-header|database-url|sensitive-config-key" whilly/security/secret_lint.py tests/unit/test_secret_lint.py` shows every required pattern id in code or tests.
    - `rg -n "AKIAIOSFODNN7EXAMPLE|ghp_|sk-ant-|gsk_|postgres://user:password@" tests/unit/test_secret_lint.py` shows only synthetic fixture values.
    - `.venv/bin/python -m pytest -q tests/unit/test_secret_lint.py --maxfail=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_secret_lint.py --maxfail=1</automated>
  </verify>
  <done>`secret_lint.py` exists, exports the shared contract, detects/redacts all required fake patterns, and never exposes the raw fake secret in finding payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate sanitizer and GitHub issue warnings to shared secret lint</name>
  <files>whilly/security/prompt_sanitizer.py, whilly/sources/github_issues.py, tests/unit/test_prompt_sanitizer.py, tests/unit/test_prompt_sanitizer_wiring.py, tests/test_github_issues_source.py</files>
  <read_first>
    .planning/phases/08-sandbox-and-secrets-hardening/08-CONTEXT.md
    .planning/phases/08-sandbox-and-secrets-hardening/08-RESEARCH.md
    .planning/phases/08-sandbox-and-secrets-hardening/08-VALIDATION.md
    docs/CODEX-MISSION.md
    whilly/security/secret_lint.py
    whilly/security/prompt_sanitizer.py
    whilly/sources/github_issues.py
    whilly/sinks/github_pr.py
    whilly/workflow/pr_iterate.py
    tests/unit/test_prompt_sanitizer.py
    tests/unit/test_prompt_sanitizer_wiring.py
    tests/test_github_issues_source.py
  </read_first>
  <behavior>
    - Sanitizing text containing fake AWS, GitHub, Slack, OpenAI, Anthropic, Groq, private-key, auth-header, or database-URL values removes the raw value and preserves fences.
    - `sanitize_title_slot()` strips controls, caps length, and redacts through the shared `redact_secrets()` helper.
    - `_detect_secrets()` in `github_issues.py` returns shared pattern ids such as `aws-access-key-id`, not raw regex strings.
    - Existing external feedback fencing tests continue to pass without adding prompt-injection blocking for PR comments or diffs.
  </behavior>
  <action>
    Replace `whilly/security/prompt_sanitizer.py` private secret regex handling with `from whilly.security.secret_lint import contains_secret, redact_secrets`.

    In `sanitize_external_text()` and `sanitize_title_slot()`, call `redact_secrets(payload)` instead of iterating a local `_SECRET_REPLACEMENTS` tuple. In `_is_already_sanitized()`, use `contains_secret(payload)` to reject already-fenced text that still contains a raw secret.

    Remove the local `_SECRET_REPLACEMENTS` tuple from `prompt_sanitizer.py` so there is only one secret pattern registry.

    In `whilly/sources/github_issues.py`, remove `_SECRET_PATTERNS` and implement `_detect_secrets(text: str) -> list[str]` with the shared secret lint module. It should return a list containing the matched `pattern_id` values, preserving the existing return type and warning behavior.

    Extend tests in `tests/unit/test_prompt_sanitizer.py` and `tests/unit/test_prompt_sanitizer_wiring.py` to cover synthetic Anthropic, Groq, auth-header, private-key, and credential-bearing database URL values. Extend `tests/test_github_issues_source.py` so secret warning tests assert shared ids like `aws-access-key-id` and `github-token`.
  </action>
  <acceptance_criteria>
    - `rg -n "from whilly.security.secret_lint import .*redact_secrets" whilly/security/prompt_sanitizer.py` shows the sanitizer imports shared redaction.
    - `! rg -n "_SECRET_REPLACEMENTS|_SECRET_PATTERNS" whilly/security/prompt_sanitizer.py whilly/sources/github_issues.py` returns no matches.
    - `rg -n "scan_text|pattern_id|aws-access-key-id|github-token" whilly/sources/github_issues.py tests/test_github_issues_source.py` shows GitHub issue warnings use shared ids.
    - `.venv/bin/python -m pytest -q tests/unit/test_secret_lint.py tests/unit/test_prompt_sanitizer.py tests/unit/test_prompt_sanitizer_wiring.py tests/test_github_issues_source.py --maxfail=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_secret_lint.py tests/unit/test_prompt_sanitizer.py tests/unit/test_prompt_sanitizer_wiring.py tests/test_github_issues_source.py --maxfail=1</automated>
  </verify>
  <done>Existing sanitizer and GitHub issue secret-warning behavior route through `secret_lint.py`, broader provider/config secret patterns are covered, and external feedback remains fenced/redacted rather than prompt-injection blocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire config-value secret lint into project-config validation</name>
  <files>whilly/project_config/loader.py, tests/unit/test_project_config.py</files>
  <read_first>
    .planning/phases/08-sandbox-and-secrets-hardening/08-CONTEXT.md
    .planning/phases/08-sandbox-and-secrets-hardening/08-RESEARCH.md
    .planning/phases/08-sandbox-and-secrets-hardening/08-VALIDATION.md
    whilly/security/secret_lint.py
    whilly/project_config/loader.py
    whilly/project_config/models.py
    whilly/cli/project_config.py
    tests/unit/test_project_config.py
  </read_first>
  <behavior>
    - `project_config_from_dict()` raises `ProjectConfigError` when persisted project config data contains a plaintext token-like value under a config-like key, for example `sinks[0].config.token = "ghp_" + "A" * 36`.
    - The error message contains these exact substrings: `secret_lint_blocked`, `project_config.sinks[0].config.token`, `github-token`, and `use env:, keyring:, or file: references`.
    - The same config shape passes when `sinks[0].config.token` is `env:GITHUB_TOKEN`, `keyring:whilly/github`, or `file:/tmp/whilly-token`.
    - `whilly project-config validate` returns `EXIT_VALIDATION_ERROR` and writes stderr containing `secret_lint_blocked` for the plaintext-token fixture.
  </behavior>
  <action>
    In `whilly/project_config/loader.py`, import `SecretFinding` and `scan_mapping` from `whilly.security.secret_lint`.

    Add:
    `def _project_config_secret_finding(data: dict[str, Any]) -> SecretFinding | None:`

    The helper must recursively flatten dictionaries, lists, and tuples into a mapping whose keys are project-config paths without a prefix. Use these exact path formats:
    - dict field: `sinks`, `release_policy`, `outputs`, `task_sources`
    - list item: `sinks[0]`
    - nested dict field: `sinks[0].config.token`

    The helper must call `scan_mapping(flattened, field_path_prefix="project_config")` and return its first finding. Do not resolve secret references, read environment variables, read files, or call keyring during validation; `env:`, `keyring:`, and `file:` references are valid by prefix only.

    At the start of `project_config_from_dict()`, before dataclass construction and before any generated plan validation, call `_project_config_secret_finding(data)`. If it returns a finding, raise `ProjectConfigError` with this exact message shape:
    `f"{source}: secret_lint_blocked: plaintext secret-like value at {finding.field_path} matched {finding.pattern_id}; use env:, keyring:, or file: references"`

    In `tests/unit/test_project_config.py`, add focused tests:
    - `test_project_config_rejects_plaintext_secret_like_config_value()` using a minimal generic project config with `sinks=[{"type": "jsonl", "config": {"token": "ghp_" + "A" * 36}}]`; assert the four required substrings above appear in the raised `ProjectConfigError`.
    - `test_project_config_allows_secret_reference_values()` parameterized over `env:GITHUB_TOKEN`, `keyring:whilly/github`, and `file:/tmp/whilly-token`; assert `project_config_from_dict()` returns a config and does not resolve the reference.
    - `test_project_config_cli_validate_reports_secret_lint_blocked()` writing the plaintext-token fixture to a temporary JSON config and calling `main(["project-config", "validate", str(config_path)])`; assert exit code `1` and stderr contains `whilly project-config validate:` plus `secret_lint_blocked`.

    Keep the validation limited to persisted project-config data. Do not add a runtime secret resolver, do not change `ProjectConfig` dataclasses, and do not block safe non-secret scalar config such as `environment = "STAGE"`.
  </action>
  <acceptance_criteria>
    - `rg -n "_project_config_secret_finding|scan_mapping|secret_lint_blocked|use env:, keyring:, or file: references" whilly/project_config/loader.py tests/unit/test_project_config.py` shows the validation hook, helper call, error reason, and tests.
    - `rg -n "ghp_.*A|env:GITHUB_TOKEN|keyring:whilly/github|file:/tmp/whilly-token" tests/unit/test_project_config.py` shows only synthetic plaintext-token fixtures and allowed reference fixtures.
    - `.venv/bin/python -m pytest -q tests/unit/test_secret_lint.py tests/unit/test_project_config.py --maxfail=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_secret_lint.py tests/unit/test_project_config.py --maxfail=1</automated>
  </verify>
  <done>`project_config_from_dict()` and `whilly project-config validate` reject plaintext token-like persisted config values with reason `secret_lint_blocked`, while `env:`, `keyring:`, and `file:` references pass unchanged.</done>
</task>

</tasks>

<verification>
Run the plan-level focused check:

```bash
.venv/bin/python -m pytest -q tests/unit/test_secret_lint.py tests/unit/test_prompt_sanitizer.py tests/unit/test_prompt_sanitizer_wiring.py tests/unit/test_project_config.py tests/test_github_issues_source.py --maxfail=1
.venv/bin/python -m ruff check whilly/security/secret_lint.py whilly/security/prompt_sanitizer.py whilly/project_config/loader.py whilly/sources/github_issues.py tests/unit/test_secret_lint.py tests/unit/test_prompt_sanitizer.py tests/unit/test_prompt_sanitizer_wiring.py tests/unit/test_project_config.py tests/test_github_issues_source.py
```
</verification>

<success_criteria>
- `whilly/security/secret_lint.py` is the only runtime secret-pattern registry used by sanitizer and GitHub issue warning code.
- SEC-01 coverage includes task/external/project-config/prompt-compatible helpers and audit-safe redacted excerpts.
- Persisted project configs reject plaintext token-like config values with `secret_lint_blocked` and allow `env:`, `keyring:`, and `file:` references.
- No raw synthetic fake token appears in event payload assertions except as test input.
</success_criteria>

<output>
After completion, create `.planning/phases/08-sandbox-and-secrets-hardening/08-01-SUMMARY.md`.
</output>
