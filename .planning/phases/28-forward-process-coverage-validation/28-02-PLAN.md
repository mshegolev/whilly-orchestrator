---
phase: 28-forward-process-coverage-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/COVERAGE-MATRIX.md
autonomous: true
requirements: [COV-01, VAL-01, VAL-02]
must_haves:
  truths:
    - "The coverage matrix body-row count equals the live `find whilly/ -name '*.py' -not -path '*/__pycache__/*' | wc -l` count (275 at phase start), with 0 UNMAPPED, 0 double-mapped, every slug in the 32 TAXONOMY capabilities, and every one of the 32 capabilities mapped by >=1 module."
    - "`openspec validate --all --strict` reports 32 passed / 0 failed, recorded with the date observed."
    - "Every one of the 32 capability specs is normatively accurate: SHALL/MUST requirement bodies + >=1 '#### Scenario:', grounded in real v4 code, with no spec pinning removed/legacy behavior as live — consolidated from the 6 cluster VERIFICATION.md reports plus a final spot-sweep, captured in a VAL-02 review note."
  artifacts:
    - path: "openspec/COVERAGE-MATRIX.md"
      provides: "COV-01 audit note + VAL-01 validate record + VAL-02 review note recording the closeout results"
      contains: "COV-01"
  key_links:
    - from: "openspec/COVERAGE-MATRIX.md"
      to: "live find count"
      via: "audited body-row equality"
      pattern: "275"
---

<objective>
Audit the coverage matrix at 100%, assert all 32 specs are strict-valid, and produce a
consolidated normative-accuracy review of every capability spec — the three closeout
gates that prove the v1.3 baseline is complete.

Purpose: COV-01 proves every whilly/ module is accounted for (matrix body rows == live
module count, 0 UNMAPPED, 0 double-map, all 32 capabilities covered). VAL-01 proves all
32 specs pass openspec validate --all --strict. VAL-02 proves no spec is descriptive-only
or pins legacy-as-current — leveraging the already-written cluster VERIFICATION reports
plus a final spot-sweep. All three results are recorded in COVERAGE-MATRIX.md so the
milestone closeout is auditable.

Output: COV-01 audit note, VAL-01 validate record, and a VAL-02 review note — all written
into openspec/COVERAGE-MATRIX.md. Documentation-only; no whilly/ changes. A capability
spec.md is touched ONLY if VAL-02 finds a concrete inaccuracy (then a minimal fix + note).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-forward-process-coverage-validation/CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@openspec/COVERAGE-MATRIX.md
@openspec/TAXONOMY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: COV-01 coverage-matrix audit + VAL-01 strict validation record</name>
  <files>openspec/COVERAGE-MATRIX.md</files>
  <read_first>
    - openspec/COVERAGE-MATRIX.md (existing matrix, counts section, inclusion policy, locked rules)
    - openspec/TAXONOMY.md (the canonical 32 capability slugs — the matrix's capability column must be a subset of these, and all 32 must appear)
    - .planning/phases/28-forward-process-coverage-validation/CONTEXT.md (COV-01 + VAL-01 how-to rows; the exact find/validate commands)
  </read_first>
  <action>
    Run the COV-01 audit with exact commands and record the results in openspec/COVERAGE-MATRIX.md. Compute the live module count with the canonical command and compare to the matrix body-row count: live = `find whilly/ -name "*.py" -not -path "*/__pycache__/*" | wc -l`; body rows = count of `| whilly/...` data rows in the matrix table. Assert live == body rows. Assert 0 UNMAPPED (no data row whose capability column is the literal UNMAPPED). Assert 0 double-mapped (no module path appears in two data rows). Assert every capability used in the matrix's capability column is one of the 32 slugs in TAXONOMY.md, and that every one of the 32 TAXONOMY slugs is used by at least one module row (every capability has >=1 module). If the live count has drifted from the recorded count, reconcile the matrix (add/remove/remap rows per the locked inclusion policy and locked rules) until all five assertions hold; if no drift, leave rows unchanged. Then run VAL-01: `openspec validate --all --strict` and capture the totals line. Add a dated "COV-01 Audit" note/section to COVERAGE-MATRIX.md recording: the live module count, the body-row count, the five pass/fail results, and any reconciliation performed; and add a "VAL-01 Validation" line recording the validate totals (expected "32 passed, 0 failed") with the date observed. Do NOT modify any whilly/ file. Do NOT modify capability specs in this task.
  </action>
  <verify>
    <automated>LIVE=$(find whilly/ -name "*.py" -not -path "*/__pycache__/*" | wc -l | tr -d ' '); ROWS=$(grep -cE '^\| whilly/' openspec/COVERAGE-MATRIX.md); UNM=$(grep -cE '^\| whilly/.*\| *UNMAPPED' openspec/COVERAGE-MATRIX.md); CAPS=$(grep -E '^\| whilly/' openspec/COVERAGE-MATRIX.md | awk -F'|' '{print $3}' | sed 's/ //g' | sort -u | wc -l | tr -d ' '); VAL=$(openspec validate --all --strict 2>&1 | grep -i 'Totals'); echo "live=$LIVE rows=$ROWS unmapped=$UNM caps=$CAPS"; echo "$VAL"; test "$LIVE" = "$ROWS" && test "$UNM" = "0" && test "$CAPS" = "32" && echo "$VAL" | grep -q '32 passed, 0 failed' && grep -q 'COV-01' openspec/COVERAGE-MATRIX.md && grep -qi 'VAL-01' openspec/COVERAGE-MATRIX.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `find whilly/ -name "*.py" -not -path "*/__pycache__/*" | wc -l` equals the matrix body-row count (`grep -cE '^\| whilly/' openspec/COVERAGE-MATRIX.md`).
    - 0 UNMAPPED rows; 0 double-mapped module paths.
    - The matrix capability column uses exactly the 32 TAXONOMY slugs, and all 32 are each mapped by >=1 module.
    - `openspec validate --all --strict` prints "Totals: 32 passed, 0 failed".
    - COVERAGE-MATRIX.md contains a dated COV-01 Audit note and a VAL-01 Validation record.
    - No whilly/ file and no capability spec.md modified in this task.
  </acceptance_criteria>
  <done>The five COV-01 assertions hold, VAL-01 records 32 passed / 0 failed, both are noted in COVERAGE-MATRIX.md, and the verify command prints PASS.</done>
</task>

<task type="auto">
  <name>Task 2: VAL-02 consolidated normative-accuracy review note</name>
  <files>openspec/COVERAGE-MATRIX.md</files>
  <read_first>
    - .planning/phases/22-orchestration-cluster/22-VERIFICATION.md
    - .planning/phases/23-prd-pipeline-decision/23-VERIFICATION.md
    - .planning/phases/24-integrations-cluster/24-VERIFICATION.md
    - .planning/phases/25-operator-surface-cluster/25-VERIFICATION.md
    - .planning/phases/26-platform-cluster/26-VERIFICATION.md
    - .planning/phases/27-safety-quality-cluster/27-VERIFICATION.md
    - .planning/phases/28-forward-process-coverage-validation/CONTEXT.md (VAL-02 row: no descriptive-only specs; SHALL/MUST bodies + >=1 #### Scenario: each; legacy-as-current spot-sweep)
    - openspec/TAXONOMY.md (the 32 slugs whose specs the review must cover)
  </read_first>
  <action>
    Produce a consolidated VAL-02 normative-accuracy review and record it as a dated "VAL-02 Review" note/section in openspec/COVERAGE-MATRIX.md. Consolidation basis: the 6 cluster VERIFICATION.md reports (phases 22-27) already adversarially code-grounded each capability cluster — summarize their verdicts per cluster, mapping each to the capability slugs it covered, so all 32 specs are accounted for across the six reports. Then run a final mechanical spot-sweep across all 32 openspec/specs/<slug>/spec.md to confirm none is descriptive-only: each spec must contain SHALL or MUST requirement bodies AND at least one "#### Scenario:" line. Flag any spec missing either. Additionally do a targeted legacy-as-current sweep: scan for specs that describe removed/legacy v4 behavior as if it were live (the known-truthful ones — decomposition, reporting, recovery-self-healing, verification-gates verify_task, StateStore in state-persistence, v3 budget lore in budget-resource-guards — should already MARK such behavior as legacy/unwired/no-op, not assert it as current; confirm they still read truthfully and none silently flipped). Record the review note with: the per-cluster VERIFICATION verdict summary, the spot-sweep result (count of specs with SHALL/MUST + >=1 Scenario, expected 32/32), and the legacy-sweep result (no spec pins legacy-as-current). If the sweep finds a concrete inaccuracy in a specific spec.md, apply the MINIMAL fix to that spec.md (and add its path to files_modified in the SUMMARY), then re-run `openspec validate --all --strict` to confirm still 32 passed / 0 failed; otherwise touch no spec. Do NOT modify any whilly/ file.
  </action>
  <verify>
    <automated>OK=0; for d in openspec/specs/*/; do f="$d/spec.md"; grep -qE 'SHALL|MUST' "$f" && grep -q '#### Scenario:' "$f" && OK=$((OK+1)); done; echo "specs_with_normative_body_and_scenario=$OK/32"; VAL=$(openspec validate --all --strict 2>&1 | grep -i 'Totals'); echo "$VAL"; test "$OK" = "32" && echo "$VAL" | grep -q '32 passed, 0 failed' && grep -qi 'VAL-02' openspec/COVERAGE-MATRIX.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 32 specs contain SHALL/MUST requirement bodies AND >=1 "#### Scenario:" (mechanical sweep returns 32/32).
    - The VAL-02 note summarizes the 6 cluster VERIFICATION verdicts mapped to capability slugs so all 32 specs are covered.
    - The legacy-as-current sweep is recorded: no spec pins removed/legacy behavior as live (the truthfully-legacy specs still read as legacy/unwired/no-op).
    - If any spec.md was minimally fixed, `openspec validate --all --strict` still prints "32 passed, 0 failed" and the changed path is listed in the SUMMARY.
    - COVERAGE-MATRIX.md contains a dated VAL-02 Review note.
    - No whilly/ file modified.
  </acceptance_criteria>
  <done>VAL-02 review note recorded in COVERAGE-MATRIX.md confirming all 32 specs are normative (SHALL/MUST + >=1 Scenario) and none pins legacy-as-current; validate still 32/0; the verify command prints PASS.</done>
</task>

</tasks>

<verification>
- Matrix body rows == live find count (275 at phase start); 0 UNMAPPED; 0 double-map; 32 TAXONOMY slugs each with >=1 module.
- `openspec validate --all --strict` -> "32 passed, 0 failed", recorded with date.
- All 32 specs have SHALL/MUST bodies + >=1 "#### Scenario:"; no spec pins legacy-as-current.
- COVERAGE-MATRIX.md carries dated COV-01 Audit, VAL-01 Validation, and VAL-02 Review notes.
- No whilly/ Python files modified; capability spec.md touched only if VAL-02 found a concrete inaccuracy (minimal fix + note).
</verification>

<success_criteria>
- COV-01: coverage matrix audited at 100% (body rows == live count, 0 gaps, all 32 capabilities covered), recorded.
- VAL-01: openspec validate --all --strict passes (32/0), recorded.
- VAL-02: all 32 specs reviewed normatively accurate (no descriptive-only, no legacy-as-current), consolidated note recorded.
</success_criteria>

<output>
Create `.planning/phases/28-forward-process-coverage-validation/28-02-SUMMARY.md` when done.
</output>
