---
phase: 13.1-version-update-checks-and-manual-automatic-update-modes
plan: 01
type: execute
wave: 1
files_modified:
  - whilly/update.py
  - whilly/cli/update.py
  - whilly/cli/__init__.py
  - tests/unit/test_update_service.py
  - tests/unit/test_update_cli.py
  - tests/unit/test_cli_operator_commands.py
requirements:
  - UPD-01
  - UPD-02
  - UPD-03
  - UPD-04
must_haves:
  truths:
    - "whilly update check is non-mutating and reports installed/latest/update availability."
    - "whilly update install performs an explicit manual update and supports --dry-run."
    - "whilly update auto defaults to off and only runs check/install when an explicit mode is selected."
    - "Network and subprocess boundaries are mocked in tests; no live package index or real install is required."
---

<objective>
Implement the inserted update mechanism: check for newer versions, run explicit manual updates, and
support a safe opt-in automatic policy.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add update service contract</name>
  <files>tests/unit/test_update_service.py, whilly/update.py</files>
  <behavior>
    - Fetch latest version from PyPI JSON through an injectable opener.
    - Compare installed/latest versions without adding a runtime dependency.
    - Build pip/pipx update commands and support dry-run/apply results.
    - Resolve automatic update mode from `WHILLY_UPDATE_MODE` or explicit override.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add update CLI</name>
  <files>tests/unit/test_update_cli.py, whilly/cli/update.py, whilly/cli/__init__.py</files>
  <behavior>
    - Add `whilly update check`.
    - Add `whilly update install [--dry-run] [--installer auto|pip|pipx]`.
    - Add `whilly update auto [--mode off|check|install] [--dry-run]`.
    - Wire `whilly update ...` through the lazy CLI dispatcher and help text.
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify and close planning traceability</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <behavior>
    - Mark UPD-01 through UPD-04 complete after focused verification.
    - Mark Phase 13.1 complete and route next work back to Phase 14.
  </behavior>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest -q tests/unit/test_update_service.py tests/unit/test_update_cli.py tests/unit/test_cli_operator_commands.py`
- `.venv/bin/python -m ruff check whilly/update.py whilly/cli/update.py whilly/cli/__init__.py tests/unit/test_update_service.py tests/unit/test_update_cli.py tests/unit/test_cli_operator_commands.py`
</verification>
