---
phase: 20-jira-watcher-daemon
plan: 03
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
files_modified:
  - whilly/cli/jira.py
  - tests/unit/test_jira_cli.py
  - docs/Whilly-Usage.md
  - tests/unit/test_docs_live_smoke.py
autonomous: true
requirements: [WATCH-01, WATCH-02, WATCH-03]
must_haves:
  truths:
    - "`whilly jira watch` is a registered ACTION that dispatches to _run_jira_watch through run_jira_command"
    - "`whilly jira watch-status` reads jira-watch-status.json and prints it (human-readable default, --json structured)"
    - "The watch subparser declares --interval, --issue (repeatable), --timeout, --dispatch, --readiness-repo-path, --allow-unready-run, --interactive-config, --no-interactive-config"
    - "Production dispatch is wired through the existing Phase-17-gated jira run path, default-off"
    - "docs/Whilly-Usage.md has a watch section and the docs regression test pins it"
  artifacts:
    - path: "whilly/cli/jira.py"
      provides: "watch + watch-status subparsers, run_jira_command dispatch branches, lazy import of jira_watch_loop"
      contains: "watch-status"
    - path: "docs/Whilly-Usage.md"
      provides: "Jira watcher daemon section: command, interval, status file, exit codes, gates"
      contains: "whilly jira watch"
    - path: "tests/unit/test_docs_live_smoke.py"
      provides: "regression test pinning the watch docs section and command names"
      contains: "whilly jira watch"
  key_links:
    - from: "whilly/cli/jira.py run_jira_command"
      to: "whilly.cli.jira_watch_loop._run_jira_watch"
      via: "lazy import in the watch action branch (avoids circular import)"
      pattern: "jira_watch_loop"
    - from: "whilly/cli/jira.py watch subparser"
      to: "_ensure_jira_config"
      via: "--interactive-config / --no-interactive-config flags present"
      pattern: "interactive-config"
    - from: "docs/Whilly-Usage.md"
      to: "tests/unit/test_docs_live_smoke.py"
      via: "docs regression assertions on the watch section"
      pattern: "whilly jira watch"
---

<objective>
Wire the watch loop into the CLI surface and document it. Add `watch` and `watch-status`
subparsers to `build_jira_parser()`, route both actions in `run_jira_command` via a lazy
import of `whilly.cli.jira_watch_loop` (avoiding the circular import), supply the production
Phase-17-gated dispatch runner so `--dispatch` actually routes through `jira run` semantics,
implement the `watch-status` reader (human-readable + `--json`), and add a watcher section to
`docs/Whilly-Usage.md` pinned by a docs regression test.

Purpose: completes WATCH-01 (operator-runnable `whilly jira watch`), WATCH-02 (status
inspection via `watch-status`), and WATCH-03 (gated dispatch wired through the real run path).
This is the only plan that makes the daemon operator-facing.

Output: modified `whilly/cli/jira.py`, CLI dispatch test, docs section, docs regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-jira-watcher-daemon/20-CONTEXT.md
@.planning/phases/20-jira-watcher-daemon/20-RESEARCH.md
@.planning/phases/20-jira-watcher-daemon/20-PATTERNS.md
@.planning/phases/20-jira-watcher-daemon/20-01-SUMMARY.md
@.planning/phases/20-jira-watcher-daemon/20-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: watch + watch-status subparsers and run_jira_command dispatch wiring</name>
  <files>whilly/cli/jira.py, tests/unit/test_jira_cli.py</files>
  <read_first>
    - whilly/cli/jira.py lines 287-318 (poll + smoke subparser shapes; smoke's `--interactive-config`/`--no-interactive-config` pair to copy verbatim)
    - whilly/cli/jira.py lines 364-448 (`run_jira_command` dispatch block; the `tui` lazy-import branch at lines 434-436 to mirror)
    - whilly/cli/jira.py lines 622-638 + 516 (`_run_intake` run path / `_run_plan_command` — the production dispatch the `--dispatch` runner must route through; preserve `--allow-unready-run`)
    - whilly/cli/jira_watch_loop.py (`_run_jira_watch` signature: snapshot_collector, environ, stop_event, install_signal_handlers, pause_control, dispatch_runner; status-path helpers)
    - tests/unit/test_jira_cli.py (existing action-dispatch test pattern for poll/smoke/tui — mirror for watch/watch-status)
    - 20-RESEARCH.md Finding 6 + Pitfall 1 (must add --interactive-config; _ensure_jira_config accesses args.interactive_config directly)
  </read_first>
  <behavior>
    - Test: `run_jira_command(["watch", "--issue", "ABC-123", "--interval", "0", ...], snapshot_collector=fake, ...)` dispatches to `_run_jira_watch` (injected collector called, loop stops via a pre-set stop event passed through) and returns 0.
    - Test: the watch subparser exposes --interval, repeatable --issue, --timeout, --dispatch (default False), --readiness-repo-path, --allow-unready-run, --interactive-config, --no-interactive-config (assert via parse_args on the built parser).
    - Test: `run_jira_command(["watch-status"])` with a status file present prints the status; `["watch-status", "--json"]` prints valid JSON; missing status file yields a clear "no watcher status found" message and a non-crash exit.
    - Test: without --dispatch, the production dispatch runner is not invoked (default-off preserved end-to-end through the CLI).
  </behavior>
  <action>
    In `build_jira_parser()` (after the smoke block, lines ~287-318) add `p_watch =
    sub.add_parser("watch", help="Run a continuous Jira intake daemon wrapping the one-shot
    poll cycle on a configurable interval.")` with: `--issue` (action="append", required, help
    "Jira key or browse URL; repeatable."), `--interval` (type=int, default=None, help
    mentions WHILLY_JIRA_WATCH_INTERVAL and 300s default), `--timeout` (type=int, default=15),
    `--dispatch` (action="store_true", default False, help "Enable autonomous dispatch of ready
    issues through the gated jira run path; OFF by default."), `--readiness-repo-path`
    (default=None), `--allow-unready-run` (action="store_true"), and the `--interactive-config`
    / `--no-interactive-config` pair copied verbatim from the smoke subparser (jira.py:308-318)
    — REQUIRED because `_ensure_jira_config` reads `args.interactive_config` directly
    (Phase 19 pitfall, STATE.md). Add `p_watch_status = sub.add_parser("watch-status",
    help="Print the current Jira watcher status (running/stopped, last poll, error count).")`
    with a single `--json` (action="store_true").

    In `run_jira_command`, add two branches before the final `parser.error`, mirroring the
    `tui` lazy-import branch (jira.py:434-436): for `action == "watch"`, `from
    whilly.cli.jira_watch_loop import _run_jira_watch` then call it with
    `snapshot_collector=snapshot_collector or collect_jira_work_snapshot`, `environ=environ`,
    the credential collaborators (config_loader/config_reader/prompt/secret_prompt/
    browser_opener/stdin_isatty), and a production `dispatch_runner` that, when `--dispatch` is
    set, routes through the existing Phase-17-gated run path (reuse `plan_runner` /
    `_run_plan_command` + the `_run_intake` run semantics; pass `--allow-unready-run` through).
    For `action == "watch-status"`, `from whilly.cli.jira_watch_loop import
    _run_watch_status` and call it with `environ=environ`. Keep the loop's credential gate
    inside `_run_jira_watch` (it owns `_ensure_jira_config`) — but `_ensure_jira_config` lives
    in jira.py, so pass it (or the already-resolved collaborators) so the loop module stays
    free of a `whilly.cli.jira` import (inject the config gate as a callable if needed; Claude's
    Discretion on the exact seam, consistent with plan 01/02 injectables).

    Also add `_run_watch_status(args, *, environ=None) -> int` — place it in
    `whilly/cli/jira_watch_loop.py` (NOT jira.py) so jira.py only imports it: read
    `_status_path()`, if missing print "whilly jira watch-status: no watcher status found at
    <path>" to stderr and return EXIT_OK (status absence is not an error); else load JSON and
    either pretty-print key fields (state, pid, last_poll_at, cycle_count, error_count,
    backoff_seconds) or `print(json.dumps(...))` when `args.json`. (This is a small addition to
    the plan-02 module surface; it does not conflict because plan 03 runs after plan 02 in a
    later wave — the file is owned by the loop module but this plan's `files_modified` lists
    jira.py only; add `_run_watch_status` here is acceptable since waves are sequential and 03
    depends on 02. If the executor finds `_run_watch_status` already present from an earlier
    plan, reuse it.)

    Extend `tests/unit/test_jira_cli.py` with the four behavior tests above, building args via
    the real parser and injecting a counting collector + pre-set stop event through
    `run_jira_command`'s `snapshot_collector` (and, for the loop's stop, rely on a collector
    that stops after one cycle since `run_jira_command` does not expose stop_event — assert via
    cycle count / status file instead).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_jira_cli.py -k "watch" -q</automated>
  </verify>
  <acceptance_criteria>
    `whilly jira watch` and `whilly jira watch-status` are registered and dispatch correctly;
    watch subparser carries all required flags including --interactive-config; watch-status
    reads/prints (human + --json) and tolerates a missing file; dispatch stays off without
    --dispatch.
  </acceptance_criteria>
  <done>
    `tests/unit/test_jira_cli.py -k watch` passes; `run_jira_command` routes watch/watch-status
    via lazy import; `grep -c "from whilly.cli.jira" whilly/cli/jira_watch_loop.py` remains 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Watcher docs section + docs regression test</name>
  <files>docs/Whilly-Usage.md, tests/unit/test_docs_live_smoke.py</files>
  <read_first>
    - docs/Whilly-Usage.md lines 88-101 (existing `whilly jira poll` section — watcher section follows this voice and references that watch wraps poll)
    - docs/Whilly-Usage.md lines 627-732 (Live smoke section: command blocks, report-path callout, exit-code table — mirror structure for the watcher section)
    - tests/unit/test_docs_live_smoke.py lines 1-47 (assertion style: section heading present, command names present, report path present, exit codes present)
    - tests/unit/test_docs_bash_blocks_parse.py (bash blocks must pass `bash -n`; Phase 19 lesson: use env-var refs not angle-brackets in bash blocks)
  </read_first>
  <action>
    Add a `## Jira watcher daemon` section to `docs/Whilly-Usage.md` (after the poll section or
    near Live smoke) documenting: `whilly jira watch --issue ABC-123 --interval 300` as a
    foreground daemon the operator backgrounds via tmux/systemd; that it wraps the validated
    one-shot poll cycle; interval via `--interval` and `WHILLY_JIRA_WATCH_INTERVAL` (default
    300s); graceful stop with SIGINT/SIGTERM (Ctrl-C) finishing the current cycle and exiting
    0; the status file at `whilly_logs/watch/jira-watch-status.json` (honoring `WHILLY_LOG_DIR`)
    and the `whilly jira watch-status` / `whilly jira watch-status --json` reader; the
    single-instance guard (a second watcher refuses to start, never kills the first); the
    backoff behavior on transient failures; and the gates — global pause (`.whilly_pause`) keeps
    polling but dispatches nothing, and `--dispatch` is OFF by default and routes ready issues
    through the gated `jira run` path (readiness must be `ready_for_testing` unless
    `--allow-unready-run`). Include the DB audit-event note (best-effort when
    `WHILLY_DATABASE_URL` is set), mirroring the smoke `--persist` callout. Bash code blocks
    MUST use environment-variable references (not angle-bracket placeholders) so `bash -n`
    passes (Phase 19 docs lesson).

    Add regression tests to `tests/unit/test_docs_live_smoke.py` (or a sibling
    `test_docs_watch.py` if cleaner) mirroring the smoke assertions: the `## Jira watcher
    daemon` heading is present; `whilly jira watch` and `whilly jira watch-status` appear;
    `whilly_logs/watch/` appears; the status-file name `jira-watch-status.json` appears; and a
    note about `--dispatch` being off-by-default appears in the watcher section.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_docs_live_smoke.py tests/unit/test_docs_bash_blocks_parse.py -q</automated>
  </verify>
  <acceptance_criteria>
    docs/Whilly-Usage.md has a watcher section covering command, interval, status file, gates,
    and default-off dispatch; new regression assertions pin the section; all bash blocks pass
    `bash -n`.
  </acceptance_criteria>
  <done>
    Docs regression tests pass; `grep -c "whilly jira watch" docs/Whilly-Usage.md` >= 2.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → watch action | operator flags (`--issue`, `--dispatch`, `--allow-unready-run`) cross into the loop |
| watch action → dispatch runner | `--dispatch` is the only path to autonomous mutation |
| watch-status → status file | reads local JSON the daemon wrote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-10 | Elevation of Privilege | `--dispatch` default | mitigate | flag default False; production runner routes through the existing Phase-17 readiness gate (`ready_for_testing` unless `--allow-unready-run`); not a new bypass |
| T-20-11 | Information Disclosure | watch-status output | mitigate | watch-status prints only the status JSON, which plan 01/02 guarantee secret-free; no env dump |
| T-20-12 | Tampering | `--interactive-config` missing → AttributeError | mitigate | watch subparser declares the flag pair verbatim from smoke (Phase 19 STATE.md lesson) |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | no new packages this phase (RESEARCH Package Legitimacy Audit: none) — no install tasks |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_jira_cli.py -k watch -q` green
- `.venv/bin/python -m pytest tests/unit/test_docs_live_smoke.py tests/unit/test_docs_bash_blocks_parse.py -q` green
- `.venv/bin/python -m pytest tests/unit -q` full unit suite green (phase gate)
- `ruff check whilly/cli/jira.py tests/unit/test_jira_cli.py` clean
- `whilly jira watch --help` lists --interval, --issue, --dispatch, --interactive-config (manual / parser-level test)
</verification>

<success_criteria>
- Operator can run `whilly jira watch` and inspect via `whilly jira watch-status` (WATCH-01, WATCH-02)
- Dispatch wired off-by-default through the Phase-17-gated run path (WATCH-03)
- Watch subparser carries --interactive-config (no AttributeError regression)
- Docs section present and pinned by regression test; full unit suite green
</success_criteria>

<output>
Create `.planning/phases/20-jira-watcher-daemon/20-03-SUMMARY.md` when done
</output>
