---
phase: 21-spec-baseline-taxonomy
plan: 04
type: execute
wave: 3
depends_on: ["21-01", "21-02"]
files_modified:
  - openspec/COVERAGE-MATRIX.md
autonomous: true
requirements: [BASE-02]
nyquist_compliant: true

must_haves:
  truths:
    - "openspec/COVERAGE-MATRIX.md has one row for every whilly/ .py module, with the row count equal to the live `find whilly/ -name '*.py' -not -path '*/__pycache__/*' | wc -l` count"
    - "Every module row names exactly one capability slug from the 32 in TAXONOMY.md, or the literal UNMAPPED — no row is blank (zero silent gaps)"
    - "No module is mapped to two capabilities (zero double-mapping)"
    - "The matrix header reconciles the live module count against the 242 figure in REQUIREMENTS.md and states the inclusion policy (whether __init__.py / __main__.py are in scope)"
    - "whilly/adapters/confluence/ modules are explicitly assigned to a capability, not left UNMAPPED by omission"
  artifacts:
    - path: "openspec/COVERAGE-MATRIX.md"
      provides: "module -> capability coverage matrix (BASE-02)"
      contains: "whilly/task_manager.py"
  key_links:
    - from: "openspec/COVERAGE-MATRIX.md"
      to: "openspec/TAXONOMY.md"
      via: "every capability slug in the matrix is one of the 32 in the taxonomy"
      pattern: "task-model-fsm"
---

<objective>
Build openspec/COVERAGE-MATRIX.md (BASE-02): a single Markdown table mapping every
whilly/ Python module to exactly one of the 32 capability slugs, with zero silent
gaps (anything pending is the explicit literal UNMAPPED) and zero double-mapping.
The module count MUST be derived programmatically at execution time — the research
docs say 242/244 but the codebase has grown; the live `find` count is authoritative.
Documentation only — NO whilly/ Python changes.

Purpose: Prove (and make auditable for Phase 28 COV-01) that every module is
accounted for under exactly one capability.
Output: openspec/COVERAGE-MATRIX.md with one explicit row per module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-spec-baseline-taxonomy/21-RESEARCH.md
@.planning/phases/21-spec-baseline-taxonomy/21-VALIDATION.md
@.planning/REQUIREMENTS.md
@openspec/TAXONOMY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate modules and resolve the count policy</name>
  <read_first>
    - .planning/phases/21-spec-baseline-taxonomy/21-RESEARCH.md (sections "Module Count", "Pitfall 6: Double-Counting", "Pitfall 7: Treating __init__.py as Non-Modules", "Open Questions" #1 and #3) — the count discrepancy and the inclusion-policy guidance
    - .planning/REQUIREMENTS.md (BASE-02 wording: "every one of the 242 whilly/ modules ... exactly one capability, zero unmapped, zero double-mapped")
  </read_first>
  <action>
    Run `find whilly/ -name "*.py" -not -path "*/__pycache__/*" | sort` and capture the
    full list and its `wc -l` count. This live count is authoritative — do NOT hardcode
    242 or 244; the codebase has grown since research. Decide and record the inclusion
    policy explicitly: include ALL .py files (recommended — every __init__.py and
    __main__.py gets a row, marked "package initializer" / "entry shim" in Notes) so
    there are zero silent exclusions. Write the matrix header to openspec/COVERAGE-MATRIX.md
    stating: the live total module count, the inclusion policy, and a one-line
    reconciliation note explaining the live count differs from the "242" in
    REQUIREMENTS.md (codebase grew; the live `find` count supersedes the prior figure).
    Do not write the body rows yet — Task 2 fills them.
  </action>
  <verify>
    <automated>LIVE=$(find whilly/ -name "*.py" -not -path "*/__pycache__/*" | wc -l | tr -d ' '); echo "live=$LIVE"; test -f openspec/COVERAGE-MATRIX.md && grep -q "$LIVE" openspec/COVERAGE-MATRIX.md && grep -qi 'inclusion\|policy\|__init__\|all .py\|all \.py' openspec/COVERAGE-MATRIX.md && grep -q '242' openspec/COVERAGE-MATRIX.md && echo HEADER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `openspec/COVERAGE-MATRIX.md` exists with a header containing the live module count (the value of `find whilly/ -name "*.py" -not -path "*/__pycache__/*" | wc -l`).
    - The header states the inclusion policy (whether __init__.py/__main__.py are included) — `grep -qi 'inclusion\|policy\|__init__' openspec/COVERAGE-MATRIX.md`.
    - The header reconciles against the 242 figure — `grep -q '242' openspec/COVERAGE-MATRIX.md`.
  </acceptance_criteria>
  <done>openspec/COVERAGE-MATRIX.md has a header with the live module count, the inclusion policy, and the reconciliation note against REQUIREMENTS.md's 242.</done>
</task>

<task type="auto">
  <name>Task 2: Map every module to exactly one capability slug (BASE-02)</name>
  <read_first>
    - openspec/TAXONOMY.md (the 32 valid capability slugs — every row MUST use one of these or the literal UNMAPPED)
    - .planning/phases/21-spec-baseline-taxonomy/21-RESEARCH.md (section "Module -> Capability Pre-Assignment" — the high-confidence first-pass mapping table and the adapters/db / classifier / workflow / confluence / llm_ops guidance)
    - openspec/COVERAGE-MATRIX.md (the header + module list from Task 1)
  </read_first>
  <action>
    Fill openspec/COVERAGE-MATRIX.md with one table row per enumerated module:
    `| <module path> | <capability-slug> | <notes> |`. Use the pre-assignment table in
    21-RESEARCH.md as the starting point and assign each remaining module to the
    capability it PRIMARILY serves (Pitfall 6: never list a module under two slugs —
    pick the primary consumer; route cross-cutting infra such as adapters/db to
    state-persistence or web-status-ui per the research guidance). Every capability
    slug used MUST be one of the 32 in TAXONOMY.md. Explicitly assign
    whilly/adapters/confluence/__init__.py and whilly/adapters/confluence/publisher.py
    to a real capability (research candidate: github-integration as documentation
    publishing, or quality-compliance-audit) — record the chosen slug in Notes; do NOT
    leave confluence UNMAPPED by omission. For any module whose home is genuinely
    undecided, write the literal UNMAPPED in the slug column with a Notes reason — never
    leave the slug column blank. The number of body rows MUST equal the live module
    count from Task 1. Keep lines reasonable; the path column may be long. This is a
    one-to-one mapping: each module appears exactly once.
  </action>
  <verify>
    <automated>LIVE=$(find whilly/ -name "*.py" -not -path "*/__pycache__/*" | wc -l | tr -d ' '); ROWS=$(grep -c '| whilly/' openspec/COVERAGE-MATRIX.md); echo "live=$LIVE rows=$ROWS"; test "$ROWS" -eq "$LIVE" || { echo "ROW COUNT MISMATCH"; exit 1; }; for m in $(find whilly/ -name "*.py" -not -path "*/__pycache__/*" | sort); do grep -qF "| $m " openspec/COVERAGE-MATRIX.md || grep -qF "| $m|" openspec/COVERAGE-MATRIX.md || { echo "MISSING ROW: $m"; exit 1; }; done; grep -qF '| whilly/adapters/confluence/publisher.py' openspec/COVERAGE-MATRIX.md && ! grep -E '\| whilly/adapters/confluence/publisher.py \| *\|' openspec/COVERAGE-MATRIX.md && echo MATRIX_OK</automated>
  </verify>
  <acceptance_criteria>
    - Body row count equals the live module count: `grep -c '| whilly/' openspec/COVERAGE-MATRIX.md` equals `find whilly/ -name "*.py" -not -path "*/__pycache__/*" | wc -l`.
    - Every enumerated module path has its own row (the for-loop over `find` output finds each path; prints MATRIX_OK on success, no "MISSING ROW").
    - No blank slug column — every row's capability cell is a non-empty token (a taxonomy slug or the literal UNMAPPED).
    - whilly/adapters/confluence/publisher.py is assigned to a real capability slug, not UNMAPPED and not blank.
    - Every non-UNMAPPED slug used is one of the 32 in TAXONOMY.md (spot-check: `grep -oE '\| [a-z-]+ \|' openspec/COVERAGE-MATRIX.md | sort -u` yields only taxonomy slugs plus UNMAPPED).
    - No double-mapping: each module path appears in exactly one row (`for m in $(find whilly/ -name "*.py" -not -path "*/__pycache__/*"); do test $(grep -cF "| $m" openspec/COVERAGE-MATRIX.md) -le 1; done`).
  </acceptance_criteria>
  <done>openspec/COVERAGE-MATRIX.md maps every whilly/ module to exactly one capability slug (or explicit UNMAPPED), confluence assigned explicitly, zero blank rows, zero double-mapping, row count equals the live module count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

A single Markdown table under openspec/. No runtime code, no input, no auth surface,
no network. No high or medium application threats.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Information Disclosure | module paths in the matrix | accept | Module paths are non-secret repo file paths already public in git; no tokens/secrets are written |
| T-21-05 | Tampering | silent coverage gaps | mitigate | Row count gated == live find count; per-module presence loop; blank-slug rows forbidden (UNMAPPED is explicit); double-mapping check |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `grep -c '| whilly/' openspec/COVERAGE-MATRIX.md` equals the live `find` module count.
- Every module path is present (per-module for-loop gate passes).
- No blank slug cells; confluence explicitly assigned; no double-mapping.
- Header reconciles the live count vs the 242 figure and states the inclusion policy.
</verification>

<success_criteria>
- openspec/COVERAGE-MATRIX.md maps every whilly/ module to exactly one capability or explicit UNMAPPED (BASE-02).
- Zero silent gaps, zero double-mapping.
- Count reconciliation and inclusion policy documented in the header.
</success_criteria>

<output>
Create `.planning/phases/21-spec-baseline-taxonomy/21-04-SUMMARY.md` when done.
</output>
