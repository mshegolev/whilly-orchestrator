---
phase: 21-spec-baseline-taxonomy
plan: 03
type: execute
wave: 3
depends_on: ["21-01", "21-02"]
files_modified:
  - openspec/specs/task-model-fsm/spec.md
autonomous: true
requirements: [BASE-03]
nyquist_compliant: true

must_haves:
  truths:
    - "openspec/specs/task-model-fsm/spec.md passes openspec validate task-model-fsm --strict with zero errors and zero warnings (this is ROADMAP success criterion SC-5)"
    - "The exemplar follows the conventions documented in openspec/AUTHORING.md and serves as the copy-template for phases 22-27, demonstrating BASE-03 rather than authoring it"
    - "The exemplar is normative (every requirement body uses SHALL/MUST and is testable), not a descriptive snapshot"
  artifacts:
    - path: "openspec/specs/task-model-fsm/spec.md"
      provides: "Reference exemplar capability spec (SC-5)"
      contains: "## Purpose"
  key_links:
    - from: "openspec/specs/task-model-fsm/spec.md"
      to: "whilly/task_manager.py"
      via: "spec requirements describe the real FSM (status values, stale reset, terminal immutability)"
      pattern: "in_progress"
---

<objective>
Write the end-to-end reference exemplar capability spec for task-model-fsm
that passes `openspec validate task-model-fsm --strict`.
It is the template every phase 22-27 spec copies, so it must follow openspec/AUTHORING.md
exactly and be normative (SHALL/MUST + testable scenarios), reverse-spec'd from the
real FSM in whilly/task_manager.py. Documentation only — NO whilly/ Python changes.

This plan is the primary vehicle for ROADMAP success criterion SC-5 (a reference
exemplar passing `openspec validate --strict`): it DEMONSTRATES the BASE-03 authoring
conventions (authored in plan 21-01) by producing one fully-validated spec — it does
NOT re-author BASE-03. `requirements: [BASE-03]` reflects that demonstration linkage.

Purpose: Anchor the whole milestone with one validated, normative spec proving the
format works against real Whilly behavior.
Output: openspec/specs/task-model-fsm/spec.md passing strict validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-spec-baseline-taxonomy/21-RESEARCH.md
@.planning/phases/21-spec-baseline-taxonomy/21-VALIDATION.md
@openspec/AUTHORING.md
@whilly/task_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the task-model-fsm reference exemplar spec (SC-5)</name>
  <read_first>
    - openspec/AUTHORING.md (the conventions this spec MUST follow exactly — from plan 21-01)
    - .planning/phases/21-spec-baseline-taxonomy/21-RESEARCH.md (sections "Reference Exemplar Recommendation" and "Multi-Scenario, Multi-Requirement Spec (verified)" — the researcher already verified a task-model-fsm spec passes --strict; use its structure. Also "Environment Availability": the openspec binary lives in the nvm path and may not be on PATH)
    - whilly/task_manager.py (the real FSM: status values, get_ready_tasks, stale in_progress reset on startup, mark_status, terminal states) — reverse-spec the actual behavior, do not invent
    - CLAUDE.md ("Architecture big picture" — task statuses pending|in_progress|done|failed|skipped, stale in_progress reset on startup, status transitions through TaskManager.mark_status)
  </read_first>
  <action>
    Create openspec/specs/task-model-fsm/spec.md as a normative capability spec with
    exactly two `## ` sections: `## Purpose` (>= 50 characters of plain prose covering
    the FSM scope) and `## Requirements`. Under Requirements, write `### Requirement:`
    blocks each with a first body line containing SHALL or MUST and at least one
    `#### Scenario:` (4 hashtags) using `- **WHEN**` / `- **THEN**` (optional `- **AND**`).
    Cover at minimum these guarantees, reverse-spec'd from whilly/task_manager.py:
    (1) Legal status values — SHALL restrict task status to exactly the five values
    pending, in_progress, done, failed, skipped.
    (2) Startup stale reset — any task found in_progress at startup SHALL be reset to
    pending before any agent is dispatched.
    (3) Terminal state immutability — SHALL NOT transition a task out of done, failed,
    or skipped once reached.
    (4) Ready-set selection — done/failed/skipped tasks SHALL be excluded from the
    ready/candidate set on each iteration.
    Verify the actual transition/reset behavior against whilly/task_manager.py before
    writing each requirement (e.g. confirm the stale-reset path and the exact status
    strings). Do NOT use any delta header (`## ADDED Requirements` etc.) — this is a
    main spec. Do NOT put SHALL/MUST only in headers. Keep each requirement body under
    500 characters and lines <= 120 chars. After writing, the spec MUST pass
    `openspec validate task-model-fsm --strict`. NOTE: the openspec binary is in the
    nvm node path and may not be on PATH — resolve it first (prefer
    `command -v openspec` and fall back to the full path
    `~/.reflex/.nvm/versions/node/v20.19.6/bin/openspec`) so a missing binary fails
    loudly instead of a skipped validation masking a real failure.
  </action>
  <verify>
    <automated>OPENSPEC=$(command -v openspec || echo "$HOME/.reflex/.nvm/versions/node/v20.19.6/bin/openspec"); test -x "$OPENSPEC" || { echo "OPENSPEC BINARY NOT FOUND at $OPENSPEC"; exit 1; }; test -f openspec/specs/task-model-fsm/spec.md && "$OPENSPEC" validate task-model-fsm --strict --json 2>&1 | tee /tmp/tmfsm-validate.json && "$OPENSPEC" validate task-model-fsm --strict && echo STRICT_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `test -f openspec/specs/task-model-fsm/spec.md` succeeds.
    - Resolve the binary with `OPENSPEC=$(command -v openspec || echo "$HOME/.reflex/.nvm/versions/node/v20.19.6/bin/openspec")` and assert `test -x "$OPENSPEC"` (a missing binary MUST fail the task, not silently pass).
    - `"$OPENSPEC" validate task-model-fsm --strict` exits 0 (zero errors AND zero warnings — prints STRICT_PASS). This is ROADMAP success criterion SC-5.
    - `"$OPENSPEC" validate task-model-fsm --strict --json` reports valid=true with 0 errors and 0 warnings.
    - `grep -c '#### Scenario:' openspec/specs/task-model-fsm/spec.md` >= 4 (>=1 scenario per requirement).
    - `grep -c '### Requirement:' openspec/specs/task-model-fsm/spec.md` >= 4.
    - `grep -q '## ADDED Requirements\|## MODIFIED Requirements\|## REMOVED Requirements\|## RENAMED Requirements' openspec/specs/task-model-fsm/spec.md` returns NO match (exit 1 — no delta headers in a main spec).
    - The four status strings pending/in_progress/done/failed/skipped all appear in the spec (`for s in pending in_progress done failed skipped; do grep -q "$s" openspec/specs/task-model-fsm/spec.md; done`).
  </acceptance_criteria>
  <done>openspec/specs/task-model-fsm/spec.md exists, is normative, reverse-spec'd from whilly/task_manager.py, and passes openspec validate task-model-fsm --strict with zero issues (SC-5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

A single Markdown spec under openspec/specs/. No runtime code, no input, no auth
surface, no network. No high or medium application threats.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering | task-model-fsm spec accuracy vs code | mitigate | Requirements are reverse-spec'd against whilly/task_manager.py (read_first) and gated by openspec validate --strict; status strings grep-checked against the real FSM values |
| T-21-06 | Tampering | openspec binary not on PATH masking validation | mitigate | Verify resolves the binary via command -v with nvm full-path fallback and asserts test -x, so a missing binary fails loudly instead of silently skipping --strict |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `openspec validate task-model-fsm --strict` exits 0 (this is success criterion SC-5), using the nvm-resolved binary path.
- `openspec validate --specs --strict --json` passes for all specs present (only task-model-fsm has a spec.md after this plan).
- The spec is normative, not descriptive (manual read: every requirement uses SHALL/MUST and is testable).
</verification>

<success_criteria>
- openspec/specs/task-model-fsm/spec.md passes openspec validate --strict (SC-5).
- The exemplar is normative and reverse-spec'd from the real FSM.
- No delta headers; follows openspec/AUTHORING.md conventions.
</success_criteria>

<output>
Create `.planning/phases/21-spec-baseline-taxonomy/21-03-SUMMARY.md` when done.
</output>
