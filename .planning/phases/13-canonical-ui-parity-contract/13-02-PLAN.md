---
phase: 13-canonical-ui-parity-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - whilly/operator_views.py
  - whilly/api/static/whilly-hotkeys.js
  - tests/unit/test_wui_contract_static.py
  - tests/integration/test_htmx_dashboard.py
autonomous: true
requirements:
  - UI-02
must_haves:
  truths:
    - "Every WUI template and static JavaScript artifact is explicitly active, routeable noncanonical, or inactive quarantined."
    - "Active WUI files reject stale 1-7 surface switching assumptions."
    - "Active WUI files reject stale .tabs [data-key] selectors."
    - "Active WUI files reject stale non-API /admin/workers/ routes with (?<!/api/v1)/admin/workers/."
    - "whilly/api/static/whilly-hotkeys.js is either fixed to the five-surface contract or not classified as active."
  artifacts:
    - path: "whilly/operator_views.py"
      provides: "WUI artifact status metadata and artifact filtering helpers"
      exports:
        - "OperatorUiArtifactStatus"
        - "OperatorUiArtifact"
        - "operator_wui_artifacts"
    - path: "whilly/api/static/whilly-hotkeys.js"
      provides: "Static WUI hotkey implementation matching five canonical surfaces and supported worker routes"
      contains: "/api/v1/admin/workers/"
    - path: "tests/unit/test_wui_contract_static.py"
      provides: "Static WUI artifact classification and stale-pattern regression tests"
      contains: "BANNED_ACTIVE_WUI_REGEXES"
    - path: "tests/integration/test_htmx_dashboard.py"
      provides: "Rendered WUI stale-pattern and contract-derived surface assertions"
      contains: "test_dashboard_mirrors_operator_surfaces_and_hotkeys"
  key_links:
    - from: "tests/unit/test_wui_contract_static.py"
      to: "whilly/operator_views.py"
      via: "loads operator_wui_artifacts and scans only active artifact paths"
      pattern: "operator_wui_artifacts\\("
    - from: "tests/unit/test_wui_contract_static.py"
      to: "whilly/operator_views.py"
      via: "derives static JS expectations from operator_surface_items, operator_wui_selectors, and operator_wui_route_prefixes"
      pattern: "operator_surface_items\\(\\)|operator_wui_selectors\\(\\)|operator_wui_route_prefixes\\("
    - from: "tests/unit/test_wui_contract_static.py"
      to: "whilly/api/static/whilly-hotkeys.js"
      via: "active artifact stale-pattern scan"
      pattern: "whilly/api/static/whilly-hotkeys\\.js"
    - from: "tests/integration/test_htmx_dashboard.py"
      to: "whilly/operator_views.py"
      via: "derives expected rendered WUI surfaces and hotkey copy from contract helpers"
      pattern: "operator_surface_items\\(\\)"
---

<objective>
Add WUI artifact classification and static/rendered stale-pattern guards.

Purpose: satisfy UI-02 by making orphan or stale WUI files test-visible before later WUI wiring phases.
Output: artifact metadata, static tests, a fixed active `whilly-hotkeys.js`, and rendered WUI assertions against stale contract drift.
</objective>

<execution_context>
@/Users/mshegolev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mshegolev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-canonical-ui-parity-contract/13-CONTEXT.md
@.planning/phases/13-canonical-ui-parity-contract/13-RESEARCH.md
@.planning/phases/13-canonical-ui-parity-contract/13-VALIDATION.md
@.planning/phases/13-canonical-ui-parity-contract/13-01-PLAN.md
@whilly/operator_views.py
@whilly/api/static/whilly-hotkeys.js
@whilly/api/templates/index.html.j2
@whilly/api/templates/_tasks_table.html
@whilly/api/templates/_workers_table.html
@whilly/api/templates/_logs.html
@whilly/api/templates/_admin.html
@whilly/api/templates/_prd.html
@tests/integration/test_htmx_dashboard.py

<interfaces>
Known stale patterns that must fail in active WUI files:

```text
1-7
/^[1-7]$/
.tabs [data-key]
regex: (?<!/api/v1)/admin/workers/
```

Canonical current values:

```text
surfaces: overview, compliance, plans_tasks, workers, events
surface selector: [data-surface-tab]
worker API prefix: /api/v1/admin/workers/
surface hotkey regex: /^[1-5]$/
```

WUI artifact classification target:

```text
active:
  whilly/api/templates/index.html.j2
  whilly/api/templates/_tasks_table.html
  whilly/api/templates/_workers_table.html
  whilly/api/static/whilly-hotkeys.js
routeable_noncanonical:
  whilly/api/templates/_logs.html
inactive_quarantined:
  whilly/api/templates/_admin.html
  whilly/api/templates/_prd.html
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WUI artifact classification and static guards</name>
  <read_first>
    @whilly/operator_views.py
    @whilly/api/templates/index.html.j2
    @whilly/api/templates/_tasks_table.html
    @whilly/api/templates/_workers_table.html
    @whilly/api/templates/_logs.html
    @whilly/api/templates/_admin.html
    @whilly/api/templates/_prd.html
    @whilly/api/static/whilly-hotkeys.js
    @.planning/phases/13-canonical-ui-parity-contract/13-CONTEXT.md
    @.planning/phases/13-canonical-ui-parity-contract/13-RESEARCH.md
  </read_first>
  <files>whilly/operator_views.py, tests/unit/test_wui_contract_static.py</files>
  <behavior>
    - Every WUI template or static JavaScript artifact is listed in the operator UI artifact contract.
    - Active artifacts are initially `index.html.j2`, `_tasks_table.html`, and `_workers_table.html`.
    - `_logs.html` is `routeable_noncanonical` with reason `Routeable by ?fragment=logs but not in canonical nav/TUI parity yet.` and followup phase `14`.
    - `_admin.html` is `inactive_quarantined` with reason `Contains admin controls with unsupported /admin/* routes.` and followup phase `14`.
    - `_prd.html` is `inactive_quarantined` with reason `Contains PRD controls with unsupported /prd/* routes.` and followup phase `14`.
    - `whilly-hotkeys.js` is initially `inactive_quarantined` with reason `Static hotkey file still contains pre-contract selectors/routes; Task 2 fixes it before Phase 13 completes.` and followup phase `13`.
    - Static tests scan active artifacts only and reject `1-7`, `/^[1-7]$/`, `.tabs [data-key]`, and stale non-API worker routes matching `(?<!/api/v1)/admin/workers/`.
  </behavior>
  <action>
    Create `tests/unit/test_wui_contract_static.py` first. Add:
    - `import re`
    - `BANNED_ACTIVE_WUI_PATTERNS = ("1-7", "/^[1-7]$/", ".tabs [data-key]")`
    - `BANNED_ACTIVE_WUI_REGEXES = (r"(?<!/api/v1)/admin/workers/",)`
    - `test_wui_artifacts_are_classified` discovering actual WUI artifacts from the repository instead of using a hardcoded expected set:
      - Set `project_root = Path(__file__).resolve().parents[2]`.
      - Discover template artifacts from files directly under `project_root / "whilly/api/templates"`; convert each file to `path.relative_to(project_root).as_posix()`.
      - Discover static JavaScript artifacts from `(project_root / "whilly/api/static").glob("*.js")`; convert each file to `path.relative_to(project_root).as_posix()`.
      - Do not include CSS, fonts, images, or other static assets; Phase 13 classifies templates and static JavaScript only.
      - Compare the discovered set to `{artifact.path for artifact in operator_wui_artifacts()}`.
      - Build `artifacts_by_path = {artifact.path: artifact for artifact in operator_wui_artifacts()}` and keep explicit status assertions for:
        - `whilly/api/templates/_logs.html` is `OperatorUiArtifactStatus.ROUTEABLE_NONCANONICAL`, reason `Routeable by ?fragment=logs but not in canonical nav/TUI parity yet.`, followup phase `14`.
        - `whilly/api/templates/_admin.html` is `OperatorUiArtifactStatus.INACTIVE_QUARANTINED`, reason `Contains admin controls with unsupported /admin/* routes.`, followup phase `14`.
        - `whilly/api/templates/_prd.html` is `OperatorUiArtifactStatus.INACTIVE_QUARANTINED`, reason `Contains PRD controls with unsupported /prd/* routes.`, followup phase `14`.
        - `whilly/api/static/whilly-hotkeys.js` is initially `OperatorUiArtifactStatus.INACTIVE_QUARANTINED`, reason `Static hotkey file still contains pre-contract selectors/routes; Task 2 fixes it before Phase 13 completes.`, followup phase `13`.
    - `test_non_active_wui_artifacts_have_reason_and_followup` asserting every non-active artifact has non-empty `reason` and `followup_phase`.
    - `test_active_wui_artifacts_reject_stale_patterns` reading each active file from `Path(__file__).resolve().parents[2]`, asserting none of the banned literal strings are present, and asserting `re.search(regex, text) is None` for each regex in `BANNED_ACTIVE_WUI_REGEXES`.

    Extend `whilly/operator_views.py` with:

    ```python
    class OperatorUiArtifactStatus(str, Enum):
        ACTIVE = "active"
        ROUTEABLE_NONCANONICAL = "routeable_noncanonical"
        INACTIVE_QUARANTINED = "inactive_quarantined"

    @dataclass(frozen=True)
    class OperatorUiArtifact:
        path: str
        status: OperatorUiArtifactStatus
        reason: str = ""
        followup_phase: str = ""
    ```

    Add `OPERATOR_WUI_ARTIFACTS: Final[tuple[OperatorUiArtifact, ...]]` with these exact entries for Task 1:
    - `OperatorUiArtifact("whilly/api/templates/index.html.j2", OperatorUiArtifactStatus.ACTIVE)`
    - `OperatorUiArtifact("whilly/api/templates/_tasks_table.html", OperatorUiArtifactStatus.ACTIVE)`
    - `OperatorUiArtifact("whilly/api/templates/_workers_table.html", OperatorUiArtifactStatus.ACTIVE)`
    - `OperatorUiArtifact("whilly/api/templates/_logs.html", OperatorUiArtifactStatus.ROUTEABLE_NONCANONICAL, reason="Routeable by ?fragment=logs but not in canonical nav/TUI parity yet.", followup_phase="14")`
    - `OperatorUiArtifact("whilly/api/templates/_admin.html", OperatorUiArtifactStatus.INACTIVE_QUARANTINED, reason="Contains admin controls with unsupported /admin/* routes.", followup_phase="14")`
    - `OperatorUiArtifact("whilly/api/templates/_prd.html", OperatorUiArtifactStatus.INACTIVE_QUARANTINED, reason="Contains PRD controls with unsupported /prd/* routes.", followup_phase="14")`
    - `OperatorUiArtifact("whilly/api/static/whilly-hotkeys.js", OperatorUiArtifactStatus.INACTIVE_QUARANTINED, reason="Static hotkey file still contains pre-contract selectors/routes; Task 2 fixes it before Phase 13 completes.", followup_phase="13")`

    Add helper:

    ```python
    def operator_wui_artifacts(
        status: OperatorUiArtifactStatus | str | None = None,
    ) -> tuple[OperatorUiArtifact, ...]:
        if status is None:
            return OPERATOR_WUI_ARTIFACTS
        artifact_status = status if isinstance(status, OperatorUiArtifactStatus) else OperatorUiArtifactStatus(status)
        return tuple(artifact for artifact in OPERATOR_WUI_ARTIFACTS if artifact.status is artifact_status)
    ```

    Add `OperatorUiArtifactStatus`, `OperatorUiArtifact`, `OPERATOR_WUI_ARTIFACTS`, and `operator_wui_artifacts` to `__all__`.
  </action>
  <acceptance_criteria>
    - `tests/unit/test_wui_contract_static.py` contains `BANNED_ACTIVE_WUI_PATTERNS`.
    - `tests/unit/test_wui_contract_static.py` contains `BANNED_ACTIVE_WUI_REGEXES`.
    - `tests/unit/test_wui_contract_static.py` contains `(?<!/api/v1)/admin/workers/`.
    - `tests/unit/test_wui_contract_static.py` contains `test_wui_artifacts_are_classified`.
    - `tests/unit/test_wui_contract_static.py` contains `operator_wui_artifacts`.
    - `tests/unit/test_wui_contract_static.py` contains `.glob("*.js")`.
    - `tests/unit/test_wui_contract_static.py` contains `OperatorUiArtifactStatus.ROUTEABLE_NONCANONICAL`.
    - `tests/unit/test_wui_contract_static.py` contains `test_active_wui_artifacts_reject_stale_patterns`.
    - `whilly/operator_views.py` contains `class OperatorUiArtifactStatus(str, Enum):`.
    - `whilly/operator_views.py` contains `OperatorUiArtifact("whilly/api/templates/_logs.html", OperatorUiArtifactStatus.ROUTEABLE_NONCANONICAL`.
    - `whilly/operator_views.py` contains `OperatorUiArtifact("whilly/api/static/whilly-hotkeys.js", OperatorUiArtifactStatus.INACTIVE_QUARANTINED`.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_operator_ui_contract.py tests/unit/test_wui_contract_static.py</automated>
  </verify>
  <done>WUI artifact metadata exists, every template/static-JS artifact is classified, and static tests reject stale patterns in active artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix and activate static WUI hotkeys</name>
  <read_first>
    @whilly/operator_views.py
    @whilly/api/static/whilly-hotkeys.js
    @whilly/api/templates/index.html.j2
    @tests/unit/test_wui_contract_static.py
    @.planning/phases/13-canonical-ui-parity-contract/13-CONTEXT.md
  </read_first>
  <files>whilly/operator_views.py, whilly/api/static/whilly-hotkeys.js, tests/unit/test_wui_contract_static.py</files>
  <behavior>
    - `test_static_hotkeys_file_uses_current_contract` imports `operator_surface_items`, `operator_wui_selectors`, and `operator_wui_route_prefixes` from `whilly.operator_views`.
    - The test derives the expected surface order from `operator_surface_items()` and asserts `whilly/api/static/whilly-hotkeys.js` contains that order as a JavaScript array.
    - The test derives the expected surface hotkey regex from the number of `operator_surface_items()` entries and asserts `whilly/api/static/whilly-hotkeys.js` contains it.
    - The test derives selector expectations from `operator_wui_selectors()` and asserts `whilly/api/static/whilly-hotkeys.js` uses the current surface tab selector, filter selector, and review actionable row selector.
    - The test derives worker route expectations from `operator_wui_route_prefixes()["worker_control"]` and asserts `whilly/api/static/whilly-hotkeys.js` posts pause/resume through that prefix.
    - The test asserts `whilly/api/static/whilly-hotkeys.js` no longer contains any `BANNED_ACTIVE_WUI_PATTERNS` and no longer matches any `BANNED_ACTIVE_WUI_REGEXES`.
    - `whilly-hotkeys.js` artifact status changes to `active`.
  </behavior>
  <action>
    Update `tests/unit/test_wui_contract_static.py` first:
    - Change the expected status for `whilly/api/static/whilly-hotkeys.js` to `OperatorUiArtifactStatus.ACTIVE`.
    - Add `test_static_hotkeys_file_uses_current_contract` importing `json`, `re`, `operator_surface_items`, `operator_wui_selectors`, and `operator_wui_route_prefixes`.
    - In that test, read `whilly/api/static/whilly-hotkeys.js` from `Path(__file__).resolve().parents[2]`.
    - Derive `surface_values = [surface.value for surface, _label in operator_surface_items()]` and assert `json.dumps(surface_values)` is present in the JavaScript.
    - Derive `surface_key_regex = f"/^[1-{len(surface_values)}]$/"` and assert it is present in the JavaScript.
    - Derive `selectors = operator_wui_selectors()` and assert:
      - `selectors["surface_tab"].removesuffix("]") + "=\""` is present in the JavaScript.
      - `selectors["filter"]` is present in the JavaScript.
      - `selectors["review_actionable_row"]` is present in the JavaScript.
    - Derive `worker_prefix = operator_wui_route_prefixes()["worker_control"]` and assert both `f'{worker_prefix}pause'` and `f'{worker_prefix}resume'` are present in the JavaScript.
    - Assert every value in `BANNED_ACTIVE_WUI_PATTERNS` is absent from the JavaScript.
    - Assert `re.search(regex, javascript_text) is None` for every regex in `BANNED_ACTIVE_WUI_REGEXES`.
    - Keep `test_active_wui_artifacts_reject_stale_patterns` scanning active artifacts; after this task it must scan `whilly-hotkeys.js`.

    Update `whilly/api/static/whilly-hotkeys.js` to the current active dashboard contract:
    - Header comment must say `1-5 switch surface tabs (reads [data-surface-tab])`.
    - Define `const surfaceOrder = ["overview", "compliance", "plans_tasks", "workers", "events"];`.
    - Use `/^[1-5]$/.test(k)` for surface switching.
    - Resolve a tab with `document.querySelector('[data-surface-tab="' + surface + '"]')`, where `surface = surfaceOrder[Number(k) - 1]`.
    - Focus filter with `document.querySelector("#dashboard-filter")`.
    - Select review rows from `#review-gaps tbody tr[data-review-actionable="true"]`.
    - Store row selection with class `review-row-selected` and `aria-selected`.
    - Dispatch review decisions with detail `{ task_id: row.dataset.reviewTask || "", decision: decision, stage_id: row.dataset.reviewStage || "" }`.
    - Pause workers with `fetch("/api/v1/admin/workers/pause", { method: "POST", headers, body: JSON.stringify({}) })`.
    - Resume workers with `fetch("/api/v1/admin/workers/resume", { method: "POST", headers })`.
    - Build `headers` from `#dashboard-admin-token`: include `"Content-Type": "application/json"` always and include `Authorization: "Bearer " + token` only when the trimmed token is non-empty.
    - Refresh with `window.htmx && htmx.ajax("GET", "/", { target: "body", swap: "outerHTML" });`.
    - Quit with `document.body.dataset.dashboardState = "stopped";` and `window.htmx && htmx.trigger(document.body, "whilly:liveStop");`.
    - Do not keep the theme-cycle `t` hotkey in this file; it is outside the Phase 13 canonical shared action list.

    Update the `OPERATOR_WUI_ARTIFACTS` entry for `whilly/api/static/whilly-hotkeys.js` in `whilly/operator_views.py` to:

    ```python
    OperatorUiArtifact("whilly/api/static/whilly-hotkeys.js", OperatorUiArtifactStatus.ACTIVE)
    ```
  </action>
  <acceptance_criteria>
    - `whilly/api/static/whilly-hotkeys.js` contains `/^[1-5]$/`.
    - `whilly/api/static/whilly-hotkeys.js` contains `/api/v1/admin/workers/pause`.
    - `whilly/api/static/whilly-hotkeys.js` contains `/api/v1/admin/workers/resume`.
    - `whilly/api/static/whilly-hotkeys.js` contains `#dashboard-filter`.
    - `whilly/api/static/whilly-hotkeys.js` does not contain `1-7`.
    - `whilly/api/static/whilly-hotkeys.js` does not contain `.tabs [data-key]`.
    - `whilly/api/static/whilly-hotkeys.js` does not match stale route regex `(?<!/api/v1)/admin/workers/`.
    - `tests/unit/test_wui_contract_static.py` contains `operator_surface_items`.
    - `tests/unit/test_wui_contract_static.py` contains `operator_wui_selectors`.
    - `tests/unit/test_wui_contract_static.py` contains `operator_wui_route_prefixes`.
    - `tests/unit/test_wui_contract_static.py` contains `json.dumps(surface_values)`.
    - `whilly/operator_views.py` contains `OperatorUiArtifact("whilly/api/static/whilly-hotkeys.js", OperatorUiArtifactStatus.ACTIVE)`.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_wui_contract_static.py</automated>
  </verify>
  <done>The served static hotkey file is no longer stale and is classified as active under the contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add rendered WUI stale-pattern regression checks</name>
  <read_first>
    @whilly/operator_views.py
    @whilly/api/dashboard.py
    @whilly/api/templates/index.html.j2
    @whilly/api/templates/_logs.html
    @tests/integration/test_htmx_dashboard.py
    @tests/unit/test_wui_contract_static.py
    @.planning/phases/13-canonical-ui-parity-contract/13-VALIDATION.md
  </read_first>
  <files>tests/integration/test_htmx_dashboard.py, tests/unit/test_wui_contract_static.py</files>
  <behavior>
    - Rendered `/` WUI body does not contain `1-7`.
    - Rendered `/` WUI body does not contain `/^[1-7]$/`.
    - Rendered `/` WUI body does not contain `.tabs [data-key]`.
    - Rendered `/` WUI body does not match stale route regex `(?<!/api/v1)/admin/workers/`.
    - Rendered `/` WUI body contains all five canonical `data-surface-tab` values.
    - `/?fragment=logs` remains routeable and `_logs.html` is classified as `routeable_noncanonical`.
  </behavior>
  <action>
    In `tests/integration/test_htmx_dashboard.py`, update or add rendered WUI assertions:
    - Import `re`.
    - In `test_dashboard_mirrors_operator_surfaces_and_hotkeys`, loop over the active stale literal pattern tuple `("1-7", "/^[1-7]$/", ".tabs [data-key]")` and assert each pattern is absent from `response.text`.
    - In the same test, loop over rendered stale route regexes `(r"(?<!/api/v1)/admin/workers/",)` and assert `re.search(regex, response.text) is None`.
    - In the same test, derive expected surface values from `operator_surface_items()` and assert each rendered tab contains `data-surface-tab="{surface.value}"`.
    - In the same test, assert `operator_surface_hotkey_help()` is present and equals `1-5=switch`.
    - In the same test, assert `operator_wui_route_prefixes()["worker_control"]` is present and `operator_wui_route_prefixes()["task_human_review"]` is present.

    Add an async integration test named `test_logs_fragment_is_routeable_noncanonical_artifact`:
    - Import `OperatorUiArtifactStatus` and `operator_wui_artifacts`.
    - Find the artifact with path `whilly/api/templates/_logs.html`.
    - Assert its status is `OperatorUiArtifactStatus.ROUTEABLE_NONCANONICAL`.
    - `await client.get("/?fragment=logs")`.
    - Assert response status is `200`.
    - Assert response text contains `role="log"`.
    - Assert response text does not contain `<!DOCTYPE html>`.

    In `tests/unit/test_wui_contract_static.py`, keep `BANNED_ACTIVE_WUI_PATTERNS` and `BANNED_ACTIVE_WUI_REGEXES` as the static-test sources for exact banned literal and regex checks. Do not broaden the scan to `.planning/`, `docs/`, or `tests/`; stale patterns are intentionally mentioned there.
  </action>
  <acceptance_criteria>
    - `tests/integration/test_htmx_dashboard.py` contains `test_logs_fragment_is_routeable_noncanonical_artifact`.
    - `tests/integration/test_htmx_dashboard.py` contains `OperatorUiArtifactStatus.ROUTEABLE_NONCANONICAL`.
    - `tests/integration/test_htmx_dashboard.py` contains `(?<!/api/v1)/admin/workers/`.
    - `tests/integration/test_htmx_dashboard.py` contains `re.search`.
    - `tests/integration/test_htmx_dashboard.py` contains `".tabs [data-key]"`.
    - The rendered dashboard stale-pattern assertions run inside `test_dashboard_mirrors_operator_surfaces_and_hotkeys`.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/unit/test_wui_contract_static.py tests/integration/test_htmx_dashboard.py::test_dashboard_mirrors_operator_surfaces_and_hotkeys tests/integration/test_htmx_dashboard.py::test_logs_fragment_is_routeable_noncanonical_artifact tests/integration/test_control_state_admin_api.py::test_admin_can_pause_resume_and_read_control_state</automated>
  </verify>
  <done>Static and rendered WUI tests fail on stale active UI patterns, and the routeable logs fragment has an explicit contract status.</done>
</task>

</tasks>

<verification>
Run the focused Phase 13 UI-02 suite:

```bash
.venv/bin/python -m pytest -q tests/unit/test_operator_ui_contract.py tests/unit/test_wui_contract_static.py tests/integration/test_htmx_dashboard.py::test_dashboard_mirrors_operator_surfaces_and_hotkeys tests/integration/test_htmx_dashboard.py::test_logs_fragment_is_routeable_noncanonical_artifact tests/integration/test_control_state_admin_api.py::test_admin_can_pause_resume_and_read_control_state
```

Run the full focused validation suite from `13-VALIDATION.md`:

```bash
.venv/bin/python -m pytest -q tests/unit/test_operator_views.py tests/unit/test_tui.py tests/unit/test_wui_contract_static.py tests/integration/test_htmx_dashboard.py tests/integration/test_control_state_admin_api.py
```

Run lint for touched Python files:

```bash
.venv/bin/python -m ruff check whilly/operator_views.py whilly/cli/tui.py whilly/api/dashboard.py tests/unit/test_operator_ui_contract.py tests/unit/test_wui_contract_static.py tests/unit/test_tui.py tests/integration/test_htmx_dashboard.py
```
</verification>

<success_criteria>
- UI-02 appears in this plan frontmatter.
- Every WUI template and static JavaScript artifact is classified in `OPERATOR_WUI_ARTIFACTS`.
- Non-active WUI artifacts have non-empty reason and follow-up phase values.
- `whilly/api/static/whilly-hotkeys.js` is fixed to five canonical surfaces, `[data-surface-tab]`, `#dashboard-filter`, and `/api/v1/admin/workers/*`.
- Static tests fail if active WUI files contain `1-7`, `/^[1-7]$/`, `.tabs [data-key]`, or match stale route regex `(?<!/api/v1)/admin/workers/`.
- Rendered WUI tests fail if the active dashboard regresses to stale surface selectors, stale route prefixes, or non-canonical surface counts.
</success_criteria>

<output>
After completion, create `.planning/phases/13-canonical-ui-parity-contract/13-02-SUMMARY.md`.
</output>
