---
phase: 13.2-github-feedback-issue-reporter
plan: 01
type: execute
wave: 1
files_modified:
  - whilly/feedback.py
  - whilly/cli/feedback.py
  - whilly/cli/__init__.py
  - tests/unit/test_feedback.py
  - tests/unit/test_feedback_cli.py
  - tests/unit/test_cli_operator_commands.py
requirements:
  - FEED-01
  - FEED-02
  - FEED-03
must_haves:
  truths:
    - "Operator can create a GitHub issue from Whilly with bug or idea kind."
    - "Feedback body includes Whilly/runtime context and redacts known secret patterns."
    - "Dry-run prints the GitHub CLI command without creating an issue."
---

<objective>
Implement the fastest support channel: `whilly feedback` creates GitHub Issues through `gh`.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add feedback service</name>
  <files>tests/unit/test_feedback.py, whilly/feedback.py</files>
  <behavior>
    - Build a markdown report with kind, title, command, details, and environment.
    - Redact known secrets from details.
    - Create GitHub issues through `gh issue create`.
    - Support dry-run without subprocess execution.
  </behavior>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add feedback CLI</name>
  <files>tests/unit/test_feedback_cli.py, tests/unit/test_cli_operator_commands.py, whilly/cli/feedback.py, whilly/cli/__init__.py</files>
  <behavior>
    - Add `whilly feedback --kind bug|idea --title ... --body ...`.
    - Add `--body-file`, `--repo`, `--label`, `--command`, and `--dry-run`.
    - Wire the command through the lazy top-level dispatcher.
  </behavior>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify and close planning traceability</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <behavior>
    - Mark FEED-01 through FEED-03 complete.
    - Mark Phase 13.2 complete and route next work back to Phase 14.
  </behavior>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest -q tests/unit/test_feedback.py tests/unit/test_feedback_cli.py tests/unit/test_cli_operator_commands.py`
- `.venv/bin/python -m ruff check whilly/feedback.py whilly/cli/feedback.py whilly/cli/__init__.py tests/unit/test_feedback.py tests/unit/test_feedback_cli.py tests/unit/test_cli_operator_commands.py`
</verification>
