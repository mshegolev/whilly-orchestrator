---
phase: 22-orchestration-cluster
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/plan-json-contract/spec.md
  - openspec/specs/result-collection/spec.md
autonomous: true
requirements: [ORCH-03, ORCH-07]
must_haves:
  truths:
    - "openspec/specs/plan-json-contract/spec.md exists and passes openspec validate plan-json-contract --strict"
    - "openspec/specs/result-collection/spec.md exists and passes openspec validate result-collection --strict"
    - "plan-json-contract enumerates the required task fields and the {project, prd_file, tasks[]} envelope matching task_manager.py"
    - "plan-json-contract specifies atomic writes, round-trip drop-extra-keys tolerance, and that validate_schema checks only the first 3 tasks"
    - "result-collection specifies AgentResult(usage, exit_code, is_complete) parsing and the <promise>COMPLETE</promise> completion signal"
  artifacts:
    - path: "openspec/specs/plan-json-contract/spec.md"
      provides: "Normative spec of the plan JSON contract (ORCH-03)"
      contains: "## Requirements"
    - path: "openspec/specs/result-collection/spec.md"
      provides: "Normative spec of agent result collection (ORCH-07)"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/plan-json-contract/spec.md"
      to: "whilly/task_manager.py (Task.from_dict/to_dict, validate_schema)"
      via: "reverse-spec'd field + envelope requirements"
      pattern: "SHALL|MUST"
    - from: "openspec/specs/result-collection/spec.md"
      to: "whilly/adapters/runner/result_parser.py (AgentResult)"
      via: "reverse-spec'd parsing + completion-signal requirements"
      pattern: "SHALL|MUST"
---

<objective>
Author two independent normative OpenSpec specs: `plan-json-contract` (ORCH-03) and
`result-collection` (ORCH-07), each reverse-spec'd from the real code.

Purpose: Make the plan-on-disk contract and the agent-result parsing contract explicit and machine-checkable.
Output: Two strict-valid spec.md files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-orchestration-cluster/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write plan-json-contract spec.md (ORCH-03)</name>
  <files>openspec/specs/plan-json-contract/spec.md</files>
  <read_first>
    - whilly/task_manager.py (Task.from_dict / Task.to_dict round-trip, validate_schema, field set)
    - whilly/adapters/filesystem/plan_io.py (plan load/save, atomic write behavior)
    - openspec/AUTHORING.md (format + strict-validation rules — MUST follow exactly)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar — copy its structure)
  </read_first>
  <action>
    READ the source modules above first; ground every requirement in observed behavior. Write
    `openspec/specs/plan-json-contract/spec.md` for the slug `plan-json-contract`, mirroring the
    exemplar: `## Purpose` (plain prose, >=50 chars), then `## Requirements` with
    `### Requirement: <name>` blocks. Every requirement's first body line contains SHALL or MUST;
    every requirement has >=1 `#### Scenario:` (4 hashtags) with `- **WHEN**` / `- **THEN**`.
    Capture: the required task fields recognized by Task.from_dict/to_dict — `id`, `status`,
    `dependencies`, `key_files`, `priority` (critical|high|medium|low), `description`,
    `acceptance_criteria`, `test_steps`; the plan envelope `{project, prd_file, tasks[]}`; atomic
    writes (write-temp-then-replace so a crash never leaves a half-written plan); round-trip
    tolerance (extra on-disk keys are tolerated on read but DROPPED on to_dict round-trip); and
    that `validate_schema` validates only the FIRST 3 tasks (so partial plans can be silently
    accepted past task 3). Keep `status` value detail out — reference `task-model-fsm` for legal
    statuses rather than re-listing them. Keep each requirement body under 500 chars.
  </action>
  <verify>
    <automated>cd /opt/develop/whilly-orchestrator && PATH="$HOME/.reflex/.nvm/versions/node/v20.19.6/bin:$PATH" openspec validate plan-json-contract --strict</automated>
  </verify>
  <acceptance_criteria>
    - The file openspec/specs/plan-json-contract/spec.md exists and is non-empty.
    - `openspec validate plan-json-contract --strict` prints "is valid" and exits 0 (zero errors/warnings).
    - The spec contains `## Purpose` (>=50 chars) and `## Requirements`; every requirement has SHALL/MUST on the first body line and >=1 `#### Scenario:`.
    - A requirement enumerates the required task fields (id, status, dependencies, key_files, priority, description, acceptance_criteria, test_steps) and the {project, prd_file, tasks[]} envelope.
    - A requirement captures atomic writes, round-trip drop-extra-keys tolerance, and the validate_schema-checks-first-3-tasks behavior.
  </acceptance_criteria>
  <done>plan-json-contract/spec.md authored, normative, passing strict validation.</done>
</task>

<task type="auto">
  <name>Task 2: Write result-collection spec.md (ORCH-07)</name>
  <files>openspec/specs/result-collection/spec.md</files>
  <read_first>
    - whilly/adapters/runner/result_parser.py (AgentResult dataclass + parsing from Claude CLI JSON)
    - whilly/agent_runner.py (collect_result / collect_result_from_file call sites for context)
    - openspec/AUTHORING.md (format + strict-validation rules — MUST follow exactly)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar — copy its structure)
  </read_first>
  <action>
    READ the source modules above first; ground every requirement in observed behavior. Write
    `openspec/specs/result-collection/spec.md` for the slug `result-collection`, mirroring the
    exemplar structure. Every requirement's first body line contains SHALL or MUST; every
    requirement has >=1 `#### Scenario:` with `- **WHEN**` / `- **THEN**`.
    Capture: parsing of the Claude CLI JSON output into an `AgentResult` carrying `usage`,
    `exit_code`, and `is_complete`; how the `<promise>COMPLETE</promise>` token in the agent
    result text sets the completion signal (marks the task done); and the parser's handling of
    malformed / missing JSON (what is returned when output cannot be parsed — ground this in the
    actual code, do not invent). DO NOT specify retry/backoff or the FSM transition to `done`
    here — reference task-model-fsm and recovery-self-healing (Phase 27) for those; this spec
    owns only the parse-and-detect-completion contract. Keep each requirement body under 500 chars.
  </action>
  <verify>
    <automated>cd /opt/develop/whilly-orchestrator && PATH="$HOME/.reflex/.nvm/versions/node/v20.19.6/bin:$PATH" openspec validate result-collection --strict</automated>
  </verify>
  <acceptance_criteria>
    - The file openspec/specs/result-collection/spec.md exists and is non-empty.
    - `openspec validate result-collection --strict` prints "is valid" and exits 0 (zero errors/warnings).
    - The spec contains `## Purpose` (>=50 chars) and `## Requirements`; every requirement has SHALL/MUST on the first body line and >=1 `#### Scenario:`.
    - A requirement specifies AgentResult parsing with usage, exit_code, and is_complete.
    - A requirement specifies the <promise>COMPLETE</promise> completion signal.
  </acceptance_criteria>
  <done>result-collection/spec.md authored, normative, passing strict validation.</done>
</task>

</tasks>

<verification>
Run `openspec validate plan-json-contract --strict` and `openspec validate result-collection --strict`
— both must report 0 errors and 0 warnings. Confirm neither file contains delta headers.
</verification>

<success_criteria>
ORCH-03 and ORCH-07 satisfied: both specs are normative, testable, grounded in real code, and pass strict validation.
</success_criteria>

<output>
Create `.planning/phases/22-orchestration-cluster/22-02-SUMMARY.md` when done.
</output>
