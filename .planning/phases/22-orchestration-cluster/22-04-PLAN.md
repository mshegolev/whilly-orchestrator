---
phase: 22-orchestration-cluster
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/batch-planning/spec.md
  - openspec/specs/worktree-isolation/spec.md
autonomous: true
requirements: [ORCH-04, ORCH-06]
must_haves:
  truths:
    - "openspec/specs/batch-planning/spec.md exists and passes openspec validate batch-planning --strict"
    - "openspec/specs/worktree-isolation/spec.md exists and passes openspec validate worktree-isolation --strict"
    - "batch-planning specifies non-overlapping key_files grouping via plan_batches (tasks sharing a key_file cannot share a batch), the max_parallel<=1 single-task-per-batch path, and plan_batches_llm as the LLM variant that falls back to plan_batches on any error"
    - "worktree-isolation specifies the v4 plan workspace RepoTargetWorkspaceResolver (.whilly_workspaces/repos/<repo>/<plan>/<task>, with repo-target-less tasks reusing the process cwd) and the per-task WorktreeManager .whilly_worktrees/{task_id} create -> merge_back (cherry-pick) -> cleanup lifecycle"
  artifacts:
    - path: "openspec/specs/batch-planning/spec.md"
      provides: "Normative spec of batch planning (ORCH-04)"
      contains: "## Requirements"
    - path: "openspec/specs/worktree-isolation/spec.md"
      provides: "Normative spec of workspace + worktree isolation (ORCH-06)"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/batch-planning/spec.md"
      to: "whilly/orchestrator.py (plan_batches, plan_batches_llm)"
      via: "reverse-spec'd grouping requirements"
      pattern: "SHALL|MUST"
    - from: "openspec/specs/worktree-isolation/spec.md"
      to: "whilly/workspaces.py (RepoTargetWorkspaceResolver), whilly/worktree_runner.py (WorktreeManager)"
      via: "reverse-spec'd workspace + worktree lifecycle requirements"
      pattern: "SHALL|MUST"
---

<objective>
Author two independent normative OpenSpec specs: `batch-planning` (ORCH-04) and
`worktree-isolation` (ORCH-06), each reverse-spec'd from the real v4 code.

Purpose: Make the batching rule and the workspace/worktree isolation contracts explicit and machine-checkable.
Output: Two strict-valid spec.md files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-orchestration-cluster/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write batch-planning spec.md (ORCH-04)</name>
  <files>openspec/specs/batch-planning/spec.md</files>
  <read_first>
    - whilly/orchestrator.py (plan_batches — key_files-overlap grouping, the max_parallel<=1 one-task-per-batch early return, the max_parallel batch-size cap, tasks with empty key_files always batchable; plan_batches_llm — LLM-driven variant that validates returned ids and falls back to plan_batches() on exit!=0 / parse error / non-list)
    - openspec/AUTHORING.md (format + strict-validation rules — MUST follow exactly)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar — copy its structure)
  </read_first>
  <action>
    READ whilly/orchestrator.py FIRST and reverse-spec the OBSERVED behavior of `plan_batches` and
    `plan_batches_llm`. These are pure grouping helpers — spec what they GUARANTEE about the
    batches they return; do NOT couple the spec to a v3 "first batch then re-evaluate the loop"
    dispatch mechanic (the v4 run path claims one task at a time and is owned by the
    orchestration-loop capability — reference it, do not re-specify a batch-dispatch iteration here).
    Write `openspec/specs/batch-planning/spec.md` for the slug `batch-planning`, mirroring the
    exemplar: `## Purpose` (plain prose, >=50 chars), then `## Requirements` with
    `### Requirement: <name>` blocks; first body line contains SHALL or MUST; each requirement has
    >=1 `#### Scenario:` (4 hashtags) with `- **WHEN**` / `- **THEN**`.
    Capture: (1) `plan_batches` groups ready tasks into batches by NON-OVERLAPPING `key_files` —
    two tasks that share a key_file are never placed in the same batch; a task with empty key_files
    never blocks a batch. (2) when `max_parallel <= 1`, `plan_batches` returns one task per batch
    (single-task batching). (3) a batch never exceeds `max_parallel` tasks. (4) `plan_batches_llm`
    is the optional LLM-driven grouping variant; it validates the ids the LLM returns against the
    ready set and FALLS BACK to `plan_batches` on any error (non-zero agent exit, unparseable
    output, or a non-list response), so it can never produce a worse-than-deterministic result.
    Reference the orchestration-loop capability for how/whether batches are consumed by the run
    path rather than re-specifying it. Keep each requirement body under 500 chars. No fenced code
    blocks in the spec body.
  </action>
  <verify>
    <automated>cd /opt/develop/whilly-orchestrator && PATH="$HOME/.reflex/.nvm/versions/node/v20.19.6/bin:$PATH" openspec validate batch-planning --strict</automated>
  </verify>
  <acceptance_criteria>
    - The file openspec/specs/batch-planning/spec.md exists and is non-empty.
    - `openspec validate batch-planning --strict` prints "is valid" and exits 0 (zero errors/warnings).
    - The spec contains `## Purpose` (>=50 chars) and `## Requirements`; every requirement has SHALL/MUST on the first body line and >=1 `#### Scenario:`.
    - A requirement specifies non-overlapping key_files grouping (shared key_file => not co-batched; empty key_files => never blocks).
    - A requirement specifies the max_parallel<=1 single-task-per-batch behavior and the per-batch max_parallel cap.
    - A requirement specifies plan_batches_llm as the LLM variant that falls back to plan_batches on error.
  </acceptance_criteria>
  <done>batch-planning/spec.md authored, normative, grounded in plan_batches/plan_batches_llm, passing strict validation.</done>
</task>

<task type="auto">
  <name>Task 2: Write worktree-isolation spec.md (ORCH-06)</name>
  <files>openspec/specs/worktree-isolation/spec.md</files>
  <read_first>
    - whilly/workspaces.py (RepoTargetWorkspaceResolver.prepare — the v4 plan workspace: repo-target-less tasks reuse the process cwd (reused_current_cwd=True); repo-targeted tasks are cloned/fetched into a deterministic path under WHILLY_WORKSPACE_BASE / DEFAULT_WORKSPACE_BASE = ".whilly_workspaces/repos", laid out as <repo>/<plan>/<task>, on branch whilly/<plan>/<task>; prepare_git_workspace clone/fetch/checkout; the workspace.prepared event; ResolvedWorkspace shape)
    - whilly/worktree_runner.py (WorktreeManager — per-task .whilly_worktrees/{task_id} lifecycle: create (git worktree add -b whilly/{task_id}) -> merge_back (cherry-pick HEAD..branch back, conflict aborts) -> cleanup (remove worktree + branch); cleanup_all; note this class is NOT instantiated by the live v4 run path — its activation is direct construction by a caller, with no env-flag gate)
    - whilly/cli/run.py (RepoTargetWorkspaceResolver is the resolver actually wired into _async_run via the workspace_runner closure — this is the live v4 workspace path)
    - whilly/config.py (WORKTREE / USE_WORKSPACE are REMOVED NO-OPS — do NOT gate any behavior on them)
    - openspec/AUTHORING.md (format + strict-validation rules — MUST follow exactly)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar — copy its structure)
  </read_first>
  <action>
    READ the source modules above FIRST and reverse-spec the OBSERVED v4 behavior. The v4 plan
    workspace is `RepoTargetWorkspaceResolver`, NOT a `.whilly_workspaces/{slug}/` plan-level
    worktree, and there is NO `WHILLY_WORKTREE` / `WHILLY_USE_WORKSPACE` activation gate (both are
    removed no-ops). Do NOT carry over either v3 claim.
    Write `openspec/specs/worktree-isolation/spec.md` for the slug `worktree-isolation`, mirroring
    the exemplar structure; first body line of each requirement contains SHALL or MUST; each has
    >=1 `#### Scenario:` with `- **WHEN**` / `- **THEN**`.
    Capture two distinct isolation layers: (1) the v4 per-task workspace via
    `RepoTargetWorkspaceResolver.prepare` — a task WITHOUT a `repo_target_id` runs in the process
    cwd (ResolvedWorkspace.reused_current_cwd=True); a task WITH a `repo_target_id` is resolved into
    a deterministic git checkout under `WHILLY_WORKSPACE_BASE` (default `.whilly_workspaces/repos`),
    laid out as `<repo>/<plan>/<task>` and checked out on branch `whilly/<plan>/<task>`, with a
    `workspace.prepared` event recorded; an unregistered repo target raises. (2) the
    `WorktreeManager` per-task worktree at `.whilly_worktrees/{task_id}` with its lifecycle —
    `create` (git worktree add on branch `whilly/{task_id}`) -> `merge_back` (cherry-pick the
    worktree branch's commits HEAD..branch back into the current branch, aborting on conflict and
    reporting it) -> `cleanup` (remove the worktree and delete its branch). State the REAL
    activation of `WorktreeManager` as you observe it in the code (direct construction by a caller;
    NOT wired into the live `whilly run` path; NOT gated by an env flag) — do not invent a trigger.
    Reference `task-model-fsm` for the `DONE` status meaning rather than re-specifying it. Reference
    `agent-dispatch` for how the prepared workspace becomes the dispatched agent's cwd. Keep each
    requirement body under 500 chars. No fenced code blocks in the spec body.
  </action>
  <verify>
    <automated>cd /opt/develop/whilly-orchestrator && PATH="$HOME/.reflex/.nvm/versions/node/v20.19.6/bin:$PATH" openspec validate worktree-isolation --strict</automated>
  </verify>
  <acceptance_criteria>
    - The file openspec/specs/worktree-isolation/spec.md exists and is non-empty.
    - `openspec validate worktree-isolation --strict` prints "is valid" and exits 0 (zero errors/warnings).
    - The spec contains `## Purpose` (>=50 chars) and `## Requirements`; every requirement has SHALL/MUST on the first body line and >=1 `#### Scenario:`.
    - A requirement specifies the v4 RepoTargetWorkspaceResolver workspace (.whilly_workspaces/repos/<repo>/<plan>/<task>; repo-target-less tasks reuse the process cwd).
    - A requirement specifies the per-task WorktreeManager .whilly_worktrees/{task_id} create -> merge_back (cherry-pick) -> cleanup lifecycle, with its real (non-env-gated) activation.
    - The spec asserts NEITHER a .whilly_workspaces/{slug}/ plan-level worktree as the v4 model NOR a WHILLY_WORKTREE / WHILLY_USE_WORKSPACE activation gate.
  </acceptance_criteria>
  <done>worktree-isolation/spec.md authored, normative, grounded in RepoTargetWorkspaceResolver + WorktreeManager, passing strict validation.</done>
</task>

</tasks>

<verification>
Run `openspec validate batch-planning --strict` and `openspec validate worktree-isolation --strict`
— both must report 0 errors and 0 warnings. Confirm neither file contains delta headers.
Confirm worktree-isolation contains NO ".whilly_workspaces/{slug}/" plan-worktree-as-v4-model claim
and NO WHILLY_WORKTREE / WHILLY_USE_WORKSPACE gating claim.
</verification>

<success_criteria>
ORCH-04 and ORCH-06 satisfied: both specs are normative, testable, grounded in real v4 code, and pass strict validation.
</success_criteria>

<output>
Create `.planning/phases/22-orchestration-cluster/22-04-SUMMARY.md` when done.
</output>
