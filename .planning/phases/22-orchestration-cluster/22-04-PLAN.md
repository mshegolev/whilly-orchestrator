---
phase: 22-orchestration-cluster
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/batch-planning/spec.md
  - openspec/specs/worktree-isolation/spec.md
autonomous: true
requirements: [ORCH-04, ORCH-06]
must_haves:
  truths:
    - "openspec/specs/batch-planning/spec.md exists and passes openspec validate batch-planning --strict"
    - "openspec/specs/worktree-isolation/spec.md exists and passes openspec validate worktree-isolation --strict"
    - "batch-planning specifies non-overlapping key_files grouping and first-batch-then-re-evaluate dispatch, active only when MAX_PARALLEL > 1"
    - "worktree-isolation specifies the plan workspace .whilly_workspaces/{slug}/ (off by default since v3.3.0) and the per-task .whilly_worktrees/{task_id} create -> cherry-pick-on-done -> cleanup lifecycle"
  artifacts:
    - path: "openspec/specs/batch-planning/spec.md"
      provides: "Normative spec of batch planning (ORCH-04)"
      contains: "## Requirements"
    - path: "openspec/specs/worktree-isolation/spec.md"
      provides: "Normative spec of workspace + worktree isolation (ORCH-06)"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/batch-planning/spec.md"
      to: "whilly/orchestrator.py (plan_batches, plan_batches_llm)"
      via: "reverse-spec'd grouping + dispatch-re-evaluation requirements"
      pattern: "SHALL|MUST"
    - from: "openspec/specs/worktree-isolation/spec.md"
      to: "whilly/worktree_runner.py, whilly/workspaces.py"
      via: "reverse-spec'd lifecycle requirements"
      pattern: "SHALL|MUST"
---

<objective>
Author two independent normative OpenSpec specs: `batch-planning` (ORCH-04) and
`worktree-isolation` (ORCH-06), each reverse-spec'd from the real code.

Purpose: Make the batching rule and the workspace/worktree lifecycle explicit and machine-checkable.
Output: Two strict-valid spec.md files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-orchestration-cluster/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write batch-planning spec.md (ORCH-04)</name>
  <files>openspec/specs/batch-planning/spec.md</files>
  <read_first>
    - whilly/orchestrator.py (plan_batches — key_files grouping; plan_batches_llm — LLM variant)
    - openspec/AUTHORING.md (format + strict-validation rules — MUST follow exactly)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar — copy its structure)
  </read_first>
  <action>
    READ whilly/orchestrator.py first; ground every requirement in observed behavior. Write
    `openspec/specs/batch-planning/spec.md` for the slug `batch-planning`, mirroring the exemplar:
    `## Purpose` (plain prose, >=50 chars), then `## Requirements` with `### Requirement: <name>`
    blocks; first body line contains SHALL or MUST; each requirement has >=1 `#### Scenario:`
    (4 hashtags) with `- **WHEN**` / `- **THEN**`.
    Capture: batch grouping puts ready tasks into batches by NON-OVERLAPPING `key_files` (tasks
    that touch the same file cannot share a batch); `plan_batches_llm` is the optional LLM-driven
    grouping variant; batching is active only when `MAX_PARALLEL > 1` (with MAX_PARALLEL <= 1 the
    loop is single-task / no batching); and only the FIRST batch of each iteration is dispatched,
    after which the loop re-evaluates the ready set so newly-unblocked tasks can join the next
    batch. Reference the orchestration-loop capability for the surrounding iteration order rather
    than re-specifying it. Keep each requirement body under 500 chars.
  </action>
  <verify>
    <automated>cd /opt/develop/whilly-orchestrator && PATH="$HOME/.reflex/.nvm/versions/node/v20.19.6/bin:$PATH" openspec validate batch-planning --strict</automated>
  </verify>
  <acceptance_criteria>
    - The file openspec/specs/batch-planning/spec.md exists and is non-empty.
    - `openspec validate batch-planning --strict` prints "is valid" and exits 0 (zero errors/warnings).
    - The spec contains `## Purpose` (>=50 chars) and `## Requirements`; every requirement has SHALL/MUST on the first body line and >=1 `#### Scenario:`.
    - A requirement specifies non-overlapping key_files grouping.
    - A requirement specifies first-batch-then-re-evaluate dispatch and the MAX_PARALLEL > 1 precondition.
  </acceptance_criteria>
  <done>batch-planning/spec.md authored, normative, passing strict validation.</done>
</task>

<task type="auto">
  <name>Task 2: Write worktree-isolation spec.md (ORCH-06)</name>
  <files>openspec/specs/worktree-isolation/spec.md</files>
  <read_first>
    - whilly/worktree_runner.py (WorktreeManager — per-task .whilly_worktrees/{task_id} lifecycle)
    - whilly/workspaces.py (plan workspace .whilly_workspaces/{slug}/ create/reuse)
    - openspec/AUTHORING.md (format + strict-validation rules — MUST follow exactly)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar — copy its structure)
  </read_first>
  <action>
    READ the source modules above first; ground every requirement in observed behavior. Write
    `openspec/specs/worktree-isolation/spec.md` for the slug `worktree-isolation`, mirroring the
    exemplar structure; first body line of each requirement contains SHALL or MUST; each has >=1
    `#### Scenario:` with `- **WHEN**` / `- **THEN**`.
    Capture two distinct isolation layers: (1) the plan-level workspace at
    `.whilly_workspaces/{slug}/` — a git worktree created/reused for a plan, OFF BY DEFAULT since
    v3.3.0 (opt in via --workspace / --worktree / WHILLY_USE_WORKSPACE=1), into which the loop
    chdirs; and (2) the per-task worktree at `.whilly_worktrees/{task_id}` with its lifecycle:
    create on dispatch -> cherry-pick the task's commits back into the main repo when the task
    reaches `done` -> cleanup (remove the worktree) afterward. Reference agent-dispatch for the
    precondition that gates per-task worktrees (WHILLY_WORKTREE=1 AND MAX_PARALLEL > 1) rather
    than re-deciding it, and reference task-model-fsm for the `done` status meaning. Keep each
    requirement body under 500 chars.
  </action>
  <verify>
    <automated>cd /opt/develop/whilly-orchestrator && PATH="$HOME/.reflex/.nvm/versions/node/v20.19.6/bin:$PATH" openspec validate worktree-isolation --strict</automated>
  </verify>
  <acceptance_criteria>
    - The file openspec/specs/worktree-isolation/spec.md exists and is non-empty.
    - `openspec validate worktree-isolation --strict` prints "is valid" and exits 0 (zero errors/warnings).
    - The spec contains `## Purpose` (>=50 chars) and `## Requirements`; every requirement has SHALL/MUST on the first body line and >=1 `#### Scenario:`.
    - A requirement specifies the plan workspace .whilly_workspaces/{slug}/ (off by default since v3.3.0).
    - A requirement specifies the per-task .whilly_worktrees/{task_id} create -> cherry-pick-on-done -> cleanup lifecycle.
  </acceptance_criteria>
  <done>worktree-isolation/spec.md authored, normative, passing strict validation.</done>
</task>

</tasks>

<verification>
Run `openspec validate batch-planning --strict` and `openspec validate worktree-isolation --strict`
— both must report 0 errors and 0 warnings. Confirm neither file contains delta headers.
</verification>

<success_criteria>
ORCH-04 and ORCH-06 satisfied: both specs are normative, testable, grounded in real code, and pass strict validation.
</success_criteria>

<output>
Create `.planning/phases/22-orchestration-cluster/22-04-SUMMARY.md` when done.
</output>
