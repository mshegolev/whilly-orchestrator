---
phase: 22-orchestration-cluster
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/orchestration-loop/spec.md
autonomous: true
requirements: [ORCH-01]
must_haves:
  truths:
    - "openspec/specs/orchestration-loop/spec.md exists and is non-empty"
    - "openspec validate orchestration-loop --strict exits 0 with zero errors and zero warnings"
    - "The spec describes the run_plan iteration order load -> ready -> batch -> dispatch -> collect -> guard -> persist"
    - "The spec specifies the _original_cwd restore-on-all-exit-paths invariant"
    - "The spec references task-model-fsm for FSM detail and does NOT re-specify status transitions or budget thresholds"
  artifacts:
    - path: "openspec/specs/orchestration-loop/spec.md"
      provides: "Normative spec of the run_plan orchestration loop (ORCH-01)"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/orchestration-loop/spec.md"
      to: "whilly/cli/run.py::run_plan"
      via: "reverse-spec'd requirements grounded in real iteration code"
      pattern: "SHALL|MUST"
---

<objective>
Author the normative OpenSpec capability spec for `orchestration-loop` (ORCH-01),
reverse-spec'd from the real `run_plan` loop and core domain modules.

Purpose: Make the core loop's per-iteration guarantees explicit and machine-checkable.
Output: `openspec/specs/orchestration-loop/spec.md` passing `openspec validate orchestration-loop --strict`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-orchestration-cluster/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write orchestration-loop spec.md (ORCH-01)</name>
  <files>openspec/specs/orchestration-loop/spec.md</files>
  <read_first>
    - whilly/cli/run.py (run_plan — the main loop body, iteration ordering, exit paths)
    - whilly/cli/plan.py (plan run loop entry; iteration orchestration helpers)
    - whilly/core/models.py (core domain models)
    - whilly/core/gates.py (gate domain models)
    - whilly/core/governance.py (governance models)
    - whilly/core/task_id.py (task id domain)
    - whilly/llm_ops.py, whilly/llm_otel.py (LLM plumbing/tracing touched by the loop)
    - openspec/AUTHORING.md (format + strict-validation rules — MUST follow exactly)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar — copy its structure)
  </read_first>
  <action>
    READ the source modules above first; ground every requirement in observed behavior — no
    aspirational or descriptive-only claims. Write `openspec/specs/orchestration-loop/spec.md`
    for the slug `orchestration-loop`, mirroring the exemplar exactly: a `## Purpose` section of
    plain prose (>=50 chars, no headings/code blocks), then `## Requirements` with one or more
    `### Requirement: <name>` blocks. Each requirement's FIRST body line MUST contain SHALL or
    MUST (case-sensitive), and each requirement MUST have >=1 `#### Scenario:` (exactly 4
    hashtags) with `- **WHEN**` / `- **THEN**` bullets (`- **AND**` optional). Avoid
    should/may/might/can.
    Capture these guaranteed behaviors of run_plan: the per-iteration ordering load (TaskManager
    loads the plan) -> ready (derive the ready set) -> batch (group ready tasks) -> dispatch
    (launch agents) -> collect (gather AgentResults) -> guard (budget/deadlock/stall/timeout
    checks) -> persist (StateStore + reporter write); that only the first batch of an iteration
    is dispatched and the loop re-evaluates afterward so newly-unblocked tasks join the next
    batch; and the cwd invariant that `_original_cwd` is restored on ALL exit paths (including
    early returns) when the loop chdirs into a workspace, with the plan file resolved to an
    absolute path before any chdir.
    Boundary discipline (per CONTEXT.md): DO NOT re-specify the task status FSM or its
    transitions — reference the `task-model-fsm` capability instead. DO NOT specify budget
    thresholds / kill semantics or verification gates — those belong to Phase 27 capabilities;
    reference them, do not duplicate. Keep each requirement body under 500 characters; split if
    longer. Do not place fenced code blocks or full file contents in the spec body beyond what
    the exemplar uses (it uses none).
  </action>
  <verify>
    <automated>cd /opt/develop/whilly-orchestrator && PATH="$HOME/.reflex/.nvm/versions/node/v20.19.6/bin:$PATH" openspec validate orchestration-loop --strict</automated>
  </verify>
  <acceptance_criteria>
    - The file openspec/specs/orchestration-loop/spec.md exists and is non-empty.
    - `openspec validate orchestration-loop --strict` prints "is valid" and exits 0 (zero errors, zero warnings).
    - The spec contains a `## Purpose` section (>=50 chars) and a `## Requirements` section.
    - Every `### Requirement:` has SHALL or MUST on its first body line and >=1 `#### Scenario:`.
    - A requirement captures the load -> ready -> batch -> dispatch -> collect -> guard -> persist iteration order.
    - A requirement captures the `_original_cwd` restore-on-all-exit-paths invariant.
    - The spec references task-model-fsm rather than re-specifying status transitions; no budget thresholds are duplicated.
  </acceptance_criteria>
  <done>orchestration-loop/spec.md authored, normative, and passing strict validation.</done>
</task>

</tasks>

<verification>
Run `openspec validate orchestration-loop --strict` — must report 0 errors and 0 warnings.
Confirm no delta headers (`## ADDED Requirements` etc.) appear in the file.
</verification>

<success_criteria>
ORCH-01 satisfied: orchestration-loop has a normative, testable spec.md grounded in run_plan and
passing `openspec validate orchestration-loop --strict`.
</success_criteria>

<output>
Create `.planning/phases/22-orchestration-cluster/22-01-SUMMARY.md` when done.
</output>
