---
phase: 22-orchestration-cluster
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/agent-dispatch/spec.md
autonomous: true
requirements: [ORCH-05]
must_haves:
  truths:
    - "openspec/specs/agent-dispatch/spec.md exists and passes openspec validate agent-dispatch --strict"
    - "The spec specifies tmux vs subprocess runner selection (tmux preferred when tmux is available AND USE_TMUX=1, subprocess otherwise)"
    - "The spec names the whilly-{task_id} tmux session convention (one session per task) and the subprocess fallback"
    - "The spec describes the v4 dispatch seam: each dispatched agent receives the build_task_prompt prompt and runs in the task's prepared workspace cwd, and references worktree-isolation for the workspace/worktree layer rather than gating on a removed env flag"
  artifacts:
    - path: "openspec/specs/agent-dispatch/spec.md"
      provides: "Normative spec of agent dispatch / runner selection (ORCH-05)"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/agent-dispatch/spec.md"
      to: "whilly/tmux_runner.py, whilly/agent_runner.py, whilly/core/agent_runner.py, whilly/adapters/runner/*"
      via: "reverse-spec'd runner-selection + dispatch requirements"
      pattern: "SHALL|MUST"
---

<objective>
Author the normative OpenSpec spec for `agent-dispatch` (ORCH-05), reverse-spec'd from the tmux and
subprocess runners and the v4 agent / runner abstractions.

Purpose: Make the runner-selection and dispatch contract explicit and machine-checkable.
Output: `openspec/specs/agent-dispatch/spec.md` passing strict validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-orchestration-cluster/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write agent-dispatch spec.md (ORCH-05)</name>
  <files>openspec/specs/agent-dispatch/spec.md</files>
  <read_first>
    - whilly/tmux_runner.py (tmux session dispatch — the whilly-{task_id} session naming; how a tmux session is created/launched per task)
    - whilly/agent_runner.py (top-level dispatch entry — run_agent and the result it returns)
    - whilly/core/agent_runner.py (core agent runner abstraction + the pre-dispatch command/secret surface scans)
    - whilly/adapters/runner/__init__.py, whilly/adapters/runner/claude_cli.py, whilly/adapters/runner/env.py (the v4 runner adapter surface: run_task subprocess wrapper, cwd handling, env allowlist)
    - whilly/cli/run.py (workspace_runner closure — how the v4 run path injects the prepared workspace cwd into run_task before dispatch; this is the live dispatch seam)
    - whilly/config.py (USE_TMUX flag; note WORKTREE / USE_WORKSPACE are REMOVED NO-OPS — do NOT gate dispatch on them)
    - whilly/agents/base.py, whilly/agents/claude.py (agent abstractions — what a dispatched agent is)
    - whilly/worker/__init__.py, whilly/worker/local.py (worker runtime entry points — how the loop hands a task to the runner)
    - openspec/AUTHORING.md (format + strict-validation rules — MUST follow exactly)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar — copy its structure)
  </read_first>
  <action>
    READ the source modules above FIRST and reverse-spec the OBSERVED v4 behavior. Do NOT carry
    over the v3 CLAUDE.md claim that per-task worktrees are gated on `WHILLY_WORKTREE=1` AND
    `MAX_PARALLEL > 1` — `WHILLY_WORKTREE` / `WHILLY_USE_WORKSPACE` are REMOVED NO-OPS in v4
    (config.py) and gate nothing. The module set is large — focus on the DISPATCH contract, not
    every helper. Write `openspec/specs/agent-dispatch/spec.md` for the slug `agent-dispatch`,
    mirroring the exemplar: `## Purpose` (plain prose, >=50 chars), then `## Requirements` with
    `### Requirement: <name>` blocks; first body line of each contains SHALL or MUST; each
    requirement has >=1 `#### Scenario:` (4 hashtags) with `- **WHEN**` / `- **THEN**`.
    Capture the grounded v4 behaviors: (1) runner selection — the tmux runner is preferred when
    tmux is available AND `USE_TMUX=1` (`WHILLY_USE_TMUX`), otherwise the subprocess runner
    (`run_task` / the Claude CLI wrapper) is used. (2) each tmux agent runs in a `whilly-{task_id}`
    session — exactly one session per task. (3) the dispatch seam — a dispatched agent receives the
    prompt built by `build_task_prompt` and runs in the cwd of the task's prepared workspace
    (the `workspace_runner` closure in `cli/run.py` injects `cwd=workspace.path` into `run_task`).
    Boundary discipline: DO NOT specify the workspace/worktree directory layout or lifecycle — that
    is the `worktree-isolation` capability; reference it for where the dispatched agent's cwd comes
    from. DO NOT specify result parsing (that is `result-collection` — reference it). DO NOT
    re-specify FSM transitions. DO NOT assert any `WHILLY_WORKTREE` / `WHILLY_USE_WORKSPACE` gate.
    Keep each requirement body under 500 chars. No fenced code blocks in the spec body.
  </action>
  <verify>
    <automated>cd /opt/develop/whilly-orchestrator && PATH="$HOME/.reflex/.nvm/versions/node/v20.19.6/bin:$PATH" openspec validate agent-dispatch --strict</automated>
  </verify>
  <acceptance_criteria>
    - The file openspec/specs/agent-dispatch/spec.md exists and is non-empty.
    - `openspec validate agent-dispatch --strict` prints "is valid" and exits 0 (zero errors/warnings).
    - The spec contains `## Purpose` (>=50 chars) and `## Requirements`; every requirement has SHALL/MUST on the first body line and >=1 `#### Scenario:`.
    - A requirement specifies tmux-preferred-when-available-and-USE_TMUX=1 with subprocess fallback.
    - A requirement specifies the whilly-{task_id} tmux session convention (one session per task).
    - A requirement specifies that a dispatched agent receives the build_task_prompt prompt and runs in the prepared workspace cwd, referencing worktree-isolation for that layer.
    - The spec asserts NO WHILLY_WORKTREE / WHILLY_USE_WORKSPACE env-flag dispatch gate.
  </acceptance_criteria>
  <done>agent-dispatch/spec.md authored, normative, grounded in the v4 runners and dispatch seam, passing strict validation.</done>
</task>

</tasks>

<verification>
Run `openspec validate agent-dispatch --strict` — must report 0 errors and 0 warnings.
Confirm no delta headers in the file.
Confirm the spec contains NO WHILLY_WORKTREE / WHILLY_USE_WORKSPACE gating claim.
</verification>

<success_criteria>
ORCH-05 satisfied: agent-dispatch has a normative, testable spec grounded in the v4 runners and the
live dispatch seam, passing strict validation.
</success_criteria>

<output>
Create `.planning/phases/22-orchestration-cluster/22-03-SUMMARY.md` when done.
</output>
