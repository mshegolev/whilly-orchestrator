---
phase: 22-orchestration-cluster
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - openspec/specs/agent-dispatch/spec.md
autonomous: true
requirements: [ORCH-05]
must_haves:
  truths:
    - "openspec/specs/agent-dispatch/spec.md exists and passes openspec validate agent-dispatch --strict"
    - "The spec specifies tmux vs subprocess runner selection (tmux preferred when available and USE_TMUX=1)"
    - "The spec specifies the per-task worktree isolation precondition (WHILLY_WORKTREE=1 AND MAX_PARALLEL > 1)"
    - "The spec names the whilly-{task_id} tmux session convention and the subprocess fallback"
  artifacts:
    - path: "openspec/specs/agent-dispatch/spec.md"
      provides: "Normative spec of agent dispatch / runner selection (ORCH-05)"
      contains: "## Requirements"
  key_links:
    - from: "openspec/specs/agent-dispatch/spec.md"
      to: "whilly/tmux_runner.py, whilly/agent_runner.py, whilly/core/agent_runner.py"
      via: "reverse-spec'd runner-selection + dispatch requirements"
      pattern: "SHALL|MUST"
---

<objective>
Author the normative OpenSpec spec for `agent-dispatch` (ORCH-05), reverse-spec'd from the tmux
and subprocess runners and the agent abstractions.

Purpose: Make the runner-selection and per-task isolation preconditions explicit and machine-checkable.
Output: `openspec/specs/agent-dispatch/spec.md` passing strict validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-orchestration-cluster/CONTEXT.md
@openspec/AUTHORING.md
@openspec/specs/task-model-fsm/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write agent-dispatch spec.md (ORCH-05)</name>
  <files>openspec/specs/agent-dispatch/spec.md</files>
  <read_first>
    - whilly/tmux_runner.py (tmux session dispatch — whilly-{task_id} sessions)
    - whilly/agent_runner.py (top-level dispatch + result collection entry)
    - whilly/core/agent_runner.py (core agent runner abstraction)
    - whilly/adapters/runner/__init__.py, whilly/adapters/runner/claude_cli.py, whilly/adapters/runner/env.py (runner adapter surface)
    - whilly/agents/base.py, whilly/agents/claude.py (agent abstractions — what a dispatched agent is)
    - whilly/worker/__init__.py, whilly/worker/local.py (worker runtime entry points)
    - openspec/AUTHORING.md (format + strict-validation rules — MUST follow exactly)
    - openspec/specs/task-model-fsm/spec.md (worked exemplar — copy its structure)
  </read_first>
  <action>
    READ the source modules above first; ground every requirement in observed behavior. The
    module set is large — focus on the DISPATCH contract, not every helper. Write
    `openspec/specs/agent-dispatch/spec.md` for the slug `agent-dispatch`, mirroring the exemplar:
    `## Purpose` (plain prose, >=50 chars), then `## Requirements` with `### Requirement: <name>`
    blocks; first body line of each contains SHALL or MUST; each requirement has >=1
    `#### Scenario:` (4 hashtags) with `- **WHEN**` / `- **THEN**`.
    Capture: runner selection — the tmux runner is preferred when tmux is available AND
    `USE_TMUX=1`, otherwise the subprocess fallback is used; each tmux agent runs in a
    `whilly-{task_id}` session (one session per task); and the per-task worktree isolation
    precondition — per-task worktrees are used only when `WHILLY_WORKTREE=1` AND `MAX_PARALLEL > 1`.
    Boundary discipline: DO NOT specify the worktree create/cherry-pick/cleanup lifecycle
    (that is the `worktree-isolation` capability — reference it). DO NOT specify result parsing
    (that is `result-collection` — reference it). DO NOT re-specify FSM transitions. Keep each
    requirement body under 500 chars.
  </action>
  <verify>
    <automated>cd /opt/develop/whilly-orchestrator && PATH="$HOME/.reflex/.nvm/versions/node/v20.19.6/bin:$PATH" openspec validate agent-dispatch --strict</automated>
  </verify>
  <acceptance_criteria>
    - The file openspec/specs/agent-dispatch/spec.md exists and is non-empty.
    - `openspec validate agent-dispatch --strict` prints "is valid" and exits 0 (zero errors/warnings).
    - The spec contains `## Purpose` (>=50 chars) and `## Requirements`; every requirement has SHALL/MUST on the first body line and >=1 `#### Scenario:`.
    - A requirement specifies tmux-preferred-when-available-and-USE_TMUX=1 with subprocess fallback.
    - A requirement specifies the WHILLY_WORKTREE=1 AND MAX_PARALLEL > 1 per-task isolation precondition.
  </acceptance_criteria>
  <done>agent-dispatch/spec.md authored, normative, passing strict validation.</done>
</task>

</tasks>

<verification>
Run `openspec validate agent-dispatch --strict` — must report 0 errors and 0 warnings.
Confirm no delta headers in the file.
</verification>

<success_criteria>
ORCH-05 satisfied: agent-dispatch has a normative, testable spec grounded in the runners and agent
abstractions, passing strict validation.
</success_criteria>

<output>
Create `.planning/phases/22-orchestration-cluster/22-03-SUMMARY.md` when done.
</output>
