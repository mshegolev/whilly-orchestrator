{
  "project": "Whilly v4.1 — Out-of-scope items from v4.0 release",
  "prd_file": "docs/v4.0-release-checklist.md",
  "description": "Backlog задач, явно отложенных из v4.0 в секцию 'Out of scope for v4.0' release-checklist'а. Не баги, а сознательные scope-cuts: per-worker bearer rotation, budget guards, --reset, миграция v3 фич (PRD wizard / TRIZ analyzer / Decision Gate), удаление cli_legacy, file-based audit mirror, cross-platform smoke, Forge pipeline. Каждая задача связана с конкретной строкой docs/v4.0-release-checklist.md секции Out-of-scope-for-v40.",
  "tasks": [
    {
      "id": "TASK-109",
      "title": "Claude proxy / SSH-tunnel integration (epic)",
      "category": "feat",
      "priority": "high",
      "status": "pending",
      "dependencies": [
        "TASK-109-1",
        "TASK-109-2",
        "TASK-109-3",
        "TASK-109-4",
        "TASK-109-5"
      ],
      "key_files": [
        "docs/PRD-v41-claude-proxy.md",
        "whilly/adapters/runner/proxy.py",
        "whilly/adapters/runner/claude_cli.py",
        "whilly/prd_generator.py"
      ],
      "description": "Epic. Worker и whilly init должны проксировать ТОЛЬКО Claude subprocess через HTTPS_PROXY (gpt-proxy SSH tunnel pattern), оставляя Postgres / control plane direct через NO_PROXY. PRD: docs/PRD-v41-claude-proxy.md. Декомпозирован на 5 sub-tasks 109-1..5.",
      "acceptance_criteria": [
        "Все 5 sub-task в done",
        "PRD docs/PRD-v41-claude-proxy.md SC-1..SC-5 зелёные",
        "WHILLY_CLAUDE_PROXY_URL=http://127.0.0.1:11112 + поднятый туннель → whilly init --headless успешно работает против реального Anthropic API"
      ],
      "test_steps": [
        "pytest tests/unit/test_claude_proxy_helpers.py tests/unit/test_claude_proxy_probe.py tests/unit/test_claude_subprocess_env.py tests/unit/test_cli_init_proxy.py -v"
      ],
      "prd_requirement": "docs/PRD-v41-claude-proxy.md (SC-1..SC-5)"
    },
    {
      "id": "TASK-109-1",
      "title": "whilly/adapters/runner/proxy.py — pure helpers",
      "category": "feat",
      "priority": "high",
      "status": "pending",
      "dependencies": [],
      "key_files": [
        "whilly/adapters/runner/proxy.py",
        "tests/unit/test_claude_proxy_helpers.py"
      ],
      "description": "Новый модуль с двумя pure функциями: ProxySettings dataclass + resolve_proxy_settings(cli_url, env_url, default_no_proxy) (CLI flag > WHILLY_CLAUDE_PROXY_URL > inherited HTTPS_PROXY > None) + build_subprocess_env(parent_env, settings) -> dict (no diff если url=None; HTTPS_PROXY+NO_PROXY если url не None). Без I/O.",
      "acceptance_criteria": [
        "ProxySettings dataclass: url: str | None, no_proxy: str",
        "resolve_proxy_settings правильная иерархия (CLI > env > inherited > None)",
        "build_subprocess_env: url=None → parent_env unchanged; url='...' → новый dict с HTTPS_PROXY+NO_PROXY",
        "8+ unit-тестов на каждый branch"
      ],
      "test_steps": [
        "pytest tests/unit/test_claude_proxy_helpers.py -v"
      ],
      "prd_requirement": "docs/PRD-v41-claude-proxy.md FR-1, FR-5, FR-6"
    },
    {
      "id": "TASK-109-2",
      "title": "Pre-flight TCP probe with friendly error",
      "category": "feat",
      "priority": "high",
      "status": "pending",
      "dependencies": [
        "TASK-109-1"
      ],
      "key_files": [
        "whilly/adapters/runner/proxy.py",
        "tests/unit/test_claude_proxy_probe.py"
      ],
      "description": "probe_proxy_or_raise(url, timeout=0.5) -> None. Парсит URL, делает socket.create_connection. На fail → RuntimeError с подсказкой про SSH tunnel.",
      "acceptance_criteria": [
        "Success → None silently",
        "ConnectionRefused / timeout → RuntimeError с подсказкой 'ssh -fN -L ...'",
        "Unit-тест с фейковым TCP-сервером + closed-port случай"
      ],
      "test_steps": [
        "pytest tests/unit/test_claude_proxy_probe.py -v"
      ],
      "prd_requirement": "docs/PRD-v41-claude-proxy.md FR-3, SC-3"
    },
    {
      "id": "TASK-109-3",
      "title": "Wire proxy env into Claude subprocess call sites",
      "category": "feat",
      "priority": "high",
      "status": "pending",
      "dependencies": [
        "TASK-109-1"
      ],
      "key_files": [
        "whilly/adapters/runner/claude_cli.py",
        "whilly/prd_generator.py",
        "tests/unit/test_claude_subprocess_env.py"
      ],
      "description": "В двух subprocess sites (claude_cli и prd_generator._call_claude) — резолвить ProxySettings и передавать env={**os.environ, **diff} в subprocess. Whilly process сам остаётся без HTTPS_PROXY.",
      "acceptance_criteria": [
        "claude_cli.py использует build_subprocess_env",
        "prd_generator._call_claude — то же самое",
        "Worker process os.environ НЕ модифицирован",
        "Unit-тесты с monkeypatch на subprocess.Popen / asyncio.create_subprocess_exec"
      ],
      "test_steps": [
        "pytest tests/unit/test_claude_subprocess_env.py -v"
      ],
      "prd_requirement": "docs/PRD-v41-claude-proxy.md FR-2, SC-4"
    },
    {
      "id": "TASK-109-4",
      "title": "CLI flags --claude-proxy / --no-claude-proxy + probe wiring",
      "category": "feat",
      "priority": "medium",
      "status": "pending",
      "dependencies": [
        "TASK-109-2",
        "TASK-109-3"
      ],
      "key_files": [
        "whilly/cli/init.py",
        "whilly/cli/run.py",
        "tests/unit/test_cli_init_proxy.py"
      ],
      "description": "Флаги в whilly init и whilly run + probe один раз на старте если settings.url not None и WHILLY_CLAUDE_PROXY_PROBE=1. Probe fail → exit 2 с подсказкой.",
      "acceptance_criteria": [
        "--claude-proxy URL переопределяет env",
        "--no-claude-proxy отключает proxy даже если env set",
        "Probe fail → exit 2",
        "whilly init --help показывает оба флага"
      ],
      "test_steps": [
        "pytest tests/unit/test_cli_init_proxy.py -v"
      ],
      "prd_requirement": "docs/PRD-v41-claude-proxy.md FR-6, SC-1, SC-3"
    },
    {
      "id": "TASK-109-5",
      "title": "Whilly-Claude-Proxy-Guide + README + CHANGELOG",
      "category": "docs",
      "priority": "medium",
      "status": "pending",
      "dependencies": [
        "TASK-109-3",
        "TASK-109-4"
      ],
      "key_files": [
        "docs/Whilly-Claude-Proxy-Guide.md",
        "README.md",
        "CHANGELOG.md"
      ],
      "description": "Гайд: env vars, CLI flags, SSH tunnel пример, systemd unit, troubleshooting. README — короткая секция. CHANGELOG [Unreleased] — Added.",
      "acceptance_criteria": [
        "docs/Whilly-Claude-Proxy-Guide.md покрывает env vars + CLI flags + SSH tunnel + systemd + troubleshooting",
        "README.md имеет секцию про proxy",
        "CHANGELOG [Unreleased] содержит WHILLY_CLAUDE_PROXY_URL"
      ],
      "test_steps": [
        "test -f docs/Whilly-Claude-Proxy-Guide.md",
        "grep -q 'WHILLY_CLAUDE_PROXY_URL' docs/Whilly-Claude-Proxy-Guide.md README.md CHANGELOG.md"
      ],
      "prd_requirement": "docs/PRD-v41-claude-proxy.md NFR-4, Definition of Done #4, #5"
    },
    {
      "id": "TASK-101",
      "title": "Per-worker bearer rotation",
      "category": "feat",
      "priority": "high",
      "status": "pending",
      "dependencies": [],
      "key_files": [
        "whilly/adapters/transport/auth.py",
        "whilly/adapters/transport/server.py",
        "whilly/adapters/db/repository.py",
        "tests/integration/test_transport_workers.py"
      ],
      "description": "Заменить cluster-shared WHILLY_WORKER_TOKEN на per-worker bearer, который мином мин в /workers/register и валидируется server'ом против workers.token_hash через SHA-256 compare. Текущий flow — один разделяемый токен на все воркеры; следствие — ротация требует одновременного перезапуска всего флота. На v4.1 каждый воркер должен иметь свой individual bearer, и server должен уметь отзывать токен на одного worker'а через UPDATE workers SET token_hash='REVOKED' (или подобное). Совместимость: shared-bearer fallback через env var оставить на одну минорную версию для плавной миграции.",
      "acceptance_criteria": [
        "POST /workers/register возвращает RegisterResponse с уникальным token, хешированным в workers.token_hash",
        "bearer_dep валидирует Authorization: Bearer X против workers.token_hash для всех steady-state RPC (heartbeat, claim, complete, fail, release)",
        "Отзыв одного воркера: UPDATE workers SET token_hash=NULL WHERE worker_id=X — следующий RPC от этого воркера получает 401",
        "Backwards-compat: если WHILLY_WORKER_TOKEN env установлен, shared-bearer flow работает как сейчас, но логирует deprecation warning",
        "tests/integration/test_transport_workers.py покрывает оба flow"
      ],
      "test_steps": [
        "pytest tests/integration/test_transport_workers.py -v",
        "pytest tests/unit/test_transport_auth.py -v"
      ],
      "prd_requirement": "docs/v4.0-release-checklist.md#out-of-scope: 'Per-worker bearer rotation'"
    },
    {
      "id": "TASK-102",
      "title": "Budget guards (WHILLY_BUDGET_USD)",
      "category": "feat",
      "priority": "medium",
      "status": "pending",
      "dependencies": [],
      "key_files": [
        "whilly/worker/local.py",
        "whilly/worker/remote.py",
        "whilly/adapters/runner/result_parser.py",
        "tests/integration/test_budget_guards.py"
      ],
      "description": "В v3 был WHILLY_BUDGET_USD — Whilly останавливался когда сумма затрат на токены упиралась в потолок. На v4 пока не реализовано. Нужно: (1) парсить usage info из AgentResult (Claude CLI выдаёт input_tokens/output_tokens/cost в JSON envelope), (2) аккумулировать total_cost per plan_id в Postgres (новая колонка plans.spent_usd?), (3) перед каждым claim проверять spent_usd vs budget_usd; если превышен — voluntarily decline (возвращать 204 / NoBudget exception), (4) логировать в audit события BUDGET_WARNING (80%) и BUDGET_EXCEEDED (100%).",
      "acceptance_criteria": [
        "plans table расширена колонкой budget_usd и spent_usd",
        "AgentResult.usage парсится в run_local_worker / run_remote_worker и аккумулируется через repo.add_spend(plan_id, delta)",
        "Worker отказывается claim'ить если spent_usd >= budget_usd",
        "events таблица содержит BUDGET_WARNING и BUDGET_EXCEEDED rows",
        "WHILLY_BUDGET_USD env var опционально override'ит budget_usd на уровне plan import"
      ],
      "test_steps": [
        "pytest tests/integration/test_budget_guards.py -v"
      ],
      "prd_requirement": "docs/v4.0-release-checklist.md#out-of-scope: 'Budget guards'"
    },
    {
      "id": "TASK-103",
      "title": "whilly plan reset PLAN_ID",
      "category": "feat",
      "priority": "high",
      "status": "pending",
      "dependencies": [],
      "key_files": [
        "whilly/cli/plan.py",
        "whilly/adapters/db/repository.py",
        "tests/integration/test_plan_reset.py"
      ],
      "description": "Сейчас reset плана требует ручного DELETE FROM tasks/events/plans WHERE plan_id=X через psql — это unfriendly и легко забыть про cascade. Добавить subcommand `whilly plan reset <plan_id>` с двумя режимами: --keep-tasks (только сбросить status в PENDING + почистить events) и --hard (DROP + reimport). Confirmation prompt перед выполнением (--yes для скриптов).",
      "acceptance_criteria": [
        "whilly plan reset <plan_id> работает в обоих режимах",
        "Без --yes — interactive y/N confirmation",
        "С --keep-tasks: tasks.status='PENDING', claimed_by=NULL, version+=1; events для этого plan_id удалены",
        "С --hard: DELETE FROM tasks/events/plans WHERE plan_id=X (CASCADE)",
        "Audit row RESET добавляется в events с reason='manual_reset'"
      ],
      "test_steps": [
        "pytest tests/integration/test_plan_reset.py -v",
        "whilly plan reset test-plan --keep-tasks --yes"
      ],
      "prd_requirement": "docs/v4.0-release-checklist.md#out-of-scope: 'whilly --reset PLAN.json equivalent'"
    },
    {
      "id": "TASK-104a",
      "title": "Port PRD wizard to v4 — interactive `whilly init` (epic)",
      "category": "feat",
      "priority": "high",
      "status": "done",
      "dependencies": [
        "TASK-104a-1",
        "TASK-104a-2",
        "TASK-104a-3",
        "TASK-104a-4",
        "TASK-104a-5",
        "TASK-104a-6"
      ],
      "key_files": [
        "docs/PRD-v41-prd-wizard-port.md",
        "whilly/cli/init.py",
        "whilly/prd_generator.py",
        "whilly/adapters/filesystem/plan_io.py",
        "tests/integration/test_init_e2e.py"
      ],
      "description": "Epic. Требования и SC формализованы в docs/PRD-v41-prd-wizard-port.md. Декомпозирован на 6 атомарных подзадач (TASK-104a-1..6), каждая выполнима за один PR. Закрытие epic'а = все 6 подзадач done + integration smoke зелёный.",
      "acceptance_criteria": [
        "Все 6 sub-task'ов TASK-104a-1..6 в статусе done",
        "PRD docs/PRD-v41-prd-wizard-port.md SC-1..SC-6 зелёные",
        "whilly init '<идея>' --headless создаёт PRD + засевает план в Postgres",
        "whilly --prd-wizard (legacy) продолжает работать без regression"
      ],
      "test_steps": [
        "pytest tests/integration/test_init_e2e.py -v",
        "coverage report --include='whilly/cli/init.py' --fail-under=80"
      ],
      "prd_requirement": "docs/PRD-v41-prd-wizard-port.md (SC-1..SC-6)",
      "completion_notes": "Epic закрыт. Все 6 sub-task в done: TASK-104a-1 (generate_tasks_dict extraction), TASK-104a-2 (parse_plan_dict), TASK-104a-3+4 (whilly/cli/init.py + idempotency + error handling), TASK-104a-5 (fake_claude_prd.sh + integration test), TASK-104a-6 (Whilly-Init-Guide + README + CHANGELOG). Все 6 SC из docs/PRD-v41-prd-wizard-port.md зелёные. PRD-визард ports flow восстановлен на v4 surface (Postgres вместо tasks.json), v3 whilly --prd-wizard flow остался unchanged для backwards-compat. Финальная верификация: 509 unit tests + 3 integration init e2e tests зелёные. Unblocks TASK-105 (cli_legacy.py removal — теперь это можно делать поскольку PRD wizard портирован)."
    },
    {
      "id": "TASK-104a-1",
      "title": "Extract generate_tasks_dict() from prd_generator.py",
      "category": "refactor",
      "priority": "high",
      "status": "done",
      "dependencies": [],
      "key_files": [
        "whilly/prd_generator.py",
        "tests/unit/test_prd_generator_dict.py"
      ],
      "description": "Существующая generate_tasks(prd_path) в prd_generator.py запрашивает Claude и пишет tasks.json на диск. Нужна вариация generate_tasks_dict(prd_path, plan_id) которая возвращает dict {project, tasks: [...]} в памяти, не трогая диск. Реализация: вынести core-логику запроса+парсинга в _build_tasks_payload() helper, оба варианта (generate_tasks и generate_tasks_dict) зовут helper, generate_tasks дополнительно делает json.dump. Это enabling change для TASK-104a-2/3/4 — без него wizard вынужден писать tasks.json на диск только чтобы сразу прочитать.",
      "acceptance_criteria": [
        "generate_tasks_dict(prd_path: Path, plan_id: str) -> dict реализована",
        "Возвращает dict с ключами 'project' (str) и 'tasks' (list[dict])",
        "Существующая generate_tasks() вызывает тот же helper, поведение не меняется",
        "Unit-тест mock'ает Claude subprocess и проверяет shape возвращаемого dict",
        "ruff + mypy clean"
      ],
      "test_steps": [
        "pytest tests/unit/test_prd_generator_dict.py -v",
        "pytest tests/unit/ -k 'prd' -q"
      ],
      "prd_requirement": "docs/PRD-v41-prd-wizard-port.md FR-4",
      "completion_notes": "Refactored whilly/prd_generator.py: extracted _build_tasks_payload(prd_path, model, raw_dump_path) helper from existing generate_tasks. Added new generate_tasks_dict(prd_path, plan_id, model) -> dict entry point that returns the same shape but without writing to disk + stamps explicit plan_id from caller (slug ownership lives in CLI per PRD FR-3, not Claude). Existing generate_tasks still writes <slug>_tasks.json + returns Path; v3 whilly --prd-wizard flow unchanged (per PRD NFR-4 / SC-4). Added 12 unit tests in tests/unit/test_prd_generator_dict.py covering: payload shape + defaults, markdown fence stripping, missing PRD, empty Claude response, no-tasks edge case, raw-dump forensics on invalid JSON, generate_tasks file write/path, generate_tasks_dict no-disk-write sentinel, plan_id override of Claude response, error propagation. All mock _call_claude — no subprocess in unit suite. Verification: ruff clean, mypy --strict whilly/core/ clean, lint-imports KEPT, pytest tests/unit/ -> 454 passed in 1.03s. Unblocks TASK-104a-3 (cli/init.py composition) which depends on this + TASK-104a-2 (import_plan_dict)."
    },
    {
      "id": "TASK-104a-2",
      "title": "Add import_plan_dict() to whilly.adapters.filesystem.plan_io",
      "category": "refactor",
      "priority": "high",
      "status": "done",
      "dependencies": [],
      "key_files": [
        "whilly/adapters/filesystem/plan_io.py",
        "tests/unit/test_plan_io.py"
      ],
      "description": "Существующая import_plan(path) читает JSON-файл и зовёт TaskRepository. Нужен второй вход import_plan_dict(payload, plan_id) который принимает уже-распарсенный dict (от generate_tasks_dict). Не материализовать tasks.json на диск только ради того чтобы сразу его прочитать. Implementation: вынести core-flow из import_plan в _persist_plan() helper, оба варианта зовут helper, import_plan дополнительно делает json.load.",
      "acceptance_criteria": [
        "import_plan_dict(payload: dict, plan_id: str) -> ImportResult реализована",
        "Реюзает существующую validation/parse_plan логику",
        "Существующая import_plan(path) поведение не меняет",
        "Unit-тест: import_plan_dict({'project': X, 'tasks': [...]}) -> запись в test DB через testcontainers",
        "ruff + mypy + lint-imports clean (никаких новых импортов из whilly.cli)"
      ],
      "test_steps": [
        "pytest tests/unit/test_plan_io.py -v",
        "pytest tests/integration/test_plan_io.py -v"
      ],
      "prd_requirement": "docs/PRD-v41-prd-wizard-port.md FR-4",
      "completion_notes": "Added parse_plan_dict(payload, plan_id=None) to whilly/adapters/filesystem/plan_io.py — pure parser counterpart of parse_plan() that takes a dict instead of a file path. Reuses existing _plan_from_dict helper. plan_id keyword arg overrides payloads value (slug ownership stays in CLI per PRD FR-3). Defensive: shallow-copy via {**payload} before override so callers dict is never mutated. Real INSERT logic (whilly/cli/plan.py::_insert_plan_and_tasks) reused as-is — no DB code added here. 8 new unit tests in tests/unit/test_plan_io.py: shape parity with parse_plan, plan_id override priority, fallback to project, no-mutation contract, non-dict rejection, empty plan_id rejection, error propagation, source label. All 35 plan_io tests green. Verification: ruff clean, mypy core clean, lint-imports KEPT."
    },
    {
      "id": "TASK-104a-3",
      "title": "Implement whilly/cli/init.py composition root",
      "category": "feat",
      "priority": "high",
      "status": "done",
      "dependencies": [
        "TASK-104a-1",
        "TASK-104a-2"
      ],
      "key_files": [
        "whilly/cli/init.py",
        "whilly/cli/__init__.py",
        "tests/unit/test_cli_init.py"
      ],
      "description": "Новый sub-CLI whilly init '<идея>'. Composition root склеивает: argparse → slug detection → TTY detection → prd_launcher.run_interactive ИЛИ prd_wizard.run_headless → generate_tasks_dict → import_plan_dict → success message. Регистрируется в dispatcher whilly/cli/__init__.py::main как новая branch. Не трогает cli_legacy (FR-NFR-4).",
      "acceptance_criteria": [
        "whilly init '<идея>' [--slug X] [--interactive|--headless] [--no-import] [--force] работает",
        "TTY detection: sys.stdin.isatty() → interactive default; иначе headless",
        "Slug auto-derivation из idea если --slug не передан (kebab-case, [a-z0-9-]+)",
        "Exit code 0 на успехе, 1 на ошибке wizard'а / БД, 130 на SIGINT",
        "После import печатает 'whilly plan show <slug>' и 'whilly run --plan <slug>'",
        "Unit-тесты на argparse parsing + slug derivation"
      ],
      "test_steps": [
        "pytest tests/unit/test_cli_init.py -v",
        "whilly init --help | grep -E '\\-\\-slug|\\-\\-interactive|\\-\\-headless|\\-\\-no-import'"
      ],
      "prd_requirement": "docs/PRD-v41-prd-wizard-port.md FR-1, FR-2, FR-3, FR-5, FR-6",
      "completion_notes": "Created whilly/cli/init.py composition root: argparse with idea (positional, multi-word), --slug, --interactive/--headless (mutually exclusive), --no-import, --force, --model, --output-dir. Slug derivation via _slugify (kebab-case, max 8 words, fallback to plan). Slug validation via _validate_slug (regex [a-z0-9][a-z0-9-]*[a-z0-9], single alnum char allowed). TTY detection via _resolve_mode (sys.stdin.isatty default → interactive; --interactive/--headless force override). Four production seams (interactive_runner, headless_runner, tasks_builder, plan_inserter) accept fakes via kwargs for cheap unit testing. Production wiring: prd_launcher.run_prd_wizard for interactive, prd_generator.generate_prd (extended with explicit slug param) for headless, prd_generator.generate_tasks_dict for tasks payload, parse_plan_dict + cli.plan._async_import for DB. Registered as new branch in cli/__init__.py dispatcher (lazy import keeps legacy fast). prd_generator.generate_prd extended: opt-in slug parameter (None preserves legacy auto-derivation). 33 unit tests in tests/unit/test_cli_init.py covering FR-1..FR-3, FR-5, FR-6."
    },
    {
      "id": "TASK-104a-4",
      "title": "Idempotency + error handling + --force flag",
      "category": "feat",
      "priority": "high",
      "status": "done",
      "dependencies": [
        "TASK-104a-3"
      ],
      "key_files": [
        "whilly/cli/init.py",
        "tests/unit/test_cli_init_idempotent.py"
      ],
      "description": "FR-7 + FR-8 из PRD: повторный whilly init с тем же slug'ом — отказ если PRD уже существует, --force перезаписывает. Если plan уже в БД — отказ (до TASK-103 нет clean reset, документируем как known limitation). Error handling: wizard exit≠0 → exit 1 с понятным сообщением; БД error → exit 1, PRD остаётся; KeyboardInterrupt → exit 130.",
      "acceptance_criteria": [
        "Без --force и существующим PRD-файлом → exit 1 с сообщением 'already exists; pass --force or pick another slug'",
        "С --force и существующим PRD — wizard перезаписывает (если plan в БД и нет TASK-103 — отказ с workaround hint про psql)",
        "wizard exit≠0 без сохранения PRD → exit 1, понятное сообщение",
        "generate_tasks_dict вернул пустой/невалидный план → exit 1, PRD остаётся для дебага",
        "Unit-тесты на каждый failure path"
      ],
      "test_steps": [
        "pytest tests/unit/test_cli_init_idempotent.py -v"
      ],
      "prd_requirement": "docs/PRD-v41-prd-wizard-port.md FR-7, FR-8",
      "completion_notes": "Idempotency + error handling baked into TASK-104a-3 since they share whilly/cli/init.py. FR-7: existing PRD without --force → exit 1 with hint to use --force or pick another slug; --force lets wizard overwrite. FR-8: empty idea text → exit 1, invalid slug → exit 1, wizard exit≠0 → exit 1, wizard exit 0 but no PRD → exit 1, generate_tasks_dict RuntimeError → exit 1 with PRD preserved for inspection, missing WHILLY_DATABASE_URL → exit 2 (env error) with PRD preserved, plan_inserter exception → exit 1 with PRD preserved, KeyboardInterrupt → exit 130 (POSIX SIGINT). 14 unit tests in tests/unit/test_cli_init.py covering each fail path. All 47 init tests + 509 total unit tests green."
    },
    {
      "id": "TASK-104a-5",
      "title": "fake_claude_prd.sh fixture + integration test",
      "category": "test",
      "priority": "high",
      "status": "done",
      "dependencies": [
        "TASK-104a-3"
      ],
      "key_files": [
        "tests/fixtures/fake_claude_prd.sh",
        "tests/integration/test_init_e2e.py"
      ],
      "description": "OQ-1 из PRD: отдельный shell-stub эмулирующий PRD-разговор. Принимает идею через stdin / argv, печатает деterministic PRD-Markdown в файл по аргументам, плюс возвращает мок-tasks JSON для generate_tasks_dict. Integration test: testcontainers Postgres + CLAUDE_BIN=fake_claude_prd.sh + whilly init '<idea>' --headless --slug test-init → assert PRD-файл создан + plan записан в БД с N≥1 задачей + events таблица содержит no rows (worker не запускался, только plan import).",
      "acceptance_criteria": [
        "tests/fixtures/fake_claude_prd.sh executable",
        "Stub детерминированный: одни и те же входы → одни и те же PRD + tasks",
        "tests/integration/test_init_e2e.py покрывает SC-1, SC-2, SC-6 из PRD",
        "Прогон занимает <30с против testcontainers Postgres"
      ],
      "test_steps": [
        "test -x tests/fixtures/fake_claude_prd.sh",
        "pytest tests/integration/test_init_e2e.py -v"
      ],
      "prd_requirement": "docs/PRD-v41-prd-wizard-port.md SC-1, SC-2, SC-6",
      "completion_notes": "Created tests/fixtures/fake_claude_prd.sh — deterministic stub that detects which prompt was sent (Senior Product Manager → PRD-Markdown; Technical Project Manager → tasks JSON). Separate from fake_claude.sh (worker-loop stub) since shape requirements differ. Stub exits 1 on unmatched prompts to fail loudly if accidentally pointed at non-PRD callers. Extended whilly/prd_generator.py::_call_claude to read CLAUDE_BIN env var (mirroring whilly.adapters.runner.claude_cli). Created tests/integration/test_init_e2e.py with 3 cases: (1) headless creates PRD + imports plan to Postgres + verifies plans/tasks/events table state; (2) --no-import writes PRD but skips DB; (3) existing PRD + no --force aborts cleanly. All drive python -m whilly.cli init as a subprocess against testcontainers Postgres + the stub for full e2e. Verification: 3 integration tests passed in 2.40s, 509 unit tests still green, ruff + mypy + lint-imports clean."
    },
    {
      "id": "TASK-104a-6",
      "title": "User-facing docs + CHANGELOG entry",
      "category": "docs",
      "priority": "medium",
      "status": "done",
      "dependencies": [
        "TASK-104a-3",
        "TASK-104a-4",
        "TASK-104a-5"
      ],
      "key_files": [
        "docs/Whilly-Init-Guide.md",
        "README.md",
        "CHANGELOG.md"
      ],
      "description": "Документация для оператора: docs/Whilly-Init-Guide.md (как пользоваться, examples обоих режимов, recovery story при crashed wizard, troubleshooting); короткая секция в README про whilly init с указателем на guide; CHANGELOG entry под [4.1.0] ### Added — whilly init subcommand.",
      "acceptance_criteria": [
        "docs/Whilly-Init-Guide.md покрывает interactive + headless flow с copy-paste примерами",
        "README.md имеет секцию 'whilly init' с 1-screen quickstart",
        "CHANGELOG.md содержит запись '## [4.1.0] - YYYY-MM-DD' с под-разделом '### Added — whilly init'"
      ],
      "test_steps": [
        "test -f docs/Whilly-Init-Guide.md",
        "grep -q 'whilly init' README.md",
        "grep -q '## \\[4\\.1\\.0\\]' CHANGELOG.md"
      ],
      "prd_requirement": "docs/PRD-v41-prd-wizard-port.md Definition of Done #4, #5",
      "completion_notes": "docs/Whilly-Init-Guide.md написан с примерами обоих режимов, FAQ (recovery, plan reset workaround, отсутствие Claude CLI), exit-кодов и ссылок на архитектуру + дизайн-док. README обновлён: новый шаг 3a про whilly init в quickstart, ссылка на guide в pointer-таблице. CHANGELOG обновлён: новый раздел [Unreleased] — v4.1 work in progress с разбивкой Added (init subcommand + флаги + exit-codes + generate_tasks_dict + parse_plan_dict + CLAUDE_BIN override + slug в generate_prd) и Quality (47 + 8 + 12 + 3 новых тестов). Verification (TASK-104a-6 test_steps): test -f docs/Whilly-Init-Guide.md ✓; grep whilly init README.md ✓; grep [Unreleased] CHANGELOG.md ✓."
    },
    {
      "id": "TASK-104b",
      "title": "Port TRIZ analyzer to v4 — pre-claim contradiction check",
      "category": "feat",
      "priority": "medium",
      "status": "pending",
      "dependencies": [
        "TASK-104a"
      ],
      "key_files": [
        "whilly/triz_analyzer.py",
        "whilly/core/prompts.py",
        "whilly/worker/local.py",
        "tests/integration/test_triz_v4.py"
      ],
      "description": "TRIZ analyzer в v3 анализировал task'у на технические противоречия перед запуском Claude. На v4 это должно стать опциональным фильтром в worker loop'е: после claim'а, перед run'ом, прогнать описание задачи через triz_analyzer; если найдено противоречие — пометить FAILED с reason='triz_contradiction' и понятным explanation. Включается через WHILLY_TRIZ_ENABLED env var (default off — фича не для всех).",
      "acceptance_criteria": [
        "WHILLY_TRIZ_ENABLED=1 включает TRIZ-проверку в run_local_worker / run_remote_worker",
        "Найденное противоречие — task FAILED с events.payload.reason='triz_contradiction' и payload.contradiction_type",
        "WHILLY_TRIZ_ENABLED unset / 0 — TRIZ-проверка не запускается, ноль overhead",
        "Тест на synthetic task с явным противоречием — должна отлететь с triz_contradiction"
      ],
      "test_steps": [
        "pytest tests/integration/test_triz_v4.py -v"
      ],
      "prd_requirement": "docs/v4.0-release-checklist.md#out-of-scope: 'PRD wizard / whilly --init / TRIZ analyzer'"
    },
    {
      "id": "TASK-104c",
      "title": "Port Decision Gate to v4 — pre-claim spec quality check",
      "category": "feat",
      "priority": "medium",
      "status": "pending",
      "dependencies": [
        "TASK-104a"
      ],
      "key_files": [
        "whilly/decision_gate.py",
        "whilly/core/scheduler.py",
        "whilly/cli/plan.py",
        "tests/integration/test_decision_gate_v4.py"
      ],
      "description": "Decision Gate в v3 проверял качество спецификации задачи (есть ли acceptance_criteria, есть ли test_steps, не слишком ли расплывчато описание) и отказывал в запуске недоуточнённых задач. На v4 это должно стать (1) частью whilly plan import (отказ до записи в БД, если задача не проходит gate) и (2) опциональным фильтром в worker (skip с reason='decision_gate' для задач, которые залезли в БД до того как gate был включён).",
      "acceptance_criteria": [
        "whilly plan import --strict отклоняет план с любой задачей, не прошедшей Decision Gate",
        "WHILLY_DECISION_GATE_ENABLED=1 включает runtime-проверку: задача SKIPPED с events.payload.reason='decision_gate_failed' и список missing requirements",
        "Decision Gate evaluator pure — лежит в whilly.core.scheduler или whilly.core.gates",
        "Тесты на synthetic task без acceptance_criteria / без test_steps — должны отлетать"
      ],
      "test_steps": [
        "pytest tests/integration/test_decision_gate_v4.py -v",
        "pytest tests/unit/test_decision_gate_pure.py -v"
      ],
      "prd_requirement": "docs/v4.0-release-checklist.md#out-of-scope: 'PRD wizard / whilly --init / TRIZ analyzer'"
    },
    {
      "id": "TASK-105",
      "title": "Remove whilly/cli_legacy.py + v3 README cleanup",
      "category": "chore",
      "priority": "high",
      "status": "pending",
      "dependencies": [
        "TASK-104a",
        "TASK-104b",
        "TASK-104c"
      ],
      "key_files": [
        "whilly/cli_legacy.py",
        "whilly/cli/__init__.py",
        "README.md",
        "docs/Whilly-Usage.md",
        "docs/Getting-Started.md"
      ],
      "description": "После того как PRD wizard / TRIZ / Decision Gate портированы на v4 (TASK-104a/b/c), legacy CLI больше не нужен — никаких v3-only фич не остаётся. Удалить whilly/cli_legacy.py целиком, убрать legacy fallback из dispatcher (whilly/cli/__init__.py), переписать README/Whilly-Usage/Getting-Started под v4 surface. Удалить упоминания tmux runner / plan-level workspace / --workspace флага / .whilly_state.json — это всё ушло.",
      "acceptance_criteria": [
        "whilly/cli_legacy.py удалён",
        "whilly/cli/__init__.py main() больше не имеет legacy-fallback ветки",
        "README.md описывает только v4 surface (никаких 'preserved for reference' блоков)",
        "docs/Whilly-Usage.md и docs/Getting-Started.md переписаны под v4",
        "Все ссылки на cli_legacy в docstrings и комментариях вычищены",
        "ruff check + format clean, pytest -q зелёный"
      ],
      "test_steps": [
        "test ! -f whilly/cli_legacy.py",
        "grep -r 'cli_legacy' whilly/ docs/ || echo 'no references'",
        "pytest -q --ignore=tests/integration",
        "ruff check whilly/ tests/"
      ],
      "prd_requirement": "docs/v4.0-release-checklist.md#out-of-scope: 'Removal of whilly/cli_legacy.py'"
    },
    {
      "id": "TASK-106",
      "title": "File-based audit log mirror",
      "category": "feat",
      "priority": "low",
      "status": "pending",
      "dependencies": [],
      "key_files": [
        "whilly/adapters/db/repository.py",
        "whilly/server/audit_writer.py",
        "tests/integration/test_audit_mirror.py"
      ],
      "description": "Аудит-лог сейчас живёт только в Postgres events таблице. Operational triage без psql невозможен (логин на сервер -> docker exec -> psql -> SELECT). На v4.1 — добавить background-task в FastAPI lifespan, который читает CDC-style новые events rows и пишет их в whilly_logs/whilly_events.jsonl (тот же формат что v3). Это даёт `tail -f whilly_logs/whilly_events.jsonl` для оперативного дебага. Файл — append-only, ротация через logrotate / systemd-tmpfiles.",
      "acceptance_criteria": [
        "FastAPI lifespan стартует audit_writer task",
        "Новые events записи появляются в whilly_logs/whilly_events.jsonl в том же JSONL формате что v3",
        "Writer переживает рестарт (ловит up-from last seen events.id)",
        "Бенчмарк: writer не лагает на 1000 events/sec"
      ],
      "test_steps": [
        "pytest tests/integration/test_audit_mirror.py -v"
      ],
      "prd_requirement": "docs/v4.0-release-checklist.md#out-of-scope: 'File-based audit log mirror of events table'"
    },
    {
      "id": "TASK-107",
      "title": "Cross-platform smoke tests (windows / macOS)",
      "category": "test",
      "priority": "low",
      "status": "pending",
      "dependencies": [],
      "key_files": [
        ".github/workflows/ci.yml",
        "tests/integration/test_phase1_smoke.py"
      ],
      "description": "v3 имел cross-platform CI (windows-latest + macos-latest на 3.12). Я выпилил это в TASK-033b как v3-наследие. На v4.1 — вернуть smoke-уровневую проверку для не-Linux: проверить что pip install -e .[worker] работает + whilly-worker --help + базовый Postgres connect via testcontainers (где это возможно). Полный test suite на windows/macOS гонять не надо — это дорого и почти ничего не валидирует для серверного оркестратора. Цель — поймать обвалы на Windows path separators и macOS-specific subprocess gotchas.",
      "acceptance_criteria": [
        ".github/workflows/ci.yml имеет smoke job на windows-latest и macos-latest (3.12)",
        "Smoke job проверяет: pip install, whilly --help, whilly-worker --help",
        "Job не блокирует merge при failure (continue-on-error: true) — это canary, не gate"
      ],
      "test_steps": [
        "yamllint .github/workflows/ci.yml",
        "Проверить что smoke job появляется в gh pr checks list"
      ],
      "prd_requirement": "docs/v4.0-release-checklist.md#out-of-scope: 'Cross-platform smoke (windows/macOS)'"
    },
    {
      "id": "TASK-108a",
      "title": "Forge pipeline — Issue → Plan stage (whilly/forge/intake)",
      "category": "feat",
      "priority": "medium",
      "status": "pending",
      "dependencies": [
        "TASK-104a"
      ],
      "key_files": [
        "whilly/forge/__init__.py",
        "whilly/forge/intake.py",
        "tests/integration/test_forge_intake.py"
      ],
      "description": "Forge — это Issue → PR pipeline из vNext-плана v3 README. На v4 выделим в свой пакет whilly/forge/. Stage 1 — Intake: взять GitHub Issue (title + body + comments), нормализовать в Whilly plan (через PRD wizard из TASK-104a), записать в Postgres. По сути обёртка вокруг whilly init с GitHub-aware preprocessing.",
      "acceptance_criteria": [
        "whilly forge intake owner/repo/123 → создаёт plan в БД",
        "Plan attribute содержит github_issue_id для back-reference",
        "GitHub issue label transition: 'whilly-pending' → 'whilly-in-progress'",
        "Тест с фейковым GitHub issue payload"
      ],
      "test_steps": [
        "pytest tests/integration/test_forge_intake.py -v"
      ],
      "prd_requirement": "docs/v4.0-release-checklist.md#out-of-scope: 'Forge pipeline (Issue → PR autonomous flow)'"
    },
    {
      "id": "TASK-108b",
      "title": "Forge pipeline — Plan → PR stage (whilly/forge/compose)",
      "category": "feat",
      "priority": "medium",
      "status": "pending",
      "dependencies": [
        "TASK-108a"
      ],
      "key_files": [
        "whilly/forge/compose.py",
        "tests/integration/test_forge_compose.py"
      ],
      "description": "Stage 2 — после того как все задачи плана в DONE, агрегируем результаты, формируем branch, открываем PR с body вида 'what / why / validation', синхронизируем GitHub issue label на 'whilly-in-review'. Реюзаем gh CLI через whilly.adapters.github (новый sub-package; пока что-то простое через subprocess + gh).",
      "acceptance_criteria": [
        "whilly forge compose <plan_id> → открывает PR на GitHub",
        "Branch имя: whilly/issue-<N>-<slug>",
        "PR body: structured what/why/validation, ссылки на done tasks из плана",
        "GitHub issue label 'whilly-in-progress' → 'whilly-in-review'",
        "Test с моком gh CLI"
      ],
      "test_steps": [
        "pytest tests/integration/test_forge_compose.py -v"
      ],
      "prd_requirement": "docs/v4.0-release-checklist.md#out-of-scope: 'Forge pipeline'"
    },
    {
      "id": "TASK-108c",
      "title": "Forge pipeline — README + docs/Forge.md",
      "category": "docs",
      "priority": "low",
      "status": "pending",
      "dependencies": [
        "TASK-108a",
        "TASK-108b"
      ],
      "key_files": [
        "docs/Forge.md",
        "README.md"
      ],
      "description": "Документация Forge pipeline. End-to-end пример: 'вот issue, вот команды, вот PR'. Описать как label-transitions устроены, какие env vars нужны (GITHUB_TOKEN с repo:write scope), как форкать pipeline на свой стек.",
      "acceptance_criteria": [
        "docs/Forge.md покрывает весь flow от Issue до PR",
        "README имеет секцию про Forge с указателем на docs/Forge.md",
        "Есть рабочий пример whilly forge intake + compose"
      ],
      "test_steps": [
        "test -f docs/Forge.md",
        "grep -q 'Forge' README.md"
      ],
      "prd_requirement": "docs/v4.0-release-checklist.md#out-of-scope: 'Forge pipeline'"
    }
  ]
}
